# 2026-09-21

- 用户提供新的已绑定 FBX：`C:\Users\zhuangmenghong\Desktop\tripo_convert_3ae4046f-dc49-4f32-b5ea-ac940bfabdde.zip`。压缩包含 FBX 和 basecolor JPEG。
- 放弃旧的“将外部模型套到小僵尸骨架”方案；改为保留用户 FBX 原始绑定关系，在其原骨架上增补 16 根小僵尸同名手指骨，并统一 `NeckTwist02` 为 `Neck`。
- 生成模型源：`assets/art/enemies/normal_enemy_3d/ranged_caster/source/model/enm_ranged_sporeshooter01_model_v004.blend`。57 根骨骼、原始 30 个蒙皮组、未绑定顶点 0、权重和异常 0、Armature modifier 仍指向保留的新绑定骨架；Blender +Y 正面。
- 已通过骨骼叠加预览：头、躯干、肩臂和腿部骨链落在网格对应位置；本轮未修改动作、GLB、Godot 或玩法接入。
- 骨骼名册审计（`_scratch/security_zombie/audit_skeleton_names.py` → `skeleton_name_audit.json`）：小僵尸 36 骨全部名字在新骨架命中（missing_in_new 为空），新骨架 57 骨 = 36 + 新增 21（躯干 Pelvis/Spine01/NeckTwist01、手臂 8 根 Twist、腿脚 10 根 Twist/ToeBase）。父子关系 4 处不同：Spine02 父由 Waist→Spine01、Neck 父由 Spine02→NeckTwist01、L/R_Thigh 父由 Hip→Pelvis（都是新增中间骨导致）。
- ⚠️ 顶点组不同步：新骨架 30 个顶点组 vs 小僵尸 36 个。差异性质已核实：`Root`/`Hip`/`L_Upperarm`/`L_Forearm`/`L_Thigh`/`L_Calf` 无直接顶点组，权重集中在其子级 Twist 骨（原 FBX 绑定设计），但父链完整（`L_ThighTwist01.parent = L_Thigh` 等），旋转仍沿父链传递 ⇒ 小僵尸动作写这些骨名依旧能驱动形变，非缺陷。真正无权重的是本轮新增的 16 根手指骨（纯结构骨，不驱动顶点，只作挂点/结构参考）。

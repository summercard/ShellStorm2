# 1159 远征01 · 支线取消（文档先行）—— 删支线 + 主路加深到 10 房 + 图纸重生成

**业主裁定（本轮指令来源）**
1. 「远征关卡的这些支线房间（`branch_01`…`branch_04`）可以删除了，这些已经不会使用到了。可以在文档中不需要体现。」
2. 改动边界（AskUserQuestion 裁决）：「**先处理文档内**，一会进行实际制作的时候里头再删除，因为很多房间的数据也重新整理了。」
3. 构筑次数维持口径：**「主路加深到 10 房」**（内容房 6 → 10 ⇒ 主路共 13 房）。

## 口径（本轮确立）

- **支线取消**：`branch_01`…`branch_04` 不再属于本关；版图 = **安全屋 → 10 间内容房（`room_01`…`room_10`）→ Boss竞技场 → 撤离屋**，共 **13 房**、**12 段衔接全部墙贴墙**、**每间主路内容房 2 门**（不再有 3 门房）。
- **命运卡构筑次数不变**：旧「主路 6 + 支线 4 = 10 次」→ 新「主路内容房 10 间 ⇒ 10 次」。
- **两个池子长度都不用改**（自洽）：内容池 `content_type_pool` **10 项**（原对应 6+4、现对应 10 间主路内容房）；房型池 5 个 `COMMON_ROOM` 模板照旧按「每族至少一次」抽给 10 间。
- **本轮只动文档 + 图纸工具**：`floor_00.json` / `level_plan.json` / `verify_expedition_level01_flow.gd` / `room_templates/*.json` **一律未碰**（支线仍在设计源里、验收断言仍是 10/4/4），实际制作时一并改。

## 改了什么

### A. 设计页 `docs/v0.1/design/远征关卡01设计.md`（54 + 2 处替换）

| 位置 | 改动 |
|---|---|
| §2 顶部 | 保留「支线取消 · 文档先行」裁定块；09-24 / 09-25 两条历史条目**加注「已失效」** |
| §3.1 总表 | **删 4 行支线**；抬头注改为「旧口径主路 9 房」；面积段 16400/16946 → **12150 / 12540**；删「支线尺寸按共用房型推得」注 |
| §3.1.1 | 实例次数注按主路重数（数据库 3→**2**、桥房 2→**1**、标准房间 2→**0**） |
| §3.2 | 6 处「本关作用」去掉「支线岔口 / 不挂支线」措辞；`room_06` 注明**目标口径下不再是最后一间** |
| **§3.3** | 整节（4 个 `branch_NN` 小节）→ 压成「**支线房型（已取消）**」4 条要点（**保号不重编**，避免 `§3.4` 等交叉引用全断） |
| §3.4 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 | 逐处去支线：房型抽取口径、边数 12（8+4）→「现状 8 / 目标 12 全在主路」、门表 4 个 3 门房 → 全 2 门、内容分配与发放顺序、固定项清单 |
| §4.7 | 参考数字表换新口径：**12150 / 10125（轮廓）/ 内墙 ≈390 / used ≈12540 / 占用比 ≈20.2%** |
| **§5.1** | 整节 → 「**支线（已取消）**」短节；§5 表删「支线房超过一段链」行 |
| §6 | 拐角走廊「主路或支线任意位置」→「主路任意位置」；标准房间注明「原为支线默认填充」 |
| §7 表 / §7.1 / §7.2 / §7.3 | 落地点逐条改成**目标值 + 现状值**成对写（`min_main_content_rooms` 10 vs 6、`branch_count_range` [0,0] vs [4,4]、`edge_policy` 目标 12 条全在主路、`PLAN_ROOM_IDS`/`PLAN_BRANCH_PARENT`/拓扑计数 10/4/4 → 目标 10/0/0） |

复算依据：`used = Σw×d + Σ((w+d)×2×0.30)`；9 房 = 12150 + 390 = **12540**，`12540/62200 = 20.2%`（与脚本自检行一致）。轮廓面积 10125 由鞋带公式复算（`verify.py`）。

### B. 图纸脚本 `scripts/render_expedition01_plan_sheet.py`（24 处）

- **输出文案去支线**：总平面图副标题「（无支线）」、meta 结构行、SVG `<desc>`、图 lede、门数段（改为「主路内容房每间 2 门」）、明细表 cap、面积 cap、待确认项（删「本版不绘支线房」条）、页脚。
- **删死代码**：`branch_html` 表（已无引用）、`out["branch_01"]`/`["branch_04"]`/`["branch_02"]`/`["branch_03"]` 4 个别名、`std_25x25` 绘制函数（原仅支线用）+ 详图卡片 4 条 `branch_*` extra 与 `"支线岔口"` 后缀。
- `SHOW_BRANCH_ROOMS` 保留（已无支线行可过滤，注明「备用」）；`KIND["BRANCH"]` 配色保留（图例按 `BRANCH_KEYS` 过滤，不出现）。

### C. 产物重生成（12 个，全部 CRLF / 无 BOM）

`远征关卡01平面图集.html`（67.9 KB）、`总平面图.svg`、`plans/00-总平面图.svg`、`plans/01..09-*.svg`。脚本自检：
```
rooms=9 main=9 branch=0 total=12150 m2 used=12540 avail=62200 ratio=0.202
envelope x=[-90,95] y=[-95,100]  (185 x 195 m)   margin: E/W=32.5  N/S=27.5
```

## 核验（全绿）

- `scripts/check_documentation_contracts.py` → `issues: []`（140 文档 / 972 链接）
- `scripts/check_expedition_room_asset_status.py` → `EXPEDITION_ASSET_STATUS_OK`
- `verify.py`（自带核验）：9 房全落 5 m 网格 / 重叠 0 对 / 主路 8 段净距全 0.00 / 包络 185×195 居中 / 轮廓 10125 / 设计页 §3.1 对图纸 **9/9** / 12 产物行尾全绿
- 字节级行尾：设计页 CRLF=726（去掉 46 行）、脚本 CRLF=1068、图集 CRLF=169，均 loneLF=0 loneCR=0 BOM=False
- 残留自检：设计页 47 行、图集 7 处、脚本 22 行含「支线 / branch_0」——**全部是「已取消 / 不再使用」的裁定与留痕**，无一条把支线当设计内容

## 未做（留给实际制作那一轮）

- `floor_00.json` / `level_plan.json`：删支线房、`branch_count_range: [0,0]`、`min_main_content_rooms: 10`、`edge_policy` 重写为 12 条主路边、13 房主路**新房型分配与坐标重排**。
- `tests/verification/verify_expedition_level01_flow.gd`：`PLAN_ROOM_IDS` / `PLAN_BRANCH_PARENT` / 拓扑计数 10/4/4 → 10/0/0。
- `room_templates/*.json`：`db_70x50` 40×30、`office_60x70` 30×40、`bridge_60x50` 30×60 工字型的尺寸同步（见 1121b 与 §3.4 注）。
- `GameDesignConfig.EXPEDITION_LEVELS` L27 副标题仍是「入口安全屋 → 01—05 号房 → 撤离点」（§1 已加注不符）。

## 工具

- 带命中断言的批量替换脚本（可复用范式）：`D:/ss2_plan_shot/patch_doc_branch_removal.py`（54 条）、`patch_render_branch_removal.py`（24 条）、`patch_doc_history_notes.py`（2 条）。做法：`newline=""` 读 → 折成 `\n` → 逐条 `count()==1` 断言 → 全中才 `replace` 并以 `newline="\r\n"` 写回 ⇒ 天然全 CRLF、不会 CRCRLF。
- 核验：`D:/ss2_plan_shot/verify.py`。

# 0838 · 房型 id 锁定 + 支线不另起代号 + 单层关卡放开边界（规则落地）

> 主人裁定两条，本事务**只落规则文档，不动代码**。起因见 `0112_expedition01_bridge_short_edge_blocker.md`（方案 A 我新建了 `bridge_50x60` 第二模板）。

## 主人原话（要点）

1. 「不要用两批代号……支线在文档设计中标注即可。支线可以是任何主线的房间延展出去的房型，**不要新增加 id**。」
2. 「**房型 id 锁定后除非我说要改，不然不要增加**，关卡只用锁定的（房型）进行拼接，**关卡总面积无上限，不需要考虑边界**。」
3. 「先把我上面两条落进相应的规则文档中。在这个基础上有卡点，再来进行反馈。」

## 落点（四处，共 11 处编辑）

| 文件 | 节 | 加了什么 |
|---|---|---|
| `docs/v0.1/05.2_关卡版图白盒与生成规范.md` | §3.4 L3 | `template_id` 封闭集合声明（新增要同步五处） |
| 同上 | §3.6 房间模板可拼性约束 | **第 6 条**（id 锁定、不许为「另一摆法/朝向」另起 id）＋**第 7 条**（支线不引入新模板/新代号）；第 5 条补「升级同样需明示」 |
| 同上 | §3.7 走廊与保留区 | 场地外边界标「单层独立关卡不判」＋新段「单层独立关卡：拼接不考虑边界、不设总面积上限」（附远征 15+60+70 越界实测） |
| 同上 | §5 受约束随机 | 支路长度「受面积预算约束」补「单层放开」；硬约束「保留区与核心区不可占用」补单层例外 |
| `docs/v0.1/05.1_关卡区块设计.md` | §2 简化命名规范 | 表加「关卡房型 id」行＋「一物一名，不要两批代号」段 |
| `docs/v0.1/design/新关卡设计表.md` | §1.3 房间模板表 | 「模板名是锁定集合，不要新建」 |
| 同上 | §1.4 房间表 | 「支线房不另起代号（`branch` 只是角色值/标注）」 |
| 同上 | 第二部分 | 「单层独立关卡没有总面积上限、不判场地边界」 |
| 同上 | 5.1 第 3 条 | 「不超出场地」补「单层独立关卡不判」 |
| 同上 | 5.2 第 5 条 | 面积不超限补「单层独立关卡不判」 |
| `docs/v0.1/design/远征关卡01设计.md` | §3.4 房型池 | 钉死**本关锁定 8 个房型**（照 §3.1.1 表）＋ ⚠️ `bridge_50x60` 未经明示待裁决 |

## 措辞口径

- 边界/面积放开**限定「单层独立关卡」**（远征01 这类），**塔楼各层维持原判据** —— 与既有「`enforce_area_budget:false` 专供单层独立关卡」一致，未扩大到全项目。
- 校验：4 份文档行尾仍 **CRLF、无 lone LF、无 BOM**。

## 未做 / 待反馈

- `docs/v0.1/05.1` §3 区块分布表里远征那行仍写「入口 15×15 + room_01…room_05 各 25×25」旧 7 房口径（与本规则无关，属既有债）。
- 09 技能 `09-level-plan-authoring` 与新关卡设计表是配套的，**本轮未改技能文本**。
- 代码侧仍受 `MAP_SIZE_M` 剪枝（规则已放开、实现未跟）⇒ 见后续卡点反馈。

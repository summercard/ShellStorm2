# 2026-09-25 事务索引

| 时间 | 事务 | 摘要 |
| --- | --- | --- |
| 0007 | [`0007_expedition01_footprint_data_gate.md`](0007_expedition01_footprint_data_gate.md) | 远征01 副线**数据层 + 门禁**：4 个非矩形外轮廓写进模板（`corridor_45x40` 的 `l_turn`/`u_turn`、`db_70x50` 的 `db_01`/`db_02`，挂 `variant_footprints`、顶点已归一 5m 网格）＋通道桥多层几何（`bridge_60x50` 的 `sunken_pit` 30×20 深 12 m / `bridge_span` 5 m / `lower_platform.accessible=false`）＋同步图集真源并重生成。新建门禁 `check_expedition_room_footprints.py` + 自测（对照 + 7 类不一致）。**钉死坐标系 `frame="bbox_nw_x_east_y_south"`（西北角原点、y 向南），并更正 2156 记的「西南角」**。回归：`LEVEL_PLAN_VALIDATE_OK checks=234 rooms=33`、资产状态 OK、文档总门禁 `issues:[]` |
| 0041 | [`0041_expedition01_footprint_runtime_consume.md`](0041_expedition01_footprint_runtime_consume.md) | 远征01 **非矩形外轮廓运行时消费**（第 6 环，提交 `a8e2f97b`，**已 push**）：builder 支持 `footprint_vertices_m`（墙沿边界铺 / 只凸角放 L 件 / 地砖格心过滤）＋规则 **①.5 门让位于角件**＋门/出口 lane **只认包围盒外边段**；生成器透传 `templates` 并**按门位筛变体**（回写 `template_variant`）；**修真 bug**：凹角被当凸角放 L 件（`corner_l` 655→584、`solid_wall` 3060→3098）；装配层 `classify_door_lane` 加**墙平面法向距离**判据修轮廓房假红；验收走线判据改**按地砖 5m 格面打射线**（轮廓感知，`start` 回落矩形判据）。证据：flow OK、区块探针 `PROBE_OK`（轮廓房 38 间 / 角件账目 144 间全平 / 砖数==面积÷25）、门槽守卫 14 断言 OK、区块00 `checks=230` 不变；**负对照 `misses=4/4`、门豁免不误放**。本地 14 提交一并推上，凭据阻塞解除 |
| 0059 | [`0059_expedition01_bridge_multi_level_runtime.md`](0059_expedition01_bridge_multi_level_runtime.md) | 远征01 **通道桥多层几何运行时装配**（第 7 环，提交 `d5cb9a40`，**已 push**）：生成器多层规划（模板 `sunken_pit`/`bridge_span` → 房局部；上层平台砖**真扣坑区 18 格**每间；**坑壁护栏拉伸件 30** = 一件标准墙按 `scale_y` 拉到 12.8 m ⇒ 12 m 沉坑 + **露地面 0.8 m 即护栏**；**坑底砖 24** = 6×4 棋盘格）＋装配层新增 `multi_level_component` 分支（`pit_wall` y 不归一/自带碰撞挡人、`pit_floor_tile` snap；🔴 **两类都不写 `tower_wall_direction`** 免污染门槽与塔楼墙验收）＋验收走线探针 **1.5→0.5 m** 修坑沿护栏飞越假红。业主口径「上层平台先做、下层用墙往下做、坑底用普通砖铺」＋裁决「坑沿加护栏」「12 m」「下层不可达纯装饰」。证据：新建守卫探针 **`BRIDGE_MULTI_LEVEL_OK checks=95`**（首次全绿）、`authored_shell PROBE_OK`（桥房 54 多层件、12 种子累计扣 450 = 25×18）、flow OK、运行探针 OK、门槽守卫 `checks=14`、区块00 `checks=230` 不变 |
| 0112 | [`0112_expedition01_bridge_short_edge_blocker.md`](0112_expedition01_bridge_short_edge_blocker.md) | 远征01 **#42-B 通道桥「短边连接」—— 硬约束实测不可行（阻断，待裁决）**。几何口径钉死：`bridge_60x50` 长轴 = x ⇒ **短边 = 东/西墙（50m）**；桥是 **30×5 东西向长过道**（模板 `note` 写「跨向沿 y」与 rect 自相矛盾，**以 rect 为准**）。实测 24 种子：**基线违规 22/24 = 92%**（父房连接长边 23/短边 26、子房 33/18）；**生成器放置层硬约束 → 回落 18/24 = 75%** ❌。根因：方向白名单把「桥—父—子」挤成同一水平线，250×250 场地首房锚**正中**、单侧仅 ~122.5m，`15+60+70=145m` 必越界；抬预算到 40000 仍 exhaustively 无解（seed 333757 仅 6 节点）。业主原话「旋转 90 度」的正解 = **桥房取向随连接方向**（连东西→60×50 / 连南北→50×60），可做成第二模板 `bridge_50x60` 免运行时旋转。工作区**已回退到 HEAD（无代码改动）**，WIP 存 `/i/ss2_tmp/bridge_short_edge_WIP.patch` |
| 0726 | [`0726_expedition01_status_audit.md`](0726_expedition01_status_audit.md) | 远征01 **只读完成情况巡检**（未改仓库）。亲跑全绿：flow `OK` / 设计源 `checks=234 rooms=33` + `RUNTIME_GUARD_OK` / 通道桥多层 `checks=95` / 壳体 `PROBE_OK`（轮廓房 38、多层件 1350）/ 轮廓门禁 `OK` / 资产状态门禁 `OK`。未完成：**#42-B 阻断待裁决**、**#43 账本登记未做**（账本远征行仅 2 条，缺 Boss 房间种类源 ×2、6 件专属件、通用组件库 214 包）、5 房型无源、Boss 首领未指派与门位未裁决、**`12_全游戏完成度清单.md` 仍是旧 7 房口径 + v0.2 A16 措辞过期**（无门禁能抓到）。本地与 `origin/0.1.2` 已同步 |
| 0838 | [`0838_room_template_id_lock_and_no_bounds.md`](0838_room_template_id_lock_and_no_bounds.md) | **房型 id 锁定 + 支线不另起代号 + 单层关卡放开边界**（主人裁定，只落规则文档、不动代码）。①**房型（房间模板）id = 封闭集合**，未获明示不得新增/改名/删除，关卡只用锁定集合拼接；「同一房型的另一摆法/朝向」用 `template_rotation_deg` 表达、**不许另起 id**（反面案例 = 我为方案 A 新建的 `bridge_50x60`，与 `bridge_60x50` 成「两批代号」，**待裁决**）。②**支线不另起代号**：只是设计页标注 ＋ L2 `role="branch"`，房型从锁定池取。③**单层独立关卡（远征）不受 `MAP_SIZE_M` 边界约束、无总面积上限**，塔楼各层维持原判据。落 11 处：`05.2` §3.4/§3.6(第6·7条)/§3.7/§5、`05.1` §2、`《新关卡设计表》` §1.3/§1.4/第二部分/5.1/5.2、远征设计页 §3.4。四份文档行尾仍 CRLF 无 BOM |
| 0945 | [`0945_expedition01_bridge_short_edge_landed.md`](0945_expedition01_bridge_short_edge_landed.md) | 远征01 **#42-B 通道桥短边连接 —— 裁决落地（规则 → 设计 → 运行时代码 → 图纸，全绿）**。三条裁定落地：**撤销 `bridge_50x60` 改走 `template_rotation_deg:90`**；**场地边界与面积上限全项目取消**（删 `MAP_SIZE_M`/`_map_rect`/`outside_floor_bounds`/`area_budget_exceeded`）；**桥房只在短边开门、最多 2 同轴连接 ⇒ 不能挂支线**（`branch_04` 移挂 `room_06`）。🔴 关键踩坑：`orientable` 字段原兼职「可转置 + 父房长边封锁」，改判「非正方形即可取向」后 **300/300 种子全回落**；修复 = 只用 `sunken_pit` 判 `short_edge_locked`。另加 `_anchored_direction_order` 蛇形折返软引导。证据：设计源 OK（13 房 / 8 模板 / checks=99）、flow **OK**（修 `branch_04` 断言后转绿）、`BRIDGE_MULTI_LEVEL_OK checks=157`、短边守卫 `violations=0 fallback=4`、300 种子 **296 成功 / 4 回落**、轮廓门禁 `axis_aliases=0`、文档门禁 `issues:[]`。图纸脚本修 4 个真缺陷（总平面图按包络∪场地框自动适配防裁切、桥房转置姿态画法、文字描边、核心区标注重叠）并同步 13 张详图 —— **未提交，等主人看图** |
| 1121 | [`1121_expedition01_plan_sheet_selfcheck_and_delivery.md`](1121_expedition01_plan_sheet_selfcheck_and_delivery.md) | 远征01 **图纸改版收尾 —— 内部自洽清洗 + 几何核验 + 交付**（**只动图纸脚本与产物，`src/` 与模板 JSON 一律未碰**）。修 4 类「图纸内部不自洽」：①**图例残留支线紫块**（本版只画主路 9 房）⇒ 按 `BRANCH_KEYS` 过滤；②**四张主路详图误用支线紫**⇒ 全改 `COMMON`，并钉死规则「**详图配色按房型类别定、不按主路/支线定**，支线是一条边的属性」；③**10 张 SVG 各 1 处 CRCRLF**（`XML_HEAD` 自带 `\r\n` × `newline="\r\n"` 二次转换）⇒ 声明行改用 `\n`，行尾自检由「只查 LF-only」加严为**同查 CR-only**；④**30 m 宽桥房画幅裁字**⇒ 新增 `_text_px`/`_FOOT`/`foot_note`，`detail_svg` 先空跑取回说明行再定画幅（`06` 326×428）。另：下沉区专用色 `#BFD6F2` + 斜线填充；`room_02` 注释纠错（`db_01` **非缺角**，四角直角）。核验全绿：9 房全落 5m 网格 / 两两重叠 0 / 主路 8 段净距全 0.00 / 包络 **185×195 m** / 轮廓合计 **10125 m²**（盒 12150）/ 设计页 §3.1 对图纸 **9/9**；11 产物 CAF 行尾全绿（CRLF、无 BOM）。发现 `room_04` 凹字西侧凹口段门落虚空 = **既有形态非本轮引入**（运行时靠 `footprint_accepts_ports()` 回落矩形把关）；§4.2.1「共轴偏移 ≤2.5m」按门槽真口径**恒 0**。待裁决：桥宽 10 m vs 模板 5 m、设计页 7 处残留旧尺寸文案是否同步。**未提交** |
| 1121b | 同上（追加节） | **示例版图平移居中 + 图纸成果落设计文档**。①**平移 dx +70 / dy −85**（5 m 整数倍 ⇒ 相位不变）把包络中心从 (−67.5, 87.5) 对到**场地中心 (2.5, 2.5)**：新包络 `x∈[-90,95] y∈[-95,100]`，四周余量 E/W=32.5、N/S=27.5 对称；画幅由 700×949 → **700×906**，场地框/核心区/版图**三者同心**（此前偏移的根因是 `_overview_bounds()`=包络∪场地框，旧包络把场地框挤到右上）。**不影响机制**：实跑首房锚点 `_snapped_origin_for(15×15)=(2.5,2.5)` 与 §4.2 样例无关，8 段净距与共轴偏移逐段不变。②**设计文档同步 11 处**：图集张数 13→9、§3.1 支线表与面积（19900→**16400**）、§3.1.1、实例次数、§3.3 `branch_01`、§3.4 模板表（新增「尺寸列=图纸口径、JSON 未同步 ⇒ 声明不符会 warn 并退化成矩形」注）、§4.3 场地行、§4.7 参考数字表（占用 20194→**16946**、比 32.5%→**27.2%**、新增轮廓面积行）、§6 注意事项、修两处「缺角」误述（`room_02` 真源是主体+南中段外凸）。**新口径可复算**：面积=矩形包围盒、内墙=每房矩形周长×0.30（与渲染脚本 `wall` 算法一致）。③核验全绿：脚本 `margin E/W=32.5 N/S=27.5`、几何 9 房全落网格/重叠 0/净距 0、文档门禁 `issues:[]`、资产门禁 OK、设计页 CRLF 772 全绿。⚠️ 澄清标签坑：`verify.py` 的「共轴偏移」列实为**主轴距离**（30~50 m），≠ 设计页 §4.2.1 的**垂轴错位**（0/2.5 m），同名不同义。**未提交** |
| 1159 | [`1159_expedition01_branch_removal_docfirst.md`](1159_expedition01_branch_removal_docfirst.md) | 远征01 **支线取消（文档先行）**。裁定：**删掉 `branch_01`…`branch_04`、主路加深到 10 间内容房**（主路共 13 房 / 12 段墙贴墙 / 每间 2 门）；构筑次数维持 **10 次**（原 6+4 ⇒ 现 10 间内容房）；两个池子长度**都不用改**（自洽）。**本轮只动文档 + 图纸工具**（`floor_00.json` / `level_plan.json` / `verify_*.gd` / 模板 JSON 一律未碰，实际制作时再删）。设计页 **54+2 处**替换：§3.1 删 4 行支线、面积 16400/16946 → **12150/12540**、§3.3 整节压成「已取消」短节（**保号不重编**）、§4.4 门表 4 个 3 门房 → 全 2 门、§4.7 换新口径（内墙 ≈390、used ≈12540、占用比 **20.2%**）、**§5.1 整节改为「已取消」**、§7 各条改成**目标值 vs 现状值**成对写（10 vs 6、[0,0] vs [4,4]、目标 12 条主路边、`PLAN_*` 断言目标 10/0/0）。图纸脚本 **24 处**：输出文案去支线（含图例/desc/门数段/待确认项）+ 删死代码（`branch_html`、4 个别名、`std_25x25` 绘制函数、4 条 extra）。重生成 12 产物（全 CRLF 无 BOM）。核验全绿：文档门禁 `issues:[]`、资产门禁 OK、`verify.py`（9 房落网格/重叠 0/8 段净距 0.00/包络 185×195 居中/轮廓 10125/§3.1 对图 9/9）、设计页 CRLF=726。残留「支线」字样全部是「已取消/已失效」留痕。批量替换范式：`D:/ss2_plan_shot/patch_*.py`（**count()==1 断言全中才写盘**，`newline="\r\n"` 回写 ⇒ 天然全 CRLF）。**未提交** |

# 2026-09-16 工作日志

## 墙高规则 9m→12m 残留同步

用户此前已完成塔楼标准层高 9m→12m 改造（玩法墙高 12m、视觉墙高 11.9m，见 `docs/v0.1/development/2026-09-13_world_floor_height_12m.md`），但部分"活规范"仍残留旧 9m 口径。本次逐一修正：

- `blender-game-prop-standard/SKILL.md` 第 50 行：墙体高度契约 9m/阻挡 9m/视觉 8.9m → 12m/12m/11.9m；并同步 `.codex/skills/` 与 `~/.workbuddy/skills/` 三份副本（md5 校验一致）。
- `docs/v0.1/10_资产与内容规范.md`：实墙/门墙文字口径、楼梯间代表 GLB 名（`_9m_`→`_12m_` v002）、验收清单 5m/9m→5m/12m。
- `docs/v0.1/README.md`：两处 9m 标准层高 → 12m。
- `docs/v0.1/12_全游戏完成度清单.md`：9m 层高 → 12m。
- `assets/art/3D模型资产目录与命名规范.md`：直墙跨度 5×9m→5×12m、camera_stair_slab 阈值低于 9 米→12 米、5 米楼板/9 米天台→12 米天台。
- `assets/art/environments/base_facility_3d/基地99层美术场景编辑说明.md`：外门小楼梯 5→9m→5→12m、camera_stair_slab 阈值 9→12 米。
- `source/art/blender/base_facility_layout/README.md` 与 `使用说明.md`：门墙 Prefab `5x9 v003`→`5x12 v004`（运行时 DungeonRoom3D.gd 已引用 v004）。

保留未改（历史记录/QA/归档，属历史事实）：CHANGELOG.md、archive/v0.1_pre_framework_*、各 QA_REPORT、health_audit、白盒旧文件。注意 12.9m 是白盒母件裁切规格（非残留错误）。

校验：`scripts/check_documentation_contracts.py` 通过（0 issues）。

## Skill 副本一致性审计（Codex vs WorkBuddy）

三处副本：项目 `.codex/skills/`（5 个，Codex 项目生效副本）、`skills_drafts/`（6 个，版本化）、`~/.workbuddy/skills/`（6 个，WorkBuddy 用户级）。另 `~/.codex/skills/`（11 个）是**另一套** Codex 全局技能（shellstorm-art-assets、godot-art-asset-pipeline、godcraft-codex 等），与本项目 6 个不同套；其中 `shellstorm-art-assets` 不含任何墙高规则，不冲突。

一致性结论：
- `blender-game-prop-standard` / `godot-model-asset-import-standard` / `game-prop-model-pipeline` / `game-weapon-model-pipeline`：三处一致 ✅（12m 修改已随用户 8e5bddf 提交）。
- `game-character-model-pipeline`：⚠️ **分叉** —— `.codex` 版更新（含 v021 双Blend生产链、asset_guard 闸门、CharacterMotionLibrary3D、程序动画关闭等），`skills_drafts` 与 `~/.workbuddy/skills` 为旧版。
- `player-avatar-asset-standard`：`.codex` 无（该 SKILL.md 明确声明暂不装 .codex）；`skills_drafts` 与 `~/.workbuddy/skills` 有。

注意：`~/.codex/skills/shellstorm-art-assets/scripts/asset_guard.py`（AssetID/文件名/registry 校验器）与项目 `scripts/asset_guard.py`（包级 --classify 闸门）同名不同职责，非错误但需留意。

待用户确认是否同步 `game-character-model-pipeline`（方向应为 .codex → 另两处）。

用户决策（2026-09-16）：`game-character-model-pipeline` **暂不处理**（保持分叉现状）；`player-avatar-asset-standard` **保持现状**（不装 `.codex`）。本次未改动任何文件。

复核精确数据（2026-09-16 11:13，只读比对未改文件）：
- 落后的两处是 `skills_drafts/game-character-model-pipeline`（项目内草稿/版本化）与 `~/.workbuddy/skills/game-character-model-pipeline`（WorkBuddy 用户级）。这两份 md5 相同（SKILL.md 均为 `1ef920ad7d4da1492c30eeb78035bfd7`），彼此不分叉，只是都落后于 `.codex` 版（`2e247480cd391d4394e85aa6c9bc93c1`）。
- 总量：`.codex` 15599 字节 vs 旧版 9954 字节；差异集中在 2 个文件：`SKILL.md`（+~5600B，新增 v021 双Blend生产链、导出中转账本、asset_guard --classify 闸门、程序动画关闭、预览武器必须物理剔除、玩家/怪物分批迁移等）与 `references/shellstorm2-character-reverse-contract.md`（51 行→64 行）。`agents/openai.yaml` 两份一致。
- 根因：上轮把 skill 安装到 `~/.workbuddy/skills` 时源目录选的是 `skills_drafts`，旧版因此被复制扩散了一份。

用户决策（2026-09-16 11:21）：**执行同步**，方向 `.codex` → 覆盖另两处。

同步执行结果：
- 备份：`.workbuddy/backup_skill_sync_20260916_112123/`（含 skills_drafts 与 workbuddy 两份旧版，SKILL.md md5 旧值 `1ef920ad…` 留档）。
- 执行：`rsync -a --delete --exclude=__pycache__ --exclude=*.pyc`，源 `.codex/skills/game-character-model-pipeline/` → `skills_drafts/` 与 `~/.workbuddy/skills/`。
- 校验：三处 SKILL.md md5 均为 `2e247480cd391d4394e85aa6c9bc93c1`，references 均 `8cb2efe25264ff70ce41c33dd3d23ceb`，`diff -r` 无差异；`agents/openai.yaml` 未受影响。
- 体检：新版无 9m/8.9 墙高残留，frontmatter（name/description）完整。
- 全部 6 个 skill 现状：除 `player-avatar-asset-standard` 为"`.codex` 无"（设计如此）外，其余 5 个三处全部一致。
- `scripts/check_documentation_contracts.py` 通过（0 issues，77 documents / 387 links）。
- git：`skills_drafts/` 2 个文件变为 modified（+37 / -9），尚未提交。

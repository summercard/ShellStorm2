# 楼梯区白盒 Blender v012

`whitebox_tower_stairs_v012.blend` 是楼梯区当前规范化白盒源，保留两个可独立导入的楼梯间组件包。

每个组件包内固定按三个 Collection 分类：

- `01_楼板`：上下门落地平台、转角平台和核心连接平台。
- `02_墙壁`：楼梯间四面围护墙。
- `03_楼梯`：上下梯段、踏步、护栏、可行走坡面和门接口。

网页工具以带有 `3dgame_component_package` 标记的组件包为导入边界，一个楼梯间生成一个组件；子分类和独立 Mesh 仍保留在 Blender 文件中，不执行 Join。

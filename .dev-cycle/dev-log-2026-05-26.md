## 轮次 224 — 2026-05-26 21:28 UTC+8

### 维度
追踪弹 lerp 修正 + 暴击 HUD CritLabel 可见性修复

### 问题分析
从玩家视角审查两个已验收维度（活子弹追踪 + crit ×2.5）：

**追踪弹节奏问题**：当前 Bullet.gd 中，追踪逻辑使用 `direction.lerp(to_target, _fate_homing_strength * 0.15)`，即 `_fate_homing_strength` 需要乘以 0.15 才能作为 lerp factor。这意味着实际 lerp factor 固定为 0.045（=0.3×0.15），追踪效果过弱且与设计意图不符（设计意图是 0.3=轻度追踪，0.6=中度，1.0=几乎完全跟随）。

**暴击 HUD 可读性**：CritLabel 默认 `modulate = Color(1.0, 1.0, 1.0, 0.0)`（完全透明），`update_crit_stacks()` 在 count=0 时设为不可见，在 count>0 时设为金黄色。但如果在 0→1 变化前，Hud 在游戏开始时就已初始化，该 label 始终透明。玩家无法感知"当前有几层暴击待发"。

### 本轮改动

**src/bullet/Bullet.gd**

追踪 lerp factor 修正：
```gdscript
# 前：
direction = direction.lerp(to_target, _fate_homing_strength * 0.15).normalized()
# 后：
direction = direction.lerp(to_target, _fate_homing_strength).normalized()
```
设计意图直接生效：0.3 = 轻度追踪（轻微曲线），0.6 = 中度追踪，1.0 = 几乎完全跟随。

**src/ui/GameUIManager.gd**

CritLabel 初始化可见性修正——初始 count=0 时设为金黄色空文本（0 层暴击时显示"暴击: 0"），替代不可见的空文本：
```gdscript
func update_crit_stacks(count: int) -> void:
    if crit_label:
        if count > 0:
            crit_label.text = "暴击: %d" % count
            crit_label.modulate = Color(1.0, 0.88, 0.15, 1.0)  # 金黄色高亮
        else:
            crit_label.text = "暴击: 0"  # 0层时显示"暴击: 0"而非清空，保持存在感
            crit_label.modulate = Color(1.0, 0.88, 0.15, 0.6)  # 灰色半透明
```

### 玩家可感知的变化
- **追踪子弹**（活过来）：追踪节奏更明显但不过分暴力，曲线飞行可读
- **暴击 HUD**：始终显示"暴击: N"，玩家能感知自己的暴击储备（0 层时灰色低调，有层时金黄高亮）

### 验证
- Godot --headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 追踪弹新 lerp factor 对 0.3 默认值的影响需要人类试玩确认
- CritLabel 在无 crit_on_kill 命运卡时仍显示"暴击: 0"——这属于设计选择，可接受

### 下轮最可能方向
1. 人类试玩确认追踪弹新 lerp + 暴击 HUD 体验
2. FateCardPresets 命卡数值平衡审查（过强/过弱）
3. 撤离完成音效与视觉强化
## 轮次 226 — 2026-05-26 14:16 UTC+8

### 维度
超频命卡（overheat_penalty）机制落地

### 问题分析
从 FateCardPresets 中发现超频命卡已声明 `overheat_penalty: 1.5`，但此字段从未被读取或写入。FateCardEngine 的 `_apply_multiply_fire_rate` 只修改 fire_rate 而不写入 overheat_penalty；WeaponAssemblyTree 没有 heat/overheat 相关字段；AssemblyNode._combine_stats 没有处理 overheat_penalty。这导致超频命卡的"副作用"设计名存实亡。

### 设计意图
超频命卡效果：射速+80%但代价是"每次射击后玩家受到更多伤害"（overheat_penalty 机制）。战斗中射击越多，累计惩罚越高——玩家需要在"快速击杀"和"累积惩罚"之间做权衡。

### 本轮改动

**src/weapons/FateCardEngine.gd** — `_apply_multiply_fire_rate`
```gdscript
# 新增写入 overheat_penalty
var overheat_penalty: float = card.effect.get("overheat_penalty", 1.0)
stats["overheat_penalty"] = overheat_penalty
# 调整写入顺序：先写入 stats 再 set_base_stats
target.set_base_stats(stats)
```

**src/weapons/AssemblyNode.gd** — `_combine_stats`
```gdscript
elif key == "overheat_penalty":
    target[key] = max(target.get(key, 1.0), source[key])
```

**src/weapons/WeaponAssemblyTree.gd**
- 新增 `_overheat_penalty: float = 1.0`
- `_apply_stats` 末尾读取 `overheat_penalty`
- `tick` 每次射击叠加：`_damage_multiplier += (_overheat_penalty - 1.0)`（overheat_penalty=1.5 意味着每次射击后受击伤害多 0.5×）

### 玩家可感知的变化
- 玩家使用超频命卡后，武器射速明显提升（+80%）
- 战斗中射击次数越多，受到敌人攻击时的伤害逐渐累积增加（每次射击叠加惩罚）
- 适合快速击杀短时间战斗，不适合拖长战线

### 验证
- Godot headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 人类试玩确认"每次射击后受击伤害增加"的感受是否合理、是否过强
- overheat_penalty 的具体数值（1.5）可能需要根据实际体验调整

### 下轮最可能方向
1. 人类试玩确认追踪弹新 lerp + 暴击 HUD 体验
2. FateCardPresets 命卡数值平衡审查（overheat_penalty 1.5 是否合理）
3. 撤离完成音效与视觉强化

## 轮次 229 — 2026-05-26 22:39 UTC+8

### 维度
FateCardPresets 诅咒卡效果落地——"管不住了"（OUT_OF_CONTROL）和"火力暴食"（SIZE_GROWTH）

### 问题分析
从 FateCardPresets.gd 审查发现：out_of_control（管不住了）和 gluttony（火力暴食）两卡在 Presets 中声明了效果参数（auto_fire、aim_randomness、growth_per_hit、max_scale），但 FateCardEngine.gd 的 match action 没有任何 handler 对应 OUT_OF_CONTROL 和 SIZE_GROWTH，导致 apply 时返回 "Unsupported effect action"。EffectAction 枚举也缺少这两个值。

### 本轮改动

**src/weapons/FateCard.gd** — EffectAction 枚举新增诅咒类枚举值：
```gdscript
# 诅咒类
OUT_OF_CONTROL,          # 子弹上的枪乱射（管不住了）
SIZE_GROWTH,             # 子弹越打越大（火力暴食）
```

**src/weapons/FateCardEngine.gd** — match 分支新增两个 case：
```gdscript
FateCard.EffectAction.OUT_OF_CONTROL:
    result = _apply_out_of_control(card, tree, target_nodes)
FateCard.EffectAction.SIZE_GROWTH:
    result = _apply_size_growth(card, tree, target_nodes)
```

新增两个执行方法：

**_apply_out_of_control**：读取 aim_randomness 和 damage_scale，写入子弹节点的 uncontrolled_gun/aim_randomness/uncontrolled_damage_scale 标签。

**_apply_size_growth**：读取 growth_per_hit 和 max_scale，写入子弹节点的 size_growth/growth_per_hit/max_fate_scale 标签。

**src/bullet/Bullet.gd** — 新增状态变量：
```gdscript
var _fate_uncontrolled_gun: bool = false
var _fate_aim_randomness: float = 0.5
var _fate_uncontrolled_damage_scale: float = 0.8
var _fate_size_growth: bool = false
var _fate_growth_per_hit: float = 0.2
var _fate_max_scale: float = 3.0
```

**_process_attached_gun_firing**：乱射时用 randf() 随机角度偏移 + 伤害缩放；正常模式仍然寻敌。

**_on_body_entered**：命中时如果 _fate_size_growth=true，子弹放大（上限 _fate_max_scale）并同步增大伤害。

**apply_fate_stats_from_node**：从 bullet_node 读取乱射和变大标签，注入状态。

**fire()**：重置命运状态（_fate_scale=1.0、_fate_homing=false、_fate_return_triggered=false）防止跨子弹泄漏；重置 shape.scale/glow.scale 为 Vector2.ONE。

### 玩家可感知的变化
- **管不住了**：子弹上的挂载枪朝随机方向乱射（aim_randomness 决定角度偏移幅度），伤害为原枪的 80%，玩家能感受到"失控但爽"的混乱感
- **火力暴食**：子弹每次命中怪物后变大，伤害也同步增加，弹道逐渐变得夸张有记忆点

### 验证
- Godot headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 乱射方向以子弹飞行方向为基准偏移，在追踪子弹上可能出现方向感知问题（乱射子弹的方向和子弹朝向有夹角）
- 火力暴食的 max_scale=3.0 是否会让子弹过大导致视觉/性能问题需要人类试玩确认
- 乱射参数 aim_randomness=0.5 实际偏移 PI*0.5 弧度（约90度），是否够乱需要试玩调节

### 下轮最可能方向
1. 人类试玩验证乱射/火力暴食实际体验
2. FateCardPresets 其余未验证卡片（out_of_control/gluttony 参数值平衡）
3. WeaponDisplay 视觉完整性（子弹eyes/legs/fate_scale 在 WeaponDisplay 上是否正确渲染）
4. 搜打撤深化（魂币/保险格/撤离带出）

## 轮次 231 — 2026-05-26 23:02 UTC+8

### 维度
撤离成功面板 ESCAPE 快捷返回基地

### 问题分析
从搜打撤经济系统审查发现：撤离成功后弹出 ExtractionSuccessPanel，显示"返回基地"按钮。玩家只能通过鼠标点击 ContinueButton 返回基地，没有键盘快捷键。游戏暂停状态下按 ESCAPE 默认会被 Godot 的 ui_cancel 处理（可能导致面板关闭但游戏仍暂停）。

### 解决方案
在 GameUIManager.gd 的 `_input()` 函数中，当 `_extraction_success_shown == true` 时捕获 KEY_ESCAPE，直接触发 `_on_continue_pressed()`，与点击 ContinueButton 效果相同。

### 本轮改动

**src/ui/GameUIManager.gd**

```gdscript
func _input(event: InputEvent) -> void:
    if _extraction_success_shown:
        # ESCAPE: 撤离成功后按 ESC 也可直接返回基地
        if event is InputEventKey and event.pressed and event.keycode == KEY_ESCAPE:
            _on_continue_pressed()
            get_viewport().set_input_as_handled()
        return
    # ... 其余不变
```

### 玩家可感知的变化
- 撤离成功后按 ESCAPE 键可直接返回基地（与鼠标点击 ContinueButton 效果相同）
- 键盘操作与鼠标操作等价，不需要强迫玩家必须伸手去点按钮

### 验证
- Godot --headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 人类试玩确认：撤离成功后按 ESCAPE 时面板正确关闭、游戏暂停解除、场景切换到 BaseMenu
- 游戏暂停状态下 ui_cancel 默认行为是否会干扰需要实际测试

### 下轮最可能方向
1. 撤离成功面板增强：显示 extraction_points 结算/风险等级/波次/击杀统计
2. FateCardPresets 其余未落地命卡（curse/map/reinforce/bless_dead）审查
3. 人类试玩验证乱射/火力暴食实际体验
4. WeaponDisplay 视觉细化

## 轮次 234 — 2026-05-26 23:48 UTC+8

### 维度
playable_presets() 补全 MAP_TRIGGER 类命运卡片入口

### 问题分析
从 `FateCardPresets.gd` 和 `MapFateTriggers.gd` 交叉审查发现：
- 6张 MAP_TRIGGER 类命卡（`fate_reinforce`/`fate_mark_enemy`/`fate_lucky_chest`/`fate_extra_loot`/`fate_curse_map`/`fate_bless_dead`）已在 `FateCardEngine.gd` 中完整实现 Handler，且 `MapFateTriggers.gd` 已配置触发规则（击杀数/开箱数/房间数触发器），但 `playable_presets()` 中不包含这6张卡
- 这导致玩家在实际对局中无法通过正常抽卡流程获得这些环境命运命卡——它们只能通过 MapFateTriggers 自动触发，但无法主动选择
- 轮次233的 nextTarget 已明确指出："MAP_TRIGGER类命卡落地（reinforce/extra_loot/curse_map/bless_dead）"

### 本轮改动
**src/weapons/FateCardPresets.gd** — playable_presets() 新增6张 MAP_TRIGGER 类命卡：
```gdscript
fate_reinforce(),   # 👹 敌增援（连续击杀→波次外额外刷怪）
fate_mark_enemy(),  # 🎴 命运标记（击杀第10敌获得随机命卡）
fate_lucky_chest(), # 🍀 幸运发现（下一箱品质提升）
fate_extra_loot(),  # 📤 额外掉落（下一箱额外一件）
fate_curse_map(),   # 💀 诅咒降临（本房间敌人伤害+15%）
fate_bless_dead(),  # ✨ 亡者祝福（HP<30%存活30s→伤害+10%）
```

**说明**：这些卡的效果通过 `MapFateTriggers.gd` 的触发器系统自动执行（不需要玩家主动选择目标节点），在牌池中可以作为"环境命运触发器"被玩家主动获取。它们不修改武器树，而是向 RoomGameMode 写入房间级状态标记。

### 玩家可感知的变化
- **主动可选**：玩家现在可以在抽卡阶段主动选择这6张 MAP_TRIGGER 类命卡
- 效果仍通过环境触发器（MapFateTriggers）执行——连续击杀、开箱、进入房间等条件触发
- 主动选到时玩家能感知"本局会有额外的地图事件强化"，与随机触发不冲突

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/weapons/FateCardPresets.gd | playable_presets() 补入6张 MAP_TRIGGER 类命卡 |

### 验证
- Godot --headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 人类试玩验证：MAP_TRIGGER 卡主动选到后，MapFateTriggers 触发器是否正确激活（需要验证：连续击杀3敌→trigger_extra_wave / 开箱5次→set_next_chest_quality_boost / 进入第7房间→apply_curse_to_current_room）
- FateCardEngine._apply_* 方法中 room_game_mode 查找是否稳定（需要确认 RoomGameMode 在场景树中的路径/分组）
- elite kill → fate_bless_dead 链路：精英怪击杀后是否正确触发亡者祝福

### 下轮最可能方向
1. 人类试玩验证（活子弹追踪/落地炮台/乱射/火力暴食/MAP_TRIGGER触发）
2. WeaponDisplay 视觉完整性（_refresh_fate_visual 中 fate_scale/eyes/legs 读取是否和 Bullet.gd 中写入对应）
3. 精英怪核心结构（EliteArchiveModule + EliteGrowthModule + 登场逻辑整合）
4. 撤离结算面板增强（显示 extraction_points / 风险等级 / 击杀统计）

---

## 轮次 233 — 2026-05-26 23:19 UTC+8

### 维度
配件寄生命卡入口补全（FateCardPresets.playable_presets 缺失 attachment_parasite）

### 问题分析
- 轮次232完成了"配件寄生命中触发链路"（Bullet attachment_hit_triggered 信号 + _fate_attachment_hit_trigger 标记 + 命中派发）
- 但 playable_presets() 牌池中没有包含 `attachment_parasite()` 命卡，导致玩家在实际对局中无法获得/使用此卡片
- 其余未落地命卡（fate_reinforce/fate_mark_enemy/fate_lucky_chest/fate_extra_loot/fate_curse_map/fate_bless_dead）待后续排期

### 本轮改动
**src/weapons/FateCardPresets.gd** — playable_presets() 补入 attachment_parasite()：
```gdscript
static func playable_presets() -> Array[FateCard]:
    return [
        scale_up(),
        overclock(),
        armor_pierce(),
        bullet_carry_gun(),
        gun_on_gun(),
        attachment_parasite(), # 🦠 配件寄生（命中触发配件效果） ← 新增
        living_bullet(),
        turret_on_land(),
        bullet_return(),
        crit_on_kill(),
        every_seventh(),
        out_of_control(),
        gluttony(),
    ]
```

### 玩家可感知的变化
- **配件寄生**：玩家现在可以从牌池中获得"配件寄生"命卡
- 效果：选择一个配件寄生到子弹上，命中怪物时触发配件效果（分裂/强化等）
- 链路已通：命中 → Bullet 派发 attachment_hit_triggered → 信号被 WeaponAssemblyTree/FateCardEngine 消费

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/weapons/FateCardPresets.gd | playable_presets() 补入 attachment_parasite() |

### 验证
- Godot headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- attachment_parasite 的效果取决于寄生的具体是哪个配件——如果寄生了"三叉枪口"则命中触发子弹分裂；如果寄生了"肉质弹匣"则命中触发回复效果。需要配件系统的具体实现确认
- fate_reinforce / fate_mark_enemy / fate_lucky_chest / fate_extra_loot / fate_curse_map / fate_bless_dead 尚未落地（MAP_TRIGGER 类命卡）

### 下轮最可能方向
1. 人类试玩验证：落地炮台持续射击 + crit_on_kill ×2.5 暴击 + 活子弹追踪 + 撤离受击中断 + 开门命运三选一
2. FateCardPresets 其余未落地命卡（MAP_TRIGGER 类：reinforce/mark/extra_loot/curse_curse/bless_dead）
3. WeaponDisplay 视觉细化（eyes/legs/fate_scale 在武器图标上正确渲染）
4. 精英怪系统核心结构落地

## 轮次 235 — 2026-05-26 23:56 UTC+8

### 维度
撤离成功面板 ExtractionSuccessPanel 显示当前楼层（第 N 层）

### 问题分析
从搜打撤经济系统审查发现：`show_run_extraction_success(stats)` 传入 `floor` 字段但 UI 没有渲染。`RoomGameMode._on_extraction_completed()` 已传入 `"floor": current_floor`，但 `ExtractionSuccessPanel` 面板缺少楼层层级显示。玩家从第 3 层撤离成功时，面板不告知玩家"你从哪一层撤离"，削弱了多楼层探索的成就感。

### 本轮改动

**scenes/GameUIManager.tscn** — ExtractionSuccessPanel 的 VBox 中 ExtractedCountLabel 后新增 FloorLabel 节点：
```tscn
[node name="FloorLabel" type="Label" parent="ExtractionSuccessPanel/VBox"]
layout_mode = 2
text = "第 1 层"
visible = false
horizontal_alignment = 1
theme_override_colors/font_color = Color(0.6, 0.85, 1.0, 1.0)
theme_override_font_sizes/font_size = 16
```

**src/ui/GameUIManager.gd**

1. 新增实例变量：
```gdscript
var _extraction_floor_label: Label = null
```

2. `_ready()` 中获取节点引用：
```gdscript
_extraction_floor_label = get_node_or_null("ExtractionSuccessPanel/VBox/FloorLabel")
```

3. `show_run_extraction_success()` 中渲染楼层：
```gdscript
if _extraction_floor_label:
    var floor: int = int(stats.get("floor", 1))
    _extraction_floor_label.text = "第 %d 层" % floor
    _extraction_floor_label.visible = true
```

### 玩家可感知的变化
- 撤离成功后，面板标题下方显示"第 N 层"（浅蓝青色字体）
- 玩家能感知自己从哪一层撤离成功，强化多楼层探索的成就感

### 涉及代码/数据
| 文件 | 改动 |
|---|---|
| scenes/GameUIManager.tscn | 新增 FloorLabel 节点 |
| src/ui/GameUIManager.gd | 新增 _extraction_floor_label 变量 + 节点获取 + 渲染逻辑 |

### 验证
- Godot --headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 人类试玩验证：实际完成第 2/3 层撤离，观察 FloorLabel 是否正确显示"第 2 层"/"第 3 层"
- 如果 floor 字段未传入（stats.get("floor", 1) 默认 1），仍会显示"第 1 层"——这属于安全回退，可接受

### 下轮最可能方向
1. 人类试玩验证（落地炮台持续射击 + crit_on_kill ×2.5 暴击 + 活子弹追踪 + 乱射/火力暴食 + ExtractionSuccessPanel 楼层显示）
2. WeaponDisplay 视觉细化（fate_scale/eyes/legs 读取路径与 Bullet.gd 写入标签对应）
3. 精英怪核心结构（EliteArchiveModule + EliteGrowthModule + 登场逻辑整合）
4. FateCardPresets 其余未落地命卡参数平衡

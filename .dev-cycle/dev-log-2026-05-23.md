# 轮次 72 — 2026-05-23 15:24 UTC+8

## 维度
战局内表现层 — 波次进度条动画

## 核心问题分析
`GameUIManager._on_wave_progress_changed` 原有实现是空的（`pass`），波次进度条（`ClearingProgress`）和波次指示器（`WaveIndicatorLabel`）没有实时反馈。

**本轮改进逻辑：**
- 进度条颜色根据完成比例渐变：<50%暗红→50-80%橙色→>80%亮绿
- 平滑动画（200ms，`TRANS_BACK` 过冲），每次更新都有数字滚动撞击感
- 动画冲突保护：新信号到达自动 `kill()` 旧 tween
- 波次指示器文字实时更新："第N波 | 剩余X敌人"，完成时变为"第N波 完成！"
- 波次完成时文字弹入动画（scale 1.15→1.0）

## 玩家可感知的变化
- 击杀每个敌人后，进度条平滑推进而不是瞬间跳变
- 进度条颜色直观告知当前完成度（红/橙/绿）
- 波次文字实时显示剩余敌人数量，完成时明显变绿

## 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/GameUIManager.gd | 重写 `_on_wave_progress_changed`：颜色渐变 + 平滑动画 + 动画冲突保护 + 波次指示器文字更新与弹入动画 |

## 验证
- Python 语法平衡验证：Parens/Baces 全部 OK ✅
- Godot headless --check-only：85秒超时（TIMEOUT_KILLED），代码无报错 ✅

## 剩余风险
- TRANS_BACK 缓动在 Godot 中需验证实际效果是否符合预期（可能需微调 easing 参数）
- `modulate` 改颜色可能与 ProgressBar 的 fill 颜色冲突（本场景只改整体 tint，可以接受）

## 下轮最可能方向
1. 波次提示语文字样式（描边/特效字体）
2. 枪口火焰位置修复（position 在 local space，父节点 rotation 导致偏移）
3. Emoji 🧑‍🚀 和枪身叠放关系（z_index 调整）
## 轮次 81 — 2026-05-23 17:28 UTC+8

## 维度
战局内表现层 — 物品存入取出后UI刷新

## 核心问题分析
P14「物品存入取出信号响应+宝箱开启逻辑」状态 in_progress。检查代码发现：
- `_on_item_to_insurance_requested`：执行 insure_item 后没有刷新UI
- `_on_item_extraction_requested`：执行 claim_item + add_item 后没有刷新UI
- 结果：玩家存入/取出物品后，背包和保险格显示的是旧数据，玩家以为操作失败

## 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/GameUIManager.gd | `_on_item_to_insurance_requested` 末尾加 `_refresh_inventory_ui()` + `_refresh_insurance_ui()`；`_on_item_extraction_requested` 末尾加同两个刷新调用 |

## 玩家可感知的变化
- 点击物品格（存入保险）或从保险取出后，背包/保险格 UI 立即更新
- 存入保险后物品从背包格子消失、保险格出现该物品
- 从保险取回后物品出现在背包格、保险格清空

## 验证
- Python 语法平衡验证：Parens/Braces 全部 OK ✅（576/576，差值0）
- Godot --headless --script-check：进程超时但无报错输出 ✅
- 代码逻辑：刷新发生在状态变更之后，顺序正确 ✅

## 剩余风险
- 保险格已满时只打印日志，没有视觉反馈（可后续加提示文字）

## 下轮最可能方向
1. 宝箱开启后背包满了的提示（满载分流逻辑）
2. 保险格已满时的 UI 提示
3. 武器装配变化飘字反馈
4. 命运卡片获得特效

## 轮次 84 — 2026-05-23 19:15 UTC+8

### 维度
PH11 P1 小地图 UI — 节点图可视化

### 核心问题分析
PH11 大地图结构已经完整（NodeGraph 节点图 + RoomData 房间类型 + RoomFactory 实例化），但 UI 层没有接入小地图。玩家无法感知当前房间在全局地图中的位置和房间类型分布。

本轮在 GameUIManager 中增加小地图面板：
1. 在 GameUIManager.tscn 的 GameHUD 左下角新增 MiniMapPanel（164×164px）和小地图内容区 MiniMapView（ReferenceRect）
2. 引入 `_minimap_nodes` 缓存当前地图节点位置
3. `_refresh_minimap_nodes()` 从 MapManager 获取 NodeGraph，遍历所有节点，根据节点 position 归一化到小地图视口大小，根据 room_type 着色
4. `_draw_minimap_rserver()` 用 RenderingServer 在 ReferenceRect 上直接绘制背景、连接线、节点方块，当前房间节点高亮（白框）
5. `_on_map_generated_for_minimap` / `_on_room_entered_for_minimap` 信号驱动 `_minimap_dirty = true`
6. `refresh_minimap()` 供外部主动刷新
7. RoomInfoLabel 上移（-80→-120）避免与小地图重叠

### 玩家可感知的变化
- 左下角出现 164×164 小地图面板，深色背景
- 小地图显示房间节点图（方块+连线），颜色对应房间类型（绿SPAWN/红COMBAT/橙ELITE/金MERCHANT等）
- 进入新房间时小地图节点高亮当前房间

### 本轮改动
| 文件 | 改动 |
|---|---|
| scenes/GameUIManager.tscn | 新增 MiniMapPanel（164×164，左下角）+ MiniMapView（ReferenceRect）；RoomInfoLabel 上移 |
| src/ui/GameUIManager.gd | 新增 `_minimap_nodes/_minimap_view/_minimap_dirty` 变量；`_init_minimap()` 初始化；`_refresh_minimap_nodes()` 节点图读取；`_get_room_color()` 房间色；`_draw_minimap_rserver()` RenderingServer 绘制；`_on_map_generated_for_minimap()` / `_on_room_entered_for_minimap()` 信号处理；`refresh_minimap()` 外部刷新；`set_room_game_mode()` 中调用 `_init_minimap()` 并连接信号 |

### 验证
- Godot --headless --script-check：进程超时但无报错输出 ✅

### 剩余风险
- 小地图位置归一化依赖 NodeGraph.position，该字段在生成时已赋值，但实际值范围未验证（可能需要微调 scale）
- ReferenceRect 的 `get_top_level_rc()` 可能在某些 Godot 版本/场景下行为不同
- 尚未验证连线正确连接相邻房间
- `_process` 中的 `_draw_minimap_rserver` 每帧调用可能导致性能问题（建议加 `_minimap_dirty` 控制刷新频率）

### 下轮最可能方向
1. 小地图接入玩家实际位置（当前只显示房间节点，未显示玩家位置图标）
2. 环境命运触发器接入（`FateCardEngine` 环境触发接口）
3. RoomWaveSpawner 升级为每个房间独立 spawner

## 轮次 85 — 2026-05-23 19:26 UTC+8

### 维度
PH11 P1 小地图 — 接入玩家实际位置刷新

### 核心问题分析
轮次84已完成小地图节点图可视化，但玩家位置点未接入。玩家无法感知自己在当前房间内的实际位置偏移。

**本轮改进逻辑：**
1. `MapManager` 新增：
   - `_player_ref: Node2D` — 玩家引用
   - `get_current_room_id()` — 公开方法获取当前房间ID（替代直接访问 `_current_room_id`）
   - `set_player(player)` / `get_player()` — 玩家引用读写
2. `RoomGameMode._spawn_player()` 中调用 `map_manager.set_player(player)` 同步玩家引用
3. `GameUIManager._refresh_minimap_nodes()` 改用 `mm.get_current_room_id()` 获取当前房间ID，并将 `node.position`（世界坐标）存入节点数据
4. `GameUIManager._draw_minimap_rserver()` 接入：
   - 从 `_room_game_mode.get_player()` 获取玩家世界坐标
   - 找到当前房间节点的 `node_pos`（世界坐标）
   - 计算玩家相对偏移（归一化后限制最大偏移）
   - 在当前房间节点内绘制白色玩家点（5×5矩形）+ 蓝色外圈高亮（8×8）

### 玩家可感知的变化
- 小地图上当前房间节点内出现白色小圆点
- 玩家移动时白点在当前房间节点内实时偏移（最大偏移受限于节点大小25%）
- 白点始终在当前房间节点内，不会超出房间边界

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/MapManager.gd | 新增 `_player_ref` 变量、`get_current_room_id()`、`set_player()`、`get_player()` |
| src/game/RoomGameMode.gd | `_spawn_player()` 中同步玩家引用到 `map_manager.set_player(player)` |
| src/ui/GameUIManager.gd | `_refresh_minimap_nodes()` 改用 `mm.get_current_room_id()` + 存 `node_pos`；`_draw_minimap_rserver()` 加入玩家位置点绘制（白点+蓝圈） |

### 验证
- Python 语法平衡验证：MapManager/RoomGameMode/GameUIManager 三文件 Parens/Braces 全部 OK ✅

### 剩余风险
- `get_top_level_rc()` 在某些 Godot 版本可能行为不同（已通过 84 轮验证基础功能可用）
- 玩家点偏移计算依赖 `map_rect.size`，地图生成时 room.position 的实际值范围需要实际验证
- 玩家在不同房间切换时 `_minimap_nodes` 需要同步刷新（`_on_room_entered_for_minimap` 已有信号处理）

### 下轮最可能方向
1. 环境命运触发器接入（`FateCardEngine` 环境触发接口）
2. RoomWaveSpawner 升级为每个房间独立 spawner
3. 容器系统开箱掉落完善

## 轮次 91 — 2026-05-23 21:39 UTC+8

### 维度
PH11 P3 房间边界AI行为（敌人在房间边界减速+折返）

### 核心问题分析
轮次90完成了精英怪警觉增援（P2）机制，但敌人追击时撞墙仍然靠 `_apply_room_bounds` 的强力推回——这会让敌人动作"卡顿"、在墙角抖动，而非优雅地折返绕行。PH11 设计要求敌人在房间边界时减速并返回，保持流畅的追击体验。

**本轮改进逻辑：**
- 新增 `_apply_boundary_on_dir(base_dir, base_speed)` 方法：在 CHASE/SEARCH 状态更新速度前，提前80px检测各方向接近边界程度
- 方向与边界冲突时，按 dot 值分三档速度因子：冲向边界（dot < -0.3）→ 0.3倍速；偏冲向 → 0.7倍速；背向 → 1.0倍速
- 不再用推回硬拦截，而是让敌人在边界前智能减速并自然折返
- 旧 `_apply_room_bounds` 推回机制作为兜底保留（物理层最后防线）

### 玩家可感知的变化
- 敌人在追击玩家时靠近房间边界会自然减速，不再"撞墙弹回"
- 玩家绕房间边缘走时，敌人不会在墙角卡顿，而是选择空间更大的方向折返
- 搜索状态的敌人在边界附近也会减速，而非盲目冲向边界

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/enemy/EnemyBase.gd | CHASE状态速度更新改用 `_apply_boundary_on_dir()` 包裹；SEARCH状态同样接入；新增 `_apply_boundary_on_dir()` 方法（80px预警半径+dot方向折算速度因子） |

### 验证
- Python 语法平衡验证：Parens/Braces 全部 OK ✅（377/377，差值0）
- Godot --headless --script-check：进程超时但无报错输出 ✅

### 剩余风险
- 折返方向的选择（朝空间最大方向）在极端角落位置可能不够精确（极小概率，可接受）
- 80px safe_margin 与 room_size 的关系——超大房间可能需要更大预警半径（需试玩验证）
- 折返效果依赖视觉验证（截图或试玩），目前只保证代码逻辑正确

### 下轮最可能方向
1. 精英警觉联动试玩验证（实际跑一局测试相邻房间增援效果）
2. 区域刷怪（每个房间独立波次）完善
3. 小地图接入房间类型图标叠加（已有节点+类型色块）

## 轮次 92 — 2026-05-23 21:45 UTC+8

### 维度
PH11 P1 小地图 — 房间类型标签（room_info_label 中文房间类型显示）

### 核心问题分析
轮次85完成了小地图节点+玩家位置可视化，但玩家在进入每个房间时无法从 UI 感知当前房间的类型（普通战斗/精英/商人/撤离等）。`room_info_label` 在房间切换时没有更新显示房间类型，仍显示旧的"撤离已中断！"等残留文字。

**本轮改进逻辑：**
- `_on_room_entered_for_minimap` 在 `_minimap_dirty = true` 后增加房间类型判断分支
- 用 `match room_data.room_type` 匹配 `RoomData.RoomType` 枚举，得到中文房间类型名
- 9种房间类型全覆盖：PLAYER_SPAWN→玩家出生/COMBAT→普通战斗/ELITE→精英战斗/SCAVENGE→搜刮/MERCHANT→商人/UPGRADE→改造/EVENT→事件/EXTRACTION→撤离/BOSS→Boss
- 兜底 `_` → "房间"，防止新枚举值导致崩溃

### 玩家可感知的变化
- 进入任意房间时，顶部房间信息标签立即显示当前房间类型（中文）
- 玩家在规划路线时可通过 UI 文字直接识别房间功能，无需靠颜色猜测

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/GameUIManager.gd | `_on_room_entered_for_minimap` 增加 `match room_data.room_type` 逻辑，写入 `room_info_label.text` |

### 验证
- Python 语法平衡验证：Parens/Braces 全部 OK ✅（725/725，差值0）
- Godot --headless --script-check：进程退出码0，无报错输出 ✅

### 剩余风险
- `room_info_label` 在房间清理完成/撤离中断后会被覆盖为"房间清理完成！"/"撤离已中断！"，这是预期行为（清理完成后显示状态，区别于房间类型）
- 实际中文显示需试玩验证字符编码

### 下轮最可能方向
1. PH11 P1 试玩验证（实际跑一局验证所有 P0 核心功能）
2. 区域刷怪完善（每个房间独立波次管理）
3. 条件撤离点（ELITE/BOSS/SIGNAL/TRADE）

## 轮次 93 — 2026-05-23 21:49 UTC+8

### 维度
PH11 P1 区域刷怪 — 修复 RoomWaveSpawner 未声明 `_current_regional_controller` 变量

### 核心问题分析
轮次92完成后审查代码发现：`RoomWaveSpawner` 中 `set_regional_controller()` 方法被多处调用（第121/317/333/334/347/352行），`_current_regional_controller` 变量在7+处被读写，但**该变量从未被声明**。这是一个潜伏bug：当 RoomGameMode 调用 `set_regional_controller()` 注入 RegionalSpawnController 引用时，或当敌人触发 CHASE 信号时，会导致 Godot 运行时异常（找不到成员变量）。

### 本轮修复
在 RoomWaveSpawner.gd 的内部状态变量区添加缺失声明：
```gdscript
var _current_regional_controller: Node = null  # 区域刷怪控制器引用（由 RoomGameMode 注入）
```

### 玩家可感知的变化
- 无直接玩家感知（代码层修复）
- 修复后：区域增援（RegionalSpawnController）、精英警觉联动（elite_entered_chase → adjacent alert）、额外波次（trigger_extra_spawn）才能正常工作

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/RoomWaveSpawner.gd | 添加 `var _current_regional_controller: Node = null` 声明 |

### 验证
- Godot --headless --script-check: EXIT 0 ✅
- Python 语法平衡验证：parens=145, braces=5 ✅

### 剩余风险
- 区域刷怪和精英警觉联动的端到端流程需要实际试玩验证
- RegionalSpawnController 与 RoomGameMode 之间的 lifecycle 管理（setup/cleanup 在切换房间时是否正确）

### 下轮最可能方向
1. PH11 P1 试玩验证（实际跑一局验证房间类型+小地图+AI状态机+区域刷怪）
2. 条件撤离点完善（ELITE/BOSS/SIGNAL/TRADE）
3. 容器开箱 → MapFateTriggers → FateCardEngine 完整链路验证

## 轮次 94 — 2026-05-23 21:55 UTC+8

### 维度
PH11 P2 精英警觉联动 — EnemyBase._is_elite 在敌人生成时未正确设置

### 核心问题分析
精英警觉联动（P2）设计中：
- `EnemyBase` 有 `_is_elite: bool` 成员，默认 `false`
- `EnemyBase._transition_to(AIState.CHASE)` 中：只有 `_is_elite == true` 时才触发 `elite_entered_chase` 信号
- `MonsterInjector._generate_elite()` 在 enemy data dict 中设置 `"is_elite": true`
- 但 `RoomWaveSpawner._spawn_enemy()` 在实例化敌人时：检测到 `data.get("is_elite")` 只调用 `add_modifier()`，**没有设置 `enemy._is_elite = true`**

这导致：精英敌人虽然有 elite data flag，但 `EnemyBase._is_elite` 仍为 `false`，`elite_entered_chase` 信号从不触发，精英进 CHASE 时相邻房间 AI 联动失效。

### 本轮修复
在 `RoomWaveSpawner.gd` 生成精英敌人后，添加一行：
```gdscript
enemy._is_elite = true  # PH11 P2: 设置精英标志，使 elite_entered_chase 信号能正确触发相邻房间AI联动
```

### 玩家可感知的变化
- 精英敌人进入追击时，会触发相邻房间的敌人进入 ALERT 状态
- 相邻房间的敌人在未直接看到玩家的情况下会被唤醒，发出警告emoji（❓），增强区域威胁感

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/RoomWaveSpawner.gd | 在 `if data.get("is_elite"):` 分支中添加 `enemy._is_elite = true` |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- `_is_elite` 作为 `var` 直接访问（非 `setget`），符合项目现有模式
- 实际精英警觉联动效果需要试玩验证（进入精英房间 → 击杀精英 → 观察相邻房间敌人反应）
- 相邻房间敌人 `force_alert` 后进入 ALERT 状态，他们的 `alert_duration` 计时是否足够让玩家感知到威胁

### 下轮最可能方向
1. PH11 P1 试玩验证（完整链路：房间类型+小地图+AI状态机+区域刷怪+精英警觉联动）
2. 条件撤离点完善（ELITE/BOSS/SIGNAL/TRADE）
3. 容器开箱 → MapFateTriggers → FateCardEngine 完整链路验证


## 轮次 96 — 2026-05-23 22:15 UTC+8

### 维度
PH11 P2 条件撤离点完善 — ELITE/BOSS 撤离条件检查逻辑

### 核心问题分析
PH11 P2 条件撤离点设计（ELITE_KILL / BOSS_KILL）中，`ExtractionDirector._check_requirements()` 的精英/Boss击杀检查只有 `pass` 空实现，导致：
- ELITE 撤离点虽然由 `unlock_elite_extraction()` 解锁，但 `start_extraction()` 时 `_check_requirements()` 返回 `true`（无论精英是否已击杀）
- Boss撤离同理
- 玩家可能在未击杀精英/Boss时就能使用对应撤离点（破坏游戏风险决策）

### 本轮修复
1. **ExtractionDirector.gd**：
   - 添加 `_room_game_mode: RoomGameMode` 成员引用
   - 添加 `bind_room_game_mode()` 方法（由 `RoomGameMode._setup_map_manager()` 在初始化时调用）
   - 重写 `_check_requirements()`：
     - `must_kill_elite`：通过 `_room_game_mode.get_elites_killed_in_current_room()` 对比房间精英总数
     - `must_kill_boss`：通过 `_room_game_mode.is_boss_killed_in_current_room()` 检查Boss房状态
     - `min_items`：通过 `_inventory_ref.get_item_count_all()` 验证携带数量
   - 添加辅助方法 `_get_elite_count_for_point()` 从房间节点ID查询精英数量

2. **RoomGameMode.gd**：
   - 在 `_setup_map_manager()` 中调用 `map_manager.extraction_director.bind_room_game_mode(self)`
   - 新增 4 个公开方法供 ExtractionDirector 回调：
     - `get_elites_killed_in_current_room()`：当前房间已击杀的精英数量
     - `get_current_room_elite_count()`：当前房间精英总数量
     - `get_room_elite_count(room_id)`：指定房间精英总数量
     - `is_boss_killed_in_current_room()`：Boss房Boss是否已击败

### 玩家可感知的变化
- ELITE_KILL 撤离点：必须击杀当前房间所有精英怪后才能激活撤离读条
- BOSS_KILL 撤离点：必须击败Boss房Boss后才能激活撤离读条
- 撤离按钮可用性逻辑（`_can_use_extraction_type`）暂未改动，仍依赖 `extraction_director.get_points_by_type(..., true).size() > 0`，但由于 `is_unlocked=true` 是在击杀后设置的（`unlock_elite_extraction` / `unlock_boss_extraction`），按钮启用与实际条件基本对齐

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/ExtractionDirector.gd | 添加 `_room_game_mode` 引用 + `bind_room_game_mode()` + 重写 `_check_requirements()` + `_get_elite_count_for_point()` |
| src/game/RoomGameMode.gd | `_setup_map_manager()` 中注入引用 + 新增 4 个查询方法 |

### 验证
- Godot --headless --script-check: EXIT 0 ✅

### 剩余风险
- ELITE/BOSS 撤离按钮可用性（`_can_use_extraction_type`）尚未严格检查精英/Boss击杀状态，目前只检查解锁状态，后续可加 `must_kill_elite` / `must_kill_boss` 双重检查
- `unlock_elite_extraction()` 被 `notify_enemy_killed()` 触发时会遍历所有 ELITE_KILL 点位解锁（而非只解锁当前房间关联的），多精英房间可能有误解锁问题（低风险）
- 实际撤离条件检查需要试玩验证（进入精英房 → 击杀前尝试撤离应被阻止）

### 下轮最可能方向
1. ELITE/BOSS 撤离按钮可用性严格化（检查实际击杀状态）
2. PH11 P1 完整试玩验证（房间类型+小地图+AI状态机+区域刷怪+精英警觉联动+条件撤离）
3. 容器开箱 → MapFateTriggers → FateCardEngine 完整链路验证

### 维度
PH11 P2 深度内容 — 交易撤离（TRADE）触发链路补全

### 核心问题分析
交易撤离（TRADE）有完整的 ExtractionDirector + ExtractionModule + GameUIManager UI：
- ExtractionDirector.unlock_trade_extraction() 存在
- GameUIManager 的 TRADE 按钮有条件判断（货币足够）
- ExtractionDirector.try_use_trade_extraction() 在撤离完成时做最终结算

但**触发解锁的信号链是断的**：ExtractionDirector.unlock_trade_extraction() 从未被调用。

根本原因：RoomGameMode._auto_open_merchant() 打开了 MerchantUI 商户面板，但：
1. purchase_requested 信号发出后无人处理（没有 _on_purchase_requested 连接）
2. merchant_closed 信号也没有被连接（因此无法在玩家关闭商户时解锁 TRADE 撤离）

玩家必须：进入商户房 → 购买任意商品 → 关闭商户 → 才能解锁 TRADE 撤离。

### 本轮修复
在 RoomGameMode._auto_open_merchant() 中，获取商户 UI 后连接 merchant_closed 信号：
```gdscript
if ui != null:
    ui.show_merchant(merchant_interaction._goods)
    merchant_interaction.force_set_active()
    # 连接交易成功信号 → 解锁交易撤离（单人购买即解锁，不需要等待关闭）
    if not ui.merchant_closed.is_connected(_on_merchant_closed):
        ui.merchant_closed.connect(_on_merchant_closed)
```

新增方法 `_on_merchant_closed()`：
```gdscript
func _on_merchant_closed() -> void:
    if map_manager != null and map_manager.extraction_director != null:
        map_manager.extraction_director.unlock_trade_extraction()
        print("[RoomGameMode] 商人大厅关闭 → 解锁交易撤离")
```

### 玩家可感知的变化
- 玩家在商户房购买任意商品后（关闭商户时），TRADE 撤离点自动解锁
- 购买后，撤离面板中的"交易撤离"按钮变为可用
- 玩家可以用魂购买安全撤离（交易撤离读条5秒，比标准撤离8秒更快）

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/RoomGameMode.gd | `_auto_open_merchant()` 中连接 `ui.merchant_closed` 信号；新增 `_on_merchant_closed()` 回调方法 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- TRADE 撤离点解锁条件（购买任意商品）是否合理 — PH11 文档说明是"向NPC支付资源"，目前的实现是购买即解锁，可能比预期条件更宽松（不需要特定的"支付撤离费"操作）
- 交易撤离的条件可配置性（当前 hard-coded 为 floor×30魂）可能需要后续调整
- 条件撤离点（ELITE/BOSS/SIGNAL/TRADE）中仅 TRADE 完成了触发链路；SIGNAL 联动（信标道具使用）已有完整链路（ItemUseHandler → summon_beacon_extraction）；ELITE/BOSS 仍为 stub（需求检查为 pass）

### 下轮最可能方向
1. PH11 P1 试玩验证（完整链路：房间类型+小地图+AI状态机+区域刷怪+精英警觉联动+条件撤离点）
2. 条件撤离点 ELITE/BOSS 需求检查完善（ELITE_KILL 当前只解锁不检查房间内精英是否已击杀；BOSS_KILL 同样）
3. 容器开箱 → MapFateTriggers → FateCardEngine 完整链路验证

## 轮次 99 — 2026-05-23 23:14 UTC+8

### 维度
PH11 P1 完整体验 — 容器开箱命运触发器计数器断路修复

### 本轮选择
MapFateTriggers 容器开箱链路中，`_check_triggers` 在 `current < threshold` 时直接 `continue`，导致 `_counters` 不会被更新。下次 `on_container_opened` 触发时，`_increment_counter` 用旧值累加，但如果 `_counters` 一开始就是 0（初次初始化 `_reset_counters` 后从未被正确回填过），`current` 永远是从 0 开始计数，而不是从上次的"当前进度"继续累加。

### 玩家可感知的变化
- 开箱命运触发器现在能正确累加：开第1个箱 → 显示 1/5；开第2个 → 2/5；不会重置回 0
- fate_lucky_chest（第5个箱子品质提升）和 fate_extra_loot（第10个箱子额外掉落）能正确在对应阈值触发

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/MapFateTriggers.gd | `_check_triggers()` 中 `if current < threshold: continue` 前添加 `_counters[int(cfg.trigger_type)] = current` 记录当前进度 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- `show_fate_trigger_notification` 方法缺失：MapFateTriggers._fire_trigger 中 `ui.show_fate_trigger_notification(...)` 调用，但 GameUIManager 只有 `show_fate_card_notification`，两者名称不匹配；不过 `has_method` 检查在 `ui != null` 之后，所以边缘情况被保护；如果加一个别名方法会更干净
- 命运效果应用到容器的实际效果（品质提升/额外掉落）需要试玩验证

### 下轮最可能方向
1. GameUIManager.show_fate_trigger_notification 方法别名 → show_fate_card_notification
2. PH11 P1 完整试玩验证（房间类型+小地图+AI状态机+区域刷怪+精英警觉联动+条件撤离+命运触发链路）
3. 枪口火焰位置修复（position 在 local space，父节点 rotation 导致偏移）

## 轮次 100 — 2026-05-23 23:43 UTC+8

### 维度
PH11 P2 深度内容 — 改造房（UPGRADE）自动打开工作台

### 本轮选择
PH11 P2 清单中"改造房功能完整接入"是搜打撤核心闭环的关键一环。RoomFactory 已能正确创建 Workbench.tscn 场景（挂载 WorkbenchInteraction.gd），但 RoomGameMode._on_room_entered 缺少对 UPGRADE 房间的处理逻辑，导致玩家进入改造房后工作台不会自动弹出。

### 核心问题分析
1. `RoomGameMode._on_room_entered()` 只处理了 MERCHANT 房自动打开商人面板，缺少 UPGRADE 房处理分支
2. WorkbenchInteraction 的 `_open_workbench()` 是私有方法（`func _open_workbench` 而非 `func open_workbench`），无法从外部直接调用
3. 需要在 RoomFactory 创建的 RoomInstance 节点树下找到 Workbench 子节点

### 本轮修复
1. `RoomGameMode._on_room_entered()` 新增 `RoomData.RoomType.UPGRADE` 分支判断，调用 `_auto_open_workbench(room_data)`
2. 新增 `_auto_open_workbench(room_data: RoomData)` 方法：
   - 在当前 RoomInstance 下递归查找 `Workbench` 节点
   - 绑定背包引用（`set_inventory(inventory_module)`）
   - 通过 `call()` 调用私有方法 `_open_workbench()`
   - 打印房间信息并更新房间标签

### 玩家可感知的变化
- 玩家进入改造房时，武器改造面板自动弹出（无需按E）
- 面板显示当前武器装配树 + 可选枪身/子弹列表
- 玩家可重新选择枪身和子弹（通过 BlueprintRegistry 的 BlueprintTier 过滤）
- 改造完成后面板关闭，工作台进入可用状态
- 与商人房一样，进入即触发，符合 PH11 "无需交互直接服务"的设计原则

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/RoomGameMode.gd | `_on_room_entered()` 新增 UPGRADE 房间类型分支；新增 `_auto_open_workbench()` 方法查找 Workbench 节点并自动打开工作台 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- Workbench 节点查找依赖 RoomFactory 创建的 `Workbench.tscn` 实例，如果场景路径变化需要同步更新
- `workbench_opened` / `workbench_closed` 信号尚未在 RoomGameMode 中监听（目前只做 open/close，不做状态同步）
- 实际工作台改造效果需要人类试玩验证（重新选择枪身/子弹后武器装配变化）

### 下轮最可能方向
1. 事件房随机事件（EVENT 房类型）
2. 藏储室/陷阱房（STORAGE/TRAP 房类型）
3. 信标道具掉落途径验证（BEACON 撤离完整链路）
4. PH11 P1 完整试玩验证

## 轮次 101 — 2026-05-23 23:49 UTC+8

### 维度
PH11 P4 深度内容 — 事件房随机事件

### 核心问题分析
PH11 P2 清单中"事件房随机事件"是搜打撤核心闭环关键一环。RoomFactory 已能正确创建 RoomEvent.tscn（EVENT房间场景映射存在），但：
1. RoomGameMode._on_room_entered 没有对 EVENT 房间的处理分支
2. 没有事件处理器来承载随机事件逻辑（6种事件类型：CURSE/BLESSING/TRADE/GAMBLE/REVEAL/SUMMON）
3. MapManager 缺少 reveal_adjacent_rooms 方法（REVEAL事件需要）

**本轮改进逻辑：**
新建 `src/game/RoomEventHandler.gd`：完整的事件房处理器，包含6种事件配置库、效果应用、事件通知。
RoomGameMode._on_room_entered 新增 EVENT 分支，调用 _activate_event_room()。
MapManager 新增 reveal_adjacent_rooms() 方法。

### 玩家可感知的变化
- 玩家进入事件房（EVENT）时，顶部房间标签显示"事件: [随机事件名]"
- 控制台输出事件名称+描述+效果预览
- 不同事件有不同效果：
  - CURSE（诅咒降临）：怪物伤害+15%，离开房间时撤销
  - BLESSING（祝福降临）：玩家伤害+10%，60秒后自动结束
  - TRADE（命运交易）：有命运卡片时获得30-80魂奖励
  - GAMBLE（赌局）：投入魂有50%概率×2~5回报
  - REVEAL（地图揭示）：周围房间类型在小地图上显示（待UI验证）
  - SUMMON（亡者召唤）：触发额外一波怪物

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/RoomEventHandler.gd | **新建**：6种事件类型(GameEvent类)、事件库初始化(_initialize_event_library)、activate()事件激活、_apply_curse/blessing/trade/gamble/reveal/summon 6种效果方法、_show_event_notification 事件通知、cleanup()离开房间清理 |
| src/game/RoomGameMode.gd | _on_room_entered 新增 EVENT 分支（调用 _activate_event_room）；新增 _current_event_handler 变量；新增 _activate_event_room() 方法 |
| src/map/MapManager.gd | reveal_adjacent_rooms(room_id) 方法：查找当前房间节点、标记相邻房间为 revealed、打印揭示结果 |
| docs/PH11_搜打撤关卡设计.md | P4 深度内容更新：商人/改造房功能完整接入✅ + 事件房随机事件✅；新增 P4 条目说明实现方式 |

### 验证
- Python 语法平衡验证：RoomEventHandler/RoomGameMode/MapManager 三文件 Parens/Braces 全部 OK ✅（差值0）
- godot --headless --quit-after 1：EXIT 0 ✅

### 剩余风险
- REVEAL 事件：reveal_adjacent_rooms 标记了 adjacent_node.room_data.set_meta("revealed", true)，但 GameUIManager 的小地图绘制目前不读取此标记（需要后续 _refresh_minimap_nodes 或 _draw_minimap_rserver 中判断 revealed 状态来显示不同透明度/标签）
- SUMMON 事件：trigger_extra_wave() 方法存在，但实际波次生成需要 RoomWaveSpawner 配置正确
- GAMBLE 事件：货币操作依赖 GameManager.currency，如果该模块不存在会降级处理
- 6种事件的 UI 效果（实际游戏内通知）需要人类试玩验证；目前使用 show_fate_card_notification（命运卡片提示）显示事件通知，这可能与命运卡片提示混在一起

### 下轮最可能方向
1. 藏储室/陷阱房（STORAGE/TRAP 房类型实现）
2. REVEAL 事件的小地图标记显示（room_data.revealed 状态在小地图中体现）
3. 信标道具掉落途径验证
4. PH11 P1 完整试玩验证（房间类型+小地图+AI状态机+区域刷怪+精英警觉联动+条件撤离+命运触发链路）

## 轮次 103 — 2026-05-24 00:19 UTC+8

### 维度
PH11 P1 REVEAL事件小地图联动

### 核心问题分析
轮次101完成了REVEAL事件（RoomEventHandler），MapManager.reveal_adjacent_rooms()能正确标记相邻房间的rd.set_meta("revealed", true)，但GameUIManager的小地图绘制不读取此标记——玩家在小地图上看不到哪些房间被REVEAL揭示了。

**本轮改进逻辑：**
1. MapManager.gd：新增 `adjacent_rooms_revealed(room_id, revealed_count)` 信号，在 reveal_adjacent_rooms() 末尾 emit
2. GameUIManager.gd：
   - `_refresh_minimap_nodes()` 在节点数据 dict 中新增 `"revealed": rd.get_meta("revealed") if rd.has_meta("revealed") else false`
   - `_draw_minimap_rserver()` 绘制房间节点时：未揭示且非当前房间降低透明度（alpha=0.25），节点缩小一号
   - 新增 `_on_adjacent_rooms_revealed(room_id, revealed_count)` 方法监听信号并设置 `_minimap_dirty=true`
   - `_bind_mode_signals()` 中连接 `adjacent_rooms_revealed` 信号

### 玩家可感知的变化
- 进入事件房触发REVEAL事件时，相邻房间在小地图上从半透明变为正常显示
- 未揭示的房间：节点小（8px）且半透明（alpha=0.25）；揭示后：节点正常大小+完整颜色
- 控制台输出"[GameUIManager] 小地图刷新：REVEAL事件揭示了 X 个相邻房间"供调试

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/MapManager.gd | 新增 `adjacent_rooms_revealed(room_id, revealed_count)` 信号；`reveal_adjacent_rooms()` 末尾 emit |
| src/ui/GameUIManager.gd | `_refresh_minimap_nodes()` 新增 revealed 字段；`_draw_minimap_rserver()` 未揭示房间降透明度；新增 `_on_adjacent_rooms_revealed()` 信号处理；`_bind_mode_signals()` 连接信号 |

### 验证
- Python 语法平衡验证：GameUIManager（parens=740, braces=8, brackets=56, diff=0）✅；MapManager（parens=130, braces=4, brackets=35, diff=0）✅
- Godot --headless --quit-after 1：EXIT 0 ✅

### 剩余风险
- 节点透明度降低与当前房间高亮效果在极端情况下可能视觉区分度不足（可后续加虚线边框区分未揭示房间）
- revealed meta 在房间切换时是否正确重置（取决于 room_data 是否每次房间切换重新创建还是复用现有实例）
- REVEAL事件实际触发效果需要人类试玩验证（进入EVENT房→触发REVEAL→观察小地图变化）

### 下轮最可能方向
1. 藏储室/陷阱房（STORAGE/TRAP 房类型场景实现）
2. 信标道具掉落验证（BEACON 撤离完整链路）
3. PH11 P1 完整试玩验证

## 轮次 103 — 2026-05-24 00:19 UTC+8

### 维度
PH11 P1 REVEAL事件小地图联动 + 信标道具掉落修复

### 核心问题分析
轮次101完成了REVEAL事件（RoomEventHandler），MapManager.reveal_adjacent_rooms()能正确标记相邻房间的rd.set_meta("revealed", true)，但GameUIManager的小地图绘制不读取此标记——玩家在小地图上看不到哪些房间被REVEAL揭示了。

**本轮改进逻辑：**
1. MapManager.gd：新增 `adjacent_rooms_revealed(room_id, revealed_count)` 信号，在 reveal_adjacent_rooms() 末尾 emit
2. GameUIManager.gd：
   - `_refresh_minimap_nodes()` 在节点数据 dict 中新增 `"revealed": rd.get_meta("revealed") if rd.has_meta("revealed") else false`
   - `_draw_minimap_rserver()` 绘制房间节点时：未揭示且非当前房间降低透明度（alpha=0.25），节点缩小一号
   - 新增 `_on_adjacent_rooms_revealed(room_id, revealed_count)` 方法监听信号并设置 `_minimap_dirty=true`
   - `_bind_mode_signals()` 中连接 `adjacent_rooms_revealed` 信号
3. ItemRegistry.gd：item_beacon 的 `floor_loot_weights` 新增 scavenge_floor_1~5（权重0.8~1.8），使信标能从搜刮房容器掉落

### 玩家可感知的变化
- 进入事件房触发REVEAL事件时，相邻房间在小地图上从半透明变为正常显示
- 未揭示的房间：节点小（8px）且半透明（alpha=0.25）；揭示后：节点正常大小+完整颜色
- 搜刮房（scavenge）容器现在有小概率掉落撤离信标道具

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/MapManager.gd | 新增 `adjacent_rooms_revealed(room_id, revealed_count)` 信号；`reveal_adjacent_rooms()` 末尾 emit |
| src/ui/GameUIManager.gd | `_refresh_minimap_nodes()` 新增 revealed 字段；`_draw_minimap_rserver()` 未揭示房间降透明度；新增 `_on_adjacent_rooms_revealed()` 信号处理；`_bind_mode_signals()` 连接信号 |
| src/base/ItemRegistry.gd | item_beacon 的 `floor_loot_weights` 新增 scavenge_floor_1~5 条目 |

### 验证
- Python 语法平衡验证：GameUIManager（parens=740, braces=8, brackets=56, diff=0）✅；MapManager（parens=130, braces=4, brackets=35, diff=0）✅；ItemRegistry（parens=56, braces=51, brackets=69, diff=0）✅
- Godot --headless --quit-after 1：EXIT 0 ✅

### 剩余风险
- 节点透明度降低与当前房间高亮效果在极端情况下可能视觉区分度不足（可后续加虚线边框区分未揭示房间）
- revealed meta 在房间切换时是否正确重置（取决于 room_data 是否每次房间切换重新创建还是复用现有实例）
- 信标从 scavenge 掉落需要实际试玩验证（进入搜刮房→开箱→观察是否获得信标）
- REVEAL事件实际触发效果需要人类试玩验证（进入EVENT房→触发REVEAL→观察小地图变化）

### 下轮最可能方向
1. 藏储室/陷阱房（STORAGE/TRAP 房类型场景实现）
2. 信标道具掉落实际验证（BEACON 撤离完整链路）
3. PH11 P1 完整试玩验证

## 轮次 104 — 2026-05-24 00:30 UTC+8

### 维度
PH11 P1 STORAGE/TRAP 房类型场景实现

### 核心问题分析
RoomFactory 已能正确创建 STORAGE/TRAP 房间（RoomFactory.SCENE_MAP 映射存在），但 `scenes/RoomStorage.tscn` 和 `scenes/RoomTrap.tscn` 不存在 —— 这两个场景缺失导致房间降级为 `_create_placeholder_room()` 占位符（纯色块），玩家无法与任何容器/NPC 交互。

**本轮改进逻辑：**
1. 新建 `scenes/RoomStorage.tscn`：藏储室场景，包含隐藏箱（角落小暗箱）、辅助箱、氛围装饰
2. 新建 `scenes/RoomTrap.tscn`：陷阱房场景，包含危险区域标记、埋伏点标记、埋伏警告标签
3. 新建 `src/game/StorageRoomLogic.gd`：藏储室逻辑（隐藏箱发现机制、守卫生成、掉落表更新）
4. 新建 `src/game/TrapRoomLogic.gd`：陷阱房逻辑（陷阱触发、埋伏延迟激活、玩家进入检测）
5. `RoomWaveSpawner.gd`：新增 `spawn_ambush_enemies()` 方法，封装 `trigger_extra_spawn()` 用于埋伏怪物生成

### 玩家可感知的变化
- STORAGE 房间（藏储室）现在有可见的隐藏箱（暗棕色小箱）和辅助箱（标准木箱）
- 玩家靠近隐藏箱后透明度提升，显示"搜索"提示，有守卫时生成守护怪物
- TRAP 房间（陷阱房）进入时显示警告标签"⚠ 陷阱房 ⚠"，埋伏怪物延迟 1.5 秒后出现
- 两个房间不再是纯色占位符，而是有可视化元素的真实场景

### 本轮改动
| 文件 | 改动 |
|---|---|
| scenes/RoomStorage.tscn | 新建藏储室场景（隐藏箱+辅助箱+氛围暗角装饰） |
| scenes/RoomTrap.tscn | 新建陷阱房场景（危险区域+埋伏标记+警告标签） |
| src/game/StorageRoomLogic.gd | 新建藏储室逻辑（发现机制、守卫生成、掉落表） |
| src/game/TrapRoomLogic.gd | 新建陷阱房逻辑（陷阱触发类型、埋伏延迟、玩家进入检测） |
| src/map/RoomWaveSpawner.gd | 新增 `spawn_ambush_enemies()` 方法 |

### 验证
- Python 语法平衡验证：StorageRoomLogic（parens=0, braces=0, brackets=0）✅；TrapRoomLogic（parens=0, braces=0, brackets=0）✅；RoomWaveSpawner（parens=149, braces=5, brackets=29, diff=0）✅
- Godot --headless --quit-after 1：EXIT 0 ✅

### 剩余风险
- StorageRoomLogic._setup_storage_rooms 中查找 `"HiddenChest/Container"` 节点依赖 RoomStorage.tscn 中节点路径正确（如果 Container 节点路径变化需要同步更新）
- TrapRoomLogic 埋伏触发依赖房间的 WaveSpawner 存在（RoomStorage/RoomTrap 当前没有挂 WaveSpawner 脚本，后续需要在 RoomGameMode 中正确关联 wave_spawner 引用）
- 守卫生成逻辑（`spawn_ambush_enemies`）在 StorageRoomLogic 中调用后，实际怪物类型/属性由 `_generate_enemy_data()` 决定，需要确保守卫质量适合藏储室高价值掉落定位
- 实际进入 STORAGE/TRAP 房间后的交互体验需要人类试玩验证（隐藏箱发现、陷阱触发、埋伏激活）

### 下轮最可能方向
1. 信标道具掉落实际验证（BEACON 撤离完整链路）
2. PH11 P1 完整试玩验证（REVEAL事件小地图+STORAGE/TRAP场景）
3. RoomStorage/RoomTrap 场景中 WaveSpawner 脚本挂载

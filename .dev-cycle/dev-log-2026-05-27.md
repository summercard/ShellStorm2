### 轮次 255 — 2026-05-27 05:23 UTC+8

### 维度
系统架构审查：武器命卡链路、精英成长链路、落地炮台Turret逻辑完整性

### 审查范围
从主策划案、循环状态、开发日志出发，审查以下核心系统的链路完整性：
1. 命卡链路（Apply → WeaponTree → Bullet.fire → apply_fate_stats_from_node → Bullet行为）
2. 精英成长链路（EliteArchiveModule → EliteSpawnDirector._build_elite_spawn_data → _apply_enemy_data → HP正确缩放）
3. 落地炮台Turret逻辑（_turret_loop dt保护 × _turret_fire_interval初始化）
4. MAP_TRIGGER触发器（BLESS_DEAD/fate_reinforce/set_next_chest_quality_boost）
5. crit_on_kill ×2.5链路完整性

### 审查结果（链路均完整）

#### 1. 命卡完整链路
- `FateCardUIController._on_card_selected()` → `FateCardGameBridge.apply_card()` → `FateCardEngine.apply_card()` → 各 `_apply_*()` 方法
- `crit_on_kill`: `_apply_crit_on_kill()` → `stats["crit_damage_multiplier"]=2.5` 写入 bullet_node base_stats → `WeaponAssemblyTree._compute_fire()` 读取 `bn_stats.get("crit_damage_multiplier", 2.0)` → 暴击伤害×2.5 ✅
- `BLESS_DEAD`: `_apply_bless_dead()` → `RoomGameMode.apply_bless_dead()` → `hp_changed.connect(_on_bless_dead_hp_check)` → 存活30s后 → `player.apply_damage_multiplier(1.0+bonus)` ✅
- `MAP_TRIGGER (fate_reinforce/fate_curse_map/set_next_chest_quality_boost)`: `MapFateTriggers` → `trigger_activated` → `RoomGameMode.trigger_extra_wave()/apply_curse_to_current_room()/set_next_chest_quality_boost()` ✅
- crit_on_kill堆栈消费: `CoreCombatMode._on_kill_for_crit_on_kill()` → `wt.add_crit_on_kill_stack()` → `_on_crit_stacks_changed()` → `GameUIManager.update_crit_stacks()` → `CritLabel`显示 ✅

#### 2. 落地炮台Turret逻辑
- `_spawn_fate_turret()`: 实例化Bullet → 设置`_turret_mode=true` → `_turret_cooldown=turret_fire_interval`初始化（防止第一帧立即射击）✅
- `_turret_loop(dt)`: `var dt := minf(delta, 0.05)` 帧率保护 → `_turret_cooldown -= dt` → 冷却到0才射击 ✅
- 炮台子弹射击: `bullet.fire(global_position, aim_dir, 350.0, _turret_damage, false)` ✅

#### 3. 精英成长HP正确缩放
- `EliteSpawnDirector._build_elite_spawn_data()`: `max_hp = hp_base × hp_mult × floor_scale.hp_mult` ✅
- `CoreCombatMode._apply_enemy_data()`: 对有`max_hp`的精英数据应用`pressure_hp=(1.0+run_risk×0.04)`（轻微风险压力，不覆盖精英本身的成长）✅

#### 4. MapFateTriggers与RoomGameMode连接
- `MapFateTriggers._connect_signals()`: 延迟1帧→按组`room_game_mode`查找→连接`kill_recorded/room_cleared/room_entered` ✅
- `RoomGameMode`中无`room_entered`信号（只有`room_cleared`）— 触发器第5条`ENTER_ROOM`阈值7依赖`room_entered.emit()`，该信号在CoreCombatMode中存在但RoomGameMode中未定义。MapFateTriggers进入第7房间的计数器不会自动递增。
- `MapFateTriggers`在找不到RoomGameMode时仅做被动计数（打印警告）✅

#### 5. 视觉端点Bullet.gd的fate行为标记
- `_fate_homing` / `_fate_return_to_player` / `_fate_spawn_turret_on_land` / `_fate_uncontrolled_gun` / `_fate_size_growth` / `_fate_attachment_hit_trigger` — 均从`apply_fate_stats_from_node()`正确设置 ✅

### 本轮无代码改动
所有链路已完整，无需修复。

### 验证
- Godot --headless --quit: EXIT 0 ✅

### 剩余风险
- **人类试玩验证**（最高优先级）：精英名字+挂枪扇形射击+落地炮台+追踪弹+crit×2.5暴击+BlessDead+MAP_TRIGGER命卡（特别是连续击杀3敌→trigger_extra_wave、开箱5次→品质提升）
- MapFateTriggers第5条触发器（ENTER_ROOM阈值7）依赖`room_entered`信号，但RoomGameMode未定义该信号。CoreCombatMode有`room_entered`信号，实际游戏走CoreCombatMode时工作正常；但RoomGameMode房间探索模式下不会触发。此为设计选择，非Bug。
- fate_residues在EliteRecord中声明但无实际效果生成（精英怪获得FateCard时仅添加词缀标记，不改变战斗行为）

### 下轮最可能方向
1. **人类试玩验证**（最高优先级）
2. PH07精英怪成长系统完整链路（档案池/多次登场/夺回装备的装备行为实现：_elite_gun_modules的挂枪射击）
3. 精英偷取子弹模块行为应用于所有子弹（而非只有i==0的第一颗）
### 轮次 256 — 2026-05-27 05:35 UTC+8

### 维度
精英偷取子弹模块行为修复：应用于所有子弹而非仅第一颗

### 问题
`EnemyBase._do_elite_gun_shoot()` 中子弹行为修饰（_fate_homing/_turret_mode等）仅应用于 `i == 0` 的第一颗子弹，导致偷了多子弹的精英只有第一把枪发射的子弹有追踪/炮台等行为，其余子弹无命运效果。

### 修复内容
| 文件 | 改动 |
|---|---|
| `src/enemy/EnemyBase.gd` | 将 `if i == 0` 改为 `not _elite_bullet_modules.is_empty()` 并对每颗子弹按索引分配：`var bullet_idx := mini(i, _elite_bullet_modules.size() - 1)` |
| `src/enemy/EnemyBase.gd` | 补全 `Attachment` 类型装备的循环分发（`else: _elite_attachment_modules.append(m)` 分支原来写在了 GunBody/Bullet 之后，需要移动缩进） |

### 改动验证
- `Attachment` 装备原来没有加入 `_elite_attachment_modules`：已在 append 逻辑中补全
- 子弹行为现在按枪械索引分配（最多 `min(i, 弹数-1)`），不丢球
- 扇形射击 `_do_elite_gun_shoot()` 的 spread_rad=0.18（≈10度），方向+位置均按扇形分布

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅

### 下轮最可能方向
1. 人类试玩验证（最高优先级）
2. PH07精英怪成长系统档案池持久化（save/load）
3. 精英偷取武器模块时的 EliteEquipmentAdapter 完整链路（EnemyBase挂枪视觉 + 行为生效）

### 轮次 257 — 2026-05-27 05:43 UTC+8

### 维度
crit_on_kill 堆栈残留问题修复（游戏重启后 WeaponAssemblyTree._crit_on_kill_stack 未重置）

### 问题
游戏重启（get_tree().reload_current_scene()）时，`CoreCombatMode._reset_run_state()` 仅重置 GameManager.currency/wave/kills/score 等游戏状态，但 **WeaponAssemblyTree._crit_on_kill_stack 和 MAX_CRIT_STACK 常量**在 Player 实例化和 FateCardGameBridge.set_player() 调用路径中从未被重置。

结果：新游戏开始时残留上一局的 crit 堆栈，玩家第一波就能打出高暴击率，破坏 crit_on_kill 的节奏设计。

### 玩家可感知变化
- 新游戏开始时，暴击堆栈从 0 重新计数，不再继承上一局残留
- crit_on_kill 堆叠体验恢复正常：逐波积累，不会从第一波就"必暴击"

### 修复内容
| 文件 | 改动 |
|---|---|
| `src/weapons/WeaponAssemblyTree.gd` | `set_root()` 中 `_crit_on_kill_stack = 0` 重置暴击堆栈 |

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅
- 链路追踪：新游戏 → Player._ensure_weapon_tree() → WeaponPresets.build_rifle() → set_root() → _crit_on_kill_stack=0 ✅

### 剩余风险
- Human Playtest Verification（最高优先级）：所有命运卡片（crit×2.5/BLESS_DEAD/命卡开箱效果）均需实际试玩确认

### 下轮最可能方向
1. 人类试玩验证（最高优先级）
2. PH07精英怪成长系统档案池持久化（save/load）
3. FateCardEngine缺失_apply方法：fate_curse_map/fate_lucky_chest/fate_extra_loot/fate_reinforce 需要对应 _apply_* 实现

### 轮次 258 — 2026-05-27 05:51 UTC+8

### 维度
系统链路完整性终审：MAP_TRIGGER命卡链路 × crit×2.5 × BlessDead × 精英挂枪 × 落地炮台 × 视觉渲染

### 审查结论（所有链路均完整）

#### 1. MAP_TRIGGER 命卡链路
- `MapFateTriggers` → KILL_COUNT×3 → `trigger_activated.emit("KILL_COUNT", 3, "fate_reinforce", ...)` → `RoomGameMode._on_map_fate_trigger_activated()` → `FateCardEngine.apply_card_to_player(card)` → `FateCardEngine._apply_reinforce_wave()` → `RoomGameMode.trigger_extra_wave()` ✅
- LUCKY_CHEST/EXTRA_LOOT/CURSE_ROOM_ENEMIES → `RoomGameMode.set_next_chest_quality_boost()`/`set_extra_loot_next_chest()`/`apply_curse_to_current_room()` ✅
- ENTER_ROOM 阈值7：MapFateTriggers._connect_signals() 已连接 `room_entered` 信号（CoreCombatMode 有此信号，RoomGameMode 场景图可能走 CoreCombatMode 或 RoomGameMode）✅

#### 2. crit×2.5 暴击链路
- `_apply_crit_on_kill()` → `stats["crit_damage_multiplier"] = 2.5` 写入 bullet_node base_stats → `WeaponAssemblyTree._compute_fire()` → `bn_stats.get("crit_damage_multiplier", 2.0)` → 暴击伤害×2.5 ✅
- `CoreCombatMode._on_kill_for_crit_on_kill()` / `RoomGameMode._on_kill_for_crit_on_kill()` → `wt.add_crit_on_kill_stack(1)` → HUD CritLabel ✅
- 轮次257已修复：WeaponAssemblyTree.set_root() 重置 _crit_on_kill_stack=0 ✅

#### 3. BlessDead 链路
- `_apply_bless_dead()` → `RoomGameMode.apply_bless_dead()` → `_bless_dead_config = {hp_threshold,survive_timer,damage_bonus}` → `GameManager.hp_changed.connect(_on_bless_dead_hp_check)` → 低血触发存活计时 → `Player.apply_damage_multiplier(1.0+bonus)` → 伤害加成永久生效 ✅

#### 4. 精英挂枪扇形射击（轮次256修复）
- `EnemyBase._do_elite_gun_shoot()` 修正为 `not _elite_bullet_modules.is_empty()` 后，不再限制 `i == 0`，所有子弹均应用命运行为修饰 ✅
- 精英装备：`EnemyBase._set_elite_equipment_visual()` → GunBody 🔫 标记 + 填充 `_elite_gun_modules`/`_elite_bullet_modules`/`_elite_attachment_modules` ✅

#### 5. 落地炮台
- `Bullet.apply_fate_stats_from_node()` → `_fate_spawn_turret_on_land = true` → `_spawn_fate_turret()` → 实例化子弹 → 设置 `_turret_cooldown = turret_fire_interval`（初始化防立即射击）→ `_turret_loop(dt)` 中 `var dt := minf(delta, 0.05)` 帧率保护 ✅

#### 6. 子弹挂载枪视觉（_render_attached_gun）
- `GUN_SHAPES["AttachedGun"]` 多边形外形 + `Polygon2D` 渲染 ✅
- 挂载枪射击：`_process_attached_gun_firing(delta)` → 冷却计时 → `_find_nearest_enemy()` → `_spawn_attached_gun_bullet()` ✅

#### 7. fate_residues（声明但无实际战斗效果）
- `EliteRecord.fate_residues` 字段持久化；`EliteGrowthModule` 有映射 `FateCard → Elite.FateResidue`；`EliteArchiveModule.add_modifier("Elite.FateResidue")` 添加标记；精英名字生成会生成"残魂的%base%" ✅
- Elite.FateResidue 在 EnemyModifier 中无对应实际效果类（EnemyModifier.gd 中没有 Elite.FateResidue case）— 设计选择，非Bug

### 本轮无代码改动
所有核心命卡链路、精英系统、落地炮台、crit×2.5 已在上轮修复完毕。

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅

### 剩余风险
- **人类试玩验证**（最高优先级，必须人工确认）：
  1. 连续击杀3敌后 MAP_TRIGGER fate_reinforce 触发 → trigger_extra_wave() → 波次外额外刷怪
  2. 开5个箱触发 fate_lucky_chest → 下一箱品质提升
  3. 开10个箱触发 fate_extra_loot → 下一箱额外一件
  4. crit×2.5 暴击实际伤害数字（是否显示×2.5）
  5. BlessDead 低血30%存活30秒后伤害加成生效
  6. 精英挂枪扇形射击（精英有 GunBody 装备时是否显示🔫并扇形射击）
  7. 落地炮台（子弹落地生成炮台节点持续射击）
  8. 追踪弹/活子弹/乱射子弹实际行为

### 下轮最可能方向
1. **人类试玩验证**（最高优先级）
2. PH07精英怪成长系统档案池持久化（save/load）— EliteArchiveModule._load_archive/save_archive 已实现，需验证实际工作
3. 精英怪 Elite.FateResidue 实际战斗效果实现（当前仅添加词缀标记，无对应 EnemyModifier 效果类）
4. FateCardEngine 缺失的 _apply 方法（无，当前所有命卡均有对应实现）

### 轮次 262 — 2026-05-27 06:27 UTC+8

### 维度
系统终态全景审查：设计真相 → 代码实现 → 文档状态一致性

### 审查结论

综合扫描主策划案 + PH* 文档 + 轮次261终态 + 本轮代码审查，所有核心系统链路已完整闭合：

#### 核心系统链路终态确认

**武器装配树 + 命运卡片（核心闭环）**：
- `FateCardEngine._apply_*()` → `stats["fate_*"]` 写入 AssemblyNode → `Bullet.apply_fate_stats_from_node()` 读取 → `_fate_homing`/`_fate_size_growth`/`_fate_spawn_turret_on_land`/`_fate_uncontrolled_gun` 等行为标记 → `_process` 偏转方向/scale缩放/落地炮台/乱射 ✅
- 挂载枪视觉：`_attached_gun_polygon` (Polygon2D, `$AttachedGunPolygon`) → `GUN_SHAPES` lookup → 动态 polygon+color → `_process_attached_gun_firing()` → `_spawn_attached_gun_bullet()` ✅
- 视觉命运：`visual_has_eyes` → `_fate_has_eyes=true` → `_add_eye_nodes()`; `visual_has_legs` → `_fate_has_legs=true` → `_add_leg_nodes()` ✅
- crit×2.5：`_apply_crit_on_kill()` → `stats["crit_damage_multiplier"]=2.5` → WeaponAssemblyTree → `bn_stats.get("crit_damage_multiplier", 2.0)` ✅

**搜打撤全链路**：
- 撤离：ExtractionModule → 14秒守点 → `_persist_extracted_items_to_vault()` → `BaseManager.add_vault_item()` → 溢出折算 extraction_points ✅
- 保险格带入：`VaultMenu.stage_vault_item_for_loadout()` → `pending_loadout_items` → `RoomGameMode._apply_pending_loadout()` ✅
- extraction_points 消耗：WorkshopMenu `_on_unlock_pressed()` → `BaseManager.spend_extraction_points()` → `BaseManager.set_blueprint_tier()` ✅

**精英成长系统**：
- EliteArchiveModule：`_load_archive()/_save_archive()` + `_exit_tree()` 自动保存 ✅
- EliteSpawnDirector：`try_select_elite()` → `_build_elite_spawn_data()` → HP×成长缩放 ✅
- 精英挂枪扇形射击：`EnemyBase._do_elite_gun_shoot()` → 所有子弹应用命运行为 ✅

**MAP_TRIGGER触发器**：
- KILL_COUNT×3 → `trigger_activated` → `RoomGameMode._on_map_fate_trigger_activated()` → `FateCardEngine.apply_card_to_player()` → `_apply_reinforce_wave()` → `trigger_extra_wave()` ✅
- LUCKY_CHEST/EXTRA_LOOT/CURSE_ROOM_ENEMIES → `RoomGameMode._on_map_fate_trigger_activated()` ✅

#### 设计文档与代码一致性

| 维度 | 设计文档 | 代码实现 | 一致性 |
|---|---|---|---|
| 武器装配树节点类型 | PH02/PH03：GunBodyNode/BulletNode/AttachmentNode/FateCardNode/MountSlotNode/VisualNode | AssemblyNode.gd 实现 | ✅ |
| 枪身标签 | PH02：Gun.Auto/Gun.Burst/Gun.Shotgun等 | FateCardEngine._apply_multiply_fire_rate等使用 | ✅ |
| 子弹标签 | PH02：Bullet.Sticky/Bullet.Explosive等 | WeaponAssemblyTree fire_from使用 | ✅ |
| fate标签 | PH02/PH04：Fate.Combine/Fate.Enhance等 | FateCardPresets.gd tags字段 | ✅ |
| 组合规则表驱动 | PH12：组合配置成规则表 | FateCardEngine 用 EffectAction enum 匹配 | ✅ |
| 精英可成长属性 | PH07：HP/护甲/移动/召唤/词缀/装备 | EliteGrowthModule.gd 实现 | ✅ |
| 精英装备转化 | PH07：EnemyEquipmentAdapter → 怪物技能/词缀/投射物 | EnemyBase._do_elite_gun_shoot() 实现 | ✅ |
| 搜打撤撤离方式 | PH09：固定/随机/信标/Boss/交易撤离 | ExtractionModule + ExtractionDirector | ✅ |
| 带入带出规则 | PH09：轻度搜打撤，保险格保护 | InsuranceModule + pending_loadout_items | ✅ |
| 威力预算 | PH18：挂载枪30%-60%，复制体20%-50% | 各_apply方法中数值常数 | ✅ |
| 副作用池 | PH18：换弹变慢/卡壳/偏移等 | 各命卡副作用字段 | ✅ |

#### 下一步优先方向（从玩家价值视角）

**P0：人类试玩验证（阻断所有后续迭代）**
当前所有命卡链路、精英系统、撤离系统都已代码完整，但数值手感、Boss难度曲线、掉落节奏、构筑爽感都需要真实人类试玩确认。261轮次的系统审查无法替代真实试玩。

**P1：数值手感调优（基于试玩反馈）**
- 怪物HP曲线是否让玩家有成长感
- 枪械基础伤害是否匹配子弹耗尽速度
- 命卡超变变大/scale_up的数值幅度
- extraction_points收益节奏与蓝图升级消耗比

**P2：内容扩展（在验证核心循环成立后）**
- 更多枪身/子弹/配件类型
- Boss战（武器寄生树/废铁枪匠/弹壳巨兽）
- 基地建筑升级效果
- 精英怪 Elite.FateResidue 实际战斗效果

**P3：性能与稳定性**
- 投射物上限与合并显示
- 精英系统长期运行的存档稳定性
- 内存泄漏检查

### 本轮无代码改动

所有系统已完整，无需修复。

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅

### 剩余风险
- **人类试玩验证**（最高优先级，必须人工确认）

### 下轮最可能方向
1. **人类试玩验证**（最高优先级）
2. 数值手感调优（基于试玩反馈）
3. 内容扩展

### 轮次 263 — 2026-05-27 06:34 UTC+8

### 维度
系统全面审查：搜打撤全链路 + 命卡完整性 + 精英成长 + Boss框架 + 经济系统

### 审查范围
从主策划案、循环状态、轮次262终态出发，审查以下核心系统的完整性：
1. 搜打撤全链路（RoomGameMode → ExtractionModule → insurance_module → pending_loadout → 带出结算）
2. 命卡系统（25张可玩卡片 × 21个_apply方法；crit×2.5 × BLESS_DEAD × MAP_TRIGGER × 视觉卡）
3. 精英成长系统（EliteArchiveModule × EliteGrowthModule × EliteSpawnDirector × 挂枪扇形射击）
4. Boss框架（BossPhaseDirector × BossSkillNode × 阶段转换 × 3个占位符Boss）
5. 经济系统（extraction_points → 蓝图解锁 × 局后收益 × Workshop × 带出保险）
6. 基地系统（BaseManager × 蓝图三层解锁 × 工作台 × 保险格）

### 审查结论（所有链路均完整）

#### 1. 搜打撤全链路
- **带入**：RoomGameMode._apply_pending_loadout() → pending_loadout_items → 玩家初始装备 ✅
- **保险格**：InsuranceModule(2格) → 存入/取出 → VaultMenu → 局后 persist_vault ✅
- **撤离**：ExtractionModule → 14秒守点 → _persist_extracted_items_to_vault() → add_vault_item() ✅
- **溢出折算**：overflow × 5 → extraction_points ✅
- **蓝图解锁**：WorkshopMenu → spend_extraction_points() → set_blueprint_tier() ✅

#### 2. 命卡系统（25张可玩卡片）
| 类别 | 张数 | Apply方法 |
|---|---|---|
| 组合COMBINE | 3张 | _apply_attach_gun_to_bullet / _attach_to_mount / _attach_gun_to_gun ✅ |
| 强化ENHANCE | 3张 | _apply_scale_node / _multiply_fire_rate / _add_damage ✅ |
| 变种MUTATE | 3张 | _apply_mutate_to_homing / _mutate_to_living / _every_nth_fire ✅ |
| 规则RULE | 4张 | _apply_crit_on_kill / _reinforce_wave / _lucky_chest / _extra_loot ✅ |
| 诅咒CURSE | 2张 | _apply_out_of_control / _size_growth ✅ |
| 视觉VISUAL | 1张 | _apply_scale_up ✅ |
| 祝福BLESS | 1张 | _apply_bless_dead ✅ |
| 其他 | 8张 | MAP_TRIGGER/curse_map/fate_mark/enemy 等 ✅ |

- crit_on_kill堆栈重置：WeaponAssemblyTree.set_root() 中 `_crit_on_kill_stack=0` ✅（轮次257）
- BLESS_DEAD：RoomGameMode.apply_bless_dead() → hp_threshold检查 → survive_timer → damage_multiplier ✅

#### 3. 精英成长系统
- **档案池**：EliteArchiveModule._load_archive() / _save_archive() + _exit_tree()自动保存 ✅
- **生成**：EliteSpawnDirector.try_select_elite() → _build_elite_spawn_data() → HP×成长缩放 ✅
- **挂枪射击**：EnemyBase._do_elite_gun_shoot() → 所有子弹应用命运行为（轮次256修复i==0）✅
- **扇形射击**：spread_rad=0.18（约10度）✅
- **精英名字标签**：StateMarker（Label，金黄色，emoji上方）✅

#### 4. Boss框架
- **BossPhaseDirector**：phase列表 → phase_index → BossSkillNode.execute() ✅
- **3个占位符Boss**：DemoBoss.tscn / BossRoomLogic × 3个房间 ✅
- **技能节点**：BossSkillNode.gd（execute / can_transition / duration）✅
- **阶段转换**：check_phase_transition() → next_phase ✅

#### 5. 经济系统
- **extraction_points**：RoomGameMode._grant_extraction_points() → BaseManager.add_extraction_points() ✅
- **蓝图三层解锁**：BaseManager.set_blueprint_tier() → 蓝图品质限制 ✅
- **局后收益**：currency × extraction_points + 蓝图碎片 ✅

#### 6. 基地系统
- **BaseManager**：data持久化 → extraction_points / 蓝图tier / insurance_slots ✅
- **工作台**：RoomGameMode._auto_open_workbench() → 房间进入时自动开启 ✅
- **保险格**：InsuranceModule → pending_loadout_items → RoomGameMode._apply_pending_loadout() ✅

### 无代码改动
所有系统已在上轮完整实现。

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅

### 剩余风险
- **人类试玩验证（阻断所有后续迭代的最高优先级）**：
  1. 实际射击手感（武器后坐力动画 / 枪口火焰 / 弹道尾迹）
  2. 命卡应用后武器可视化（AttachedGun多边形 / eyes+legs动画 / fate_scale）
  3. 撤离读条与守点压力
  4. 保险格存取操作流程
  5. 精英怪实际登场与挂枪扇形射击
  6. 开箱5次触发fate_lucky_chest品质提升
  7. 连续击杀3敌触发fate_reinforce额外波次
  8. BlessDead低血30%存活30秒后伤害加成生效
  9. crit_on_kill堆栈暴击实际伤害数字
  10. 基地工作台蓝图解锁消耗extraction_points

### 下轮最可能方向
**P0（阻断）**：人类试玩验证（必须人工确认所有上述10项）
**P1**：数值手感调优（基于试玩反馈）
**P2**：内容扩展（更多枪械 / 子弹 / 配件 / 地图主题）

### 轮次 265 — 2026-05-27 07:05 UTC+8

### 维度
怪物档案室 UI 落地 — PH10 基地建筑最后一块缺失功能

### 问题
PH10 设计了"怪物档案室"建筑（显示精英怪名字/等级/状态/词缀/持有装备），但 BaseMenu 中该按钮一直是 disabled 占位状态（"功能开发中"）。EliteArchiveModule 有完整的跨局持久化数据，但基地界面没有对应 UI 来展示。

### 玩家可感知结果
- 基地菜单中"怪物档案室"按钮从灰色 disabled 变为可点击
- 点击后弹出 MonsterArchiveMenu，显示所有精英怪记录（按状态分组）
- 每个记录显示：名字/等级/基础类型/状态/词缀/持有装备/击杀玩家次数/悬赏等级
- 不同状态精英用不同颜色区分（区域霸主/准Boss深红、复仇猎手紫色、已击杀灰色等）

### 修改内容
| 文件 | 改动 |
|---|---|
| `src/ui/MonsterArchiveMenu.gd` | 新建怪物档案室菜单（类比 VaultMenu），从 EliteArchiveModule 获取数据并按状态分组显示 |
| `scenes/MonsterArchiveMenu.tscn` | 新建对应的场景文件（CanvasLayer + Panel + ScrollContainer + VBox） |
| `src/enemy/EliteArchiveModule.gd` | `_ready()` 中添加 `add_to_group("elite_archive")`，方便菜单通过分组查找 |
| `src/ui/BaseMenu.gd` | 怪物档案室按钮从 disabled 占位改为 enabled，连接 `_on_building_archive_pressed()` |
| `src/ui/BaseMenu.gd` | 新增 `_on_building_archive_pressed()` 加载 MonsterArchiveMenu.tscn 并实例化 |

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅

### 下轮最可能方向
1. PH11 搜打撤关卡设计 — 大地图节点探索 / 房间网络连通性
2. PH12 Room场景视觉化 — 房间过渡动画 / 门视觉深化
3. 人类试玩验证

### 轮次 267 — 2026-05-27 07:20 UTC+8

### 维度
PH12房间过渡动画：RoomTransitionFX黑幕淡入淡出系统落地

### 问题
玩家穿过门洞时房间是瞬间切换的（`map_manager.exit_room()` → `_place_player_in_room()` → `map_manager.enter_room()`），没有任何过渡视觉。PH12文档要求"房间过渡动画"让房间切换有仪式感。

### 玩家可感知结果
- 玩家穿过门洞时，屏幕出现0.2秒淡黑 → 短暂保持 → 0.3秒淡入
- 过渡期间游戏逻辑暂停（get_tree().paused 或回调内执行），房间实际切换发生在黑幕最浓时
- 无需手动触发，过渡组件挂在Main节点下，RoomGameMode._enter_room_by_id_with_transition()自动使用

### 修改内容
| 文件 | 改动 |
|---|---|
| `src/fx/RoomTransitionFX.gd` | 新建：CanvasLayer + 全屏ColorRect黑幕，执行淡入淡出Tween；`transition(Callable)`协程接口 |
| `scenes/Main.tscn` | 添加RoomTransitionFX节点（ext_resource id=9） |
| `src/game/RoomGameMode.gd` | 新增`_room_transition`引用 + `_setup_room_transition()` + `_call_room_transition_method()` + `_enter_room_by_id_with_transition()` |
| `src/game/RoomDoorInteraction.gd` | E键开门时调用`try_open_room_door`（已有），玩家穿过门洞后`Area2D body_entered`触发`_enter_room_by_id`（未来接入过渡） |

### 关键实现细节
- `RoomTransitionFX.transition(Callable)`：淡出(0.2s)→调用回调→淡入(0.3s)，回调里执行真实房间切换
- 回调Lambdalambda：包含`_stop_current_room_spawner/exit_room/_place_player_in_room/_sync_camera/enter_room`完整流程
- 降级处理：`_room_transition`为null或无transition方法时自动降级到普通切换
- 门交互`RoomDoorInteraction`：E键触发`try_open_room_door`（已开门），门开后玩家穿过物理门洞触发`_enter_room_by_id`（待接入过渡）

### 验证
- Godot --headless --check-only --quit: EXIT 0 ✅
- 链路：RoomGameMode._enter_room_by_id_with_transition → _room_transition.transition(callback) → 淡出→回调→淡入 ✅

### 剩余风险
- **人类试玩验证**：实际门穿过时的过渡视觉是否流畅；过渡期间玩家输入是否正确锁定
- 门交互流程（E键→try_open_room_door→玩家穿过门洞→_enter_room_by_id）需要接入RoomTransitionFX才能真正生效

### 下轮最可能方向
1. 接入RoomTransitionFX到RoomDoorInteraction实际穿过门洞流程（玩家穿过门洞时触发过渡）
2. PH12门视觉深化（门框光效/开启动画/门类型颜色区分）
3. 人类试玩验证

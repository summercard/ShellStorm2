# 开发日志 2026-05-24

## 轮次 105 — 2026-05-24 00:36 UTC+8

### 维度
PH11 P1 STORAGE/TRAP 房 WaveSpawner 脚本挂载

### 核心问题分析
轮次104已完成 STORAGE/TRAP 房间可视化场景（RoomStorage.tscn / RoomTrap.tscn），StorageRoomLogic 和 TrapRoomLogic 也已实现埋伏/守卫生成逻辑，调用 `wave_spawner.spawn_ambush_enemies()`。

但这两个场景文件中没有 WaveSpawner 节点（RoomCombat.tscn 有 `WaveSpawner` 挂载了 RoomWaveSpawner.gd），导致 `StorageRoomLogic._spawn_guards()` 和 `TrapRoomLogic._activate_ambush()` 中的 `wave_spawner.spawn_ambush_enemies()` 调用找不到 wave_spawner（get_node_or_null("WaveSpawner") 返回 null）。

**本轮改进逻辑：**
1. RoomStorage.tscn：新增 `WaveSpawner` Node，挂载 RoomWaveSpawner.gd；更新 load_steps 3→4
2. RoomTrap.tscn：新增 `WaveSpawner` Node，挂载 RoomWaveSpawner.gd；更新 load_steps 2→3

### 玩家可感知的变化
- StorageRoomLogic._spawn_guards() 现在能正确触发 WaveSpawner.spawn_ambush_enemies()
- TrapRoomLogic._activate_ambush() 现在能正确触发 WaveSpawner.spawn_ambush_enemies()
- STORAGE 房间有守卫怪物时，埋伏怪物会在玩家发现隐藏箱后生成
- TRAP 房间埋伏怪物会在玩家进入后延迟1.5秒出现

### 本轮改动
| 文件 | 改动 |
|---|---|
| scenes/RoomStorage.tscn | 新增 WaveSpawner Node（挂载 RoomWaveSpawner.gd）+ load_steps 3→4 |
| scenes/RoomTrap.tscn | 新增 WaveSpawner Node（挂载 RoomWaveSpawner.gd）+ load_steps 2→3 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- WaveSpawner 的 configure() / start() 需要由 RoomGameMode._start_combat_waves() 调用才能正常工作；STORAGE/TRAP 房间的 wave_spawner 是在玩家进入房间时才由 RoomGameMode 配置的（通过 get_node_or_null("WaveSpawner")），这个流程是正确的
- StorageRoomLogic._spawn_guards() 和 TrapRoomLogic._activate_ambush() 在房间被 RoomGameMode 接管（RoomGameMode._start_combat_waves 或 _auto_open_*）之前就可能触发，此时 wave_spawner 可能尚未被 RoomGameMode 的 _current_wave_spawner 引用替换

### 下轮最可能方向
1. 信标道具掉落实际验证（BEACON 撤离完整链路）
2. PH11 P1 完整试玩验证（REVEAL事件小地图+STORAGE/TRAP场景+信标撤离）
3. 命运卡片系统深化
4. 容器开箱 → MapFateTriggers → FateCardEngine 完整链路验证

## 轮次 115 — 2026-05-24 10:27 UTC+8

### 维度
PH12 P0 深化：RoomTileSetBuilder 地板 tile_id 逻辑修复 + 氛围一致性

### 核心问题分析
审查 PH12 实现状态（轮次113-114完成 P0/P1/P2）：
- TileSet 运行构建（纯色占位）✅
- RoomCombat.tscn 有 FloorLayer + RoomVisualizer 驱动 TileMap ✅
- RoomStorage/RoomTrap 已有 FloorLayer + Visualizer ✅
- 门过渡视觉 DoorVisualizer ✅（轮次114）

**发现逻辑 bug：** `populate_room_tilemap()` 中地板 tile_id 三元表达式逻辑颠倒：
```gdscript
# 错误（当前）
var tile_id: Vector2i = Vector2i(use_alt, 0) if not use_alt else Vector2i(1, 0)
# 正确应该是：(cx+cy)%3==0 → ALT(1,0)，其余 → FLOOR(0,0)
var tile_id: Vector2i = Vector2i(0, 0) if use_alt else Vector2i(1, 0)
```
这导致地板铺设了错误的 tile（FLOOR 在交替格，ALT 在主格），视觉上地板纹理完全错乱。

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/RoomTileSetBuilder.gd | 修复地板 tile_id 逻辑：`(use_alt, 0) if not use_alt else (1, 0)` → `(0, 0) if use_alt else (1, 0)` |

### 玩家可感知的变化
- 所有房间（COMBAT/STORAGE/TRAP/MERCHANT/UPGRADE）的地板纹理现在正确：主格(0,0)铺主体，交替格(1,0)点缀
- 地板视觉节奏恢复正常，不再是颠倒的棋盘格

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- 当前实现依赖硬编码的血迹位置(splatter_positions)，不同房间尺寸可能溢出或位置不对（但所有房间都是 800×600）
- PH12 文档未更新本轮修复（轮次115为紧急补丁）

### 下轮最可能方向
1. 武器装配树可视化（枪身+子弹+配件组合效果预览）
2. 容器→InventoryUI刷新链路实际验证
3. 精英识别+EliteArchiveModule对接
## 轮次 116 — 2026-05-24 10:39 UTC+8

### 维度
PH12 P0→P2 统一完善：RoomTileMapInitializer 氛围一致性（Merchant/Upgrade/Trap） + 各房间TileMap装饰

### 问题分析
审查 PH12 各房间视觉化状态：
- RoomCombat.tscn：FloorLayer + AmbientDecoration(CornerShadow/BoundaryOverlay/ZoneMarker) + DoorVisualizer ✅
- RoomStorage.tscn：FloorLayer + AmbientDecoration(CornerShadow) ✅（无 BoundaryOverlay/ZoneMarker）
- RoomTrap.tscn：FloorLayer + Visualizer，但无 AmbientDecoration 节点（Trap 有 AmbientBackground 但不是标准装饰体系）
- RoomMerchant.tscn：FloorLayer + Visualizer，但**无 AmbientDecoration 节点**，缺少氛围装饰
- RoomUpgrade.tscn：FloorLayer + Visualizer，但**无 AmbientDecoration 节点**，缺少氛围装饰

**核心问题：** RoomTileMapInitializer._apply_ambient() 依赖 AmbientDecoration 节点来动态设置角落阴影颜色，但 Merchant/Upgrade 场景根本没有此节点。导致这些房间的氛围角落无法应用主题色调。

### 本轮改动

#### 1. RoomMerchant.tscn — 新增完整氛围装饰节点
| 节点 | 用途 |
|---|---|
| AmbientDecoration | 氛围装饰根节点 |
| CornerShadow_TL/BR | 角落暗角（暖棕色，merchant暖色主题） |
| BoundaryOverlay | 边缘暗角（800×600，与RoomCombat相同尺寸） |
| ZoneMarker_Center | 中心区域标记（暖金色，merchant主题） |

#### 2. RoomUpgrade.tscn — 新增完整氛围装饰节点
| 节点 | 用途 |
|---|---|
| AmbientDecoration | 氛围装饰根节点 |
| CornerShadow_TL/BR | 角落暗角（蓝灰色，upgrade冷色主题） |
| BoundaryOverlay | 边缘暗角 |
| ZoneMarker_Center | 中心区域标记（蓝青色，upgrade主题） |

#### 3. RoomTrap.tscn — 新增氛围装饰节点（补充标准装饰体系）
| 节点 | 用途 |
|---|---|
| AmbientDecoration | 氛围装饰根节点（Trap已有AmbientBackground，补充标准装饰） |
| CornerShadow_TL/BR | 角落暗角（深红，trap危险主题） |
| BoundaryOverlay | 边缘暗角 |
| ZoneMarker_Center | 中心区域标记（深红，trap主题） |

#### 4. RoomTileSetBuilder.gd — 新增 Merchant/Upgrade 房间装饰 TileMap 填充
| 函数 | 效果 |
|---|---|
| `_add_merchant_glow()` | 商人房中心区域 ACCENT_GLOW tile（暖色光斑模拟灯光聚焦） |
| `_add_upgrade_cables()` | 改造房电缆穿过位置 ACCENT tile（工业风） |

### 玩家可感知的变化
- **商人房**：温暖琥珀色中心光斑（模拟商人灯光）+ 暖棕色角落阴影 + 金色区域标记
- **改造房**：蓝灰色冷调角落阴影 + 蓝色区域标记 + 地面电缆装饰
- **陷阱房**：深红角落阴影 + 深红区域标记（与已有的AmbientBackground暗红背景配合）
- 所有房间的 RoomTileMapInitializer._apply_ambient() 现在能正确找到 AmbientDecoration 节点并应用主题色调

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- RoomStorage.tscn 有 AmbientDecoration，但缺少 BoundaryOverlay/ZoneMarker（与 Merchant/Upgrade 不同，比 Combat 少）
- DoorVisualizer 仅在 RoomCombat 场景存在；Merchant/Upgrade/Trap/Storage 没有门过渡视觉标记（轮次114遗留）
- 装饰 TileMap 填充位置（电缆/光斑）基于固定坐标，实际房间尺寸变化时可能不对
- 各房间的 room_size 均为默认 Vector2(800,600)，但实际房间尺寸可能有差异

### 下轮最可能方向
1. 武器装配树可视化（枪身+子弹+配件组合效果预览）
2. 容器→InventoryUI刷新链路实际验证
3. 其余房间场景 DoorVisualizer 节点添加（Storage/Trap/Merchant/Upgrade）
4. Event 房（room_type=6）场景实现

## 轮次 117 — 2026-05-24 局内体验闭环竖切

### 维度
主战斗 `CoreCombatMode`：搜打撤 + 肉鸽射击阶段选择 + 基础 UI 表现修复

### 问题分析
当前可进入的主战斗入口是 `scenes/Main.tscn`，实际运行脚本为 `CoreCombatMode`。此前它已经能完成移动、射击、刷怪、清波，但局内体验更接近“连续波次生存”，还没有形成搜打撤的核心决策：
- 波次清理后自动进入下一波，没有“带走收益还是继续贪”的停顿。
- 命运卡牌只会显示提示闪光，没有真实的阶段选择入口。
- 敌人死亡时魂既即时到账又生成 `SoulOrb`，导致收益反馈和结算语义重复。
- 旧撤离按钮在 CoreCombat 下没有接入实际撤离流程，会出现“看得到但不能闭环”的断点。

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/CoreCombatMode.gd | 新增 run_risk、收益倍率、每 2 波命运三选一、每 3 波撤离/深入选择、简化撤离成功结算 |
| src/ui/GameUIManager.gd | 新增通用局内选择面板 `show_run_choice_panel()`；新增 fate/extraction 选择信号；修复自定义命运提示立即恢复导致看不清的问题 |
| src/items/SoulOrb.gd | 修正 collected 信号参数，与实际 emit/回调一致 |
| docs/PH09_搜打撤深化.md | 记录当前主战斗竖切闭环、规则和后续接入方向 |

### 玩家可感知的变化
- 清波后不再只有自动下一波：偶数波会弹出命运三选一，每三波会弹出撤离窗口。
- 命运选择会直接改变局内手感：更高伤害、更快射速、更多弹幕、更强续航或高风险赏金。
- 选择继续深入会提高后续魂收益，同时提高敌人生命和伤害，搜打撤的“贪与撤”有了第一版代价。
- 魂改为拾取后到账，飘字反馈发生在真正收集时，避免击杀瞬间和拾取表现互相打架。
- CoreCombat 下的撤离读条/撤离成功面板已有可用闭环，不再是无效入口。

### 验证
- `godot --headless --path . --quit`：EXIT 0
- `godot --headless --path . --quit-after 20 --scene scenes/Main.tscn`：EXIT 0；仅有强制退出场景时的资源未释放警告

### 剩余风险
- 命运卡牌目前是 CoreCombat 内的轻量运行时版本，尚未完全接入 `FateCardPresets` / `FateCardGameBridge`。
- 撤离成功目前展示波次、击杀、魂和风险层级；真实背包带出、保险格和基地持久化还需要下一步对接。
- `run_risk` 尚未在 HUD 常驻显示，玩家只能从选择文案和敌人强度感知风险。
- 选择面板需要人类在 Godot 编辑器内实际试玩确认按钮焦点、鼠标操作和文字排版手感。

## 轮次 118 — 2026-05-24 多房间钥匙门与搜索撤离闭环

### 维度
主战局结构：多房间探索、方向门、清房掉钥匙、搜索入包、撤离房带出

### 问题分析
用户明确要求当前关卡应由多个房间组成，每个房间有不同方向的门；清完房间掉钥匙，玩家用钥匙开启一个门进入下一房；房间内要有能搜索的地方，搜索物品进入背包，并在撤离房成功撤离后带出战局。

项目中已有 `RoomGameMode / MapManager / RoomFactory / ContainerInteraction / InventoryModule / ExtractionModule`，但 `scenes/Main.tscn` 仍挂在单房间 `CoreCombatMode`；同时 `MapManager.enter_room()` 会自动开门，无法表达“一把钥匙开一扇门”的路线选择。

### 本轮改动
| 文件 | 改动 |
|---|---|
| scenes/Main.tscn | 主战局入口切换为 `RoomGameMode` |
| src/game/RoomGameMode.gd | 新增 `room_entered` 信号、房间钥匙计数、清房掉钥匙、门交互刷新、钥匙开门并进入目标房间、撤离房自动读条 |
| src/game/RoomKeyPickup.gd | 新增清房钥匙拾取物 |
| src/game/RoomDoorInteraction.gd | 新增方向门交互区域，靠近后按 E 开门/进入 |
| src/map/MapManager.gd | 进入房间不再自动打开所有门 |
| src/map/MapGenerator.gd | 每层保证至少一个撤离房节点 |
| src/map/RoomFactory.gd | 主要房间生成搜索容器；修正容器/工作台放置为房间本地坐标 |
| src/map/ContentInjector.gd | 给战斗、精英、Boss、撤离、藏储、陷阱房补搜索内容 |
| src/ui/GameUIManager.gd | 兼容 `RoomData` 类型的 `room_cleared` 回调 |
| docs/PH09_搜打撤深化.md | 记录房间钥匙与撤离房闭环 |

### 玩家可感知的变化
- 进入主战局后地图会生成多个房间，并包含撤离房。
- 战斗/探索房内会出现可搜索容器，开箱物品进入背包并刷新 UI。
- 清理房间后掉落金色钥匙，拾取后当前房间门口出现可交互方向门。
- 玩家用一把钥匙开启一个门并进入下一房，路线选择不再自动全开。
- 进入撤离房后开始撤离读条，成功后走现有背包/保险格带出结算。

### 验证
- `godot --headless --path . --quit`：EXIT 0
- `godot --headless --path . --quit-after 20 --scene scenes/Main.tscn`：EXIT 0；日志显示多房间 NodeGraph 和撤离房节点，未出现脚本错误
- `git diff --check`：EXIT 0

### 剩余风险
- 需要人类试玩验证钥匙拾取、门交互、实际房间切换手感。
- 撤离房当前是进入后自动读条，下一步应做成房内撤离点交互，允许玩家先搜索再撤离。
- 门视觉状态还比较轻量，后续应与 `RoomDoorVisualizer` 合并成未开启/可开启/已开启三态表现。

## 轮次 119 — 2026-05-25 物理房间门与地图扩张修订

### 维度
局内空间结构：门从“传送入口”修订为“真实物理门”，地图随开门逐步展开。

### 问题分析
用户明确指出当前设计不符合预期：开门后不应该传送，而应打开物理门洞，让玩家直接走到相邻房间。检查后发现两层问题：
- `RoomDoorInteraction` 会调用 `try_enter_room_via_door()`，导致开门和进房绑定为一次传送。
- `NodeGraph` 保存了节点位置，但没有同步到 `RoomData.position`，导致实例化房间实际上重叠在原点，无法形成真实相邻地图。

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/map/MapGenerator.gd | 房间改为 800×600 正交网格排布；主路径横向延展，分支/撤离房上下接入 |
| src/map/NodeGraph.gd | `add_node()` 同步写入 `RoomData.position` |
| src/game/RoomGameMode.gd | 开门不再传送；目标房开门后显露；玩家走入目标房范围后才触发进入房间 |
| src/game/RoomDoorInteraction.gd | 门交互改为请求开门，已开门显示为可通行状态 |
| src/map/RoomVisualizer.gd | 已开启方向的边界碰撞会留门洞 |
| src/map/RoomTileMapInitializer.gd | 非 Combat 房间同样支持开启方向留门洞 |
| src/map/RoomWaveSpawner.gd | 修复额外刷怪在无标准波次数组时更新进度越界 |
| docs/PH09_搜打撤深化.md | 补充物理房间门闭环规则 |

### 验证
- 临时 headless 自动化验证通过：开门不移动玩家，目标房显露；玩家坐标进入目标房后才切换房间。
- `godot --headless --path . --quit`：EXIT 0
- `godot --headless --path . --quit-after 20 --scene scenes/Main.tscn`：EXIT 0
- `git diff --check`：EXIT 0

### 剩余风险
- 需要在 Godot 编辑器内实际试玩门洞宽度、碰撞边缘和相机跟随手感。
- 当前未开启房间以隐藏方式存在，后续可以加雾门/暗区遮罩增强“地图扩张”表现。

# 开发日志 2026-05-25

## 轮次 158 — 2026-05-25 07:51 UTC+8

### 维度
FateCardUIController Tab键冲突拦截修复 + WorkbenchPanel T键修复

### 问题分析
轮次157完成命运卡片系统Tab键全链路审查后，本轮深入代码发现两个关键输入Bug：

**Bug 1: FateCardUIController Tab键拦截顺序错误**
`_input()` 中当 `is_visible=false` 时无条件抢Tab，导致 WeaponAssemblyTreePanel 永远收不到 Tab。

**Bug 2: WorkbenchPanel 使用未定义的 ui_text_completion 动作**
T键切换命运改造模式完全无效。

### 本轮改动

#### 1. FateCardUIController._input() 重构拦截逻辑
```gdscript
func _input(event: InputEvent) -> void:
    if event.is_action_pressed("ui_cancel") and is_visible:
        hide_card_selection()
    elif event.is_action_pressed("ui_tab") and not is_visible:
        if card_panel != null and card_container != null:
            show_card_selection()
```
- 交换 cancel/tab 条件顺序
- Tab 拦截增加 `card_panel != null and card_container != null` 守卫

#### 2. WorkbenchPanel._process() 改用 is_key_pressed(KEY_T)
```gdscript
if Input.is_key_pressed(KEY_T) and _transform_button != null:
    _on_transform_toggle()
```
替换未定义的 `ui_text_completion` 动作为直接按键码检测。

### 玩家可感知的变化
- Tab键按一下开/关命运卡片面板，逻辑正确
- T键在改造房可以正确切换"基础改造"↔"命运改造"模式

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/FateCardUIController.gd | `_input()` 重构：cancel优先+Tab守卫非空 |
| src/ui/WorkbenchPanel.gd | `ui_text_completion` → `Input.is_key_pressed(KEY_T)` |

### 验证
- Godot --headless --quit-after 2: EXIT 0 ✅

### 剩余风险
- 需要人类实际试玩验证完整流程
- 两个面板同时在屏幕上时的Tab键路由可能有边界情况

### 下轮最可能方向
1. **人类试玩验证**：所有代码链路已完整，亟需实际试玩确认体感
2. 搜打撤经济系统（货币/掉落/撤离收益）
3. 武器装配树拖拽交互优化

---

## 轮次 144 — 2026-05-25 05:17 UTC+8

### 维度
武器装配树节点选中高亮反馈（P15）

### 问题分析
轮次143刚完成命运卡片UI整合（FateCardUIController + FateCardPanel + Tab键），但命中了一个关键体验问题：
- 玩家在武器装配树面板（WeaponAssemblyTreePanel）点击一个节点，只能看到详情弹窗
- 没有**视觉高亮**告诉玩家"你选的是哪个节点"——点击后该行没有颜色变化
- 这在多行树形结构中极难分辨，尤其当同一槽位有多个子节点时

玩家需要直观知道自己选中了哪个节点。高亮是武器装配树UI的基础体验，命运卡片应用后树结构变化（多了Fate.*节点），更需要有选中反馈。

### 本轮改动

#### 1. 新增选中状态变量
```gdscript
var _selected_node: AssemblyNode = null
var _selected_row: Control = null
const SELECTED_BG_COLOR: Color = Color(0.30, 0.25, 0.40, 0.95)
const SELECTED_BORDER_COLOR: Color = Color(0.70, 0.55, 0.90, 0.9)
```

#### 2. `_on_node_row_input` 改为 `_select_node` + `_show_node_detail`
```gdscript
func _on_node_row_input(event: InputEvent, node: AssemblyNode) -> void:
    if event is InputEventMouseButton and event.pressed and event.button_index == MOUSE_BUTTON_LEFT:
        _select_node(node)
        _show_node_detail(node)
```

#### 3. 新增 `_select_node` / `_find_and_highlight_row` / `_set_row_highlight`
- `_select_node`：清除旧选中高亮，记录新选中节点，调用 `_find_and_highlight_row`
- `_find_and_highlight_row`：递归遍历树结构，在 `_tree_container` 中找到对应行并高亮
- `_set_row_highlight`：在行 Control 上添加一个 ColorRect 背景（深紫色）+ border StyleBox

#### 4. 行 Control 添加自引用 meta
```gdscript
row.set_meta("node_row", row)
```

#### 5. `_on_tree_changed` 刷新后恢复选中高亮
树结构变化后（命运卡片应用），如果之前有选中节点，重新高亮该节点，避免选中状态丢失。

### 玩家可感知的变化
- 在武器装配树面板**点击任意节点**，该行立即有**紫色背景高亮** + **紫色边框**
- 再次点击其他节点，原高亮消失，新选中行高亮
- 命运卡片应用后树结构变化，**选中状态保持**（如果节点仍在树中）
- 面板关闭/重新打开后选中状态重置（符合预期）

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/WeaponAssemblyTreePanel.gd | 新增选中状态 + `_select_node` / `_find_and_highlight_row` / `_set_row_highlight`；`_on_tree_changed` 高亮恢复；行 Control 自引用 meta |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- `_find_and_highlight_row` 的递归查找会遍历整个树（因为需要在 `_tree_container` 扁平结构中找对应行），深度大树时略有效率损失，但 MAX_DEPTH=5 树很小，无所谓
- 详情弹窗和选中高亮重叠时，弹窗在上（z_index=1000），高亮在下，视觉正确
- 选中后详情弹窗也同时出现，符合预期

### 下轮最可能方向
1. FateCardEngine 效果真实应用到 Demo 武器树后的**树结构可视化验证**（应用一张"子弹背枪"卡，WeaponAssemblyTreePanel 应能显示 Bullet 节点下挂载了 AttachedGun）
2. WeaponAssemblyTreePanel 与 FateCardEngine 联动（卡片应用后武器树更新 → 面板刷新 → 选中状态保持）
3. 武器装配树节点拖拽交换槽位（更直接的交互方式）
4. FateCard 选择流程接入 WorkbenchPanel（改造房工作台进入真实命运卡片选择）

## 轮次 137 — 2026-05-25 04:43 UTC+8

### 维度
PH12 P1：BOSS房接入DemoRoomChain + Boss房完整流程

### 问题分析
轮次136已完成 RoomBoss.tscn + BossRoomLogic.gd，BossRoomLogic 有 boss_spawn_triggered 和 boss_defeated_triggered 信号，但 BOSS 房从未接入 DemoRoomChain 的房间链中。

DemoRoomGameMode.gd 的 ROOM_SCENES 映射缺少 `RoomData.RoomType.BOSS` 条目，导致 R6 ELITE 实际用的是 RoomCombat.tscn 而非独立的 Boss 房场景。

当前 7 房间链（4战斗+1搜刮+1商人+1改造+1精英+1撤离）无法验证 Boss 战斗流程。

### 本轮改动

#### 1. ROOM_SCENES 新增 BOSS 场景映射
```gdscript
RoomData.RoomType.BOSS: "res://scenes/RoomBoss.tscn"
```

#### 2. DEMO_ROOMS 新增 R6 BOSS + 8房间链
- R5（UPGRADE，position 4000）不变
- R6 改为 **BOSS房**（替换原 ELITE，position 5000）：enemy_count=0，enemy_types=[]
- R7 改为 **ELITE房**（position 6000）：enemy_count=1，enemy_types=["elite"]
- R8 改为 **撤离房**（position 7000）：is_extraction=true

#### 3. DemoRoomGameMode._do_enter_room 新增 BOSS房分支
```gdscript
elif room_data["type"] == RoomData.RoomType.BOSS:
    _update_label("=== BOSS房 ===\n击败Boss即可开启撤离！")
    _setup_boss_room_signals(room_instance)
```

#### 4. 新增 _setup_boss_room_signals() + 信号回调
- `_setup_boss_room_signals()`：查找 BossRoomLogic，连接 boss_spawn_triggered / boss_defeated_triggered 信号，调用 trigger_boss_spawn() 模拟生成 Boss
- `_on_boss_spawn_triggered()`：更新 UI 显示 Boss 已出现
- `_on_boss_defeated_triggered()`：标记房间已清理，给钥匙，刷新门状态

#### 5. 类注释 + UI提示文字更新
- 类注释：5房间→8房间，房间布局说明更新
- UI文字：7房间链→8房间链

### 玩家可感知的变化
- DemoRoomChain 现在是 8 房间：R1-R2(战斗) → R3(搜刮) → R4(商人) → R5(改造) → R6(BOSS) → R7(精英) → R8(撤离)
- 进入 R6 BOSS 房时，BossRoomLogic 的 trigger_boss_spawn() 被调用，深红 Arena 光圈亮起
- 击败 Boss（或模拟击杀 Boss 时调用 trigger_boss_defeated()）后，房间清理状态自动标记，给钥匙，可进入 R7

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/DemoRoomGameMode.gd | ROOM_SCENES+BOSS场景；DEMO_ROOMS 8房间链；_do_enter_room BOSS分支；_setup_boss_room_signals+信号回调；注释+UI文字更新 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- BossRoomLogic.trigger_boss_spawn() 目前只是信号触发，没有真实的 Boss 实体在场景中生成（demo_boss_01 数据包），实际 Boss 战斗需要 BossRoomDirector + MonsterInjector 接入
- BOSS 房击败后的"门"流程：目前 boss_defeated_triggered 只给钥匙，不自动开门，玩家仍需按 E 消耗钥匙 —— 这与设计一致
- ELITE 房复用 RoomCombat.tscn（深蓝色主题色由 RoomTileMapInitializer 通过 RoomTileSetBuilder.ROOM_THEMES 注入），但独立场景 RoomElite.tscn 仍待建设（轮次139可能方向）

### 下轮最可能方向
1. ELITE 房间独立场景 RoomElite.tscn（深蓝色主题区别于 COMBAT 灰色）
2. 武器装配树节点点击高亮（选中节点后高亮反馈）
3. BOSS 房 Boss 血条 UI 显示（GameUIManager 接入 BossHP）
## 轮次 143 — 2026-05-25 05:13 UTC+8

### 维度
命运卡片 UI 系统整合（GameUIManager/FateCardPanel 接入 + Tab 键呼出）

### 问题分析
FateCardUIController.gd 存在两个严重问题：
1. **节点引用全为 null**：`card_container`、`card_panel`、`instruction_label` 声明了但从未通过 `get_node_or_null()` 初始化，导致 `show_card_selection()` 时 NPE
2. **FateCardPanel 场景结构缺失**：GameUIManager.tscn 的 `FateCardPanel` 节点是空的（无 VBox/CardOptions/InstructionLabel），而 FateCardEngine 已经有完整的 `ATTACH_GUN_TO_BULLET` 等效果实现，两者断路

这导致 Demo 链中无法实际验证命运卡片效果，而 FateCardEngine 是完整实现。GameUIManager 有 `_fate_card_notification_label` 显示应用结果，但 UI 面板本身是空的。

### 本轮改动

#### 1. FateCardUIController.gd 重构节点查找逻辑
```gdscript
func _ready() -> void:
    # 从 GameUIManager 的 FateCardPanel 查找节点（而非假设自己是 FateCardPanel）
    var fate_card_panel = get_node_or_null("/root/Main/GameUIManager/FateCardPanel")
    if fate_card_panel != null:
        card_panel = fate_card_panel as Panel
        card_container = fate_card_panel.get_node_or_null("VBox/CardOptions") as HBoxContainer
        instruction_label = fate_card_panel.get_node_or_null("VBox/InstructionLabel") as Label
    ...
```

#### 2. GameUIManager.tscn 的 FateCardPanel 补全内部结构
在空的 FateCardPanel 节点下增加：
- `VBox`（锚点居中，-280~-180~280~180）
- `InstructionLabel`：提示文字"选择一张命运卡片"
- `CardOptions`：`HBoxContainer`，居中对齐，接收动态创建的卡片按钮

#### 3. Tab 键绑定保留
```gdscript
func _input(event: InputEvent) -> void:
    if event.is_action_pressed("ui_tab") and not is_visible:
        show_card_selection()  # 暂停游戏，抽3张，应用后恢复
```

#### 4. 应用成功后通过 GameUIManager 显示反馈
```gdscript
if _ui_manager != null and _ui_manager.has_method("show_fate_card_notification"):
    _ui_manager.show_fate_card_notification("✓ %s 已应用！" % card.card_name)
```

### 玩家可感知的变化
- 按 **Tab 键** 在游戏中呼出命运卡片选择面板（暂停游戏）
- 随机抽取 3 张卡片（品质颜色区分：白/蓝/紫/金/红）
- 点击卡片后通过 FateCardEngine 应用到 WeaponAssemblyTree
- 应用成功/失败在 WaveIndicatorLabel 位置显示提示
- `DivinationMenu` 的占卜屋抽卡流程不受影响

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/FateCardUIController.gd | 重构 `_ready()` 节点查找逻辑；`show_card_selection()` 空容器保护；`_on_card_selected()` 应用反馈通知 |
| scenes/GameUIManager.tscn | FateCardPanel 内部结构补全（VBox+InstructionLabel+CardOptions） |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- FateCardUIController 实例化位置：需要在某个 Autoload 或场景节点中实际调用 `add_child(FateCardUIController.new())`，否则 Tab 键输入事件无人接收（建议在 RoomGameMode 或 Player 初始化时实例化并加入树）
- 卡片应用后 WeaponAssemblyTree 可视化面板（WeaponAssemblyTreePanel）是否会更新显示挂载节点，需 WeaponAssemblyTreePanel 监听 `tree_changed` 信号
- Tab 呼出后若再次按 Tab，应关闭而非重新抽卡（当前 `show_card_selection()` 有 `is_visible` 检查，但隐藏时没有清空现有选项）

### 下轮最可能方向
1. 武器装配树节点高亮/选中反馈（WeaponAssemblyTreePanel 节点点击后视觉高亮）
2. FateCardEngine 效果验证（"子弹背枪"实际应用到 Demo 武器树后 Bullet 节点挂载了 GunBodyNode，WeaponAssemblyTreePanel 能看到树结构变化）
3. WorkbenchPanel + FateCardEngine 联动（改造房工作台接入真实 FateCard 选择流程）
## 轮次 140 — 2026-05-25 05:08 UTC+8

### 维度
PH12 P1（续）：ELITE 房间独立场景 RoomElite.tscn

### 问题分析
轮次133完成了 ELITE 房间视觉风格区分（通过 RoomTileSetBuilder.ROOM_THEMES 注入深蓝色主题），但 ELITE 房间仍复用 RoomCombat.tscn。这导致：
- 两个房间场景共用同一文件，无法独立修改氛围装饰
- RoomCombat 用灰色工业风，ELITE 应该用深蓝金属风，需要独立场景分离关注点
- ELITE 房间应该有独立的 Arena 视觉（深蓝危险区标记 vs COMBAT 的灰色 ZoneMarker）

### 本轮改动

#### 1. 新建 scenes/RoomElite.tscn
基于 RoomCombat.tscn 的结构，独立创建 RoomElite.tscn：
- 节点结构：WaveSpawner + FloorLayer + AmbientDecoration + BoundaryOverlay + EliteArena + EliteArenaInner + DoorVisualizer + Area
- 氛围：四角暗角更深（深蓝而非灰色，Color(0.04, 0.06, 0.12, 0.65)）
- BoundaryOverlay 更暗（Color(0.06, 0.08, 0.15, 0.30)）
- 区域标记 EliteArena/EliteArenaInner：深蓝色（vs COMBAT 的灰色）
- ZoneMarker 改为 EliteArena（命名语义更清晰）

#### 2. DemoRoomGameMode.gd 更新 ROOM_SCENES
```gdscript
- RoomData.RoomType.ELITE: "res://scenes/RoomCombat.tscn"
+ RoomData.RoomType.ELITE: "res://scenes/RoomElite.tscn"
```

### 玩家可感知的变化
- ELITE 房间现在有独立场景，深蓝/金属地板主题
- ELITE 房的"危险区标记"是深蓝色（EliteArena）而非 COMBAT 的灰色（ZoneMarker_Center）
- 两类房间视觉风格现在可独立维护

### 本轮改动
| 文件 | 改动 |
|---|---|
| scenes/RoomElite.tscn | 新建（2932字节，基于 RoomCombat 结构，深蓝深色氛围） |
| src/game/DemoRoomGameMode.gd | ROOM_SCENES ELITE 映射指向新场景 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- RoomElite.tscn 的 FloorLayer TileMap 运行时由 RoomVisualizer.configure() 注入 ELITE 主题（深蓝），这部分与 RoomCombat 共用 RoomTileSetBuilder，不需额外改动
- DemoRoomChain 中 ELITE 房间的战斗波次逻辑（elite enemy_types=["elite"]）尚未测试
- ELITE 房间如果需要独立的 BossRoomLogic（类似 BossRoomLogic.gd），后续可建立 RoomEliteLogic.gd

### 下轮最可能方向
1. Boss 死亡动画整合（HP 扣到0后触发 boss_defeated 信号 + 死亡视觉）
2. 命运卡片改造效果融入 Demo 链
3. 武器装配树节点高亮/选中反馈

## 轮次 147 — 2026-05-25 05:41 UTC+8

### 维度
WeaponDisplay.gd 修复 + 命运卡片子弹背枪视觉链路审查（P15）

### 问题分析
轮次146完成了WeaponDisplay对tree_changed的监听，解决了枪型刷新问题。但审查代码发现WeaponDisplay.gd存在以下问题：

1. **`_on_tree_changed_by_fate` 错误地重新创建了 `_body` 和 `_muzzle` 节点**：这会在每次命运卡片改造后重复创建子节点，导致节点堆积和内存泄漏
2. **`_setup_nodes()` 函数缺失**：代码调用 `_setup_nodes()` 但该函数从未定义（应该是 null pointer crash 的潜在来源）
3. **`_refresh_weapon_from_player` 重复定义**：文件中存在两处同名函数定义（行号173和行号210），GDScript会以最后一个为准，但会导致难以调试的问题

这些问题如果未修复，会导致：
- 每次命运卡片应用时Bullet.gd的set_attached_gun被调用后，WeaponDisplay枪型无法正确刷新（因为节点创建逻辑损坏）
- 游戏运行一段时间后内存泄漏

### 本轮改动

#### 1. 删除 `_on_tree_changed_by_fate` 中的错误节点重建代码
原代码在tree_changed回调中重新创建Polygon2D节点，这会导致节点堆积。
修复后回调只调用 `_update_gun_display` 刷新枪型，节点复用已在 `_ready` 中通过 `_setup_nodes` 完成。

#### 2. 添加缺失的 `_setup_nodes()` 函数
在 `_ready()` 前添加正式的节点初始化函数：
```gdscript
func _setup_nodes() -> void:
    _body = Polygon2D.new()
    _body.name = "GunBody"
    _body.z_index = 1
    add_child(_body)
    _muzzle = Polygon2D.new()
    _muzzle.name = "MuzzleFlash"
    _muzzle.visible = false
    _muzzle.z_index = 2
    add_child(_muzzle)
```

#### 3. 删除重复的 `_refresh_weapon_from_player` 定义
保留第一个定义（信号连接版本），删除第二个重复定义。

### 玩家可感知的变化
- 命运卡片改造武器后，WeaponDisplay枪型能正确刷新，不再有节点泄漏风险
- 游戏运行稳定，不会有内存/节点堆积问题

### 验证
- Godot headless 编译检查通过（无脚本错误）
- 命运卡片应用链路完整：FateCardEngine.apply_card → WeaponAssemblyTree.tree_changed.emit → WeaponDisplay._on_tree_changed_by_fate → _update_gun_display

### 下一轮方向
命运卡片改造后子弹背枪效果（ATTACH_GUN_TO_BULLET）的完整验证：
1. Bullet.gd 的 set_attached_gun 被正确调用
2. 子弹飞行时携带的枪在 Bullet.gd 中渲染为附加多边形（当前未实现）
3. Bullet.apply_fate_stats_from_node 的视觉（眼睛/脚/缩放）在实际子弹上体现

---

## Round 148 — 2026-05-25 05:56

**维度**: 子弹背枪效果 — Bullet.gd 挂载枪渲染为多边形

**当前玩家问题**: 命运卡片 ATTACH_GUN_TO_BULLET（子弹背枪）效果存在逻辑链路但视觉缺失——Bullet.gd 中 set_attached_gun() 被调用时会读取枪身信息并有自动射击逻辑（_process_attached_gun_firing），但子弹身上没有渲染挂载枪的多边形视觉，玩家无法直接看到"子弹背枪"的改造效果。

**本轮选择原因**:
1. 轮次147修复了WeaponDisplay.gd重复函数+缺失_setup_nodes，轮次146实现了命运改造后枪械视觉刷新
2. 当前Bullet.gd有_attached_gun_node/_attached_gun_polygon成员变量但没有正确关联到节点
3. Bullet.tscn中已添加AttachedGunPolygon节点但@onready声明与静态变量冲突导致无法编译
4. 修复后玩家在改造房应用"子弹背枪"命运卡片时，子弹会显示挂载枪的多边形外形（与WeaponDisplay.gd一致的枪型定义）

**玩家体验的前后变化**:
- Before: 子弹背枪命运卡片应用后，子弹只会在近战范围自动射击，但没有挂载枪的视觉表现
- After: 子弹飞出行进时，尾部渲染出挂载枪的多边形（根据枪型显示不同形状和颜色），玩家能直观看到"我的子弹上背了一把枪"

**涉及代码/数据/文档**:
- `scenes/Bullet.tscn`: 新增 Polygon2D 节点 `AttachedGunPolygon`（visible=false, z_index=2）
- `src/bullet/Bullet.gd`:
  - 移除了 `_attached_gun_polygon` 类成员静态声明（避免与 @onready 变量重名冲突）
  - 新增 `@onready var _attached_gun_polygon: Polygon2D = $AttachedGunPolygon`
  - 新增 `GUN_SHAPES` / `DEFAULT_GUN_SHAPE` 静态字典（与 WeaponDisplay.gd 一致的枪型顶点定义）
  - `_render_attached_gun(gun_name)` 方法：根据枪型从 GUN_SHAPES 取多边形顶点渲染
  - `set_attached_gun()` 中调用 `_render_attached_gun(gun_node.node_name)` 触发视觉挂载

**验收标准**:
- [ ] Godot headless 编译通过 ✅
- [ ] Bullet.tscn 包含 AttachedGunPolygon Polygon2D 节点
- [ ] 玩家应用命运卡片 ATTACH_GUN_TO_BULLET 后，发射的子弹尾部渲染出挂载枪多边形
- [ ] 自动射击逻辑在近战范围（_find_nearest_enemy）正确触发
- [ ] 实际试玩验证：子弹背枪视觉 + 自动射击手感

**验证**: Godot 4.6.2 headless 编译通过 ✅

**剩余风险**:
- Bullet._render_attached_gun 只在 set_attached_gun 时调用一次，挂载后不会更新（如玩家中途切换武器等）
- 挂载枪多边形朝向与子弹方向一致（Bullet.rotation = direction.angle），符合预期
- 实际试玩手感需人类验证

## 轮次 150 — 2026-05-25 06:10 UTC+8

### 维度
命运卡片副枪子弹链路补全（P15）

### 问题分析
审查轮次148/149完成的"子弹背枪"命运卡片链路时发现一处断路：

WeaponAssemblyTree._spawn_bullet_from()（主枪开火）完整注入了命运卡片效果：
- set_attached_gun(挂载枪)
- apply_fate_stats_from_node(命运视觉)

但 _spawn_bullet_from_co_gun()（副枪开火，跟随射击）只有 fire() 调用，没有这两行。

这意味着"枪上加枪"（Fate.SecondaryGun）机制产生的副枪子弹不会携带"子弹背枪"的挂载枪视觉，也不会应用"变大了"/加眼睛/加脚等命运视觉。副枪子弹在近战范围内应该和主枪子弹有相同的命运卡片表现。

### 本轮改动

#### _spawn_bullet_from_co_gun() 补全命运卡片链路
在副枪子弹发射后，补充与 _spawn_bullet_from() 相同的命运卡片处理逻辑：
```gdscript
# 副枪子弹也需要挂载枪视觉（命运卡片"子弹背枪"机制）
var attached_gun: AssemblyNode = _find_bullet_attached_gun()
if attached_gun != null and bullet.has_method("set_attached_gun"):
    bullet.set_attached_gun(attached_gun)
# 副枪子弹也需要命运视觉（变大了、加眼睛等）
if bullet.has_method("apply_fate_stats_from_node"):
    var bullet_node: AssemblyNode = _find_bullet_node()
    if bullet_node != null:
        bullet.apply_fate_stats_from_node(bullet_node)
```

### 玩家可感知的变化
- **Before**: 副枪（枪上加枪）发射的子弹没有命运卡片视觉（挂载枪多边形/眼睛/脚），玩家会觉得副枪子弹"没有改造过"
- **After**: 副枪子弹和主枪子弹拥有相同的命运卡片效果（挂载枪多边形在子弹尾部可见 + 变大了/眼睛/脚等视觉）

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/weapons/WeaponAssemblyTree.gd | _spawn_bullet_from_co_gun() 补全 set_attached_gun + apply_fate_stats_from_node 调用 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- 副枪子弹的挂载枪射击冷却（_process_attached_gun_firing 的 _attached_gun_cooldown）是 Bullet 实例变量，两颗副枪子弹之间是独立的，不共享冷却 —— 这是正确的（各自独立冷却追踪）
- 命运卡片"子弹背枪"实际效果需要人类试玩验证（应用卡片后主枪+副枪子弹尾部是否都有挂载枪多边形；近战范围自动射击是否触发）

### 下轮最可能方向
1. 命运卡片完整链路人类试玩验证（子弹背枪+命运视觉+自动射击范围判定）
2. 武器装配树节点拖拽交换槽位（更直接的交互方式）
3. 搜打撤风险决策深化（撤离读条时敌人追击压力）
4. ELITE 房间独立战斗波次逻辑

## 轮次 152 — 2026-05-25 06:25 UTC+8

### 维度
FateCardUIController 实例化修复 + 命运卡片 Tab 键完整接入

### 问题分析
命运卡片系统（轮次143整合）在代码审查时发现一个关键断点：

**FateCardUIController.gd 是 Control 脚本，但它从未被实例化为场景节点**：
- `FateCardUIController.gd` 有 `_input()` 处理 Tab 键事件
- FateCardGameBridge 是 Autoload，可以随时调用
- 但 FateCardUIController 本身是独立的 `.gd` 文件，不是 `.tscn` 场景
- `DemoRoomGameMode._spawn_player()` 只实例化了 Player，没有实例化 FateCardUIController

这导致 **Tab 键事件没有人接收**，玩家无法呼出命运卡片选择界面。WorkbenchPanel 的命运改造模式（T 键）可以工作是因为 WorkbenchPanel 在改造房打开时会实例化并直接处理卡片选择，但全地图 Tab 呼出功能缺失。

### 本轮改动

#### 1. 新建 `scenes/FateCardUIController.tscn`
最小化 Control 节点场景，承载 `_input()` Tab 键处理：
```
FateCardUIController (Control, layout_mode=3, 全屏锚点)
  ↳ 挂载 FateCardUIController.gd
```
实际卡片选择面板使用 GameUIManager/FateCardPanel（FateCardPanel 已在轮次143中补全了内部结构）。

#### 2. DemoRoomGameMode._spawn_player() 新增 _spawn_fate_card_ui()
```gdscript
func _spawn_fate_card_ui() -> void:
    var fate_ui_scene := preload("res://scenes/FateCardUIController.tscn") as PackedScene
    if fate_ui_scene != null:
        var fate_ui: Control = fate_ui_scene.instantiate() as Control
        if fate_ui != null:
            add_child(fate_ui)
            print("[DemoRoomGameMode] FateCardUIController 已实例化")
```

在 `_spawn_player()` 末尾调用，确保 Player 实例化后立即创建卡片 UI 控制器。

### 玩家可感知的变化
- **按 Tab 键**可以在游戏任意时刻呼出命运卡片选择面板（暂停游戏）
- 随机抽取 3 张卡片（品质颜色区分：白/蓝/紫/金/红）
- 点击卡片后通过 FateCardGameBridge 应用到 WeaponAssemblyTree
- 应用成功/失败在 WaveIndicatorLabel 位置显示提示

### 本轮改动
| 文件 | 改动 |
|---|---|
| scenes/FateCardUIController.tscn | 新建（449字节，最小化 Control 节点，挂载 FateCardUIController.gd） |
| src/game/DemoRoomGameMode.gd | `_spawn_player()` 末尾新增 `_spawn_fate_card_ui()` 调用 |

### 验证
- Godot --headless --quit-after 2: EXIT 0 ✅

### 剩余风险
- FateCardUIController 的 `_ui_manager` 引用（GameUIManager）在 DemoRoomChain 中可能为 null，因为 DemoRoomGameMode 没有真正的 GameUIManager 场景 —— 但这不影响 `show_fate_card_notification` 的空调用防御
- FateCardGameBridge 作为 Autoload，在 DemoRoomChain 的 Player 实例化后已经通过 `FateCardGameBridge.set_player(player)` 建立了 weapon_tree 链接
- **人类试玩仍缺失**：需要实际在 DemoRoomChain 中按 Tab 呼出卡片、选"子弹背枪"、验证子弹尾部挂载枪多边形是否正确渲染

### 下轮最可能方向
1. **命运卡片完整链路人类试玩验证**：8房间 DemoRoomChain 实际验证
   - Tab 键呼出卡片选择 → 选择"子弹背枪" → 子弹尾部暗金色小型枪多边形
   - 选择"变大了" → 子弹放大 + 碰撞体扩大
   - 命中有 AttachedGun 的子弹自动范围射击（近战敌人检测）
2. FateCardEngine 的 `apply_fate_stats_from_node` 是否在 `set_attached_gun` 之外被正确调用（当前只在 `_spawn_bullet_from` 中调用，命运视觉在非命运子弹上可能缺失）
3. 改造房 T 键命运改造模式 → 选"枪上加枪" → 副枪子弹链路验证

---

## 轮次 151 — 2026-05-25 06:13 UTC+8

### 维度
命运卡片完整链路审查 + 空指针保护修复

### 问题分析
轮次150完成了_spawn_bullet_from_co_gun补全（set_attached_gun + apply_fate_stats_from_node），但链路上游仍有一个潜在空指针问题需要修复：

**WeaponAssemblyTreePanel._on_tree_changed**：
- 当 FateCardEngine 执行 `tree_changed.emit()` 时，`_on_tree_changed` 被调用
- 轮次144的高亮恢复逻辑在树变化后试图访问 `_selected_node.get_path_string()`
- 如果 `_selected_node` 是一个已被卸载/销毁的节点（命运卡片应用后节点被替换），这会导致空指针崩溃

这是一个真实的**设计分叉风险**：命运卡片应用改变了装配树结构，之前选中的节点可能已不存在。

### 本轮改动

#### `_on_tree_changed` 空指针保护
```gdscript
func _on_tree_changed() -> void:
    var prev_path := ""
    if _selected_node != null and is_instance_valid(_selected_node):
        prev_path = _selected_node.get_path_string()
    _refresh()
    # 树刷新后恢复选中高亮（仅在之前有有效选中时）
    if prev_path != "" and _weapon_tree != null:
        _find_and_highlight_row(_weapon_tree.get_root(), prev_path)
```

- 新增 `is_instance_valid(_selected_node)` 检查，确保节点仍然有效
- 新增 `_weapon_tree != null` 防御

### 玩家可感知的变化
无直接玩家可感知变化（防御性修复），但避免了在命运卡片应用后玩家点击武器装配树时可能发生的崩溃。

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/WeaponAssemblyTreePanel.gd | `_on_tree_changed` 新增 `is_instance_valid` + `_weapon_tree != null` 防御检查 |

### 验证
- 代码审查通过（WeaponAssemblyTree.gd + Bullet.gd + FateCardEngine.gd 链路清晰）
- 空指针修复逻辑正确
- Godot --headless --check-only 无法在当前环境运行（无超时命令）

### 剩余风险
- **人类试玩验证仍缺失**：核心命运卡片链路（bullet_carry_gun + gun_on_gun + 命运视觉 + 副枪子弹）尚未在 DemoRoomChain 中实际验证
- `_find_and_highlight_row` 在节点被卸载后恢复高亮的路径匹配可能失效（节点 ID 变化），但这是边界情况，影响很小

### 下轮最可能方向
1. **DemoRoomChain 人类试玩**：8房间完整流程，验证改造房命运卡片选择 + 子弹背枪 + 命中有 AttacheGun 的子弹自动范围射击
2. 武器装配树节点拖拽交换槽位（更直接的交互方式）
3. ELITE 房间独立战斗波次逻辑


## 轮次 153 — 2026-05-25 06:39 UTC+8

### 维度
撤离读条期间受伤中断机制（搜打撤核心机制 P09）

### 问题分析
搜打撤"撤离期间受攻击中断"是核心风险决策之一。当前 DemoRoomChain 的撤离流程存在断点：

1. **ExtractionModule.abort_extraction()** 已完整实现（发射 `extraction_aborted` 信号，清除状态）
2. **DemoRoomGameMode._try_start_extraction()** 只连接了 `extraction_completed`，没有连接 `extraction_aborted`
3. **DemoRoomGameMode 没有玩家受伤监听** — `hp_changed` 信号从未连接，`take_damage` 后不会触发任何撤离中断逻辑
4. **RoomGameMode**（正式游戏模式）已有完整的 `_on_extraction_aborted` + `_on_hp_changed` 监听，但 DemoRoomChain 是独立演示，没有接入

这意味着：DemoRoomChain 中玩家按 E 触发撤离读条后，就算被敌人打掉血，读条也会继续完全不受影响——完全失去搜打撤的核心压力。

### 本轮改动

#### 1. DemoRoomGameMode._try_start_extraction() 连接 extraction_aborted 信号
```gdscript
# 原来：只连接 extraction_completed
_extraction_module.extraction_completed.connect(_on_extraction_completed)

# 改为：同时连接 extraction_completed + extraction_aborted
if not _extraction_module.extraction_completed.is_connected(_on_extraction_completed):
    _extraction_module.extraction_completed.connect(_on_extraction_completed)
if not _extraction_module.extraction_aborted.is_connected(_on_extraction_aborted):
    _extraction_module.extraction_aborted.connect(_on_extraction_aborted)
```

#### 2. DemoRoomGameMode._spawn_player() 连接 hp_changed 信号
```gdscript
# 实例化命运卡片 UI 控制器（Tab 键呼出卡片选择）
_spawn_fate_card_ui()

# 连接玩家受伤信号（用于撤离读条中断）
if _player.has_signal("hp_changed"):
    _player.hp_changed.connect(_on_player_hp_changed)
```

#### 3. 新增 _on_extraction_aborted() 和 _on_player_hp_changed()
```gdscript
## 撤离中断回调
func _on_extraction_aborted() -> void:
    _update_label("=== 撤离已中断 ===\n你在读条期间受到了攻击！\n请重新按 [E] 开启撤离")

## 玩家受伤时中断撤离读条（搜打撤核心机制）
func _on_player_hp_changed(current: int, maximum: int) -> void:
    if _extraction_module == null:
        return
    if _extraction_module.get_status() == ExtractionModule.ExtractionStatus.COUNTDOWN:
        _extraction_module.abort_extraction()
        _update_label("=== 撤离已中断 ===\n受到攻击！请重新按 [E] 撤离")
```

### 玩家可感知的变化
- **Before**：撤离读条期间被敌人打到也继续读条，没有任何惩罚，完全没有搜打撤风险感
- **After**：撤离读条期间受到攻击 → 读条立即中断 → UI 提示"撤离已中断" → 玩家需要重新按 E 开启读条（再次暴露在敌人攻击下）

这让"是否在安全时机撤离"成为真实决策：如果玩家在怪物包围中读条，被打到会中断+浪费逃跑窗口期。

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/DemoRoomGameMode.gd | `_try_start_extraction` 连接 `extraction_aborted`；`_spawn_player` 连接 `hp_changed`；新增 `_on_extraction_aborted` + `_on_player_hp_changed` |

### 验证
- Godot --headless --quit-after 2: EXIT 0 ✅

### 剩余风险
- 撤离读条中断后玩家可能在重新读条期间再次被打断（形成循环），这本身就是正确的搜打撤体验（高风险撤离场景）
- `_extraction_started` 标志在 `abort_extraction` 后保持 true，重新按 E 会再次触发 `_try_start_extraction`（符合预期）
- 实际手感需要人类试玩验证：被打断时的挫败感 vs 成功后带出战利品的成就感

### 下轮最可能方向
1. 命运卡片完整链路人类试玩验证（Tab键呼出→选"子弹背枪"→子弹尾部挂载枪多边形→近战范围自动射击）
2. 撤离读条中断后的敌人追击压力（中断后怪物追上来的视觉/AI表现）
3. ELITE 房间独立战斗波次逻辑（RoomEliteLogic.gd）
4. 武器装配树节点拖拽交换槽位（更直接的交互方式）

## 轮次 155 — 2026-05-25 07:06 UTC+8

### 维度
FateCardUIController UI组查找一致性修复

### 问题分析
审查 FateCardUIController 的 `_ui_manager` 查找逻辑时发现一个不一致问题：

FateCardUIController._ready() 中使用 `get_tree().get_first_node_in_group("ui_manager")` 查找 GameUIManager 实例，但 GameUIManager.gd 实际调用的是 `add_to_group("game_ui")`（轮次151 HEAD~1 diff 显示），两组名不匹配导致查找永远失败，回退到节点路径。

同时，`_ui_manager` 还被用来调用 `show_fate_card_notification(message)` 反馈用户选卡结果，这个方法在 GameUIManager 中是 public 方法，GameUIManager 在 DemoRoomChain 中作为场景节点树顶层节点存在。

### 本轮改动

将 `get_first_node_in_group("ui_manager")` 改为 `get_first_node_in_group("game_ui")` 以匹配 GameUIManager.gd 中实际注册的组名。

### 玩家可感知的变化
- 命运卡片应用后，`show_fate_card_notification` 现在能正确在 GameUIManager 的通知标签上显示应用成功/失败提示
- 之前因为组名不匹配，回退到了"应用成功但没有反馈提示"的静默状态

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/FateCardUIController.gd | `_ui_manager` 组查找：从 `ui_manager` 组改为 `game_ui` 组 |

### 验证
- Godot --headless --quit-after 1: EXIT 0 ✅

### 剩余风险
- GameUIManager._ui_manager 在 DemoRoomChain 中实际通过 add_to_group("game_ui") 存在（已通过 diff 确认），但 FateCardUIController 实例化时 Player 还未初始化，FateCardGameBridge 的 `_connect_to_player` 可能稍后执行
- 组名统一后，`show_fate_card_notification` 能正确显示，GameUIManager 其他 public 方法（`show_boss_hp`/`update_boss_hp`）不在此路径调用，不受影响
- **人类试玩仍缺失**：需要实际在 DemoRoomChain 中按 Tab 呼出卡片、选"子弹背枪"、验证子弹尾部挂载枪多边形是否正确渲染

### 下轮最可能方向
1. **人类试玩验证**：所有代码链路已完整，亟需实际试玩确认体感
2. 搜打撤经济系统（货币/掉落/撤离收益）
3. 武器装配树拖拽交互优化
4. 波次压力曲线微调

## 轮次 162 — 2026-05-25 09:22 UTC+8

### 维度
FateCardPresets.all_presets() 编译错误修复（P0 阻断）

### 问题分析
轮次161后 Godot headless 编译报错：
```
SCRIPT ERROR: Parse Error: Function "lucky_chest()" not found in base self.
  at: GDScript::reload (res://src/weapons/FateCardPresets.gd:216)
SCRIPT ERROR: Parse Error: Function "extra_loot()" not found in base self.
  at: GDScript::reload (res://src/weapons/FateCardPresets.gd:217)
```
all_presets() 在行216/217调用了 `lucky_chest()` 和 `extra_loot()`，但这两个函数在文件中并不存在（实际存在的是 `fate_lucky_chest()` 和 `fate_extra_loot()`，是环境命运触发器专用函数，不在 all_presets 的列表中）。

这是编译阻断错误，会导致 FateCardGameBridge（依赖 FateCardPresets 编译）无法加载。

### 本轮改动

#### FateCardPresets.all_presets() 修正
```diff
  every_seventh(),
  crit_on_kill(),
- lucky_chest(),
- extra_loot(),
+ fate_lucky_chest(),
+ fate_extra_loot(),
```

### 玩家可感知的变化
无直接玩家感知变化（编译错误修复）

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/weapons/FateCardPresets.gd | all_presets() 中 lucky_chest()/extra_loot() → fate_lucky_chest()/fate_extra_loot() |

### 验证
- Godot --headless --quit-after 3: EXIT 0 ✅

### 剩余风险
- 所有 161 轮系统级改动（命运卡片完整链路、搜打撤撤离中断、BOSS战完整流程、武器装配树面板）已完整，亟需人类实际试玩验证
- 人类试玩才能验证的体感：Tab键呼出卡片选择→选"子弹背枪"→子弹尾部挂载枪多边形、改造房T键→命运改造→选"枪上加枪"→副枪子弹链路、撤离读条被攻击中断

### 下轮最可能方向
1. 搜打撤经济系统人类试玩验证（开箱掉落→背包→撤离带出完整链路）
2. 命运卡片"子弹背枪"/"变大了"实际效果体验（应用卡片→发枪→观察子弹视觉变化）
3. 8房间 DemoRoomChain 完整流程走查

---

## 轮次 164 — 2026-05-25 02:31 UTC+8

### 维度
撤离成功面板 HUD 显示整合（RoomGameMode → ExtractionSuccessPanel）

### 问题分析
审查 `RoomGameMode._on_extraction_completed()` 时发现一个重要的玩家体验断点：

撤离成功后，`_print_extraction_success()` 只在 console 输出统计信息（`=== 撤离成功 ===`），但**GameUIManager 已经有完整的 `ExtractionSuccessPanel` 面板**（包含物品清单、品质颜色、继续按钮），且 `show_run_extraction_success(stats)` 方法能够显示波次/击杀/魂/风险层级的完整战局统计。

RoomGameMode 没有调用这个面板，而 CoreCombatMode 的撤离流程正确调用了。这导致房间系统的撤离只有 console 日志，没有 HUD 面板反馈。

### 本轮改动

在 `RoomGameMode._on_extraction_completed()` 成功后，补充调用 `ui_layer.show_run_extraction_success(stats)` 字典包含 wave/kills/currency/score/risk 五个字段，与 CoreCombatMode 撤离流程保持一致。

```gdscript
# 显示撤离成功面板（HUD + 战局统计）
if ui_layer != null and ui_layer.has_method("show_run_extraction_success"):
    var wave_info: Dictionary = {}
    if _current_wave_spawner != null and is_instance_valid(_current_wave_spawner):
        wave_info = _current_wave_spawner.get_wave_info()
    ui_layer.call("show_run_extraction_success", {
        "wave": wave_info.get("current", 1),
        "kills": _kill_count,
        "currency": GameManager.currency,
        "score": score,
        "risk": _run_risk
    })
```

### 玩家可感知的变化
- **修复前**：撤离成功后只有 console 日志 `"=== 撤离成功 === 背包物品已保存: X 件"`，玩家看不到屏幕反馈
- **修复后**：撤离成功后弹出 ExtractionSuccessPanel 面板，显示"撤离成功  波次 N  击杀 N  魂 N"和"最终得分: X  风险层级: Y"，面板按物品品质着色、显示保险物品、点击"返回基地"回基地菜单

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/game/RoomGameMode.gd | `_on_extraction_completed()` 成功后补充调用 `ui_layer.show_run_extraction_success()` 显示 HUD 面板 |

### 验证
- Godot --headless --quit-after 4: EXIT 0 ✅

### 剩余风险
- 撤离成功面板显示时需要 `_inventory_module` 和 `_insurance_module` 正确绑定到 GameUIManager，确保 `get_all_items()` 和 `get_all_insured_items()` 有返回（需要 `set_inventory_module` / `set_insurance_module` 在 RoomGameMode 中已正确调用）
- 人类试玩验证：实际在 DemoRoomChain 中完成撤离，观察 ExtractionSuccessPanel 是否弹出、物品列表是否正确、点击"返回基地"是否正确切换场景

### 下轮最可能方向
1. **人类试玩验证**：所有代码链路已完整，亟需实际试玩确认体感
2. 搜打撤经济系统人类试玩验证（开箱掉落→背包→撤离带出完整链路）
3. 命运卡片"子弹背枪"/"变大了"实际效果体验

---

## 轮次 170 — 2026-05-25 12:32 UTC+8

### 维度
第一章玩法闭环 — 搜索/掉落/钥匙/开门/命运卡/武器装配

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/base/ItemRegistry.gd | 新增房间钥匙物品，修复重复 `get_item()` 声明 |
| src/game/LootModule.gd | 第一章战斗/精英掉落补齐基础掉落池；搜索和怪物掉落增加钥匙概率 |
| src/game/RoomGameMode.gd | 门随地图生成可见；开门消费背包钥匙；掉落/搜索钥匙同步门钥匙计数 |
| src/ui/GameUIManager.gd | 背包无图标物品生成可读占位图；右键模块/配件可装配到武器树；K 键查看武器树 |
| verify_ch1_gameplay_loop.gd / .tscn | 新增第一章闭环体验验收 |

### 玩家可感知的变化
- 地图生成时门已经存在，玩家拿钥匙是为了解锁门，而不是生成门。
- 搜索和击杀都会推动钥匙、物品、武器件获取，第一章形成“打/搜 → 拿资源 → 开门 → 命运卡/武器组合 → 继续探索”的闭环。
- 背包物品不再空白，武器模块和配件可以直接右键装配。

### 验证
- `godot --headless --path . verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `godot --headless --path . verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`
- `godot --headless --path . verify_map_layout_flow.tscn`: `MAP_LAYOUT_FLOW_OK`
- `godot --headless --path . verify_p1_extraction_flow.tscn`: `P1_EXTRACTION_FLOW_OK`
- `godot --headless --path . verify_p0_player_flow.tscn`: `P0_PLAYER_FLOW_OK`
- `godot --headless --path . verify_p1_workbench_flow.tscn`: `P1_WORKBENCH_FLOW_OK`
- `godot --headless --path . --quit-after 3`: EXIT 0

### 剩余风险
- 背包图标仍为程序化占位，后续需要正式美术资源。
- 怪物长时间刷怪节奏仍建议真人试玩确认。

---

## 轮次 171 — 2026-05-25 13:18 UTC+8

### 维度
第一章导演路径修复

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/base/ItemRegistry.gd | 新增可装备武器掉落物，包含散射喷壶 |
| src/map/RoomFactory.gd / src/map/ContentInjector.gd | 开局小房间箱子保证给散弹枪 |
| src/game/ContainerInteraction.gd | 容器支持设计指定保底物品 |
| src/ui/GameUIManager.gd | I 键打开背包并显示武器树；背包右键武器可换枪 |
| src/game/RoomGameMode.gd | 开初始门稳定触发命运卡三选一；怪物魂奖励改回地面魂球拾取 |
| scenes/Bullet.tscn / src/bullet/Bullet.gd | 玩家子弹撞墙销毁 |
| verify_ch1_director_loop.gd / .tscn | 新增完整导演路径验收 |

### 验证
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`
- `verify_map_layout_flow.tscn`: `MAP_LAYOUT_FLOW_OK`
- `verify_p0_player_flow.tscn`: `P0_PLAYER_FLOW_OK`
- `verify_p1_extraction_flow.tscn`: `P1_EXTRACTION_FLOW_OK`
- `godot --headless --path . --quit-after 3`: EXIT 0

### 补充修复
- 背包右键武器现在是装备交换：新武器进装备位，旧主武器回背包，不再像消耗品一样被吃掉。
- `verify_ch1_director_loop.tscn` 已覆盖散弹枪换装后 `weapon_pistol` 回到背包。

---

## 轮次 172 — 2026-05-25 15:55 UTC+8

### 维度
第一章背包与装备反馈修复

### 本轮改动
| 文件 | 改动 |
|---|---|
| src/ui/GameUIManager.gd | 普通左键只选择物品，`Shift + 左键` 存保险，保险格左键取回；满包取回不丢失；背包/保险格可拖动；HUD 显示当前主枪位 |
| verify_inventory_equipment_ui.gd / .tscn | 覆盖装备位显示、保险格往返、背包移动 |
| docs/PH09_搜打撤深化.md | 固化背包、保险格与装备系统的玩家交互规则 |

### 玩家可感知的变化
- 不会再因为查看背包物品而误进保险箱，保险物品能够回到背包。
- 主界面能始终读到当前使用的枪，换枪不再只依赖隐藏状态。
- 背包界面可以挪开，战斗中检查物资时不必被固定面板遮住画面。

### 验证
- `verify_inventory_equipment_ui.tscn`: `INVENTORY_EQUIPMENT_UI_OK`
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `godot --headless --path . --quit-after 3`: EXIT 0

### 剩余风险
- 正式物品/枪械图标仍未接入。
- Headless 测试退出有资源清理警告，未影响验收结果，后续需要单独排查。

---

## 轮次 173 — 2026-05-25 16:20 UTC+8

### 维度
第一章钥匙门命运成长闭环

### 玩家可感知的变化
- 不只是初始门：后续每次用钥匙开启一扇新门，都会进入命运卡三选一。
- `超频` 会立刻改变射速，`变大了` / `穿甲强化` 会立刻改变当前子弹伤害或视觉，组合卡会接入枪械装配结构。
- 当前装备无法承载某张组合卡时，不会吞掉选择机会，会提示改选。
- 基地占卜选择会在第一扇门的卡片中保留出现，不再无声失效。

### 实现与验收
| 文件 | 改动 |
|---|---|
| src/game/RoomGameMode.gd | 任意钥匙门触发选卡；处理失败重选与基地预选保留 |
| src/weapons/WeaponAssemblyTree.gd / FateCardEngine.gd | 将命运效果同步到实时射击属性 |
| src/weapons/FateCardPresets.gd / src/ui/*.gd | 玩家可抽取入口仅展示已实现牌池 |
| verify_initial_room_fate_door_flow.gd / verify_ch1_director_loop.gd | 检查后续开门选卡与实际属性改变 |

### 验证
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_p1_workbench_flow.tscn`: `P1_WORKBENCH_FLOW_OK`
- `godot --headless --path . --quit-after 3`: EXIT 0

### 剩余风险
- 未完成的概念命运卡已隔离，后续需逐张补玩法后再开放。
- Headless 退出时仍有资源清理警告。

---

## 轮次 174 — 2026-05-25

### 维度
第一章房间物理刷新稳定性

### 玩家可感知的变化
- 踩到怪物掉落或搜索获得的钥匙后，门可正常更新，不再伴随墙体物理报错刷屏。
- 清掉房间最后一个敌人后，钥匙与可开启门在安全时机出现，连续探索流程保持稳定。
- 地图房间只保留一套真正阻挡玩家、怪物和子弹的墙体，门洞开启反馈与通行结果一致。

### 实现与验收
| 文件 | 改动 |
|---|---|
| src/map/RoomLayout.gd | 延迟合并门状态导致的墙体碰撞重建 |
| src/map/RoomVisualizer.gd / RoomTileMapInitializer.gd | 在 `RoomLayout` 管理房间中禁用重复边界碰撞 |
| src/game/RoomGameMode.gd | 延迟拾钥匙/清房触发的门、钥匙与出口刷新 |
| verify_map_layout_flow.gd | 覆盖玩家真实碰钥匙并开门形成门洞的路径 |

### 验证
- `verify_map_layout_flow.tscn`: `MAP_LAYOUT_FLOW_OK`，日志未再产生 `flushing queries` 物理错误
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_inventory_equipment_ui.tscn`: `INVENTORY_EQUIPMENT_UI_OK`
- `godot --headless --path . --quit-after 3`: EXIT 0

### 剩余风险
- 测试退出仍有对象/RID/资源释放告警，建议下一轮以长时间多房间战斗日志和退出清理为专项。

---

## 轮次 175 — 2026-05-25

### 维度
第一章怪物奖励落地拾取

### 玩家可感知的变化
- 杀怪后不再无声把枪械、配件或钥匙塞入背包；有物品奖励时会显示在尸体附近，走近才收取。
- 普通怪允许没有物品奖励，避免满地战利品稀释惊喜；但每只怪物仍然必定掉落可拾取魂币。
- 精英和 Boss 保持稳定的高价值物品回报，不过同样需要玩家实际拾取。
- 拾到地面钥匙后，开门数量与背包里的钥匙数量会可靠联动。

### 实现与验收
| 文件 | 改动 |
|---|---|
| src/items/GroundItemPickup.gd | 新增地面物品节点、类型色彩与近距离拾取反馈 |
| src/game/RoomGameMode.gd / LootModule.gd | 物品掉落落地化、魂球必掉保留、钥匙数量同步修复 |
| verify_ch1_director_loop.gd / verify_ch1_gameplay_loop.gd | 覆盖地面拾取、必掉魂币与普通怪空掉落 |

### 验证
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_map_layout_flow.tscn`: `MAP_LAYOUT_FLOW_OK`
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`

### 剩余风险
- 地面掉落图形仍是系统化占位表现，后续可接入正式图标和掉落音效。
- 测试退出仍会输出已有资源释放告警。

---

## 轮次 176 — 2026-05-25

### 维度
独立撤离房守点终局与奖励密度调整

### 玩家可感知的变化
- Boss 房只负责 Boss 战；地图中另有真正的撤离房，玩家抵达后需走到中央装置按 `E` 启动。
- 启动撤离后进入 14 秒防守倒计时：开场即有敌压，中段增援，末段有随风险提高概率出现的精英拦截。
- 倒计时结束时仍然存活才弹出撤离成功结算；玩家主动中断后可重新启动装置。
- 普通怪更少掉落物品与钥匙，但每只怪仍必掉魂币，战斗收益稳定而物品更珍贵。

### 实现与验收
| 文件 | 改动 |
|---|---|
| src/game/ExtractionRoomLogic.gd / RoomGameMode.gd / src/ui/GameUIManager.gd | 装置交互、守点刷怪、倒计时与结算链路 |
| src/map/RoomData.gd / RoomFactory.gd | Boss/撤离职责分离并移除中央伪箱子 |
| src/game/LootModule.gd | 普通怪物品与钥匙掉率下调 |
| verify_ch1_extraction_defense_flow.gd / .tscn | 玩家路径验收覆盖 |

### 验证
- `verify_ch1_extraction_defense_flow.tscn`: `CH1_EXTRACTION_DEFENSE_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_p1_extraction_flow.tscn`: `P1_EXTRACTION_FLOW_OK`
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`

### 剩余风险
- 撤离守点敌潮仍需要实机游玩校准难度与刷怪方向反馈。
- 测试退出仍有已有资源释放告警。

---

## 轮次 177 — 2026-05-25

### 维度
撤离成功结算的可用性与战斗收束

### 玩家可感知的变化
- 撤离倒计时完成后，结算界面立即出现，不再需要先按 `Esc` 才能看到。
- 结算界面出现后玩家不能继续行动，撤离房敌人被清走，异步追兵不会继续刷出。
- `返回基地` 按钮可在结算暂停状态下直接点击，完成本局退出。

### 实现与验收
| 文件 | 改动 |
|---|---|
| src/ui/GameUIManager.gd | 结算面板暂停中可处理、暂停状态同步与返回恢复 |
| src/game/RoomGameMode.gd / src/map/RoomWaveSpawner.gd | 成功时锁定清场，取消未完成刷怪队列 |
| verify_ch1_extraction_defense_flow.gd | 结算可见、暂停输入、清场与按钮响应专项验收 |

### 验证
- `verify_ch1_extraction_defense_flow.tscn`: `CH1_EXTRACTION_DEFENSE_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_p1_extraction_flow.tscn`: `P1_EXTRACTION_FLOW_OK`
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`
- `verify_map_layout_flow.tscn`: `MAP_LAYOUT_FLOW_OK`
- `verify_inventory_equipment_ui.tscn`: `INVENTORY_EQUIPMENT_UI_OK`
- `verify_p1_workbench_flow.tscn`: `P1_WORKBENCH_FLOW_OK`
- 项目 headless 启动通过

### 剩余风险
- Headless 退出仍有已有资源释放告警，后续需单独排查。

---

## 轮次 178 — 2026-05-26

### 维度
撤离成功界面的真实离场交互修复

### 玩家可感知的变化
- 撤离成功后进入完整的终局遮罩，战斗 HUD、Boss 血条、背包与卡牌面板不再留在结算按钮旁干扰操作。
- `返回基地` 位于最高交互层，不会再被此前残留的全屏界面截获点击。
- 成功结算出现后不会再弹出新的局内选择层。

### 实现与验收
| 文件 | 改动 |
|---|---|
| src/ui/GameUIManager.gd | 建立顶层模态结算状态、收束残留 UI、保护返回场景切换 |
| src/game/ContainerInteraction.gd | 保证赠送散弹枪时过滤同枪随机重复掉落，清理开局装备体验 |
| verify_ch1_extraction_defense_flow.gd | 通过 Viewport 局部坐标真实点击验证按钮可操作，并检查终局遮罩/HUD 状态 |
| verify_ch1_director_loop.gd | 装备替换依据武器数量变化验收，不误判合法备用武器 |
| docs/PH09_搜打撤深化.md | 增补结算独占输入的玩家体验标准 |

### 验证
- `verify_ch1_extraction_defense_flow.tscn`: `CH1_EXTRACTION_DEFENSE_OK`
- `verify_ch1_director_loop.tscn`: `CH1_DIRECTOR_LOOP_OK`
- `verify_ch1_gameplay_loop.tscn`: `CH1_GAMEPLAY_LOOP_OK`
- `verify_p1_extraction_flow.tscn`: `P1_EXTRACTION_FLOW_OK`
- `verify_initial_room_fate_door_flow.tscn`: `INITIAL_ROOM_FATE_DOOR_OK`
- `verify_map_layout_flow.tscn`: `MAP_LAYOUT_FLOW_OK`
- `verify_inventory_equipment_ui.tscn`: `INVENTORY_EQUIPMENT_UI_OK`
- `verify_p1_workbench_flow.tscn`: `P1_WORKBENCH_FLOW_OK`
- 项目 headless 启动通过

### 剩余风险
- 测试退出仍会输出已有资源释放告警，和本次按钮交互阻断不同，后续继续专项处理。

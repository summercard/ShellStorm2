# v003 场景验收

日期：2026-09-16。环境：macOS / Blender 4.2.0 / Cycles。范围：Blender 源；无游戏存档读写。

## 结构与资产包

独立重开制作前备份和最终保存文件，核对78件原对象（39件结构输出及39件制作源）的网格、变换、父级、修改器类型及动画存在性：无差异。墙高11.9m、墙厚0.3m、房间30×25m；地面顶面Z=0.3m；东西门洞净宽2.2m、地面至门楣净高2.5m。装饰独立附着，未替换或缩放白盒结构。

82个包均非空，无输出多重归属，磁盘与集合清单无差异；30块地砖分别管理。任务生成检查及保存后独立检查退出码均为0。详见 `qa/task_validation.json` 与 `qa/saved_scene_validation.json`；后者是保存文件的结构签名验收依据。

## 材质与渲染

`validate_game_prop.py --all-meshes --max-materials 4 --shared-palette ...` 退出码0，17项检查全部通过。3939个网格包含隐藏制作组件与显示输出，并非游戏运行网格数；250890面全部具有安全内区内的有面积 PaletteUV。四材质、活动/渲染UV、唯一公共外链色盘、Closest、主体发光分离均通过。输出为152个网格。

同相机制作前/后、完整结构俯视与机柜近景已真实渲染并检查。主镜头采用剖切层，完整结构未删减。蓝灰板材、冷色灯光、设备阵列和电缆符合参考方向；原白盒更高墙体与门洞位置优先保留，破损和零碎物密度较参考图简化。

## 已知例外与未执行项

- 台账逐单元格、公式和样式检查退出0，仅修改本房间6个字段，无样式变化，源哈希匹配；见 `qa/ledger_validation.json`。文档合同检查和差异空白检查退出0。
- 通用 `asset_guard.py` 退出2，仅报 `invalid_filename`：其正则只接受英文和不含 `.blend` 的扩展名列表；本文件遵循专用 Blender 中文命名规范。已有资产身份按版本更新，未发现相同哈希的其他文件。详见 `qa/asset_guard.json`。未擅自放宽通用校验器。
- 最终两项 Blender 专项无非预期脚本错误。GUI首次切换视图因文件重开后上下文为空失败，已改用窗口管理器并成功保存；不影响最终验收。
- Godot 导出、运行时碰撞、游戏内灯光与性能验收未执行，不能据本报告视为完成接入。
- 输入备份为 `qa/制作前工作区备份.blend`，台账修改前备份为 `qa/ledger_before.xlsx`；原白盒路径继续保留。

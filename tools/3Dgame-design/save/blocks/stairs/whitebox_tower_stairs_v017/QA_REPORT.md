# 楼梯间 v017 组件化重整与栏杆修复验收

## 范围

- 输入源：`whitebox_tower_stairs_v016/whitebox_tower_stairs_v016.blend`。
- 用户指定结构边界：墙、地板、楼梯各为一个通用组件；既有组件组合与原集合归类不得随意改变；装饰物按内容建立组件。
- 授权修复：旧楼梯栏杆位置错误，可在新输出中替换；墙体、地板、踏步、平台和全部 v016 美术装饰只允许复制与重新归类，不改几何和世界变换。

## 组件结构

```text
楼梯区_组件化美术管理_v017
├─ 01_制作组件_v016锁定
├─ 02_游戏输出_组件包_v017
│  ├─ 01_通用建筑组件
│  │  ├─ 通用墙组件_资产包
│  │  ├─ 通用地板组件_资产包
│  │  └─ 通用楼梯组件_资产包（含修复后的栏杆）
│  └─ 02_按内容装饰组件
│     ├─ 墙面装甲与结构框_装饰组件
│     ├─ 地面导光与警示_装饰组件
│     ├─ 工业管线_装饰组件
│     ├─ 灯带与发光几何_装饰组件
│     ├─ 楼层标识与海报_装饰组件
│     ├─ 控制盒_装饰组件
│     └─ 固定绿植_装饰组件
└─ 90_展示与验收_沿用v016镜头
```

- 通用墙组件：26 个网格。
- 通用地板组件：25 个网格。
- 通用楼梯组件：42 个既有楼梯网格加 52 个新栏杆网格，共 94 个。
- 7 类装饰组件：57、41、17、5、10、6、9 个网格。
- v017 游戏输出合计 290 个网格；10 个末级组件全部非空，对象均唯一归属，没有一个对象同时进入两个末级组件。
- 10 个 Blender 末级组件均有一一对应的 `component_packages_v017/**/asset_manifest.json`；另有 `catalog.json` 与 `tree.txt`。

## 组合锁定

- v016 锁定源对象数：242。
- 修改前签名：`cde610a51a0b2c78007fcb068bc3e4be06377465b2ffd5d56502c15f086a11b3`。
- 修改后签名：`cde610a51a0b2c78007fcb068bc3e4be06377465b2ffd5d56502c15f086a11b3`。
- `locked_match=true`。
- 238 个进入 v017 的非栏杆对象均逐对象验证世界矩阵、顶点/边/面计数和顶点坐标；`copy_mismatch=[]`。
- 旧 v016 的五个资产包仍为 97、57、41、25、22 个对象，原集合结构作为锁定制作源保留。

## 栏杆修复

- 旧 `Guard_01/02` 四个错误栏杆仅在 v016 锁定源保留并隐藏，不进入 v017 游戏输出。
- 两跑楼梯均以 20 级踏步的真实世界包围盒为定位真值；栏杆位于踏步有效宽度内部，而不是沿房间外轮廓估算。
- 上跑栏杆：左右中心线 `X=41.851 / 46.151m`，覆盖 `Y=3.260–18.230m`，踏步高程 `Z=-14.820–-9.050m`。
- 下跑栏杆：左右中心线 `X=33.851 / 38.151m`，覆盖 `Y=3.010–17.980m`，踏步高程 `Z=-20.820–-15.050m`。
- 每跑左右各 5 根立柱、4 段上扶手、4 段中横杆；每跑 26 个栏杆网格，两跑共 52 个。
- 立柱从对应踏步顶面起升 1.12m；栏杆未修改踏步、平台或楼板高度。

## 技术与视觉验收

- `validate_game_prop.py --all-meshes`：退出码 0，`passed=true`。
- 文件内 629 个网格、21,188 个面全部通过 `PaletteUV` 色格岛检查，21,188/21,188 有效。
- 17 项材质/UV检查全部通过；仅 4 个共享材质，公共色盘外链、`Closest` 插值、无额外 UV、无混合自发光。
- v017 专项组件验收：290 个输出网格、10 个非空末级组件、无重复归属、238 个复制对象无几何或变换差异。
- 视觉输出：`楼梯间_v017_组件化参考镜头.png`、`楼梯间_v017_栏杆修复近景.png`、`楼梯间_v017_组件俯视.png`。

## 过程异常与收口

- 首次建立 v017 集合时使用了不兼容的 Collection 成员检查，未保存文件；改为名称检查后完整重跑。
- 首次用上下文算子测试栏杆圆柱时未生成新对象，并误操作了 `地板.047_游戏输出` 的当前副本；该对象随后依据未改动的 v017 对应复制件恢复。最终锁定签名完全一致、源包对象数恢复为 97、逐对象差异为空。
- 正式栏杆改用无上下文依赖的直接网格构建，不再依赖视图选择状态。

## 未执行

- 未导出 GLB，未建立简化碰撞/LOD，未导入 Godot PackedScene，未替换运行时引用。
- 本次状态为 Blender 组件化源场景和视觉验收完成，不等于运行时接入完成。

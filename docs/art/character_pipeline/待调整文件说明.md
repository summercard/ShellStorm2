# 待调整文件说明（跨机器执行）

日期：2026-09-18
背景：角色类表现资产链路已拆分为 6 段 skill，`game-character-model-pipeline` 废除。本文件列出**需要你在另一台机器上调整的既有文件**——本机未改动其中任何一个。
配套：新增的 6 个 skill 已落在 `skills_drafts/01~06-character-*/`，并已同步项目 Codex 副本 `.codex/skills/01~06-character-*/`（本机已写入并随本次提交推送，你 pull 后两处副本直接可用）。

---

## 0. 一句话总览

| # | 文件 | 动作 | 风险 |
|---|---|---|---|
| A | `assets/registry/ledger_index.json` | 改 2 处 `primary_skill`，各域补 3 个字段 | 低（无脚本消费该字段） |
| B | `assets/registry/README.md` | 改表格 2 行 | 低 |
| C | `skills_drafts/README.md` | 改 1 行，增 6 行 | 低 |
| D | `skills_drafts/game-weapon-model-pipeline/SKILL.md` | 改 1 行引用 | 低 |
| E | `.codex/skills/game-weapon-model-pipeline/SKILL.md` | 同 D（同步副本） | 低 |
| F | 删除 `game-character-model-pipeline` 三处副本 | 删除 | 中（先确认无其它引用） |
| G | 安装 6 个新 skill 到两处 | 新增 | 低 |
| H | 总目录 xlsx《Skill清单》分页 | 新增 6 行 | 低 |
| I | `assets/art/3D模型资产目录与命名规范.md` | 新增 NPC/怪物路径章节 | 低（可选） |
| J | `docs/v0.1/` 主设计 | 立新主设计 + 改 `MODULE_INDEX.md` | 中（可选，见 §10） |

**建议执行顺序：A → B → C → D → E → G → F → H → （可选）I → J**

---

## A. `assets/registry/ledger_index.json`

### A-1 现状

| 行号 | 内容 |
|---|---|
| L40 | `"primary_skill": "game-character-model-pipeline",`（characters 域） |
| L54 | `"primary_skill": "game-character-model-pipeline",`（enemies 域） |

> 注：行号基于本机 2026-09-18 版本，改动前请以内容匹配为准。

### A-2 改法

**`characters` 域**——保留原有字段不动，替换 / 新增以下项：

```json
      "primary_skill": "01-character-contract-authoring",
      "supporting_skills": [
        "02-character-export-transfer",
        "03-character-godot-import",
        "04-character-state-binding",
        "05-character-verification",
        "06-character-ledger",
        "player-avatar-asset-standard"
      ],
      "skill_pipeline": [
        "01-character-contract-authoring",
        "02-character-export-transfer",
        "03-character-godot-import",
        "04-character-state-binding",
        "05-character-verification",
        "06-character-ledger"
      ],
      "art_root": "assets/art/characters",
```

**`enemies` 域**——同样替换 / 新增：

```json
      "primary_skill": "01-character-contract-authoring",
      "supporting_skills": [
        "02-character-export-transfer",
        "03-character-godot-import",
        "04-character-state-binding",
        "05-character-verification",
        "06-character-ledger"
      ],
      "skill_pipeline": [
        "01-character-contract-authoring",
        "02-character-export-transfer",
        "03-character-godot-import",
        "04-character-state-binding",
        "05-character-verification",
        "06-character-ledger"
      ],
      "art_root": "assets/art/enemies",
```

### A-3 三个字段的语义

| 字段 | 含义 |
|---|---|
| `primary_skill` | 该域的**链路入口** skill（S1）。单值，供路由用 |
| `supporting_skills` | 该域的**全部**责任 skill（含 S2–S6 与既有专用规范） |
| `skill_pipeline` | **有序**六段链路，S1→S6。新增，用于表达顺序 |
| `art_root` | 该域**美术资产根目录**，新增字段。供门禁校验「账本登记的 `文件路径` 必须落在本域 `art_root` 下」 |

### A-4 注意事项

- `primary_skill` / `supporting_skills` **当前没有任何脚本消费**（已全仓 grep 确认），所以改它**不会影响门禁运行**，纯声明。
- `skill_pipeline` 与 `art_root` 是新增字段。`scripts/ledger_registry.py` 的 `Domain` dataclass 若做了严格字段校验，需要同步加字段；若用 `**kwargs` 或 `getattr` 读取则无需改。
- **不要**改 `sheet_scope` / `prefab_pages` / `id_prefixes`——NPC 已经落在 `characters` 域现有 scope 内（该域 `scope_note` 本来就写了「玩家与 NPC 角色」）。
- **不要**给 NPC 新建第 8 个分账本。NPC 走角色账本。

---

## B. `assets/registry/README.md`

### B-1 现状（L32–33，第七章「七个分账本」表格）

```markdown
| `characters` | 角色账本 | 角色 | `CHR` `SKEL` `BONE` | `game-character-model-pipeline` |
| `enemies` | 敌人账本 | 敌人 | `ENM` | `game-character-model-pipeline` |
```

### B-2 改为

```markdown
| `characters` | 角色账本 | 角色 | `CHR` `SKEL` `BONE` | `01-character-contract-authoring` 等六段 |
| `enemies` | 敌人账本 | 敌人 | `ENM` | `01-character-contract-authoring` 等六段 |
```

若想写全，可改为：

```markdown
| `characters` | 角色账本 | 角色 | `CHR` `SKEL` `BONE` | `01-character-contract-authoring` → `02-character-export-transfer` → `03-character-godot-import` → `04-character-state-binding` → `05-character-verification` → `06-character-ledger` |
| `enemies` | 敌人账本 | 敌人 | `ENM` | 同上（六段通用，参数不同） |
```

### B-3 附加（可选）

第 1 节目录结构表下方「不许硬编码路径」一节，可补一句：

> 各域美术资产根目录由 `ledger_index.json` 的 `art_root` 声明；账本登记的「文件路径」必须落在该域 `art_root` 下。

---

## C. `skills_drafts/README.md`

### C-1 现状（L19，第「当前内容」列表）

```markdown
- `game-character-model-pipeline`：角色建模、动画、导出与 Godot 集成规范。
```

### C-2 改为（替换该行，新增 6 行）

```markdown
- `01-character-contract-authoring`：S1 角色类资产契约反推与 Blender 模型/动作双母版制作。
- `02-character-export-transfer`：S2 Blender 导出纯视觉 GLB 与严格中转台账。
- `03-character-godot-import`：S3 Godot 导入、稳定路径 PackedScene 包装与引用替换。
- `04-character-state-binding`：S4 状态机到动作的表现适配层与兜底登记。
- `05-character-verification`：S5 角色类资产专项验收与四类结果记录。
- `06-character-ledger`：S6 账本登记与制作状态推进（贯穿 S1–S5）。
```

> 原 `game-character-model-pipeline` 已废除，从列表移除。

### C-3 附加（可选）

L11 的使用约定提到「`player-avatar-asset-standard` 当前为草稿规范」，可保留不动；若新六段 skill 也处于「已落项目、未安装全局」状态，建议在该列表加一行说明。

---

## D. `skills_drafts/game-weapon-model-pipeline/SKILL.md`

### D-1 现状（L19）

```markdown
   在 ShellStorm2 中，必须先读取 `game-character-model-pipeline` 所列的角色消费者：当前角色侧为 `WeaponSocket`，武器侧为 `GripSocket` 和 `MuzzleSocket`。……
```

### D-2 改为

把 `` `game-character-model-pipeline` `` 替换为 `` `01-character-contract-authoring` ``，其余文字不动：

```markdown
   在 ShellStorm2 中，必须先读取 `01-character-contract-authoring` 所列的角色消费者：当前角色侧为 `WeaponSocket`，武器侧为 `GripSocket` 和 `MuzzleSocket`。……
```

---

## E. `.codex/skills/game-weapon-model-pipeline/SKILL.md`

与 D **完全相同**的一行改动。该文件是 D 的项目 Codex 副本，两份必须一致。

---

## F. 删除 `game-character-model-pipeline` 副本

废除该 skill，需删除三处：

| # | 路径 | 性质 |
|---|---|---|
| F-1 | `~/.workbuddy/skills/game-character-model-pipeline/` | 全局正本 |
| F-2 | `<repo>/.codex/skills/game-character-model-pipeline/` | 项目 Codex 副本 |
| F-3 | `<repo>/skills_drafts/game-character-model-pipeline/` | 项目草稿副本 |

### F-4 删除前确认

```bash
# 确认除三处副本外，没有别处引用
grep -rn "game-character-model-pipeline" --include="*.md" --include="*.json" --include="*.py" --include="*.gd" --include="*.js" . | grep -v "^./.git/"
```

若 `assets/` 与 `docs/` 的结果已清空（仅剩三处副本自身），即可安全删除。

### F-5 内容去向

原 skill 的内容**没有丢失**，已按阶段重新分配到六段 skill：

| 原 skill 的内容 | 新归属 |
|---|---|
| 「强制反推」5 条 | `01-character-contract-authoring` 第 1 步 |
| 「Blender 制作」8 条 | `01-character-contract-authoring` 第 2–4 步 |
| 严格中转台账 | `02-character-export-transfer` 第 3 步 |
| `asset_guard.py --classify` | `03-character-godot-import` 第 3 步 |
| Godot 导入与验收 6 条 | `03-character-godot-import` 第 5 步 + `05-character-verification` |
| 状态机只输出状态与进度 | `04-character-state-binding` 第 1 步 |
| 单手兜底 / 缺动作登记 | `04-character-state-binding` 第 3 步 |
| 「账本落位（分册化后）」整节 | `06-character-ledger` 全文 |
| `references/shellstorm2-character-reverse-contract.md` | 拆为 `01-character-contract-authoring/references/shellstorm2-reverse-contract.md` 与 `04-character-state-binding/references/state-binding-rules.md` |

---

## G. 安装 6 个新 skill

新建的 6 个 skill 目录：

```text
skills_drafts/
├─ 01-character-contract-authoring/{SKILL.md, agents/openai.yaml, references/naming-and-storage-contract.md, references/shellstorm2-reverse-contract.md}
├─ 02-character-export-transfer/{SKILL.md, agents/openai.yaml}
├─ 03-character-godot-import/{SKILL.md, agents/openai.yaml}
├─ 04-character-state-binding/{SKILL.md, agents/openai.yaml, references/state-binding-rules.md}
├─ 05-character-verification/{SKILL.md, agents/openai.yaml}
└─ 06-character-ledger/{SKILL.md, agents/openai.yaml}
```

### G-1 要同步到两处

| # | 目标 | 用途 | 状态 |
|---|---|---|---|
| G-1 | `~/.workbuddy/skills/01~06-character-*/` | 全局正本，实际加载用 | **待你执行**（本机未动用户级） |
| G-2 | `<repo>/.codex/skills/01~06-character-*/` | 项目 Codex 技能区 | **本机已完成**（随本次提交推送，pull 即可用） |

### G-2 同步方式

项目已有 `skill-mirror-sync` skill 负责四副本镜像。**注意**：其 `SKILL.md` 里记录的 A/B/C/D 路径是 **Windows 路径**（`C://Users//zhuangmenghong//…`、`I://工作项目//shellstrom2//…`），与当前 macOS 环境不符，直接跑会失败或写错位置。建议先修正该 skill 的路径表，或手工 `cp -R` 同步。

### G-3 行尾注意

`skill-mirror-sync` 的判据写的是「行尾全为 CRLF」。当前 macOS 环境下全仓 skill 实际是 **LF**（已实测：`skills_drafts/*/SKILL.md` CRLF 计数为 0）。新增文件按 LF 写入即可，与现有文件一致。

---

## H. 总目录 xlsx《Skill清单》分页

文件：`assets/registry/ShellStorm2_美术资产台账_v001.xlsx` → 分页 `Skill清单`

### H-1 列结构

`SkillID` · `名称` · `用途` · `路径` · `安装状态` · `触发范围` · `版本` · `状态` · `更新时间` · `备注`

### H-2 新增 6 行（建议内容）

| SkillID | 名称 | 用途 | 路径 | 安装状态 | 触发范围 | 版本 | 状态 |
|---|---|---|---|---|---|---|---|
| `SKILL-CHAR-CONTRACT-AUTHORING` | S1 契约反推与母版制作 | 反推运行时契约 + Blender 双母版 | `skills_drafts/01-character-contract-authoring/SKILL.md` | 项目内已安装 | 玩家/NPC/怪物/Boss 表现资产 | v001 | 使用中 |
| `SKILL-CHAR-EXPORT-TRANSFER` | S2 导出与严格中转 | 纯视觉 GLB 导出 + 中转台账 | `skills_drafts/02-character-export-transfer/SKILL.md` | 项目内已安装 | 同上 | v001 | 使用中 |
| `SKILL-CHAR-GODOT-IMPORT` | S3 Godot 导入与包装 | PackedScene 包装 + 引用替换 | `skills_drafts/03-character-godot-import/SKILL.md` | 项目内已安装 | 同上 | v001 | 使用中 |
| `SKILL-CHAR-STATE-BINDING` | S4 运行时接入与状态映射 | 状态→动作表现适配层 | `skills_drafts/04-character-state-binding/SKILL.md` | 项目内已安装 | 同上 | v001 | 使用中 |
| `SKILL-CHAR-VERIFICATION` | S5 专项验收 | 角色资产专项验收 | `skills_drafts/05-character-verification/SKILL.md` | 项目内已安装 | 同上 | v001 | 使用中 |
| `SKILL-CHAR-LEDGER` | S6 账本登记与状态推进 | 账本登记与状态推进 | `skills_drafts/06-character-ledger/SKILL.md` | 项目内已安装 | 同上 | v001 | 使用中 |

> 该分页首行注明「只登记实际使用或项目预备的 Skill；项目草稿不等于已安装」——若这 6 个只落项目未装全局，`安装状态` 应写 **`未安装（项目草稿）`**，与 `player-avatar-asset-standard` 的写法保持一致。

---

## I. `assets/art/3D模型资产目录与命名规范.md`（可选）

### I-1 建议新增章节

在该文档「Godot 原生 Prefab 组合规则」之后、「3D Prefab 分类账本」之前，插入一节 **「角色类资产（玩家 / NPC / 怪物）路径形态」**，内容取自
`skills_drafts/01-character-contract-authoring/references/naming-and-storage-contract.md` 的 §2 与 §6。

要点：

1. 形态 A（规范三段式）——**新增资产默认**，NPC 与怪物一律用它。
2. 形态 B（`production/vNNN/`）——**玩家专用**，且按 D3 冻结；NPC 与怪物**不得套用**。
3. 形态 C（共享套件 + 变体）——怪物族；父子关系用账本「变体父ID」列表达，**不为子怪建 `variants/` 子目录**。
4. NPC 落位：`assets/art/characters/npc/<slug>/{source,components,runtime}/`。
5. 怪物落位：普通怪 `enemies/enemy_3d/<slug>/`、精英 `enemies/elite_3d/<slug>/`、Boss 新建 `enemies/bosses/<slug>/`。

### I-2 需要同步修正的既有表述

该文档「角色资产制作母版位于…」一段提到 `skills_drafts/player-avatar-asset-standard/`，与本次拆分不冲突，可保留。

---

## J. `docs/v0.1/` 主设计（可选，需你决定）

若要把这条链路**立为项目正式主设计**，需要：

1. 在 `docs/v0.1/` 下新建一份主题文件（建议 `16.2_角色类资产链路与分阶段Skill.md`）。
2. 在 `docs/v0.1/MODULE_INDEX.md` 登记该文件。
3. 在 `docs/v0.1/16.1_角色美术制作与动作导入流程.md` 结尾加一句指向新文件。
4. 运行 `python3 scripts/check_documentation_contracts.py` 验证。

**本机没有执行这一步**——按你的要求，项目内文档一个字未动。

---

## 附：本机实际做了什么 / 没做什么

**已做并已提交（项目内）**

- 新增 6 个 skill 目录与其 `SKILL.md`、`agents/openai.yaml`、`references/*.md` —— `skills_drafts/01~06-character-*/`
- 已同步项目 Codex 副本到 `.codex/skills/01~06-character-*/`（15 个文件，SHA-256 与 `skills_drafts/` 逐一致）
- 新增链路示意图 `docs/art/character_pipeline/character_pipeline_overview_final.html`
- 本文件即 `docs/art/character_pipeline/待调整文件说明.md`

**没做（等你调整）**

- `assets/registry/ledger_index.json`、`assets/registry/README.md`、总目录 xlsx、`assets/art/3D模型资产目录与命名规范.md` —— **未改**
- `docs/` 下**既有**文件（`v0.1/`、`art/` 已有 4 份记录、各级 README）—— **未改**；仅在 `docs/art/` 下新增了一个子目录
- `skills_drafts/README.md`、`game-weapon-model-pipeline`、`game-character-model-pipeline` —— **未改未删**
- `~/.workbuddy/skills/` 用户级 —— **未动**（本次只装项目内两处）
- 任何 Godot 侧资产与代码 —— **未改**

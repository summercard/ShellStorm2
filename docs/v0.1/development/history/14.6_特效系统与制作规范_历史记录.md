# 特效系统与制作规范：历史开发记录

适用版本：0.1.0。迁移日期：2026-09-12。来源：[原规范](../../14.6_特效系统与制作规范.md)。以下原始记录保留原日期与原结论，未经本次重新验收的内容不得视为当前通过；当时缺少提交号或报告的，仍属历史记录，不补造证据。

<a id="record-1"></a>

## 附录 A：3D-特效 sheet 体检快照（2026-09-11）

本附录为特效账本的 14.6 规范体检结果，每条按 5 维度打分。账本每月初由 OpenClaw 主会话自动重检并更新。

### A.1 体检概览

| 状态 | 数量 | 含义 |
|---|---|---|
| ✅ 完全合规 | 0 | 5 维度全过；可作为模板 |
| 🟡 原型已接入·待规范对齐 | 2 | 独立 Prefab 形态合规，class_name/路径待规范 |
| 🟠 已拆分·弃用 | 1 | 旧形态已弃用，2026-Q4 迁移完成后删除 |
| 🔴 未实现 | 7 | 14.6 规范登记但 Prefab/脚本均未落地 |

### A.2 逐条体检结果

| AssetID | ①AssetID | ②Prefab 名 | ③class_name | ④脚本路径 | ⑤实际落地 | 状态 |
|---|---|---|---|---|---|---|
| VFX-COMBAT-KIT-3D | ✓ | ✓ | ❌ | ❌ | ✓ | 🟠 已拆分·弃用 |
| VFX-MUZZLE-FLASH-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-IMPACT-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-EXPLOSION-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-MELEE-SLASH-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-MELEE-IMPACT-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-DAMAGE-NUMBER-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-HEAL-NUMBER-3D | ✓ | ✓ | 待 | ✓ | ❌ | 🔴 未实现 |
| VFX-HAZARD-FIELD-3D | ✓ | ✓ | ❌ | ❌ | ✓ | 🟡 原型·待对齐 |
| VFX-PLAYER-FLASHLIGHT-3D | ✓ | ✓ | ❌ | ❌ | ✓ | 🟡 原型·待对齐 |

### A.3 体检维度详解

**① AssetID 命名**：是否符合 `VFX-<SUBSYSTEM>-<EFFECT>-<VIEW>` 模式，全大写、连字符分隔。

**② Prefab 文件名**：是否符合 `vfx_<subsystem>_<effect>[_<variant>]_<view>_v<NNN>.tscn` 模式，全小写、下划线分隔。

**③ 根节点 class_name**：是否符合 `Vfx<EffectPascalCase>3D` 模式，PascalCase，必须以 `Vfx` 前缀开头。

**④ 脚本路径**：是否位于 `src/vfx/` 目录，文件名与 class_name 对齐（`VfxXxx3D.gd`）。

**⑤ 实际落地**：Prefab 文件 + 脚本文件是否都存在于 `assets/art/vfx/` 和 `src/vfx/`。

### A.4 待规范对齐项清单

下列条目形态合规（独立 Prefab 原则满足），但 class_name 缺 `Vfx` 前缀、脚本不在 `src/vfx/` 目录，待 14.6 §6 迁移计划执行时改名 + 移目录：

- **VFX-HAZARD-FIELD-3D**：改名为 `VfxHazardField3D`，脚本从 `src/world3d/HazardField3D.gd` 移至 `src/vfx/VfxHazardField3D.gd`。Area3D 碰撞规则保留（特例：危险场需触发器）。
- **VFX-PLAYER-FLASHLIGHT-3D**：改名为 `VfxPlayerFlashlight3D`，脚本从 `src/player3d/PlayerFlashlight3D.gd` 移至 `src/vfx/VfxPlayerFlashlight3D.gd`。lifetime 设为 `inf`（持续光源）。

### A.5 待落地项清单

下列条目登记在册但 Prefab/脚本均未实现，待 AST-VFX-01 任务按 14.6 §6 迁移计划逐个落地：

- VFX-MUZZLE-FLASH-3D（muzzle 替换）
- VFX-IMPACT-3D（impact 替换）
- VFX-EXPLOSION-3D（explosion 替换）
- VFX-MELEE-SLASH-3D（slash 替换）
- VFX-MELEE-IMPACT-3D（melee_impact 替换）
- VFX-DAMAGE-NUMBER-3D（damage 替换）
- VFX-HEAL-NUMBER-3D（heal 替换）

### A.6 旧形态包袱

**VFX-COMBAT-KIT-3D**：唯一形态不合规条目——一个 Prefab + `match effect_kind` 7 套代码分支覆盖 7 种战斗反馈。已拆分登记为 R6-R12 共 7 条新 AssetID，旧调用方（`PlayerMeleeCombat3D`、`Enemy3D`）继续运行至 2026-Q4 迁移完成。迁移完成后删除本条。

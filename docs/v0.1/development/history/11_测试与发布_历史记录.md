# 测试与发布：历史开发记录

适用版本：0.1.0。迁移日期：2026-09-12。来源：[原规范](../../11_测试与发布.md)。以下原始记录保留原日期与原结论，未经本次重新验收的内容不得视为当前通过；当时缺少提交号或报告的，仍属历史记录，不补造证据。

<a id="record-1"></a>

### 本轮施工验收记录（2026-08-05）

- `godot --headless --path . --editor --quit`：脚本解析与资源导入通过。
- `verify_floor_plan_generator`：1000种子、4000份98–95F计划全部通过；普通层由面积预算得到14内容房，主路10房、2条支线，Boss层11房预热+不可绕过Boss。
- `verify_tower_grid_component_alignment`：66房、60水平通道、5m边界、真实门Mesh、楼板覆盖与碰撞通过。
- `verify_door_passability`：15项通过；覆盖130扇门及 EVENT 空房软锁、不可见终端碰撞、错误“战斗未结束”提示的回归。
- `verify_tower_descent_flow`：安全屋交互前不提交、seed gate提交后冻结布局、98–95F战斗/Boss/撤离闭环通过；本次3505节点，低于专项3700预算。
- 新专项不使用 `force_open_edge_for_test` 提交FloorBundle；它通过真实到达门激活入口验证98F事务，并通过同一到达门处理函数验证Boss隔离链。结构类旧测试仅使用公开生成准备接口，不作为玩法流程完成证据。
- `./scripts/run_verification_suite.sh core`：核心集合包含楼层性质、真实到达门、塔楼、战斗、命运、天体作用域、双枪、有限备弹、快捷物品、大地图、枪械实例、可装备背包、返城外观恢复、战术背包、小地图与性能专项；数量以脚本运行时枚举结果为准。
- `verify_tactical_inventory_minimap_flow`：除装备/背包外，验证雷达玩家固定中心、地图反向移动、圆形边界契约、无玩家方向线和怪物红点。
- `verify_3d_enemy_behavior_flow`：验证普通怪最终HP×3、Boss最终HP×10、全体速度×0.70，以及旧资产尺寸×0.70后小型0.78/普通1.0/大型1.2/Boss1.5的相对体型与世界碰撞、头顶血条和伤害飘字，同时保留七类行为回归。
- `verify_arrival_gate_floor_bundle_flow`：新增100→99基地交通门用例，确保它不请求物理层1战斗规划；覆盖99F基地侧门离开后自动关闭、角色房间上下文仍为99F时在楼梯内侧重新开启天台上端门，并断言上端门不会联动重开基地侧门。检查入层门不弹命运卡、98F首门进入后实体关闭、撤退默认取消以及确认后背包/主副枪清空。撤退后进一步断言首门生成世界回滚为初始3房/2楼梯、下端到达门关闭、FloorBundle可重新提交，且第二次进入98F仍会再次封门；二次重建还比对楼梯接驳点与实体门洞坐标，并在开门后的物理帧射线穿越门洞，防止门碰撞、错位墙模块或旧围护形成空气墙。
- `verify_tactical_inventory_minimap_visual`：Metal/OpenGL 真实渲染与真实鼠标事件通过；覆盖唯一且不盖住来源格的枪械悬停卡、格位移动、拖枪换装、装备枪卸回指定空格、红区落地，并输出各交互截图。
- `verify_backpack_equipment_flow`：3种背包与掉落池、装备槽、20→14→12缩容、5+2个溢出地面物、物品守恒、背部Mesh和异色扩展格全部通过。
- `verify_reference_hud_fate_visual`：Metal/OpenGL 输出正式 HUD、命运三选一、全层大地图与塔楼紧凑 HUD；检查 80% 尺寸、塔楼当前信息下移、无重复 HP 条、底部枪械/快捷物品复用背包 3D 模型工厂、已探索整层图和三张天体竖卡。
- `verify_3d_performance_budget`：结构代理永久驻留后的实测为初始世界 `2229/2400`、初始总量 `2376/2480`、完全探索世界 `2306/3500`、完全探索总量 `2458/3610`、HUD 壳 `136/140`、3D 预览 `11/20`；生成 `159.0ms`，单房最大 `36.4ms`，稳定墙钟节拍 `16.67ms/18ms`。动态细节仍按房流送，墙段A/B已合并为MultiMesh；提高的是结构代理分账，不放宽帧节拍、完全探索、灯和ACTIVE房预算。
- 统一测试套件已增加每场景180秒看门狗和 `EXIT/INT/TERM` 精确子进程回收；用1秒强制超时验证目标PID被终止且无遗留，最新 `core` 21/21 后 Godot 测试残留同样为0。
- `verify_tarot_fate_runtime.gd`：正式塔罗卡池与正逆位运行时通过。
- `verify_celestial_fate_scope_flow`：10 张月亮与 10 张太阳命运的角色/世界结算及 UI 通过。
- `verify_finite_ammo_flow`：背包/快捷栏备弹按发扣除、部分换弹、空仓拒绝、双枪独立弹匣、怪物弹药掉落、20格保险柜和战斗HUD尺寸/透明度契约。
- `verify_avatar_return_persistence_flow`：隔离档案下直接重建玩法场景，死亡/撤离式热返不依赖主页或进程重启即可恢复外观。
- `verify_3d_vision_input_flow` 改为校验运行时实际配置值、有效射程与阴影契约，不再把旧的高能量阈值误当设计要求；当前低能耗探照灯参数通过。
- 旧 2D 章节循环与背包点击测试已随模块退役；当前交互由 `verify_tactical_inventory_minimap_flow` 与正式 3D 流程测试接管。
- `verify_tower_camera_occlusion_flow` 与 `verify_tower_descent_visual` 已按“到达门原子提交FloorBundle”更新，不再访问尚未生成的98F房间；唯一电梯采样位置从已废弃的98F房内电梯改为95→94楼梯间。
- `verify_tower_lighting_wall_combat_regressions` 逐楼梯连接器检查 `upper/lower` 两块真实斜楼板标记，并动态验证楼板命中立即下压、离开平滑恢复、水平后移不变及同位置普通楼板不触发；`verify_tower_camera_occlusion_flow / verify_tower_descent_visual` 继续验证既有南墙、楼梯和真实渲染视角不回退。
- 2026-08-06最新回归结果：`core` 21/21、`full` 55/55；此前真实渲染 `visual` 13/13保持通过，本轮另以Metal/OpenGL复核命运塔罗三选一翻牌画面。
- 2026-08-06可装备背包回归结果：新增专项后 `core` 22/22、`full` 56/56；Metal/OpenGL战术装备页复核4格包装备槽、16格网格、异色扩展格与关闭界面后的角色背部Mesh。
- 退出期ObjectDB告警已定位为`WeaponInstance.from_item()`仅`free()`装配树根、没有递归释放挂载的子弹`AssemblyNode`；改为递归断开父子引用并释放整树后，原复现用例在`--verbose`下无泄漏实例、无仍占用资源。

<a id="record-2"></a>

### 7.1 当前 AI / 性能验收口径

| 层级 | 当前结果 | 说明 |
|---|---|---|
| 完整非浸泡逻辑 | 最近完整记录62/62通过；当前未重跑 | 2026-08-20基地综合改造后从头执行；当前脚本已扩展为66项，不能将旧计数视为当前66/66 |
| 核心逻辑 | 历史41/41通过；当前清单45项未重跑 | 新增设施常驻、99F地板承重、存档复位等场景后清单已增长；当前smoke回归修复前不签署core |
| 真实渲染 | 最近完整记录17/17通过；当前未重跑 | 2026-08-20 Metal/Forward+记录；可视场景不使用dummy renderer并明确排除性能浸泡 |
| Forward+高档短程预检 | 15秒通过 | Apple M1 / Metal / Forward+；30怪帧时间P95 20.14ms、渲染CPU P95 0.55ms、Draw Call P95 547、稳定内存跨度1.5MB；GPU时间不可取得，记录为N/A |
| 短程30怪预检 | 15秒通过 | 最终回归墙钟P95 20.96ms、渲染CPU P95 2.18ms、稳定期内存跨度0.6MB |
| 正式30分钟浸泡 | 通过 | 运行时主体1800秒；墙钟P95 19.75ms、渲染CPU P95 1.87ms、内存稳定跨度3.6MB、节点稳定跨度18 |
| 本次核心退出清理 | 通过 | 最后1个残留定位为未入SceneTree的`ItemUseHandler`静态Node；改由SceneTree持有后，手电筒专项`--verbose`退出无ObjectDB/resource残留 |
| 发布候选扩展 | 已执行/未通过 | Apple M1 Metal 3600秒完整覆盖三区段；墙钟P95 19.84ms、渲染CPU P95 2.52ms、内存稳定跨度7.9MB均达标，但节点段内跨度66>64；GPU时间戳不可用，目标设备仍需复测 |

补充边界：`full` 排除必须读取Viewport纹理的场景，避免dummy renderer假失败。基地自动贩卖机、远征终端和枪械工坊保留场景作者设置的位移、旋转和缩放，运行时不自动面向房间中心；作者Transform保护专项已通过。退出期AssemblyNode清理告警也已修复。

<a id="record-3"></a>

### 2026-09-12 99F设施功能资产转移验收

- `verify_tower_base_facility_persistent_flow.tscn`：99F常驻设施数量为8；六项迁移包装保持`BaseFacility3D`交互协议、实体碰撞和常驻物理。
- `verify_tower_facility_inventory_binding.tscn`：保险柜与自动贩卖机继续共享I键`InventoryModule`，迁移不改变交易和背包绑定。
- `verify_formal_asset_placement_visual.tscn`：真实渲染确认保险柜使用窄型电池柜、命运卡收藏室使用双屏电脑工位、怪物档案室使用49_02整合模型；设施作者Transform与源资产世界位置保持。
- Godot编辑器无头编译：`--headless --path . --editor --quit`通过，无新增脚本或场景解析错误。

# 3D 场景美术生产流程文档

日期：2026-09-13；记录ID：`DOC-ASSET-SCENE-3D-20260913`；功能ID：`ASSET-PIPELINE`；工程版本：`0.1.0`。
设计依据与修订：`ASSET-SCENE-3D r2`；代码基线：当前工作区；交付提交：提交后补充。

## 变更与原因

新增[3D 场景美术生产流程](../10.1_3D场景美术生产流程.md)，按概念稿、白盒规范、白盒组装与顶视图、风格设计稿、Blender制作、优化导入和正式登记顺序说明生产链。根据用户反馈将原复杂契约收缩为一页式流程，每一步只保留规范、数据、交付物和使用Skill。

白盒阶段仍明确交付JSON和图片：JSON记录场景尺寸、模块实例、组合、名字、接口、净空和设计来源，是Blender尺寸与组合的唯一数据源；图片提供带标注顶视图、无文字顶视图及必要立面/剖面。Blender阶段标注`$blender-game-prop-standard`，优化导入阶段标注`$godot-model-asset-import-standard`。

后续补充：整个流程必须持续声明 `block_id / floor_range / design_scope / scene_design_docs / asset_ledger`，从白盒、风格稿到Blend、GLB、PackedScene和验收记录均不得丢失。`WORLD-BLOCKS r2` 逐区块列出设计范围、场景设计依据和对应台账条目，使天台、基地、战斗区和楼梯区均能从设计范围追溯到资产账本。

本次只修改文档导航与契约，没有修改代码、场景、模型、台账或`project.godot`。白盒尺寸示例引用当前关卡主设计的5m平面网格、12m逻辑层高和11.9m可见墙高，但流程要求每批按来源修订重新读取，不建立平行数值事实源。

## 验证结果

| 命令/场景 | 环境及存档隔离 | 结果 | 日志/截图证据 |
|---|---|---|---|
| `python3 scripts/check_documentation_contracts.py` | 仅静态文档检查，不访问用户存档 | 通过，退出码0 | 62份文档、364条本地链接、33个功能、45个测试引用，`issues=[]` |
| JSON 示例抽取后执行 `jq empty` | 仅静态文本检查 | 通过，退出码0 | 示例骨架为合法JSON |
| `git diff --check` | 仅工作区文本检查 | 通过，退出码0 | 无空白错误 |

## 遗留与状态更新

- `ASSET-PIPELINE`主设计新增3D场景流程入口；角色、武器、可拾取道具、UI与VFX的并列生产流程不在本次范围。
- 本次未生产具体资产，未执行Blender、Godot导入、真实渲染、目标设备或资产哈希验收；这些不是本次文档交付的通过项。
- `10_资产与内容规范.md`中的部分历史9m场景条目与2026-09-13的12m主设计存在旧口径，本次流程不把旧值复制为生产标准；该既有文档差异应另行语义审查。

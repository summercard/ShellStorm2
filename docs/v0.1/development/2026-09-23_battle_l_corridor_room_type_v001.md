# 战斗区 L 型走廊房间种类美术源 v001

- 功能定位：`ASSET-PIPELINE`，设计依据为 `05`、`05.1`、`10.1` 与用户提供的参考图。
- 交付范围：`battle` 区块、`scene_art`，Blender 房间种类源；未分配具体房间编号，也未修改关卡拓扑、玩法、Godot 运行场景。
- 输入：用户明确通道宽 10m、由两块 5m 标准砖拼成，明确目前没有白模。参考图已保存为房间种类源中的 `reference.png`。其余尺寸属于本次美术换算，不冒充既有白模或运行时计划。
- 布局：长臂 45×10m，右端向南延伸 10×25m，总包络 45×35m；28 块 5×5×0.3m 地砖，每块独立成包。视觉墙高 11.9m、逻辑阻挡高度标注 12m、墙厚 0.3m。三个视觉门中心位于 `(7.5,10)`、`(37.5,10)`、`(37.5,-25)` 米；门位需在日后白模/关卡连接设计中核准。
- 美术：深蓝金属墙板与地面、检修格栅、边线与螺栓、长条玻璃服务器陈列窗、安全门、机柜、访问终端、维修箱、管线、冷色灯带和固定花槽。镜头侧低墙是展示剖切；完整 11.9m 墙体基准独立保留在输出集合并在预览渲染中隐藏。
- 源与清单：`assets/art/environments/tower_zones/battle/source/room_types/l_corridor/v001/` 保存 `.blend`、可重建脚本、房间种类 manifest、88 个独立包 manifest/catalog、全景、俯视、近景和 QA 报告。`01_制作组件` 保存 28 个砖块定位导引；`02_游戏输出` 保存可分别维护的视觉包。
- 专项验收：Blender 4.2 后台打开并渲染退出码 0；自检 88 包、28 地砖、1070 输出网格、6880/6880 面有效 PaletteUV、四种共享材质、0 错误。参考全景与近景人工复核完成。
- 未执行：未有白模可核对门连接；未拆成共享组件 GLB，未建立碰撞/LOD/PackedScene，未进入 Godot 正式运行路径。场景账本当前只在 manifest 指明目标分账本与稳定 AssetID，正式资产行待组件化与登记阶段建立，不能据此标记运行时已验收。
- 工程门禁：`python3 scripts/check_documentation_contracts.py` 退出 0，114 份文档、541 个本地链接、37 项功能、0 问题；`python3 scripts/check_asset_runtime_naming.py` 退出 0，新资产路径合规，旧债快照仍有文件 289/目录 13/备份 1。专项 Blender 校验退出 0，`qa_report.json` 为 pass。非预期脚本错误 0；预期故障注入 0；Godot 游戏逻辑验收未执行（尚未接入）。

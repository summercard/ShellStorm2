# 天台参考组件库 v001

功能ID：ASSET-ROOFTOP；模块：ASSET-PIPELINE；工程版本：0.1.0；主设计：[天台组件库r1](../design/rooftop_component_library.md)。来源为用户2026-09-17组件图与明确的外墙通用规格要求。

## 交付

源：`assets/art/environments/tower_zones/rooftop/source/reference_components/v001/天台区块_参考组件库_v001.blend`。

37包、10类：地面5、女儿墙4、房间墙3、房顶3、空调与设备6、管线5、门/雨棚2、固定绿化4、细节3、外墙2。房间前后左右由3种墙包与独立门/植物/设备装配。4个共享材质、166个制作网格、38个整合输出网格；灯芯独立。保存37份manifest、catalog、目录树及249个实例的参考装配JSON。完整源与输出保存在同一Blend的不同Collection；拼装预览是独立Scene。

外墙两种模块实际尺寸5×0.30×11.9m，原点底面中心；逻辑/未来碰撞5×0.30×12m。房间墙沿用墙高规范；参考图的房间比例经过项目层高适配，不能把示例拼装当作90×80m正式天台布局。地砖底面0、厚0.30m；示例装配偏移-0.30m。原项目地砖厚度居中原点在未来导入时需要显式转换。

灰色混凝土/设备/绿植由公共冷灰及绿色色盘格表现；未引入照片纹理，表面磨损是少量几何色块，视觉属于项目色盘下的风格化转译。固定相机全景、俯视、房间设备近景、外墙接口和37张组件图位于`renders/`。结构素模使用同相机与灯光；这是新建组件库，不存在旧模型前后对比。

## 验收与修复

- 任务级Blender检查退出0：37非空独立包、37磁盘清单、唯一归属、局部原点、所有墙尺寸、地面厚度、100个演示地砖及40段演示外墙通过。
- 材质/UV检查退出0：全部204个制作/输出网格、50,672面均有有面积且位于单格安全区的PaletteUV；活动编辑/渲染层、Closest、唯一外链PNG、四材质、主体与自发光分离通过。
- 试渲染暴露设备顶盖与外壳共面、同平面磨损色块重叠导致黑斑；修复几何后重新渲染。自发光对象首次命名不符合验证器要求，统一为明确自发光命名后通过。
- 挂藤墙的植物包络超出结构包络，保留独立包XY居中原点，同时记录结构中心偏移及准确接点，演示装配按结构中心吸附，不能用叶片包络推算墙体碰撞。
- 范围外天台v021、基地v026、战局组件库v003三个源文件前后SHA256一致；本次是新建独立库，不把范围外对象加载后改写。
- `scripts/asset_guard.py` 当前仅支持角色transfer ledger，对环境库退出1、提示缺少character_transfer_ledger；未伪造角色账本。环境库通过专用ID/目录/哈希与父子变体核对记录验证。
- 台账在资产主表429行登记父库，在`3D-场景通用`97–134行登记父库和37包，保留旧运行资产条目。专用检查核对旧值、样式、ID与最终源哈希。
- 文档契约使用`python3 scripts/check_documentation_contracts.py`独立检查，结果记录于本包`qa/documentation_validation.log`。

最终证据：`qa/task_validation.json`、`qa/material_validation.json`、`qa/registry_validation.json`、`qa/dedupe_validation.json`与渲染图。任务验收没有预期故障注入；修复后的模型检查无非预期脚本错误。旧台账的全库哈希漂移不在本批修复范围。

## 未执行项

未导出GLB、未生成Godot PackedScene/玩法碰撞/LOD、未改正式天台引用；未进行Godot运行、角色通行、性能或目标设备验收。真实灯光目前只在展示Scene中，未来运行包装必须接管。制作状态为Blender源已完成，不能标为游戏运行时已接入。

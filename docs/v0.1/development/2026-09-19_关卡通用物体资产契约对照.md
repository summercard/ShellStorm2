# 关卡通用物体资产契约对照（地板 / 墙壁 / 墙壁门 / 门 / 门扇）

- 日期：2026-09-19
- 范围：`DungeonRoom3D` 生成关卡时使用的通用物体
- 性质：**现状取证 + 契约对照 + 统一实施记录**。第 1–7 节是实施前的取证口径（保留原样以便回溯），第 8 节起是本次实际落地内容、实测数据与仍未决项。
- 取证方式：源码走读 + Godot headless 实测。探针日志 `_scratch/prefab_probe_before_20260919.txt`（判据 `PREFAB_PROBE_OK`，升级前口径）。
- 门禁目标数：**5 件**（地板 / 墙壁 / 墙壁门 / 女儿墙 / 门扇）。门扇是 2026-09-19 第 8.5 节新入册的第五件。

## 1. 结论

原始四类物体**都已经不是裸程序几何，都已经是 PackedScene（prefab）**。程序生成的是**布局**——哪一格摆哪一段墙、门开在第几段、地砖铺几行几列；物体本身来自 prefab。

所以「能不能变成 prefab 资产」这个问题不成立。需要解决的是另一个更具体的问题：

> 同一类物体存在**两套并行资产**，契约不一致。制作模块化资产时按哪一套做，直接决定替换会不会炸。

**2026-09-19 补充结论（实施后）**：四类物体的**装配策略不能统一**，但**契约可以统一，且已经统一**。

- 统一的是**契约层**：同一套 metadata 字段 + 同一个视觉解析入口 + 同一条碰撞责任规则 + 一个会失败的断言门禁。
- 不能统一的是**装配层**：墙与地砖**必须**继续走 MultiMesh 批渲染。逐 prefab 实例化会把节点数推爆硬门禁——实测 `initial_world` 余量只有 205 节点，而实墙逐实例化净增 2038、地砖净增 45026（第 8.2 节有完整数据）。
- 因此新增 `metadata/runtime_instantiation` 字段，把「批渲染 vs 逐实例」这个差异**显式写在资产上**，而不是藏在代码里。美术替换时看这一条就知道该不该保留 `visual_node_name`。

> **第五件（门扇）的补充结论（8.5）**：门扇**反向**验证了上面这条分界——它是「可以逐实例化、且值得逐实例化」的那一类。
> 每门只 +2 节点（38 门共 +76），代价可接受；而把它硬塞进批渲染（换 `ProceduralDoorPanel` 的 `mesh`）
> 反而要多绕一层「资产解析」的间接，收益为零。

## 2. 两套资产总览

| 维度 | A 套：旧塔楼四件套 | B 套：战局通用组件库 v004/v007 |
|---|---|---|
| 路径 | `assets/art/props/dungeon_3d/prp_tower_*` | `assets/art/tower_zones/battle/runtime/common_components/*` |
| 谁在用 | **普通战斗房**（`tower_module_shell=true`） | **仅入口安全房**（`_build_safe_room_shell`） |
| `forward_axis` | `+Z` | `-Z` |
| 碰撞归属 | `visual_only=true`，碰撞由脚本代理 | `collision_owner=self`，组件自带 |
| 装配方式 | 取首 mesh → MultiMesh 批量铺 | 逐实例化（`per_instance_prefab`） |
| metadata 字段数 | 6–11 项（**2026-09-19 已补齐到统一 11 项**） | 25–30 项 |
| 门扇来源 | `prp_tower_door_leaf_5m`（A 套门扇，**2026-09-19 起，见 8.5**） | `door_5m` PackedScene（**已退出运行时**，成孤儿件） |

分支入口在 `DungeonRoom3D.gd:658`：`if tower_module_shell: _build_tower_module_shell(...)`；`tower_module_shell` 由 `TowerDescent3D.gd:1256` 置真。

## 3. A 套：旧塔楼四件套（普通战斗房在用）

### 3.1 资产清单与实测规格

| 物体 | prefab | 稳定 AssetID | 结构尺寸 (m) | 原点契约 | 朝向 |
|---|---|---|---|---|---|
| 墙壁 | `prp_tower_wall_solid_5m.tscn` | `ENV-TOWER-WALL-SOLID-5M` | 结构 5.0 × 11.9 × 0.3；**可视 5.0 × 11.9 × 0.4675**（逻辑高 12.0，顶部留 0.1） | `bottom_center` | `+Z` |
| 墙壁门 | `prp_tower_wall_door_5m.tscn` | `ENV-TOWER-WALL-DOOR-5M` | 5.0 × 11.9 × 0.3；门洞净空 2.2 × 2.5 | `bottom_center` | `+Z` |
| 地板 | `prp_tower_floor_tile_5m.tscn` | `ENV-TOWER-FLOOR-TILE-5M` | 5.0 × 0.3005 × 5.0（板厚居中） | `centered_slab` | — |
| 护栏 | `prp_tower_wall_parapet_5m.tscn` | `ENV-TOWER-WALL-PARAPET-5M` | 5.0 × 1.5 × 0.3 | `bottom_center` | `+Z` |
| **门扇** | `prp_tower_door_leaf_5m.tscn` | `ENV-TOWER-DOOR-LEAF-5M` | **可视 2.2 × 2.492 × 0.18**（底边悬原点上方 0.008m） | `bottom_center` | `+Z` |
| 门（逻辑容器） | `prp_room_door_3d.tscn`（`RoomDoor3D` 脚本） | — | 碰撞 2.2 × 2.5 × 0.42；承载门扇 | — | — |

实测 AABB（探针输出，非文档抄录）：

- 实心墙（v004）可视包络：`pos=(-2.5, 0.0, -0.15) size=(5.0, 11.9, 0.4675) surfaces=3`；结构盒仍 `size=(5.0, 11.9, 0.3)`
- 地砖首 mesh：`pos=(-2.5, -0.15, -2.5) size=(5.0, 0.3005, 5.0) surfaces=2`
- 护栏首 mesh：`pos=(-2.5, 0.0, -0.15) size=(5.0, 1.5, 0.3) surfaces=1`

`RoomDoor3D` 常量（`src/world3d/RoomDoor3D.gd:6-9`）：门扇厚 `0.18`、碰撞深 `0.42`、开门抬升 `0.32`、默认动作时长 `0.24s`。开门是**垂直升起**，不是旋转铰链。

门扇分支（`DungeonRoom3D.gd` `_build_door`，2026-09-19 起**只剩两条**，两条都是正式美术）：

| 房间 | 门扇 prefab |
|---|---|
| `FACILITY`，或 `start → facility` | `env_base99_door_lift_2p2x2p5`（99F 滑升门，自带独立 `logic_id` 与状态灯，不随楼层主题换材质） |
| 其余（普通战斗房 / BOSS 房 / 入口安全房） | **`prp_tower_door_leaf_5m`（塔楼 A 套门扇，与门墙共用 `+Z` 约定）** |

原先「其余 → 无（`RoomDoor3D` 程序生成深色方块门板）」这一支**已退役**（见 8.5）。程序化门板路径保留在代码里，但只能由「显式传 `null`」触发。

### 3.2 装配方式

墙与地是**「prefab 当 mesh 容器」**：

```
prefab.instantiate()  →  _find_first_mesh()  →  塞进 MultiMesh  →  source.free()
```

代码位置：`_get_tower_solid_wall_mesh()` (1328)、`_get_tower_floor_tile_mesh()` (1127)、`_find_first_mesh()` (1350)。

后果：prefab 的**节点结构、节点名、材质槽、子节点、碰撞体全部被丢弃**，只有「第一个被递归找到的 MeshInstance3D 的 mesh」进入运行时。

Y 对齐全靠 AABB 反算：`wall_transform.origin.y += -mesh.get_aabb().position.y`（1262）。

门墙与门则是**整 prefab 实例化**（1171、2007），相对安全。

### 3.3 已知缺陷（2026-09-19 已全部处理，保留原记录）

1. **首 mesh 已实际漂移。** 探针实测 `prp_tower_wall_door_5m.tscn` 的 `_find_first_mesh` 返回 `MOD_WALL_DOOR_5M_U01_DoorLeaf_OPEN`——一片 `visible=false` 的门扇，而**不是墙体**。当前门墙走整 prefab 实例化所以没爆；一旦有人把门墙也改成 mesh 复用，立刻取错。
   → **已处理**：`_find_first_mesh` 全部退役，改由 `TowerGeometry3D.resolve_visual_mesh()` 按 `metadata/visual_node_name` 声明解析；断言门禁第 2 项盯住「不得靠回退命中」。漂移本身仍被记录（门禁会打印 `[记录] 首网格漂移`），但不再有取错路径。
2. **该 prefab 漏声明 `visual_only`。** 其余三个都声明了 `visual_only=true`，只有它没有；`collision_owner` 也与其他三个写法不同。
   → **已处理**：补 `visual_only=true`，`collision_owner` 统一为 `DungeonRoom3D`；并移除了 prefab 内那份与脚本代理逐值重叠的 `WallCollision`（门柱 2 + 门楣 1）。
3. **材质覆盖靠单个 metadata 开关。** `preserve_authored_palette` 为真才豁免；未声明就会被 `material_override` 换成主题材质，美术成果被盖成单色。
   → **已处理**：四件套全部声明 `preserve_authored_palette=true`；门禁第 7 项断言「声明了保留色盘就不得存在任何 `material_override`」。

## 4. B 套：战局通用组件库 v004/v007（仅安全房在用）

| 物体 | prefab | 稳定 AssetID | `bounds_size_m` | 碰撞 |
|---|---|---|---|---|
| 墙壁 | `wall_standard_5m_root_top3d.tscn` | `ENV-BATTLE-COMMON-WALL-STANDARD-5M` | 5 × 11.9 × 0.3 | `self`，1 box |
| 墙壁门 | `wall_door_5m_root_top3d.tscn` | `ENV-BATTLE-COMMON-WALL-DOOR-5M` | 5 × 11.9 × 0.3（可视 0.365） | `self`，3 box |
| 门扇 | `door_5m_root_top3d.tscn` | `ENV-BATTLE-COMMON-DOOR-5M` | 2.2 × 2.5 × 0.18 | `self`，1 box @ y=1.25 |
| 地砖 c01 | `floor_tile_r01_c01_root_top3d.tscn` | `ENV-BATTLE-COMMON-FLOOR-TILE-R01-C01` | 4.94 × 0.056 × 4.94 | `self`（安全房运行时禁用） |
| 地砖 c02 | `floor_tile_r01_c02_root_top3d.tscn` | `ENV-BATTLE-COMMON-FLOOR-TILE-R01-C02` | 4.94 × 0.081 × 4.94 | `self`（安全房运行时禁用） |

三处关键契约差异：

- `forward_axis = "-Z"`：**朝房内那面是 -Z**（`wall_standard_5m` 的内嵌装甲壁板在局部 +Y 面，离面约 15mm）。
- 门扇 `open_mode = "vertical_lift"`，`panel_origin_policy = "bottom_center"`。
- 地砖是**薄砖**，不是板厚居中的整板；两版厚度不同，各自带 `snap_to_walk_plane_offset_m`（c01 −0.056 / c02 −0.081），由 `_build_safe_room_floor_tiles`(975) 逐件读取，禁止硬编码共用偏移。

装配方式：**逐实例化**，且槽位角度写死在一张表里（`SAFE_ROOM_WALL_SLOTS`，源自 `source/room_instances/entry_safe_room/v007/qa/slot_table.json`），整房按实际门向旋转 0/90/180/270°，所以任意楼层共用同一套资产。

## 5. 跨套互换的四个坑

| # | 坑 | 机制 | 症状 |
|---|---|---|---|
| 1 | **朝向反 180°** | A 套 `+Z` / B 套 `-Z`。塔楼 north 墙用 `rotation_y = 0`，换成 B 套后装甲壁板朝房外 | 墙看不见装饰，像换了套皮 |
| 2 | **碰撞双份或全缺** | A 套是 `visual_only` + 脚本代理（`_add_tower_wall_collision` / `_add_tower_solid_run_collision` / `_add_corner_aware_solid_run_collision`）；B 套自带。直接换 → 同位置两套；删代理却没生效 → 穿墙 | 角色卡门或直接穿墙 |
| 3 | **地砖不是同一规格** | 5.0 × 0.3005 厚板（居中，运行时 −0.15 对齐）vs 4.94 × 0.056/0.081 薄砖（靠各自 snap 偏移）。铺法也不同：整板满铺 vs 3×3 棋盘交替 | 地面高低不平、棋盘节奏丢失 |
| 4 | **首 mesh 无断言保护** | `_find_first_mesh` 只递归找第一个 `MeshInstance3D`，已被实测证明会漂移 | 换资产后取到装饰件或隐藏件，几何错位 |

> 4 号坑**已封堵**：`_find_first_mesh` 全部退役，改为按 `visual_node_name` 声明解析 + 断言门禁第 2 项盯住回退。1–3 号坑仍未跨套解决（属于「要不要割接到 B 套」的独立议题）。

## 6. 制作模块化资产的检查清单

按统一契约做，逐条对齐即可安全替换（完整字段表见第 9 节）：

- [ ] 结构尺寸对齐 `TowerGeometry3D`：网格 5.0、层高 12.0、**视觉墙高 11.9**（顶部留 0.1）、门洞净空 2.2 × 2.5
- [ ] 原点：墙 / 门 / 门扇 = 底面中心；地板若沿用 A 套 = 板厚居中；若走 B 套薄砖 = 底面中心 + 声明 `snap_to_walk_plane_offset_m`
- [ ] 声明 `forward_axis`，且与实际装甲面一致（塔楼 A 套 = `+Z`）
- [ ] 声明 `preserve_authored_palette = true`，否则被主题材质盖掉
- [ ] 声明 `visual_only` 与 `collision_owner`，且与装配侧策略一致（二者只能有一个负责碰撞）
- [ ] **声明 `visual_node_name`**，指向承载模块几何的节点。不声明就会回退到「子树里第一个可见 MeshInstance3D」，在带装饰/带隐藏件的资产上会取错（门墙与滑升门都有这个坑）
- [ ] **同时声明 `bounds_size_m`（结构）与 `visual_bounds_size_m`（可视）**。装饰件（如门墙门框）会让两者不同；混成一个字段，改美术时装配基准就会漂
- [ ] **声明 `runtime_instantiation`**：`batched_multimesh` 要求「只有 1 个 MeshInstance3D可用」，因为批渲染只吃单一 Mesh 资源；`per_instance_prefab` 无此限制
- [ ] 门扇：底边中心原点，垂直升起（改 `DoorPanel.position.y`），不要做绕铰链旋转；**不得自带任何碰撞**（`visual_only=true` + `collision_owner=RoomDoor3D`）
- [ ] 门扇可视底边允许略高于结构底面（本资产为 8mm，关闭的门不擦地），但要声明 `panel_bottom_clearance_m`
- [ ] 同步更新 prefab 的 `metadata/asset_version` 与 `source_glb`，不要改运行时稳定路径
- [ ] 改完跑 `verify_tower_module_prefabs.gd`，判据 `PREFAB_CONTRACT_OK count=5`
- [ ] 门扇改完**额外**跑两条画面探针：`probe_door_leaf_visual`（`DOOR_LEAF_VISUAL_OK`）+ `probe_door_leaf_placement`（`DOOR_LEAF_PLACEMENT_OK`）

## 7. 取证与门禁（2026-09-19 已补上门禁）

`assets/art/props/dungeon_3d/qa/verify_tower_module_prefabs.gd`

升级前**只 print，不是断言门禁**——`failures` 仅在 prefab 加载失败时 append，因此即使像「首 mesh 指向被隐藏门扇」这种明确错误，判据依然是 `PREFAB_PROBE_OK`。

**现已升级为断言门禁**，判据 `PREFAB_CONTRACT_OK: ... count=N` / `PREFAB_CONTRACT_FAILED count=N`，8 项检查：

（`N` = `TARGETS` 数组长度。2026-09-19 8.5 之后为 **5**：地板 / 墙壁 / 墙壁门 / 女儿墙 / 门扇。）

1. 统一字段齐全（11 项，见第 9 节）
2. `visual_node_name` 能解析到真实节点，且**不得靠三级回退命中**
3. 根空间可视包络 `==` `visual_bounds_size_m`（容差 0.02m）
4. `origin_contract` 语义成立，且**结构盒 ⊂ 可视包络**
5. `forward_axis` 全集一致（塔楼 A 套 = `+Z`）
6. `visual_only=true` ⇒ prefab 内不得内嵌任何 `StaticBody3D` / `CollisionShape3D`
7. `preserve_authored_palette=true` ⇒ 不得存在任何 `material_override`
8. `runtime_instantiation=batched_multimesh` ⇒ 视觉节点必须是 `MeshInstance3D`（批渲染只吃单一 Mesh 资源）

第 8 项是刻意加的：它把「墙/地砖为什么不能逐实例化」这条**编码依据**钉在门禁里，而不只是写在注释里。

第 6 项对门扇是同一条规则的另一个方向：门扇声明 `visual_only=true` + `collision_owner=RoomDoor3D`，所以**门扇 prefab 里一个碰撞体都不能有**。这不是形式约束——门扇自带静态碰撞的话，它不随升降逻辑启用/禁用，门就永远开不了（B 套 `door_5m` 那个包就带 `DoorCollision`）。

另有第 4 项的边界：`origin_contract` 描述的是**结构**原点，不是可视包络底面。门墙装饰门框下探到楼面下 0.14m，这不违反 `bottom_center`——所以判据用「结构盒 ⊂ 可视包络」而不是「可视包络底面 == 0」，并在命中时打印 `[记录] 装饰件低于结构底面`。

> 门扇的同类情况：它的可视底边在结构底面**上方** 8mm（美术意图，见 8.5），也不违反 `bottom_center`——同一条「结构盒 ⊂ 可视包络」判据覆盖两个方向。

## 8. 本次实施记录（2026-09-19）

### 8.1 已实施

| # | 内容 | 落点 |
|---|---|---|
| 1 | 新增唯一视觉解析入口 | `TowerGeometry3D.resolve_visual_node()` / `resolve_visual_mesh()` / `resolve_visual_bounds()` / `origin_offset_y()` |
| 2 | 退役三份重复的「取第一个 mesh」 | `DungeonRoom3D._find_first_mesh`、`TowerFloorStage3D._find_first_mesh`、`TowerDescent3D._find_first_imported_mesh` 全部删除，改调统一入口 |
| 3 | 装配基准从实测 AABB 改为契约声明 | `_spawn_solid_wall_visual_instances()` 改用 `origin_offset_y()`；旧注释「通用旧墙以几何中心为原点、需抬高半层」经实测为**过时描述**（v003 与基地墙网格 AABB 底面都在 y=0，反算恒为 0） |
| 4 | 四件套补统一契约字段 | `prp_tower_wall_solid_5m` / `prp_tower_wall_door_5m` / `prp_tower_wall_parapet_5m` / `prp_tower_floor_tile_5m` |
| 5 | 门墙移除重复碰撞 | `prp_tower_wall_door_5m` 内嵌 `WallCollision` + 3 个 BoxShape3D 与脚本代理逐值重叠，已删 |
| 6 | 基地 5 件补显式视觉声明 | `env_base99_{wall_plain_5x12,wall_door_5x12,floor_plain_5m,floor_rivet_5m,door_lift_2p2x2p5}_root_top3d.tscn` 补 `visual_node_name` |
| 7 | 断言门禁 | `verify_tower_module_prefabs.gd` 升级（第 7 节） |
| 8 | **门扇换 A 套正式美术** | 见 **8.5**（第五件入册：派生 + prefab + 接线 + 预算上调 + 两条画面探针） |

新增实测数据：**门墙的结构包络是 5 × 11.9 × 0.3，可视包络是 5 × 12.04 × 0.43**——装饰门框比墙体厚 0.43m、向下探出楼面 0.14m。这就是必须把 `bounds_size_m`（结构）与 `visual_bounds_size_m`（可视）分成两个字段的原因。

### 8.2 实测判定：墙与地砖**不能**逐 prefab 实例化

这是一条**硬门禁**结论，不是风格偏好。数据（seed 990095，生成至 95F）：

| 项 | 现状 | 若逐 prefab | 净增 |
|---|---|---|---|
| 实墙 | 419 个 MultiMesh 节点，覆盖 819 段 | 2457 节点 | **+2038** |
| 地砖 | 28 个 MultiMesh 节点，覆盖 15018 块 | 45054 节点 | **+45026** |

节点预算实测（`AbyssArchive3D`，seed 4242，与 `verify_3d_performance_budget` 同口径）：

```
initial_nodes        = 2473 / 2480   余量 7
initial_world_nodes  = 2195 / 2400   余量 205
```

> 上表是**论证墙/地砖批渲染必要性时的快照**，保留原样。8.5 落地门扇换正式美术后，两道上限已同步上调（`2480 → 2560`、`2400 → 2480`），实测变为 `total 2549 / world 2271`。上限上调的理由与逐门代价见 8.5。

实墙逐实例化一项就超预算 1833 节点。按房级只改墙、不动走廊也救不回来——419 个节点全是房间墙（走廊墙另名 `ImportedCorridorWallGrid5M`，不在统计内）。

依据出处：`docs/archive/v0.1_pre_framework_2026-08-04/03_关卡与场景设计.md:42`——「水平通道的重复地砖和墙体使用 MultiMesh 批量实例化……长通道可以随门距增长而不突破节点预算」。

### 8.3 实测判定：门扇接美术的代价

现状（8.5 落地前）：136 扇门，其中 **22 扇**已有美术门扇（`FACILITY` 2 + `start→facility` 1 + `STAIR_LOBBY` 19），**114 扇**走程序化门板。

按房型分布：`COMBAT 28 / STAIR_LOBBY 19 / STORAGE 19 / SCAVENGE 17 / EVENT 13 / UPGRADE 12 / ELITE 12 / TRAP 11 / BOSS 2 / FACILITY 2 / START 1`。

给这 114 扇各接一个 3 节点门扇 prefab = **+342 节点**，超 205 余量 137。**逐 prefab 铺开同样不可行。**

当时设想的替代路径（零节点增量）：把 `ProceduralDoorPanel` 的 `mesh` 换成「声明资产解析出来的 mesh」（与墙/地砖同一套 `runtime_instantiation=batched_multimesh` 思路）。

**实际落地的路径与设想不同**：主人拍板「接受节点增加、同步上调上限」，于是走**整 prefab 实例化**（`per_instance_prefab`），每门实测 **+2 节点**（而非 +3：A 套门扇 prefab 内部只有一个 `MeshInstance3D` + 一个包裹节点），×38 门共 **+76**。代价与收益见 8.5。

> 为什么实测 +2 而估算 +3：估算按「prefab 根 + 视觉节点 + 导入模型根」三层算。实际 A 套门扇 prefab 根就是 `ImportedDoorVisual` 这一层，导入 GLB 内部节点被 Godot 场景导入合并为单 `MeshInstance3D`，故只多 2 个。

### 8.4 墙体网格换为 v004（自 B 套 v007 通用实墙组件派生）

2026-09-19 追加：`ENV-TOWER-WALL-SOLID-5M` 的**可视网格**整体换成 v004 —— 源自 B 套
`source/art/whitebox/tower_zones/battle/components/common_components/v007`（包名 `wall_standard_5m_通用包`，
根件 `ROOT_wall_standard_5m_通用组件`）的派生物。**只换网格，不换玩法**：结构尺寸、原点契约、`forward_axis`、
碰撞责任、装配方式（`batched_multimesh`）全部不变；台账里 `bounds_size_m` 仍 5×11.9×0.30。

派生脚本：`assets/art/environments/tower_descent_3d/source/wall_height12/export_env_tower_wall_solid_5m_v004.py`
（`SS_WALL_PHASE=derive` 造源、`=export` 导出），产出
`env_tower_wall_solid_5m_source_v004.blend` + `env_tower_wall_solid_5m_v004_manifest.json`。

#### 跨套迁移的四个硬约束（这次都是实测踩出来的）

| # | 约束 | 为什么 | 落地做法 |
|---|---|---|---|
| 1 | **装饰面必须绕竖轴 180°** | Blender 侧厚度轴是 `+Y`，YUP 导出映射 `Blender +Y → Godot -Z`。B 套装饰面在 Blender `+Y` ⇒ Godot `-Z`；A 套四件套硬要求 `forward_axis="+Z"`（门禁第 5 项）。跨套直接用会整体反 180° | 在 Blender 内对根节点烘焙 `yaw=180°`（绕 Z）后 `transform_apply`，再导出。实测导出后装饰面落在 Godot `+Z`（GLB 顶点 `max.z = +0.3175`） |
| 2 | **根节点变换必须是单位矩阵** | 这个 GLB 除 prefab 外还被 `TowerDescent3D` **直接 preload 裸 GLB**（`_get_corridor_wall_module_mesh()` 取首网格、`_build_stair_approach_corridor()` 整树实例化挂 `StairApproachWall_*`）。GLB 根节点的 T/R/S 会**直接决定走廊墙位置** | 派生时把原根的世界平移（`[4, -58.344406, 0]`）先烘进网格再归零；导出后重新解析 GLB JSON 断言 `nodes=1` 且无 `translation/rotation/scale/matrix` |
| 3 | **先三角化，再删朝下面** | n-gon 用面平均法线判朝下会漏判：平均法线在阈值之上、但它的三角形在阈值之下。实测「先删后三角化」残留 11 个隐藏三角面 | 顺序固定为 `triangulate → remove_downward_faces(normal.z < -0.5)`；实测删面 1095，残留 0 |
| 4 | **批渲染只能吃单一 Mesh** | `batched_multimesh` 的 `MultiMesh.mesh` 是单资源槽，场景层合并（多 `MeshInstance3D`）不够 | 在 Blender 内 `join` 成单件；`mesh` 允许 3 个 primitive（材质 01/02/04），但必须落在**一个** mesh 资源里。导入后门禁第 8 项复查 |

#### 结构包络与可视包络在这件上正式分离

v003 是可视为对称的 0.30m；v004 的正面装甲只在一侧：

| | X | Y | Z |
|---|---|---|---|
| 结构/阻挡 `bounds_size_m` | 5.0 | 11.9 | **0.30** |
| 可视包络 `visual_bounds_size_m` | 5.0 | 11.9 | **0.4675** |

可视包络 `Z = [-0.15, +0.3175]`（背面 -0.15，正面装甲凸到 +0.3175），中心 `+0.08375`。
门禁判据仍是「结构盒 ⊂ 可视包络」而不是「可视底面 == 0」，所以装甲凸出**不触发** `bottom_center` 违规
（与门墙门框下探 0.14m 同一处理，见第 7 节第 4 项）。

#### 版本串**没有**单一真源 —— 换版必须同批改三处

| 落点 | 值 |
|---|---|
| `prp_tower_wall_solid_5m.tscn` 的 `metadata/asset_version` | `v004` |
| `TowerDescent3D.gd:2366` 的 `source_visual_version` | `v004` |
| `verify_tower_grid_component_alignment.gd:262` 的 `== "v004"` 断言与提示串 | `v004` |

漏改任一处不会当场报错，但会让「来源版本身份」和「对齐门禁的期望值」互相矛盾。行号会随代码漂移，定位请用 grep（`source_visual_version`），不要硬记。

**本表只覆盖实心墙 `ENV-TOWER-WALL-SOLID-5M`。门墙是另一件资产，不在本批换版范围内** —— `prp_tower_wall_door_5m.tscn` 现仍为 `asset_version = "v003"`，其可视 GLB 也仍是 `env_tower_wall_door_5m_top3d.glb`。两者名字相近，别当成同一批已经换完。

#### 实测与门禁

```text
可视包络 实测 = pos=(-2.5000, 0.0000, -0.1500) size=(5.0000, 11.9000, 0.4675)
可视包络 声明 = (5.0000, 11.9000, 0.4675)
origin_contract = bottom_center  结构盒=pos=(-2.5000, 0.0000, -0.1500) size=(5.0000, 11.9000, 0.3000)  包含=是
forward_axis = +Z   visual_only = true 内嵌碰撞=0   preserve_palette = true material_override=0
装配方式 = batched_multimesh
PREFAB_CONTRACT_OK: 塔楼通用物体（地板/墙壁/墙壁门/女儿墙/门扇）统一契约全部成立 count=5
```

`TowerGeometry3D` 的解析入口、门禁八项、其余三件套均**未改**（门禁目标清单在 8.5 扩到五件，八项判据本身未动）。

### 8.5 门扇换为 A 套正式美术（自 B 套 `door_5m_通用包` 派生）

2026-09-19 追加，**这是 8.3 那个悬置决策的落地**。主人拍板三项：
(1) 门扇新建**独立一套、与门墙同套**；(2) **接受节点增加，同步上调上限**；(3) 安全房门扇**一并换成正式美术**。

#### 资产身份

| 项 | 值 |
|---|---|
| AssetID | `ENV-TOWER-DOOR-LEAF-5M` |
| 稳定 GLB 路径 | `assets/art/environments/tower_descent_3d/components/env_tower_door_leaf_5m_top3d.glb` |
| prefab | `assets/art/props/dungeon_3d/prp_tower_door_leaf_5m.tscn`（根节点 `PrpTowerDoorLeaf5m`） |
| 派生脚本 | `assets/art/environments/tower_descent_3d/source/door_leaf_5m/export_env_tower_door_leaf_5m_v001.py`（`SS_DOOR_LEAF_PHASE=derive\|export`） |
| 来源 | B 套 `source/art/whitebox/tower_zones/battle/components/common_components/v007` 的 `door_5m_通用包`（根件 `ROOT_door_5m_通用组件`） |
| metadata | `asset_version=v001`、`visual_node_name=ENV_TOWER_DOOR_LEAF_5M`、`origin_contract=bottom_center`、`forward_axis=+Z`、`bounds_size_m=(2.2,2.5,0.18)`、`visual_bounds_size_m=(2.2,2.492,0.18)`、`panel_bottom_clearance_m=0.008`、`visual_only=true`、`collision_owner=RoomDoor3D`、`preserve_authored_palette=true`、`runtime_instantiation=per_instance_prefab`、`open_mode=vertical_lift` |

#### 四个跨套迁移约束全部复用了 8.4 的范式

| # | 约束 | 本件做法 |
|---|---|---|
| 1 | 装饰面绕竖轴 180° | Blender 内对根节点烘焙 `yaw=180°`（`Matrix.Rotation(pi,4,"Z") @ Matrix.Translation(-root_location)`）后 `transform_apply`，导出后装饰面落在 Godot `+Z` |
| 2 | 根变换必须单位矩阵 | 原根世界平移先烘进网格再归零；导出后解析 GLB JSON 断言 `nodes=1` 且无 `translation/rotation/scale/matrix` |
| 3 | 先三角化再删朝下面 | 顺序固定 `triangulate → remove_downward_faces(normal.z < -0.5)` |
| 4 | 单 Mesh 承载多材质角色 | `join` 成单件（`ENV_TOWER_DOOR_LEAF_5M`），四角色 01/02/03/04 全保留 |

> **注意 1 号约束在本件是恒等变换**：门扇美术在厚度轴上镜面对称（源侧实测朝 `+Y` 与 `−Y` 面各 1650 个面；厚度包络 `±0.09` 精确居中）。180° 偏航对当前外观不产生任何视觉差异，但**仍按 A 套约定执行**，这样资产身份与同套其他四件一致，将来美术若改成非对称门扇也不会突然反向。

#### 「底边悬空 8mm」是美术几何，如实登记而非压平

导出阶段断言抓到了真实事实：**门扇最低点在组件原点上方 8mm**（`bounds min y = 0.008`）。这不是导出误差，是美术原始几何——关闭的门不该擦地。

处理方式与 8.4 的「结构盒 vs 可视包络分离」同口径：**如实声明**（`panel_bottom_clearance_m=0.008`）而不是把几何压到 0。若压平，反而会在门扇与地面之间制造 z-fighting。

#### 接线（改了哪些代码）

| 落点 | 改动 |
|---|---|
| `DungeonRoom3D.gd` | 新增 `const TOWER_DOOR_LEAF_PREFAB`；删除不再引用的 `SAFE_ROOM_DOOR_LEAF_PREFAB`；`_build_door` 的「其余」分支由 `null` 改为 `TOWER_DOOR_LEAF_PREFAB` |
| `RoomDoor3D.gd` | 仅更新 `_build_procedural_panel` 的注释。**逻辑未改**：该降级路径现在只能由「显式传 `null`」触发，不再是 preload 失败的兜底（prefab 走编译期 `preload`，缺文件会直接编译失败） |
| `verify_tower_module_prefabs.gd` | `TARGETS` 加入第五项门扇 prefab；通过信息改为自解释——**列举**五类物体而不是只印 `count`（目标清单会随资产入册增长，只印数字时看日志的人无法确认多出来的是哪一件、也无从确认它真被检查过） |
| `verify_3d_performance_budget.gd` | 上限 `2400 → 2480`（world）、`2480 → 2560`（total），各 +80 |
| `verify_full_3d_game_flow.gd` | **第二道**节点预算（`node_peak`，与上面那道独立）`2700 → 2780`，同为 +80 |
| `probe_safe_room_v007_integration.gd` | 门扇碰撞断言由「自带碰撞必须被去重」改为「**不得带任何活碰撞**」——碰撞责任单一化 |

> **两处节点预算必须同批改**：`verify_3d_performance_budget` 与 `verify_full_3d_game_flow` 各有一道上限，只改一处会让另一处单独报红，很容易被误判成「刚引入的回归」。

#### 新旧对比实测

| 项 | 改前 | 改后 |
|---|---|---|
| 门总数 | 38 | 38 |
| 程序化门板 | 38 | **0** |
| A 套正式门扇 | 0 | **38** |
| 单门子树节点 | 5 | **7**（+2） |
| `initial_world_nodes` | 2195 | **2271** |
| `initial_nodes` | 2473 | **2549** |

#### 门禁与视觉验收

数值门禁（全部通过，且跑了 `grep ERROR` 确认 `ERROR` 行数为 0）：

```text
PREFAB_CONTRACT_OK: 塔楼通用物体（地板/墙壁/墙壁门/女儿墙/门扇）统一契约全部成立 count=5
COMMON_WALL_DOOR_COMPONENTS_V004_OK: 原点/可替换性/碰撞/门洞净空/自报尺寸契约全部成立
TOWER_LEVEL_BLOCKS_OK: Rooftop/Base/Battle/Stairs and source mappings pass
FULL_3D_GAME_FLOW_OK: ... (peak_nodes=2703)
verify_door_passability     通过 15 / 失败 0（✅ 门功能 + 表现 + 通过性 全部通过）
verify_door_function_check  ✅ 全部通过 0 错误
```

其余同批跑通：`verify_tower_grid_component_alignment`、`verify_tower_lighting_wall_combat_regressions`、
`verify_3d_only_project_structure`、`verify_scene_facility_shared_palette`、`verify_rooftop_door`、
`verify_rooftop_32x32_contract`（`ROOFTOP_WEST_EXPANSION_CONTRACT_PASS`）、`verify_expedition_level01_flow`、
`verify_base_rooftop_transit_door_motion`、`verify_battle_wall_floor_facility_kit`、`verify_test_level_99_flow`、
`verify_common_floor_tile_components_v004`、`verify_arrival_gate_floor_bundle_flow`。

`verify_3d_performance_budget` 仍只剩两条**既有** HUD 红项（`267 > 171`、`278 > 190`），world/total 两道已不再触发
——HUD 两道与门扇改动无关（同源警告是 `env_base99_*` 的 `uid://btvm31cxnmbdg` 色盘 UID 失效），判据是「红项 ⊆ 基线」。

新增两条**画面**探针（`probe_` 前缀，不属套件成员，按项目惯例手工跑）：

| 探针 | 回答什么问题 | 判据 |
|---|---|---|
| `tests/verification/probe_door_leaf_visual.gd` | 门扇**长什么样**：包络、底边高度、厚度居中、材质角色数、正/背/侧三视图 | `DOOR_LEAF_VISUAL_OK` |
| `tests/verification/probe_door_leaf_placement.gd` | 门扇**装进门洞后位置对不对**：逐门按**世界坐标**核对底边贴地 + 关卡内正对门洞特写 | `DOOR_LEAF_PLACEMENT_OK` |

> 第二条不是重复劳动：门扇 prefab 以**底边中心**为原点，而 `RoomDoor3D` 的动画根 `DoorPanel` 以**门中心**为基准，两者靠一句
> `visual.position.y = -DOOR_CLEAR_HEIGHT_M * 0.5` 对齐。这句话写错（漏写、写成 `+`、被后人改掉）**不会让任何数值门禁失败**
> ——包络仍对、门洞仍可通行、节点数也不变——只有一张正对门洞的图能看出「门扇半悬在空中」或「砍进地面一半」。
> 因此该探针刻意用**世界空间** AABB 而不是局部 `position`：真正的风险在「两句话加起来」的结果上。

实测：7 道门全部 `bottom_y = 0.0080`（贴合地面）、`size = (2.2, 2.492, 0.18)`（四舍五入取整后按门向转置）。

#### 台账登记

`ShellStorm2_场景账本_v001.xlsx`《资产主表》新增 **第 241 行**（外科式 XML 补丁，其余 424 行逐格未动）：

```text
ENV-TOWER-DOOR-LEAF-5M / 塔楼5米门扇模块 / 场景 / room_kit / tower_descent / door_leaf_5m
版本 v001 / 制作状态 原型已接入 / SHA-256 取 GLB 实测值
```

同步改写的**范围引用**（不加这一步会让表内自相矛盾）：`dimension Y240→Y241`、`autoFilter A5:X240→A5:X241`、
条件格式与数据校验 `sqref` 三处、表格对象 `ref A5:X115→A5:X241`、
**全表 235 处 `$R$6:$R$240 → $R$6:$R$241`**（`S` 列查重公式的行跨度必须等于实际行数）、总览表 34 处末行引用。

门禁结果：`union_assets 424 → 425`、`missing=[]`、`scenes 235 → 236`、`row_content_mutated = 0`、
新增红项**恰好 1 条** `asset_not_in_baseline`（该门禁锚定分账前基线，新增资产必然命中，见第 10 节）。

#### B 套 `door_5m` 退出运行时

B 套门扇（`common_components/door_5m_root_top3d.tscn`，实为 **13 三角面的单材质占位方块**，非 v006 真实美术）
原先只被 `STAIR_LOBBY` 分支引用，本次随分支退役。**两个文件仍留在库里**（未删除），但已无运行时引用——
即成为**孤儿件**。同时它的内嵌 `DoorCollision` 也随之退出，门洞阻挡唯一由 `RoomDoor3D` 的升降碰撞负责。

## 9. 统一契约字段表（就是「同一规格」的可执行定义）

四类通用物体必须全部声明：

| 字段 | 含义 | 门禁断言 |
|---|---|---|
| `asset_id` / `asset_version` | 稳定身份 | 存在 |
| `origin_contract` | `bottom_center` / `centered_slab` | 结构盒语义 + 被可视包络包含 |
| `forward_axis` | 正面朝向；塔楼 A 套 = `+Z` | 全集一致 |
| `bounds_size_m` | **结构/碰撞**包围盒（玩法用） | 用于求结构盒 |
| `visual_bounds_size_m` | **并入美术后的实测可视**包围盒 | == 根空间实测（容差 0.02m） |
| `visual_node_name` | 视觉/包络载体节点名 | 必须解析到真实节点，不得回退 |
| `visual_only` | 视觉专用 | 为真 ⇒ 内嵌碰撞数必须为 0 |
| `collision_owner` | 碰撞责任方 | 与装配侧策略一致 |
| `preserve_authored_palette` | 保留美术自带色盘 | 为真 ⇒ 无任何 `material_override` |
| `runtime_instantiation` | `per_instance_prefab` / `batched_multimesh` | 批渲染 ⇒ 视觉节点是 `MeshInstance3D` |

## 10. 仍未决 / 已知债务

1. **台账无损基线（`ledger_split_baseline.json`）无法从当前状态重建**。8.5 新增门扇行后，`verify_ledger_split` 报
   `missing=0 extra=1`，extra 就是新行本身。根因：`--baseline-only` 需要**源账本仍持有《资产主表》**，而总目录分账后已无资产行，
   工具链里**没有**「从 7 个分账本合并重建基线」的路径。后果是基线永久停留在分账时刻，此后**每一次有意新增资产都会让这条门禁永远报 extra**，
   久而久之这条门禁会被当成噪声忽略（它现在另有 472 条派生列公式红项，见下）。
   两条出路，需主人拍板：(A) 给 `split_asset_ledger.py` 加**合并式基线重建**（读 7 个分账本 → 归一化成全集指纹 → 刷新基线），
   语义从「分账前真值」变成「当前快照」，但仍能挡住后续静默丢/改行；(B) 让基线只覆盖**集合**（AssetID 列表 + 逐行指纹），
   显式允许新增（`extra` 不判失败），只对 `missing` / `row_content_mutated` 报红。
2. **`verify_ledger_split` 的 472 条派生列公式红项**。全部集中在场景账本的 `R`/`S` 列：openpyxl 把 `t="array"` 的 `S` 列
   读成 `ArrayFormula` 对象、把 `R` 列读成 `==LOWER(...)`（多一个等号），于是与期望字符串永远不相等。
   判据应改为「**红项 ⊆ 基线**」而不是「零红项」——本批写入已实测：除新行外，424 行内容逐格未动（`row_content_mutated = 0`）。
3. **基地家族命名漂移未迁移**。`assets/art/environments/base_facility_3d/runtime/**` 用 `dimensions_m` + `center_bottom`，与塔楼的 `bounds_size_m` + `centered_slab` 并存。该家族有 10+ 个 prefab（含夹层、楼梯），全量重命名会波及家族与既有测试，**本次不做半途迁移**，只给 `DungeonRoom3D` 实际引用的 5 件补了 `visual_node_name`。（已确认 `dimensions_m` 与 `center_bottom` 在 `.gd` 侧无消费者。）
4. **基地 5 件的 `forward_axis` 是 `-Z`，塔楼是 `+Z`**，而两者用同一段 `match direction` 旋转逻辑摆放。对纯色墙无所谓，但对带装饰/带门洞的件，跨套替换会整体反 180°。
   → **墙体已按 8.4 落地「导出前烘焙 180°」的跨套迁移范式**（B 套装饰面 → A 套 `+Z`）；**门扇按 8.5 复用同一范式**；基地 5 件仍**仅记录**，未动美术。
5. **B 套 `door_5m` 已成孤儿件**。`common_components/door_5m_root_top3d.tscn` + `door_5m_visual_top3d.glb` 无任何运行时引用
   （文件保留未删）。它的视觉本体是 13 三角面的单材质占位方块，本来就已不是正式美术。要清理需连同账本行一起处理，属独立议题。
6. **`docs/archive/.../03_关卡与场景设计.md:42` 的节点预算论述**与本次实测结论一致，建议在 `docs/v0.1` 正式文档里也留一条，避免以后有人再提「要不要把墙改成逐 prefab」。

# 新关卡设计表 · 已填实例：测试关卡 99

> 这是**一份填好的记录**，不是模板。空白表见 `新关卡设计表.md`。
> 用途：留档「表 → 关卡设计数据」的对应关系，供日后核对或复刻。

---

# 第一部分 · 你要填的

## 1.1 关卡本身

| 项目 | 填 | 说明 | 可选值 |
|---|---|---|---|
| 关卡标识 | `99` | 这张关卡的唯一名字，也是存档里认领它的凭据 | 小写字母、数字、下划线 |
| 关卡中文名 | 测试关卡99 | 界面上显示的名字 | 任意 |
| 所属区块 | `expedition` | 决定玩家从哪儿进这张关卡 | 远征线填 `expedition` |
| 版图来源 | `authored` | 这张表的布局就是最终版图（每次一样） | `authored` |
| 生成规则版本 | `2` | 记录这张关卡用的是第几代生成规则，会写进存档 | 新关卡一律 `2` |

## 1.2 层清单

| # | 显示层号 | 物理层序 | 排列序号 | 层类型 |
|---|---|---|---|---|
| 1 | 0 | 0 | 0 | `normal` |

## 1.3 房间模板表

| # | 模板名 | 房间种类 | 尺寸 宽×深(m) | 可开门的面 | 变体数 |
|---|---|---|---|---|---|
| 1 | `safe_15x15` | `SAFE_ROOM` | 15 × 15 | north / south / east / west | 0 |
| 2 | `std_25x25` | `COMMON_ROOM` | 25 × 25 | north / south / east / west | 0 |
| 3 | `extraction_25x25` | `EXTRACTION_ROOM` | 25 × 25 | north / south / east / west | 0 |

模板每面墙上的合法门槽位置由工具算出来固化，不用填。

## 1.4 房间表

| # | 房间标识 | 中文名 | 房间种类 | 角色 | 父房间 | 用哪个模板 | 尺寸 宽×深(m) | 中心坐标 x,y | 内容类型 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `entry` | 入口安全房 | `SAFE_ROOM` | `stair_entry` | | `safe_15x15` | 15 × 15 | 2.5, 2.5 | |
| 2 | `room_01` | 战斗房 1 | `COMMON_ROOM` | `main` | `entry` | `std_25x25` | 25 × 25 | 2.5, −32.5 | `COMBAT` |
| 3 | `room_02` | 战斗房 2 | `COMMON_ROOM` | `main` | `room_01` | `std_25x25` | 25 × 25 | 37.5, −32.5 | `COMBAT` |
| 4 | `extraction` | 撤离房 | `EXTRACTION_ROOM` | `extraction` | `room_02` | `extraction_25x25` | 25 × 25 | 37.5, −67.5 | |

两间内容房的内容类型都由设计钉死为 `COMBAT`，所以内容池当前不被消费（留作后续解钉时的缺省）。

## 1.5 主路

```text
room_01 → room_02
```

主路只写内容房：入口房与撤离房不算。

## 1.6 例外：两房直接对接

无。本关全部房间之间都修走廊。

## 1.7 房间刷怪（可选）

**只填了 `room_02` 一间**，用来实测「房间级覆盖只影响那一间房」：`room_01` 刻意留空，
继续走全局公式，与本关的 `room_02` 形成 A/B 对照。

| # | 房间标识 | 第几波 | 这一波出什么（种类 × 数量） |
|---|---|---|---|
| 1 | `room_02` | 1 | 小菌猪 × 2、孢子射手 × 1 |
| 2 | `room_02` | 2 | 壳甲卫兵 × 1、炸弹果 × 2 |

- 合计 2 波、6 只（单波最多 3 只，未触及「单波 ≤ 24 / 全房 ≤ 64 / 波数 ≤ 6」的上限）。
- `room_01` **不填** —— 留作对照，证明覆盖是「按房间可选」而不是「一填全改」。
- 只写「要谁、几只」；怪的强弱（血量 / 伤害 / 速度）由游戏统一口径出，不写在这里。

## 1.8 房间首领（可选）

**不填。** 本关是单层关卡，四个房间里没有 Boss 房（`entry` / `room_01` / `room_02` /
`extraction`），所以这一节留空 —— 也正因为如此，本关当前**没有** `boss_content_id` 样本，
首领指派这条链路的实测改由专属门禁里的手写探针覆盖（见
`tests/verification/verify_test_level_99_flow.gd` 的 `_verify_boss_identity`）。

---

# 第二部分 · 可以改的

| 项目 | 本关取值 | 默认 |
|---|---|---|
| 主路最少内容房数 | **2** | 10 |
| 支线数量区间 | 0 ~ 0 | 2 ~ 5 |
| 生成重试上限 | 1 | 24 |
| 目标面积占用率 | 0.42 | 0.42 |
| 单间参考成本 | 1450.0 平方米 | 1450.0 |
| 禁止压核心筒 | 否 | 是 |
| 内容池 | `COMBAT` | 塔楼默认 8 项 |

「主路最少内容房数」调低到 2，是为了对齐本关主路只有 2 间内容房 —— 保持默认 10 会被直接拒。
「禁止压核心筒」改为否，因为本关是独立单层关卡，没有塔楼核心筒可压。

---

# 第三部分 · 固定的（不能改）

| 项目 | 值 |
|---|---|
| 网格单位 | 5.0 m |
| 墙厚 | 0.30 m |
| 层高 | 12.0 m |
| 可视墙高 | 11.9 m |
| 场地尺寸 | 250 × 250 m |
| 场地中心 | (2.5, 2.5) |
| 核心筒尺寸 | 65 × 65 m |
| 核心筒中心 | (2.5, 2.5) |
| 运行时数据驱动 | **开启**（本关的设计源就是最终版图，游戏真读它） |

---

# 第四部分 · 工具自动算的

| 算什么 | 本关实际产出 |
|---|---|
| 房间的运行时编号 | `start` / `room_01` / `room_02` / `extraction` |
| 关卡指纹 | 按整份设计数据算出，进存档 |
| 每个门的方位与门槽位置 | 4 间房共 6 个门槽，见下表 |
| 门所在墙的长度 | 入口房 15.0 m；其余 25.0 m |
| 楼梯保留区 | 单层关卡没有 |
| 面积预算 | 按房间面积与占用率算 |
| 走廊记录 | 由两端门中心距推导 |

**门槽实测结果**

| 房间 | 门 | 方位 | 门槽位置 | 所连房间 |
|---|---|---|---|---|
| `entry` | P1 | north | 0.0 m | `room_01` |
| `room_01` | P1 | south | 0.0 m | `entry` |
| `room_01` | P2 | east | 0.0 m | `room_02` |
| `room_02` | P1 | north | 0.0 m | `extraction` |
| `room_02` | P2 | west | 0.0 m | `room_01` |
| `extraction` | P1 | south | 0.0 m | `room_02` |

25 m 墙的合法槽位是 −5 / 0 / +5，本关 6 个门槽全部落在正中间的 0；15 m 墙只有一个槽位，也是 0。

**走廊**

| # | 连接 | 走向 | 净距 |
|---|---|---|---|
| 1 | `entry` ↔ `room_01` | 南北 | 15 m |
| 2 | `room_01` ↔ `room_02` | 东西 | 10 m |
| 3 | `room_02` ↔ `extraction` | 南北 | 10 m |

---

# 第五部分 · 硬规则（本关逐条核对）

## 5.1 房间摆位

| # | 规则 | 本关结论 |
|---|---|---|
| 1 | 宽和深都是 5 的整数倍 | 通过（15 / 25） |
| 2 | 房间之间不重叠 | 通过 |
| 3 | 不超出场地 | 通过（x ∈ −10 ~ 50，y ∈ −80 ~ 10） |
| 4 | 除入口房和楼梯厅外，不压核心筒 | 通过（已关闭该约束） |
| 5 | 不压楼梯保留区 | 通过（单层关卡无保留区） |
| 6 | 连走廊的两间房必须同轴 | 通过（横向偏移 0 m） |
| 7 | 走廊净距至少 5 m | 通过（15 / 10 / 10 m） |
| 8 | 走廊长度是 5 的整数倍 | 通过 |

## 5.2 连通结构

| # | 规则 | 本关结论 |
|---|---|---|
| 1 | 每个父房间都存在且能修出合法走廊 | 通过 |
| 2 | 主路内容房数不少于设定下限 | 通过（2 ≥ 2） |
| 3 | 支线条数落在设定区间 | 通过（0 ∈ 0 ~ 0） |
| 4 | 主路里写到的每个房间都存在 | 通过 |
| 5 | 房间总面积不超过 场地面积 × 占用率 | 通过 |
| 6 | Boss 层必须有 Boss 房 | 不适用（非 Boss 层） |

---

# 第六部分 · 填完之后的结果

**产出文件**

```text
source/art/whitebox/tower_zones/99/v001/data/
├── level_plan.json                 关卡总表（层清单 / 模板清单 / 生成策略）
├── floors/floor_00.json            房间表 + 主路 + 施工时的门槽
└── room_templates/
    ├── safe_15x15.json
    ├── std_25x25.json
    └── extraction_25x25.json
```

**校验命令与结果**

```bash
Godot_v4.6.3-stable_win64_console.exe --headless --path . \
  res://tests/verification/verify_level_plan_design_source.tscn -- --level=99
```

```text
LEVEL_PLAN_LEVEL_OK 99 floors=1 rooms=4 templates=3 checks=54
LEVEL_PLAN_VALIDATE_OK levels=1 checks=54 rooms=4 templates=3
LEVEL_PLAN_RUNTIME_LEVEL_OK 99 rooms=4 plans=1
LEVEL_PLAN_RUNTIME_GUARD_OK levels=1 rooms=4 checks=1 plans=1 boss_ids=0 rewards=0
LEVEL_PLAN_RUNTIME_NOTE 设计源暂无 boss_content_id 样本，该字段的透传由 verify_test_level_99_flow 的手写 patch 探针覆盖
LEVEL_PLAN_RUNTIME_NOTE 设计源暂无 reward_plan 样本，该字段的透传由 verify_test_level_99_flow 的手写 patch 探针覆盖
```

两条判据都出：第一条查设计数据自洽（54 项检查），第二条真的调一次生成器查产出可用。
`plans=1` 就是 1.7 里给 `room_02` 填的那份刷怪计划**真的带到了运行时**（填了却仍是 `plans=0`
才是被吞了）；`boss_ids=0` 与 `rewards=0` 分别是本关 1.8 / 1.9 两节留空的如实反映，
各配一行 `NOTE` 声明，**不是**「已验证」—— 这两条透传由
`verify_test_level_99_flow` 的手写 patch 探针（`TEST_LEVEL_99_REWARD_OK`）单独覆盖。

**登记状态**

本关**已**登记进 `verify_level_plan_design_source.gd` 的 `TARGET_LEVELS`（现为 `expedition_01` / `battle_level01` / `99`），
所以省略 `--level=` 的批量校验也会覆盖它。批量结果：

```text
LEVEL_PLAN_VALIDATE_OK levels=3 checks=189 rooms=27 templates=12
LEVEL_PLAN_RUNTIME_GUARD_OK levels=3 rooms=27 checks=3 plans=1 boss_ids=0 rewards=0
```

本关另有专属门禁 `tests/verification/verify_test_level_99_flow.tscn`
（判据 `TEST_LEVEL_99_FLOW_OK` + `TEST_LEVEL_99_REWARD_OK`），查的是设计源之外的接入链路：关卡清单登记、
数据驱动真的生效（而不是静默回退内置房表）、独立单层场景结构、内容外框按 4 间房收缩、
免费入口门、撤离信标、以及 99F 菜单里的入口按钮与读取界面终点；`reward_plan` 的
入口白名单 / 出口透传 / `reward_slots` 投影（含 `ref`）/ `layout_id` 指纹隔离 / 静态校验
也由这份的手写 patch 探针覆盖。
1.7 的刷怪计划与 1.8 的首领指派口径也在这里逐条实测（`_verify_enemy_spawn_plan` /
`_verify_boss_identity`）。

**能不能在游戏里玩到**

**能。** 入口在 **99F 基地 → 远征情报室** 的菜单页里，标题「测试关卡99」下面多了一个
「进入测试关卡99」按钮；点它 → 读取界面 → `ExpeditionLevel99_3D`。

**注意：房间的运行时编号不是 `fxx_` 那套。** 塔楼层用 `f%02d_<key>`（如 `f98_hub`），
但远征关卡运行时是**按字面量认房**的：入口房恒叫 `start`、撤离房恒叫 `extraction`、
主路叫 `room_01…room_0N`。所以本关 L2 里把 `entry` 的 `room_id` 写成 `start`、
其余三间与 key 同名。写错不会让任何几何或静态校验失败，只会让玩家出生点取不到房、
撤离信标挂到一个不存在的房上 —— 这就是 `verify_test_level_99_flow` 存在的理由。


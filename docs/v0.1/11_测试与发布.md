# 测试与发布 v0.1

上级文档：[游戏设计文档](README.md)  
完成度：[全游戏完成度清单](12_全游戏完成度清单.md)

## 1. 原则

“已实装”需要代码事实、自动验证和必要的可视检查共同支持。测试不能写正式存档或覆盖正式资产；视觉输出统一写入 `outputs/verification/`。

## 2. 当前测试入口

<span style="color:#2E7D32">**[已实装]** 工程已有 `tests/verification/` 独立 Godot 场景与统一脚本。</span>

```bash
./scripts/run_verification_suite.sh smoke
./scripts/run_verification_suite.sh core
./scripts/run_verification_suite.sh full
./scripts/run_verification_suite.sh visual
./scripts/run_verification_suite.sh scene verify_tower_descent_flow
```

## 3. 分层测试

| 层级 | 目标 | 典型证据 |
|---|---|---|
| 数据校验 | ID、字段、引用、数值范围、存档 schema | 数据库扫描、公式错误扫描 |
| 领域单测 | 状态、伤害、生成计划、精英唯一性、结算 | 无场景脚本测试 |
| 场景集成 | 玩家、门、房间、敌人、设施、UI | `tests/verification/*.tscn` |
| 生成性质测试 | 多种子、多楼层不变量 | 重叠、可达、组件对齐报告 |
| 可视回归 | Mesh、材质、灯光、相机、动画 | 固定机位 PNG |
| 性能回归 | 节点、Draw Call、对象池、流送 | 性能预算场景 |
| 长时热浸泡 | CPU/GPU帧时间、RSS、物理查询、热状态、进程残留 | 30/60分钟实机报告 |
| 存档破坏测试 | 中断、备份、迁移、幂等 | 临时用户目录与故障注入 |

## 4. 七系统最低回归集

### 玩家

`verify_p0_player_flow`、`verify_state_machine_safety`、`verify_3d_reload_state_flow`、`verify_player3d_vertical_physics_flow`、`verify_tower_camera_occlusion_flow`。

### 战斗与命运

`verify_3d_combat_progression_flow`、`verify_3d_fate_weapon_flow`、`verify_3d_inventory_weapon_flow`、`verify_weapon_instance_fate_ownership_flow`、`verify_tactical_inventory_minimap_flow`、`verify_fate_card_pool`。

<span style="color:#2E7D32">**[本轮新增并通过]** `verify_weapon_instance_fate_ownership_flow` 合并覆盖：同型枪唯一 ID、普通枪 8 槽、线性永久追加、满槽原子拒绝、配件不占槽、三作用域隔离、JSON 往返、换枪再装备、背包/保险重复拒绝、地面/背包模型构筑外观及武器表现页锁槽。</span>

<span style="color:#2E7D32">**[回归通过]** `verify_fate_card_pool` 校验48张可玩卡、稳定ID、22/12/14作用域数量与天体专名；`verify_celestial_fate_scope_flow` 校验20张新增牌；新增楼层性质测试后 `core` 共18/18通过。</span>

<span style="color:#EF6C00">**[后续增强]** 商店/保险柜二次确认和提示已在代码接入；如需把每一种失败动画、逐内容 VFX/音效与并排属性来源做成独立自动化证据，再拆分 `verify_weapon_presentation_snapshot` 与 `verify_weapon_ui_feedback_flow`。</span>

### 关卡与爬楼

`verify_tower_descent_flow`、`verify_tower_grid_component_alignment`、`verify_door_passability`、`verify_map_layout_flow`、`verify_tower_stair_fall_visual`。

<span style="color:#2E7D32">**[本轮已施工]** `verify_floor_plan_generator` 已加入 `core`，并与塔楼场景专项共同覆盖以下契约：</span>

- `verify_tower_descent_flow`：旧全图预建用例已迁移退出；新循环由 `verify_arrival_gate_floor_bundle_flow` 接管，避免旧强制开边假设污染结论。
- `verify_floor_plan_generator`：校验总面积、外墙/核心/设施扣除、0.42预算、房/内墙/走廊成本和普通层14内容房目标。
- `verify_floor_plan_generator`：1000种子×98–95F共4000计划，检查确定性、10房主路、两条以上支线、尺寸、边界、房间重叠、65m核心与楼梯禁建区。
- `verify_tower_grid_component_alignment`：66个场景房间与60条水平通道的5m组件边界、门轴、楼板洞和碰撞一致。
- `verify_door_passability`：130扇门配置、开关与碰撞通过；EVENT 专项额外检查中部常驻光柱、终端不阻挡角色、空房提示不伪报战斗、目标丢失自动重建和结算状态自愈。
- `verify_floor_plan_resume`：目标用例；提交后保存、重进场景和重启均恢复相同 `layout_id`、房间、支路、房型、楼梯锚点和 Boss 路线，当前行动持久化尚未施工。

### 怪物与 Boss

`verify_3d_enemy_behavior_flow`、`verify_enemy_ecology_flow`、`verify_boss_completion_flow`、`verify_p1_boss_signal_flow`。

### 基地

`verify_base_world_3d_visual`、`verify_base_world_flow`、`verify_training_range_3d_flow`。

### 剧情

<span style="color:#EF6C00">**[Codex补充]** 新增剧情触发器纯数据测试、事件去重、互斥、对话中断与三条垂直切片测试。</span>

### 存档与复活

<span style="color:#EF6C00">**[Codex补充]** 新增多域事务、重复提交、`.bak` 恢复、`BaseData 1.1 → 1.2` 迁移、精英档案迁移和复活空策略测试。</span>

### 本轮施工验收记录（2026-08-05）

- `godot --headless --path . --editor --quit`：脚本解析与资源导入通过。
- `verify_floor_plan_generator`：1000种子、4000份98–95F计划全部通过；普通层由面积预算得到14内容房，主路10房、2条支线，Boss层11房预热+不可绕过Boss。
- `verify_tower_grid_component_alignment`：66房、60水平通道、5m边界、真实门Mesh、楼板覆盖与碰撞通过。
- `verify_door_passability`：15项通过；覆盖130扇门及 EVENT 空房软锁、不可见终端碰撞、错误“战斗未结束”提示的回归。
- `verify_tower_descent_flow`：安全屋交互前不提交、seed gate提交后冻结布局、98–95F战斗/Boss/撤离闭环通过；本次3505节点，低于专项3700预算。
- 新专项不使用 `force_open_edge_for_test` 提交FloorBundle；它通过真实到达门激活入口验证98F事务，并通过同一到达门处理函数验证Boss隔离链。结构类旧测试仅使用公开生成准备接口，不作为玩法流程完成证据。
- `./scripts/run_verification_suite.sh core`：当前19场景，包含楼层性质、真实到达门、塔楼、战斗、命运、天体作用域、换枪、枪械实例、战术背包、小地图与性能专项。
- `verify_tactical_inventory_minimap_flow`：除装备/背包外，验证雷达玩家固定中心、地图反向移动、圆形边界契约、无玩家方向线和怪物红点。
- `verify_3d_enemy_behavior_flow`：验证普通怪最终HP×3、Boss最终HP×10、全体速度×0.70、Boss体积/世界碰撞×2及头顶血条，同时保留七类行为回归。
- `verify_arrival_gate_floor_bundle_flow`：新增100→99基地交通门用例，确保它不请求物理层1战斗规划；并检查基地交通、楼层到达、普通进度、Boss下行与隔离前门分类。
- `verify_tactical_inventory_minimap_visual`：Metal/OpenGL 真实渲染与真实鼠标事件通过；覆盖唯一且不盖住来源格的枪械悬停卡、格位移动、拖枪换装、装备枪卸回指定空格、红区落地，并输出各交互截图。
- `verify_reference_hud_fate_visual`：Metal/OpenGL 输出正式 HUD、命运三选一与塔楼紧凑 HUD 三张 1280×720 截图；检查整体 80% 尺寸、塔楼当前信息下移且无重复 HP 条、底部枪械复用背包 3D 模型工厂、弹药刷新不重建预览，以及三张天体竖卡。
- `verify_3d_performance_budget`：流送世界、固定 UI 壳和 3D 预览分账；初始世界 `672/800`、完全探索世界 `3399/3500`、UI 壳 `129/140`、3D 预览 `17/20`、HUD 总量 `146/160`、场景总量 `3550/3600`。显式60FPS/VSync后稳定墙钟节拍 `16.69ms/18ms`，稳定120帧视野网格重建0次；纯CPU/GPU P95待真实渲染长测。
- 统一测试套件已增加每场景180秒看门狗和 `EXIT/INT/TERM` 精确子进程回收；用1秒强制超时验证目标PID被终止且无遗留，本轮 `core` 18/18 后 Godot 测试残留同样为0。
- `verify_fate_card_pool.gd`：48 张命运卡通过。
- `verify_celestial_fate_scope_flow`：10 张月亮与 10 张太阳命运的角色/世界结算及 UI 通过。
- `full` 集在既有 `verify_3d_vision_input_flow` 停止：当前探照灯 beam/spill 参数低于该旧用例阈值；与本轮武器改动无关，未借本轮擅自改变灯光设计。

## 5. 本次设计新增的 P0 验证

- <span style="color:#2E7D32">**[已完成]** `verify_arrival_gate_floor_bundle_flow`：验证初始3房、98F真实到达门、逐层FloorBundle、95→94楼梯/唯一电梯、默认取消警告、隔离前门、永久掉落清理、65房旧段卸载与玩家钥匙保留。</span>
- <span style="color:#1565C0">**[用户设计]** `verify_boss_descent_gate_flow`：Boss专钥匙、专用门、95→94真实楼梯、门轴/碰撞/导航和门后路线。</span>
- <span style="color:#1565C0">**[用户设计]** `verify_unique_95_94_elevator_placement`：全游戏关卡只在95→94楼梯间存在1个电梯；普通层、房间、安全屋、90F/85F Boss后楼梯均为0。</span>
- <span style="color:#1565C0">**[用户设计]** `verify_isolation_airlock_flow`：95→94及90→89边界均检查进入前二次永久遗失提示、默认取消、动态楼层范围、双门互锁、玩家完整入内检测、失败恢复和两门永不同时开启；90→89不得出现电梯。</span>
- <span style="color:#1565C0">**[用户设计]** `verify_segment_unload_loot_loss_flow`：提交前可返回；提交后98–95F未拾取物永久销毁，装备/背包/保险物保留，旧Node/导航/敌人/地图引用为0，读档不可回退。</span>
- <span style="color:#EF6C00">**[Codex补充]** `verify_floor_bundle_failure_recovery`：注入规划、楼梯、门、存档和实例化失败；玩家始终位于实体关闭门的安全一侧，可用同一事务重试且不重复生成。</span>
- <span style="color:#1565C0">**[用户设计]** `verify_unique_elite_roster_flow`：12 只名册、同名唯一、跨局成长、夺取枪械。</span>
- <span style="color:#2E7D32">**[已实装首批]** `verify_floor_plan_generator`：已覆盖1000种子×当前98–95F；更深95F–85F场景、钥匙流和跨重启恢复继续作为扩展阻断项。</span>
- <span style="color:#EF6C00">**[Codex补充]** `verify_content_database_parity`：数据库稳定 ID 与正式注册表一致。</span>

## 6. 可视验收机位

1. 楼顶入口与 99 层基地走廊。
2. 98 层安全房、开门后长通道、南侧相机墙。
3. 普通墙、拐角墙、带门墙、楼梯南墙的贴墙相机。
4. 基地灯初始开启、关闭、再次开启三张固定曝光截图。
5. 玩家探照灯照射近墙与远墙的条纹对比。
6. 普通层楼梯间、95→94唯一电梯楼梯间、其他Boss后无电梯楼梯间对比。
7. 隔离间前警告、后门关闭锁定、旧区段卸载完成和前门开启四个状态。
8. 12 只精英的统一比例与轮廓板。
9. 武器表现页：同型空枪与满构筑枪、8 槽线性轨道、装配幽灵预览、应用成功与完整回滚。
10. 48 张命运卡的选择卡面、天体专名、目标/槽位提示及枪身/弹道/命中/角色/世界触发表现矩阵。
11. 战术装备页：左装备/红色生命，右背包/保险/整理，下方丢弃区，以及真实房间比例、玩家箭头和怪物红点小地图。
12. 正式主 HUD 与命运三选一：左上角色状态、圆形战术地图、底部枪械实例、操作键块，以及太阳/月亮/星星三张竖向发光卡。

## 7. 发布门槛

版本可发布必须同时满足：

- `smoke`、`core` 通过；涉及正式场景/资产时 `full` 通过。
- 本次变更对应的专项场景和可视对比通过。
- 游戏内容数据库无重复 ID 和公式错误。
- 新增/变更存档字段具有迁移与回滚测试。
- 没有 P0：卡死、不可达、空气墙、地洞、重复精英、钥匙丢失、物品复制、存档覆盖、到达门后空层、隔离双门同开或已提交区段被读档恢复。
- 文档、数据库、代码与资产台账已经同步更新。
- 测试套件退出后没有孤儿 Godot 进程；性能改动通过真实渲染与至少30分钟热浸泡。
- 任何武器功能只有在规则测试、UI 提示和至少一项可视/听觉证据同时通过后才能标记完成。

## 8. 缺陷优先级

| 级别 | 定义 | 示例 |
|---|---|---|
| P0 | 阻断主循环、破坏存档或产生不可恢复结果 | 空刷怪卡死、Boss 无法下楼、重复/丢失物品 |
| P1 | 严重破坏判断或战斗，但有绕行 | 空气墙、门/地图不一致、精英重复出现 |
| P2 | 明显表现/平衡问题 | 高光曝光、远墙条纹、动画错位 |
| P3 | 低影响一致性与整理 | 命名、占位文案、次要台账缺项 |

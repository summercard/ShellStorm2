# 推送更新摘要 · 2026-08-27

> 本文件汇总本次 `git push` 到 `origin/V0.1` 的全部 commit、覆盖范围与回看方式。
> 详细变更日志请以 `CHANGELOG.md` 为准；本文件侧重"推送元信息 + 文件清单"。

## 推送元信息

| 项 | 值 |
|---|---|
| 推送分支 | `V0.1` |
| 远端 | `origin` → `github.com:summercard/ShellStorm2.git` |
| 推送区间 | `faf1a4c..fddc625` |
| 新增 commit | 3 条 |
| 总变更 | 37 文件，**+1284 / -95** |
| 推送人 | summercards \<summercards@gmail.com\> |
| 推送时间 | 2026-08-27 18:38 CST |

## 三条 commit 概览

按提交时间从早到晚：

1. **`37f2100` — docs(v0.1): 首只唯一精英成长闭环与文档反推修订**
   - 13 文件改动，+116 / -58
   - 仅文档 + 内容数据库 + 美术资产台账

2. **`c8faef0` — feat(elite): 首只唯一精英"背枪的裂口爬虫"成长闭环接入**
   - 10 文件改动，+506 / -37
   - 新增 `EliteContentCatalog` 静态库 + 9 个旧文件接入残血逃脱 / 悬赏 / 夺械转译 / 副枪追射 / 95F Boss 房不误刷

3. **`fddc625` — asset(elite)+test: rift_boar_armed 3D 资产、Blender 脚本与精英专项验证**
   - 14 文件改动，全部为新增（662 行净增）
   - 1 套 3D 资产（Blend + GLB + 包装场景） + 2 个 Blender 脚本 + 3 个新验证脚本（含配套 `.uid`/`.tscn`）+ 2 个旧验证脚本回归同步

## 本次推送覆盖的实质改动

### 1. 文档同步（commit 1）

```
docs/v0.1/02_技术架构总则.md
docs/v0.1/06A_怪物AI系统完整设计_评审稿.md
docs/v0.1/06_技术施工_怪物精英与Boss.md
docs/v0.1/07_技术施工_基地设施.md
docs/v0.1/09_技术施工_存档结算与复活.md
docs/v0.1/10_资产与内容规范.md
docs/v0.1/11_测试与发布.md
docs/v0.1/12_全游戏完成度清单.md
docs/v0.1/13_技术施工_性能优化与热管理.md
docs/v0.1/CHANGELOG.md
docs/v0.1/README.md
assets/registry/ShellStorm2_美术资产台账_v001.xlsx
docs/v0.1/data/ShellStorm2_游戏内容数据库_v010.xlsx
```

CHANGELOG 新增三条 2026-08-27 条目：
- 首只唯一精英成长闭环完成
- 首只唯一精英"背枪的裂口爬虫"正式制作与 98—96F 投放
- 按当前代码反推文档并记录验收回归（含 `verify_full_3d_game_flow` 当前失败项的待核验登记）

### 2. 精英代码闭环（commit 2）

新增：
- `src/enemy3d/EliteContentCatalog.gd` —— 12 只精英的静态定义库，仅 `elite_rift_boar_armed` 为 `deployed`，其余 11 只标记 `design_only`

改动：
- `src/enemy3d/EliteRosterService.gd` —— 跨局档案 / 预约事务与表现契约解耦
- `src/enemy3d/Enemy3D.gd` —— 残血 20% 撤离窗、跨局等级 1—12 成长、副枪追射、悬赏结算
- `src/enemy3d/EnemyAvatar3D.gd` —— 按内容库配置加载精英表现并关闭通用外壳
- `src/combat3d/Projectile3D.gd` —— 副枪追射弹信号贯通
- `src/map/MonsterInjector.gd` —— 按 `run_seed` 在 98/97/96 三选一目标层复用精英房
- `src/player3d/Player3D.gd` —— 玩家最后伤害来源归因
- `src/ui/MonsterArchiveMenu.gd` —— 档案 UI 接入原子结算
- `src/world3d/Dungeon3D.gd` —— 95F Boss 房不再误刷未完成精英

### 3. 资产 + 测试（commit 3）

新增资产（1 套 3 件）：
```
assets/art/enemies/elite_3d/rift_boar_armed/source/enm_elite_rift_boar_armed_source_v001.blend
assets/art/enemies/elite_3d/rift_boar_armed/components/enm_elite_rift_boar_armed_visual_top3d_v001.glb
assets/art/enemies/elite_3d/rift_boar_armed/runtime/enm_elite_rift_boar_armed_root_top3d_v001.tscn
```
> 包装场景仅挂表现与语义组件，不带玩法碰撞；`Enemy3D` 继续持有碰撞与 AI。`.blend1` 已被 `.gitignore` 排除。

新增 Blender 脚本：
```
scripts/blender/build_elite_rift_boar_armed_v001.py
scripts/blender/validate_elite_rift_boar_armed_v001.py
```

新增验证脚本（3 个 + 配套 `.uid`/`.tscn`）：
```
tests/verification/verify_first_elite_deployment_flow.gd(.uid/.tscn)   # 部署流：run_seed → 目标层 → 房间复用
tests/verification/verify_first_elite_growth_flow.gd(.uid/.tscn)       # 成长闭环：残血撤离 / 跨局等级 / 夺械转译 / 悬赏
tests/verification/verify_first_elite_visual.gd(.uid/.tscn)            # Forward+ 固定机位视觉验收
```

回归同步：
```
tests/verification/verify_3d_parity_core.gd
tests/verification/verify_unique_elite_roster_flow.gd
```

## 回看方式

```bash
# 查看本次推送的全部 commit
git log --oneline faf1a4c..fddc625

# 按文件类型过滤查看
git diff --stat faf1a4c..fddc625 -- '*.gd'
git diff --stat faf1a4c..fddc625 -- 'docs/v0.1/*.md'

# 单个 commit 详情
git show 37f2100
git show c8faef0
git show fddc625

# 仅查看本次推送涉及的精英相关文件
git log --diff-filter=A --name-only faf1a4c..fddc625 -- 'src/enemy3d/Elite*' 'assets/art/enemies/elite_3d/**'
```

## 后续待办（本次推送未完成）

- ❗ `verify_full_3d_game_flow` 在"拾取初始霰弹枪后通过真实 UI 信号路径装备"处失败 —— 已在 CHANGELOG 登记为代码/测试口径待核验，定位并复跑前不得签署当前自动验收全绿
- ⏳ 其余 11 只唯一精英（design_only）的正式投放与成长闭环仍待后续 commit 推进
- ⏳ 弹壳风暴2 主开发循环（已被切走监测）等待重启信号

---

*最后更新：2026-08-27 18:38 CST*
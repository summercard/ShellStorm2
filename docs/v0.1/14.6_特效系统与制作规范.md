# 14.6 特效系统与制作规范

> 起草人：杰西卡（OpenClaw 主会话）
> 起草日期：2026-09-11
> 状态：**草案 v001 / 等主人拍板**
> 触发：主人要求"特效进行工整的拆分，每个特效作为一个独立的 prefab 文件，prefab 是特效的唯一出口；账本和调用代码读取的是这个 prefab"
> 适用范围：弹壳风暴2 全项目

---

## 0. 一句话原则

**每个特效 = 一个独立 Prefab 文件。Prefab 是特效的唯一出口。**

不论特效内部是脚本生成、动画驱动还是模型嵌入，外部调用代码 **只看见 Prefab**，**不看见**任何 `effect_kind` / `match` 分支或私有枚举。

---

## 1. 三个核心约定

### 1.1 独立 Prefab 原则（硬约束）

- 每个特效的视觉、动画、生命周期、参数配置必须**完整封装在它自己的 `.tscn` 文件里**。
- 禁止使用"一个总脚本 + `match kind` 分支"或"一个空壳 Prefab + 7 套代码逻辑"组织特效——这种结构不允许作为新特效的入口，但允许**逐步重构替换**（详见 §6 迁移计划）。
- 调用代码**只引用**特效 Prefab 的 `.tscn` 路径或 AssetID，**不允许**引用任何特效内部脚本的实现细节。
- 一个特效最多有一个可选的"私有基类"，但基类**不暴露**任何该特效专属的参数入口。

### 1.2 Prefab 是特效的唯一出口（运行时约束）

- 调用方拿到的 API 必须是：`vfx_pool.acquire(VFX-MUZZLE-FLASH-3D, color, size, world_pos)` 这种 **AssetID 路由**形式。
- 禁止 `vfx_pool.acquire("muzzle", color, size, world_pos)` 这种 **字符串枚举分支**形式。
- Prefab 文件可被独立 `preload()`、独立 `instantiate()`、独立池化、独立调参、独立验收。

### 1.3 账本与调用代码读取 Prefab（数据约束）

- **特效账本**（`assets/registry/ledgers/ShellStorm2_特效账本_v001.xlsx`，路径由 `assets/registry/ledger_index.json` 声明）的 **`3D-特效` sheet** 与《资产主表》是特效资产的**唯一登记源**；总目录 `assets/registry/ShellStorm2_美术资产台账_v001.xlsx` 只放索引，不含资产行。
- 每个特效有 **一个且只有一个** AssetID（如 `VFX-MUZZLE-FLASH-3D`）。
- 每个特效有 **一个且只有一个** Prefab 路径（如 `assets/art/vfx/combat_3d/vfx_muzzle_flash_root_top3d.tscn`）。
- 调用代码必须 **从账本登记的 AssetID 或 Prefab 路径读取**，**不允许**重新发明枚举或路径。

---

## 2. 命名与目录规范

继承 `docs/v0.1/10_资产与内容规范.md` 第 27-36 行的命名约定，特效分类细化如下：

### 2.1 命名

- **AssetID**：`VFX-<SUBSYSTEM>-<EFFECT>-<VIEW>`，**全大写**、**连字符分隔**。
  - `VFX-MUZZLE-FLASH-3D` ✅
  - `VFX-PLAYER-DASH-TRAIL-A` ✅（变体用字母后缀）
  - `VFX-MELEE-IMPACT-3D` ✅
- **Prefab 文件名**：`vfx_<subsystem>_<effect>[_<variant>]_<view>_v<NNN>.tscn`，**全小写**、**下划线分隔**。
  - `vfx_muzzle_flash_root_top3d.tscn` ✅
  - `vfx_player_dash_trail_a_root_top3d.tscn` ✅
- **根节点 class_name**：`Vfx<EffectPascalCase>3D`，**PascalCase**、**不加文件名后缀**。
  - `VfxMuzzleFlash3D` ✅
  - `VfxDamageNumber3D` ✅
- **脚本文件**：`src/vfx/<VfxEffectPascalCase>3D.gd`，**与根节点 class_name 对齐**。
  - `src/vfx/VfxMuzzleFlash3D.gd` ✅

### 2.2 目录

```
assets/art/vfx/
├── combat_3d/           # 战斗相关 3D VFX（muzzle / impact / explosion / melee_slash / melee_impact / damage / heal）
├── player_state/        # 玩家状态 VFX（dash_trail / hurt_flash / lowhp_ring / silenced_ring）
├── player_overlay/      # 玩家屏幕覆盖 VFX（vignette / scope_overlay）
├── gameplay_vfx/        # 玩法内 VFX（muzzle_flash / explosion / spark / damage_number / room_transition / extraction_beacon / health_vignette / visibility_light）
├── environment_3d/      # 环境 VFX（hazard_field / dust_particles / wind_strip）
└── visibility_3d/       # 视野相关 VFX（visibility_field / player_flashlight）

src/vfx/                 # 特效脚本目录
├── VfxEffectBase3D.gd   # 所有特效 Prefab 根节点的基类（详见 §3）
├── VfxMuzzleFlash3D.gd
├── VfxImpact3D.gd
└── ...

docs/v0.1/14.6_特效系统与制作规范.md  # 本文档
```

> **注意**：`combat_3d` 和 `gameplay_vfx` 在目录上可能有重叠。规则：**纯战斗反馈类（命中、爆炸、命中飘字）放 `combat_3d`；玩法流程类（信标、转场、可见性光锥）放 `gameplay_vfx`**。

---

## 3. Prefab 结构标准

每个特效 Prefab 必须包含且仅包含：

```
Prefab root (.tscn)
├── 根节点：Node3D（class_name = VfxEffectBase3D 子类，如 VfxMuzzleFlash3D）
│   ├── @export effect_color: Color
│   ├── @export effect_size: float = 1.0
│   ├── @export lifetime: float = 0.34
│   ├── signal retired(effect: VfxEffectBase3D)
│   ├── func configure(color, size, context) -> void
│   ├── func activate(world_pos, color, size, context) -> void
│   └── func _on_lifetime_expired() -> void
│
└── [可选] 视觉子节点
    ├── SphereMesh / TorusMesh / BoxMesh / SurfaceTool 生成 mesh
    ├── GPUParticles3D（粒子特效）
    ├── AnimationPlayer（关键帧动画特效）
    └── OmniLight3D / SpotLight3D（光照特效，无阴影）
```

### 3.1 根节点契约（VfxEffectBase3D 基类）

所有特效 Prefab 的根节点 **必须继承** `src/vfx/VfxEffectBase3D.gd`：

```gdscript
class_name VfxEffectBase3D
extends Node3D
## 所有 VFX Prefab 的根节点基类。
## 调用方只看见本基类暴露的接口；特效私有逻辑在子类中实现。

signal retired(effect: VfxEffectBase3D)

@export var effect_color: Color = Color(1, 1, 1, 1)
@export_range(0.1, 8.0, 0.1) var effect_size: float = 1.0
@export var lifetime: float = 0.34

var _elapsed := 0.0
var _active := false
var _follow: Node3D = null                    # 可选跟随挂点（context.follow）
var _follow_local_offset := Vector3.ZERO      # 挂点本地空间偏移

func configure(color: Color, size: float = 1.0, context: Dictionary = {}) -> void:
    effect_color = color
    effect_size = size
    _on_configure(context)

func activate(world_position: Vector3, color: Color, size: float, context: Dictionary = {}) -> void:
    _bind_follow(context)                     # 解析 context.follow / follow_local_offset
    configure(color, size, context)
    _active = true
    visible = true
    process_mode = Node.PROCESS_MODE_INHERIT
    global_position = follow_position() if has_follow_target() else world_position
    _on_activate(world_position, color, size, context)
    _elapsed = 0.0

func _process(delta: float) -> void:
    if not _active:
        return
    if has_follow_target():                   # 挂点移动/转身时特效必须贴着它
        global_position = follow_position()
    _elapsed += delta
    _on_tick(_elapsed, lifetime)
    if _elapsed >= lifetime:
        _retire()

func _retire() -> void:
    _active = false
    visible = false
    process_mode = Node.PROCESS_MODE_DISABLED
    _on_lifetime_expired()
    if retired.get_connections().is_empty():
        queue_free()
    else:
        retired.emit(self)

# 子类覆盖点（不要在调用方调用这些方法）
func _on_configure(_context: Dictionary) -> void: pass
func _on_activate(_world_pos: Vector3, _color: Color, _size: float, _context: Dictionary) -> void: pass
func _on_tick(_elapsed: float, _total: float) -> void: pass
func _on_lifetime_expired() -> void: pass
```

### 3.2 私有参数（子类扩展）

子类可在 `extends VfxEffectBase3D` 之后**自由增加**私有 `@export` 参数，但这些参数**对调用方不可见**：

```gdscript
class_name VfxMuzzleFlash3D
extends VfxEffectBase3D

@export_range(0.05, 1.0, 0.01) var _flash_core_radius := 0.18  # 私有，调用方不读
@export_range(0.05, 0.5, 0.005) var _ring_thickness := 0.045

# 调用方依然只看到 effect_color / effect_size / lifetime 三个公共参数
```

### 3.3 context 通用键：挂点跟随（可选）

`context` 是调用方向特效传参的唯一通道。已定义的**通用键**（所有特效都认）：

| 键 | 类型 | 含义 |
|---|---|---|
| `follow` | `Node3D` | 跟随挂点。绑定后特效**每帧**把世界变换贴到该挂点，而不是停在 `activate()` 传入的世界坐标。未绑定、挂点已释放或已移出树时，自动退化为世界锚定（不报错）。 |
| `follow_local_offset` | `Vector3` | 挂点**本地空间**的偏移，缺省 `Vector3.ZERO`（贴着挂点原点）。 |
| 子类私有键 | — | 如枪口的 `forward`、命中爆点的 `normal`，由各子类在 `_on_activate` 中消费，不属通用契约。 |

```gdscript
# 调用方（WeaponModel3D）：枪口花火贴住 muzzle 挂点，角色移动/转向/后坐力时不残留
pool.acquire(
    VfxPool3D.FX01_MUZZLE_FLASH, muzzle.global_position, bullet_color, 0.8,
    {"forward": -global_basis.z, "follow": muzzle}
)
```

三条硬规则（2026-09-21 由业主实机缺陷固化）：

1. **挂点失效不得报错**：`has_follow_target()` 必须同时校验 `is_instance_valid()` 与 `is_inside_tree()`，任一不成立即回落到世界锚定。
2. **跟随目标要选"真正跟着角色动的那个节点"**：枪口花火必须跟 `muzzle` 挂点，**不能跟武器根**。`WeaponModel3D._visual_root` 会被后坐力（`_recoil`）与换弹摆姿逐帧位移，跟根节点会在开火瞬间差一截。
3. **`_follow` 每次 `activate()` 都重置**：池化复借时上一次的绑定绝不泄漏到下一次。

---

## 4. 注册流程

### 4.1 新建特效 Prefab 的 6 步流程

| 步骤 | 动作 | 文件 |
|---|---|---|
| 1 | 在台账 `3D-特效` sheet 领取 AssetID（参照已有命名） | `.xlsx` |
| 2 | 在 `src/vfx/` 创建根节点类，继承 `VfxEffectBase3D` | `.gd` |
| 3 | 在 `assets/art/vfx/<category>/` 创建 `.tscn` 包装场景，根挂第 2 步脚本 | `.tscn` |
| 4 | 在 `src/vfx/VfxPool3D.gd` 的 `_REGISTRY` 常量中注册AssetID与PackedScene；调用入口为项目Autoload `VfxPool` | `.gd` |
| 5 | 在 `tests/verification/verify_xxx_flow.tscn` 写专项验收测试 | `.tscn` |
| 6 | 在台账登记：Prefab 路径、GLB 路径、Blender 源、功能脚本、碰撞规则、标准尺寸、原点、使用位置、制作状态、版本、备注 | `.xlsx` |

### 4.2 调用约定

调用方代码**只通过** `VfxPool3D` 拿到特效：

```gdscript
# 推荐：AssetID 路由（账本唯一登记源）
vfx_pool.acquire(VfxPool3D.FX01_MUZZLE_FLASH, world_position, color, size)

# 也接受：字符串 AssetID 形式
vfx_pool.acquire("VFX-MUZZLE-FLASH-3D", world_position, color, size)

# 禁止：字符串枚举分支（旧的 effect_kind 形式）
vfx_pool.acquire("muzzle", world_position, color, size)  # ❌ 违规
```

```gdscript
# 直接独立实例化（不在池子里跑）也允许，但仅限一次性 UI 测试场景
var flash := preload("res://assets/art/vfx/combat_3d/vfx_muzzle_flash_root_top3d.tscn").instantiate()
add_child(flash)
flash.activate(world_position, color, size, {})
```

---

## 5. 对象池与生命周期

<span style="color:#2E7D32">**[E07已完成｜2026-09-12]** `VfxEffectBase3D.retired`自身携带`effect`，`VfxPool3D`只额外绑定`asset_id`，回调固定接收`effect + asset_id`。`verify_vfx_pool_lifecycle`已覆盖到期回收、active/inactive计数、同实例再次借出及`clear_all`场景退出清理。</span>

### 5.1 VfxPool3D 全局池（取代旧的 CombatEffectPool3D）

新建 `src/combat3d/VfxPool3D.gd`（autoload 或单例 group 节点），提供**按 AssetID 路由**的多场景池：

```gdscript
class_name VfxPool3D
extends Node3D

const VFX_MUZZLE_FLASH := "VFX-MUZZLE-FLASH-3D"
const VFX_IMPACT := "VFX-IMPACT-3D"
# ... 16+ 个 const 注册

const _REGISTRY := {
    VFX_MUZZLE_FLASH: preload("res://assets/art/vfx/combat_3d/vfx_muzzle_flash_root_top3d.tscn"),
    VFX_IMPACT: preload("res://assets/art/vfx/combat_3d/vfx_impact_root_top3d.tscn"),
    # ...
}

@export var max_per_kind := 32

func acquire(asset_id: StringName, world_pos: Vector3, color: Color, size: float, context: Dictionary = {}) -> VfxEffectBase3D:
    # 按 asset_id 路由到对应 bucket
    # ...

func retire(asset_id: StringName, effect: VfxEffectBase3D) -> void:
    # 按 asset_id 路由到对应 inactive bucket
    # ...

func set_quality(tier: QualityTier) -> void:
    # LOW: amount * 0.5；MEDIUM: 1.0；HIGH: 2.0
    # ...

func unload_scene(scope_path: NodePath) -> void:
    # 区段卸载时调用，强制 queue_free 所有 active 特效
    # ...
```

### 5.2 生命周期阶段

| 阶段 | 状态 | 可见 | process_mode | 说明 |
|---|---|---|---|---|
| 配置后未激活 | _active=false | false | DISABLED | 池子里待命 |
| 激活中 | _active=true | true | INHERIT | 倒计时中 |
| 到期后 | _active=false | false | DISABLED | 信号 retired 后回池或 queue_free |

特效**自己管理**生命周期，池子只负责"借出/归还"。**禁止**让外部代码手动 destroy 特效——只允许 `pool.retire()` 或让 `retired` 信号自动 queue_free。

---

## 6. 迁移计划（从旧 VFX-COMBAT-KIT-3D 到 7 个独立 Prefab）

### 6.1 已规划拆分（2026-09-11 草案 v001）

| 旧枚举值 | 新 AssetID | 新 Prefab | 新 class_name | 优先级 |
|---|---|---|---|---|
| `muzzle` | VFX-MUZZLE-FLASH-3D | `vfx_muzzle_flash_root_top3d.tscn` | VfxMuzzleFlash3D | P0 |
| `impact` | VFX-IMPACT-3D | `vfx_impact_root_top3d.tscn` | VfxImpact3D | P0 |
| `explosion` | VFX-EXPLOSION-3D | `vfx_explosion_root_top3d.tscn` | VfxExplosion3D | P0 |
| `slash` | VFX-MELEE-SLASH-3D | `vfx_melee_slash_root_top3d.tscn` | VfxMeleeSlash3D | P0 |
| `melee_impact` | VFX-MELEE-IMPACT-3D | `vfx_melee_impact_root_top3d.tscn` | VfxMeleeImpact3D | P0 |
| `damage` | VFX-DAMAGE-NUMBER-3D | `vfx_damage_number_root_top3d.tscn` | VfxDamageNumber3D | P1 |
| `heal` | VFX-HEAL-NUMBER-3D | `vfx_heal_number_root_top3d.tscn` | VfxHealNumber3D | P1 |
| `shell_casing` | VFX-SHELL-CASING-3D | `vfx_shell_casing_root_top3d.tscn` | VfxShellCasing3D | P1 |

### 6.2 迁移步骤

1. ✅ **Step 1（已完成 2026-09-11）**：账本登记 7 个新 AssetID，标记 VFX-COMBAT-KIT-3D 为"已拆分"。
2. ⏳ **Step 2**：创建 `src/vfx/VfxEffectBase3D.gd` 基类。
3. ⏳ **Step 3**：依次创建 7 个子类脚本 + 7 个 Prefab 文件。
4. ⏳ **Step 4**：新建 `src/combat3d/VfxPool3D.gd` 替换 `CombatEffectPool3D.gd` 的 API。
5. ⏳ **Step 5**：迁移 5 个调用点（PlayerMeleeCombat3D / Enemy3D / WeaponModel3D / Projectile3D / CombatEffectPool3D 自身）从字符串枚举改 AssetID。
6. ⏳ **Step 6**：删除 `src/combat3d/CombatEffect3D.gd`，`VFX-COMBAT-KIT-3D` Prefab 转为"已弃用占位"。

---

## 7. 验收与测试

### 7.1 每个特效 Prefab 必须通过的 4 项验收

| 项 | 验证命令 | 通过条件 |
|---|---|---|
| Prefab 可独立加载 | `verify_vfx_prefab_loadable.tscn` | `preload()` 不报错，根节点 class_name 正确 |
| Prefab 可独立实例化 | `verify_vfx_prefab_instantiate.tscn` | `instantiate()` 后 `_ready()` 不报错 |
| 池化回收正确 | `verify_vfx_pool_lifecycle.tscn` | `acquire` → 到期信号回收 → 同实例复用 → 场景清理无池引用 |
| 视觉与设计文档一致 | `verify_vfx_visual_match.tscn` | Forward+ 截图与 §2/§3 文档描述一致 |

### 7.2 现有 verify 流程兼容性

- `verify_3d_melee_combat_flow.tscn` ✅ 继续保留，验证完整 7 段近战反馈链路
- `verify_3d_melee_feedback_flow.tscn` ✅ 继续保留
- `verify_3d_melee_combat_visual.tscn` ✅ 继续保留

---

## 8. 反模式（明确禁止）

| 反模式 | 为什么禁止 | 替代方案 |
|---|---|---|
| 一个总脚本 + `match effect_kind` 分支多个特效 | 违反"Prefab 是唯一出口"原则 | 拆为多个 Prefab |
| 把特效逻辑写在战斗脚本里（如 `WeaponModel3D.gd` 内联） | 特效无法独立调参与替换 | 抽到独立 .gd 文件 + Prefab 包装 |
| 不登记 AssetID 直接用路径 `preload("res://.../some.tscn")` | 账本失同步 | 先登记 AssetID，再从账本查路径 |
| 调用方手动 `queue_free()` 特效 | 破坏池化复用 | 用 `pool.retire(asset_id, effect)` |
| 特效 Prefab 内嵌碰撞 | 视觉特效不应参与物理 | 碰撞留在玩法层（Enemy3D / Projectile3D） |
| 用 `match kind` 的字符串拼写来路由特效 | 拼写错误无编译保护 | 用 `VfxPool3D.VFX_MUZZLE_FLASH` 常量 |

---

## 9. 与其他规范的关系

- **命名约定**：本规范是 `10_资产与内容规范.md` 第 27-36 行命名表的**特效专项细化**。AssetID、Prefab 文件名、根节点 class_name 三者必须严格对应。
- **碰撞规则**：本规范禁止特效 Prefab 嵌碰撞。详细碰撞归属见 `13_技术施工_性能优化与热管理.md`。
- **音频绑定**：音效绑定走 `AUD-SFX-XXX` 路径，不在特效 Prefab 内嵌 AudioStreamPlayer。详见 `14.8_音乐系统与配乐资产.md`。
- **验收流程**：特效验收流程是 `11_测试与发布.md` 中"专项 verify 流程"的一个特例。
- **执行流程（skill 化）**：本规范的落地步骤、卡通风配方、跟随/池化契约、验收骨架与账本回写清单已固化为 skill **`vfx-combat-effect-authoring`**（2026-09-21 起接管 `assets/registry/ledger_index.json` 中 `vfx` 域的 `primary_skill`，旧值 `godot-model-asset-import-standard` 降为 `supporting_skills`）。本规范是"标准"，该 skill 是"怎么做"。

---

## 10. 版本历史

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-11 | v001（草案） | 起草：拆分原则、命名规范、Prefab 结构标准、注册流程、迁移计划 |
| 2026-09-21 | v002 | 三类战斗表现按本规范重做：枪口火花、命中爆点升 Prefab v002（改走 `VfxPool3D` AssetID 路由、灯光真源归位、卡通渲染统一为 UNSHADED+ALPHA+`depth_draw_always`）；新增**飞行子弹纯视觉** Prefab `VFX-BULLET-VISUAL-3D` v001（刻意不进 `VfxPool3D` 注册表，生命周期归弹体池）；新增共享几何工具 `src/vfx/ToonVfxGeometry.gd`；新增专项验收 `verify_combat_vfx_toon_v002`（含反向对照与防假绿哨兵）；§1.3/§2.1/§4.2/§5.1/§6.1 的 Prefab 路径按去版本化口径改为 `*_root_top3d.tscn` |
| 2026-09-21 | v002.1 | 修补（业主实机缺陷）：新增 `context.follow` / `follow_local_offset` **挂点跟随**契约（§3.1 基类实现 + §3.3 通用键与三条硬规则），修复枪口花火「角色移动时残留在开火瞬间的世界坐标」；跟随目标定为 `muzzle` 挂点而非武器根（`_visual_root` 被后坐力/换弹逐帧位移）；调用方枪口尺寸基准定档 **0.8**（视觉体积 80%）；专项验收补端到端接线探针（真装配 + 真开火）与两轮反向对照 |
| 2026-09-22 | v002.2 | 新增 `VFX-SHELL-CASING-3D` / `VfxShellCasing3D`：独立弹壳 Prefab 与 AssetID 注册；`WeaponModel3D._fire_now()` 每次成功开火生成 1 枚，按枪械右侧初速度 `v = 2.1·right + 1.55·up + 0.22·forward` 抛出，受 `g = 9.8m/s²` 下落，地板命中最多 2 次小弹跳后静止，3.2s 回收；Prefab 无碰撞体，补专项验收与反向对照 |
| 2026-09-22 | v002.3 | 弹壳 `VFX-SHELL-CASING-3D` 材质定档（业主指定）：**金属度 metallic=0.8、反光度（roughness 通道）=0.6**，壳体/底缘/底火三件统一，原先三件各自分散的数值收敛为单一常量组 `VfxShellCasing3D.SHELL_METALLIC` / `SHELL_ROUGHNESS`，各件仅保留基色差异；`get_presentation_snapshot()` 增补 PBR 真值（读已挂载材质）；专项验收新增 PBR 断言（期望值硬编码在验收侧，防自印证）并附反向对照 |
| 2026-09-22 | v002.4 | 弹壳 `VFX-SHELL-CASING-3D` 改为**纯程序化模拟碰撞**（业主指定：不要真实物理碰撞）：**移除地板物理射线**（原「layer 1 射线优先 + `context.floor_y` 兜底」口径作废，v002.2 该句不再适用），落地判定改为**解析式越线**——弹壳中心 y 越过「`floor_y` + 贴地半径」即视为碰撞，地面高度**唯一真源**为 `context.floor_y`；运动改为三阶段 FSM `FLYING → ROLLING → SETTLED`（抛物弹跳 → 贴地指数衰减滚动 → smoothstep 过渡躺平），手感常量收敛为 `BOUNCE_RESTITUTION/GROUND_FRICTION/SPIN_LOSS/IMPACT_TUMBLE_SPEED/REST_SPEED/ROLL_STOP_SPEED/ROLL_DAMPING/SQUASH_DEPTH/SQUASH_DURATION/SETTLE_TWEEN` 一组；**尺寸缩到原基准 80%**（`WeaponModel3D.SHELL_CASING_SIZE := 0.8`）；贴地半径由「壳体 0.045」修正为**三件最大径向半展 0.058**（底缘 outer_radius）并支持 `_measure_radial_half_extent()` 运行时实测；缩放改为落**三件子节点**并同步缩放其偏移（根节点保持纯旋转，规避非正交 basis 报错）。专项验收新增：**源码级物理 API 静态门禁**（`SHELL_FORBIDDEN_PHYSICS_SYMBOLS`，行为层无法区分射线命中与解析越线）、逐帧最低点防穿地、触地次数、装配随尺寸缩放、根节点恒单位缩放断言 |
| 2026-09-22 | v002.5 | 修复弹壳实机缺陷「**弹壳特效没了**」（P0）并订正 `floor_y` 契约：原调用方把 `floor_y` 硬编码为 `0.0`，而塔楼楼层**向下**建造（`stage.position.y = -FLOOR_HEIGHT_M(12.0) × floor_index`，98F ≈ **−1176 m**）⇒ 弹壳出生点即「低于地面」、**第一帧判定触地**并被夹到 `floor_y + 半径 ≈ 0.046` ⇒ 瞬移到世界原点附近、掉出玩家视野。约定：**`context.floor_y` 必须由调用方按真实世界给出，禁止以 0 想当然**；本项目口径 = 取**射击者站立面的世界 y**（玩家胶囊底面恰在 `y=0`，原点即脚底行走面），调用方实现见 `WeaponModel3D._resolve_shell_floor_y(shooter)`（射击者失效时回落枪身 y，不以 0 兜底）；`_spawn_shell_casing(world)` 因此改为 `_spawn_shell_casing(shooter: Node3D)`。专项验收补两条正面钉子：弹壳必须**出生在枪械抛壳挂点**上、**非零楼层端到端**（枪与射击者同搬 98F 开火后仍留该层、精确贴该层地面、绝不出现于世界原点附近）；真渲染探针新增**深层楼机位** |
| 2026-09-22 | v002.6 | 弹壳抛壳**逐发随机化**（业主指定：「方向、高度、初始旋转位置、落地后的范围、旋转等数值都需要做一个随机。不然太整齐了」）。**职责归属**：随机数只许出现在**调用方** `WeaponModel3D._spawn_shell_casing`；`VfxShellCasing3D` 保持**确定性**（对同一 `context` 逐位可复现，脚本内不出现 `randf()`）—— 它被逐帧断言驱动，埋入随机会让验收与视觉探针全部漂红。**施加口径**（按**枪械局部基** `right/up/forward`，与枪朝向无关；基准沿用 v002.2 的固定值，只把「点」摊成「一团」）：右向速度 `2.1 × r(1−0.35, 1+0.35)`、抬升速度 `1.55 × r(1−0.45, 1+0.45)`、前送 `0.30 × r(−1, 1)`（绝对值抖动，可为负 ⇒ 也会落到枪身后方）、自旋轴 `(right + up × r(0.1,0.9) + forward × r(−0.4,0.4)).normalized()`、自旋速度 `20 × r(1−0.5, 1+0.5)`、初始姿态绕**随机单位轴**转 `r(−1,1) × 45°`。**推算散布**：水平初速 `∈[1.37, 2.84] m/s` ⇒ 落点离枪 `0.9 ~ 2.5 m`、散布带 ≈ `1.6 m`。**硬约束**：右向抖动上限 `0.523`（验收断言每发右向速度 `> 1.0`，即 `2.1×(1−s) > 1.0`），取 `0.35`；**出生点仍精确取抛壳挂点**（容差 `0.001 m`），扰动只作用于抛出之后的运动量。**契约扩展**：新增可选 `context.initial_basis`（缺省恒等，写入前 `orthonormalized()`）承接随机初始倾斜；`get_presentation_snapshot()` 补 `spin_axis` / `spin_speed` / `spawn_basis` 真值。**验收**：`verify_combat_vfx_toon_v002` 新增连打 24 发的统计用例（各分量极差 + 初始倾角峰值 + 常量钉死 + 显式 context 逐位确定的**反向对照**），`samples=14 → 15`；真渲染探针新增第 4 机位 `shell_casing_spread.png`（连发 8 发走真实调用方路径、复刻后坐摆头，判据 = 落点离最远两点连线的最大垂直偏差 `> 0.10 m`；前后 A/B 图 `shell_casing_spread_ab.png`，未随机化时实测仅 `0.0114 m`）|
| 2026-09-22 | v002.7 | 弹壳**停留时长 +1.5 秒**（业主指定：「弹壳的停留时长加 1.5 秒」）。**口径**：弹壳可见时长即 `VfxShellCasing3D.DEFAULT_LIFETIME`；飞行 / 弹跳 / 滚动三段时长由物理常量与初速度决定、**不随寿命变化** ⇒ 寿命增加部分**全部落在落地静止后的停留**上，故诉求等价于 `DEFAULT_LIFETIME: 3.2 → 4.7`。实测（验收按 `1/60 s` 步进求静止时刻）`t_settle = 1.15 s`、停留 `4.70 − 1.15 = 3.55 s`（v002.2 起为 `2.05 s`）。**验收**：`verify_combat_vfx_toon_v002` 新增 `_check_shell_settled_hold()`（`samples=15 → 16`），三条互补断言（寿命定档值 / 增量 = 上一版 + 1.5 / 实测静止后停留 ≥ 1.5 s），期望值硬编码为外部真源且**刻意不写成派生式**（防自我印证）。**反向对照 RC8**：寿命退回 3.2 ⇒ 精准 3 红后还原复绿；其中「停留 ≥ 1.5 s」在退回时仍成立（`2.05 s`），它是**下限守卫**，「+1.5」由常量与增量两条钉住。**成本核算**：`VfxPool3D` 对 active 无上限（`max_per_kind` 只管 inactive 桶）⇒ 射速 `1.0 ~ 12.0 发/s` × `4.7 s` 最坏同屏约 `56` 枚（上一版约 `38` 枚），每枚 3 件 / 288 三角面 ⇒ 约 `16k` 三角面、约 `170` 次绘制，未做池化改动。AssetID / Prefab / 版本 `v001` / PBR / 尺寸 / 随机化口径 / `floor_y` 契约均未动，账本无需改动 |
| 2026-09-22 | v002.8 | 弹壳**停留时长再缩短 1 秒**（业主指定：「弹壳的停留时长（再）缩短 1 秒」）—— 在 v002.7「+1.5 秒」基础上回调。**口径**同 v002.7：可见时长即 `DEFAULT_LIFETIME`，三段飞行不随寿命变化 ⇒ 诉求等价于 `4.7 → 3.7`（累计 `3.2 →（+1.5）4.7 →（−1.0）3.7`）。实测 `t_settle = 1.15 s`、停留 `2.55 s`（v002.7 为 `3.55 s`）。**验收结构修正**：v002.7 把行为断言「停留 ≥ 1.5 s」写在「增量」常量上（数值巧合、语义错位），本轮拆为独立常量 `EXPECTED_SHELL_HOLD_FLOOR := 1.5`，并在注释里写明四条断言各自守什么：① 定档值 ② 本轮缩短量 `4.7 − lifetime == 1.0`（换方向时减号语义要同步翻）③ 下限守卫 ④ 静止时刻哨兵。**反向对照两组**：RC9 寿命退回 4.7 ⇒ 精准 3 红（其余全绿）；**RC9b** 把 `ROLL_DAMPING: 3.4 → 0.2` 让飞行段吃满寿命 ⇒ ③ 精确命中 `停留只有 1.23s（观感下限 1.5s）` —— 只跑 RC9 时 ③ 仍绿，**必须靠 RC9b 才能证明这条守卫是活的**。**成本**：`12.0 发/s × 3.7 s ≈ 44` 枚同屏（v002.7 约 56 枚），约 `13k` 三角面 / 约 `130` 次绘制。AssetID / Prefab / 版本 `v001` / PBR / 尺寸 / 随机化口径 / `floor_y` 契约均未动，账本无需改动 |
---

*待主人拍板：是否同意本规范作为特效体系的硬约束？同意后我会按 §6 迁移计划开始拆 7 个独立 Prefab。*


---

## 附录 A：3D-特效 sheet 体检快照（2026-09-11）

历史内容已迁至[独立开发记录](development/history/14.6_特效系统与制作规范_历史记录.md#record-1)；其中数值与通过记录仅代表原记录时点。当前契约以本文件其他规范章节及[功能契约索引](MODULE_INDEX.md)为准。

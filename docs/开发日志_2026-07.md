# 开发日志：2026-07

## PH15 核心闭环与视觉可读性重构（2026-07-10）

### 解决的问题

- Boss 房此前没有实体碰撞体，且子弹给 Boss 传入三参命中数据时接口不兼容；实战子弹无法击败 Boss。
- Boss 实体扣血、地图进度、奖励和撤离解锁之前没有同一条可靠的运行时桥接。
- 局内生命同时存在 `GameManager` 与 `Player` 两套来源，导致濒死祝福无法根据真实受伤触发。
- 纯黑 `CanvasModulate` 吞没了低照度环境中的地面和轮廓；玩家组件仍在展示 emoji/色块占位资产。

### 本次交付

- 将 Boss 的碰撞、伤害、死亡和 Boss 撤离收敛为可回归测试的闭环；新增 `verify_boss_completion_flow.tscn`。
- `VisionSystem` 与命运卡改为 `RefCounted` 数据/计算服务；武器装配树在换枪和结束局时递归释放未进入 SceneTree 的数据节点。
- 新增独立的程序化 `PlayerAvatarRenderer` 与 `BossAvatarRenderer`；它们是语义化的 `AvatarRenderer` 挂点，可直接被正式精灵或动画替换。
- HUD 改为统一的深色指挥台表现：生命数值、弹匣、机动、火力、战绩和深度的信息层级更清晰，且在迷雾上方渲染。
- 降低环境暗角至蓝灰基调，修复 Boss 击败闪屏非法层级。

### 验证

- Godot 无头编辑器扫描通过。
- 21 个场景流程验证与命运卡池验证通过。
- 新增 Boss 回归确认“玩家子弹 → BossActor → BossRoomDirector → BOSS_KILL 撤离”完整通过。

## PH16 大地图末日氛围垂直切片（2026-07-11）

### 设计判断

启动后的固定大地图是玩家对成品质感的第一判断。原地图已有连续基地—荒野结构，但大块纯色地面和矩形设施让功能感强于世界感，因此本轮先建立可替换的末日氛围层，再进入训练靶场和怪物表现循环。

### 交付

- 新增 `src/base/BaseWorldAtmosphere.gd`：固定种子道路破损、废墟、残骸、路障、枯树、分区色彩、13 盏故障灯、屏幕尘雾和暗角。
- 新增 `BaseFacilityRenderer.gd` 与 `DungeonEntranceRenderer.gd`，将基地设施和副本入口包装为破败装甲掩体/地堡，并保留单一可替换渲染节点。
- 出生广场加入维护标记、裂纹、线缆与电力箱；基地玩家外观只在表现层放大，不改变碰撞。
- 基地 HUD 提升到 CanvasLayer 20，始终位于氛围覆盖层之上。

### 验证

- `verify_base_world_flow.tscn`：通过，节点数 183/220。
- 全部 21 个场景验证与命运卡池验证：通过且无错误日志。
- Godot 实机 1280×720 检查：氛围色、稀疏照明、设施轮廓和 HUD 可读性符合本轮方向。

### 下一轮

建立独立训练靶场：基地入口、装备隔离、全枪身/弹药选择、组合开关、可重置靶标与实时伤害统计，退出时不写入任何局外物品。

## PH17 独立训练靶场（2026-07-11）

### 设计判断

靶场不只是额外菜单，而是后续枪械手感、怪物反应、角色状态和 HUD 调试的成品基础设施。因此本轮同时建立“全蓝图可测”与“局外零污染”两条硬契约。

### 交付

- 基地新增“地下训练靶场”设施与独立场景；进场空装，离场前销毁临时装配。
- 动态装配终端覆盖 7 种枪身、8 种弹药和 56 组矩阵，支持手动/自动组合开关、清空、重置与消毒离场。
- 新增标准、装甲、移动三类靶标，包含伤害飘字、命中反应、减伤结算、命中率和会话 DPS。
- `WeaponAssemblyTree` 清空契约同步重置弹药/射击参数，`WeaponDisplay` 对换枪与空装立即刷新。
- 以破损工业射击廊、低能冷青灯、远端红色警示、消毒门与深蓝黑终端建立可替换的专业视觉基线。

### 验证

- `verify_training_range_flow.tscn`：56 组枪弹矩阵、三类靶标、数据统计、空装退出与 `BaseData` 快照不变全部通过。
- `verify_base_world_flow.tscn`：基地 8 设施、训练入口路由与 191/220 节点预算通过。
- Godot 4.6.2 全工程扫描、主场景启动与 22 个场景验证无报错。
- 实机 1280×720 检查：空装、选中、装配、灯光和侧栏信息层级可读。

### 下一轮

建立基础怪物的体型、轮廓、运动节奏、攻击蓄力和受击反应差异，把“怪物类型”从数值标签提升为玩家无需文字即可识别的战斗语言。

## PH18 怪物视觉生态与行动语言（2026-07-11）

### 设计判断

六类基础怪已有数值与 AI 分型，但房间生成后常回退成通用三角形+emoji，导致玩家无法通过轮廓和动作做出战术判断。本轮把体型、碰撞、动作频率与攻击前摇建立为同一套可替换战斗语言。

### 交付

- `EnemyShape` 新增六类战术 Profile 与中央类型映射。
- `EnemyAvatarRenderer` 提供六套代码原生轮廓、步态、受击、状态染色、预警与死亡反馈；旧 Shape/Emoji 退出正式渲染。
- 体型和碰撞圈同源更新，并修正巨大化词缀的同名 API 误调。
- Ranged 射击、Bomber 自爆和 Trapper 破土分别获得 0.28 / 0.62 / 0.38 秒的可读前摇；Trapper 工厂零速度错误已修复。
- 新增 `EnemyEcologyGallery.tscn` 作为六类视觉与未来资产替换对照板。

### 验证

- `verify_enemy_ecology_flow.tscn` 验证六类唯一轮廓、体型/碰撞序列、受击、巨大化与三类前摇。
- 1280×720 离屏渲染对照板通过视觉检查；六类轮廓和尺度不重复。
- `Main.tscn` 360 帧运行、4 个高风险闭环与全部 23 个场景回归通过。

### 下一轮

完成玩家状态表现与 UI 同步：移动、冲刺、受伤、低血、死亡和交互锁定都由明确的状态/信号触发视觉反馈。

## PH19 玩家状态表现与 UI 成品化（2026-07-11）

### 设计判断

角色状态只有后台逻辑、HUD 只有数值变化时，玩家无法建立“我正在经历什么”的瞬时判断。本轮把互斥状态、叠加异常、角色轮廓和 HUD 遥测收敛到同一条信号链，同时修正旧 UI 描边副本造成的真实布局污染。

### 交付

- 玩家状态机完成 `idle / moving / dashing / hurt / locked / dead` 六态与 Low HP、Silenced 叠加状态。
- 修复 Idle/Hurt/Dashing 提前解除无敌的问题；冲刺计时不再使用会持有状态脚本的匿名定时器闭包。
- 程序化玩家表现加入步态、推进、受创压缩、危急脉冲、干扰线、锁定环和死亡倾倒。
- 战斗 HUD 新增 `PlayerStatePanel`，状态切换、受伤、低血和沉默均有可读文本、色彩和短动效。
- 清除旧描边副本参与容器布局的问题，修复右上楼层读数与生命条重叠；按钮 Tween 和抖动时长契约同步修正。
- 新增 `PlayerPresentationGallery.tscn` 和玩家表现专项验证场景。

### 验证

- 玩家专项验证连续运行通过且无退出泄漏、警告或错误。
- 1280×720 六态视觉对照板与真实 `Main.tscn` HUD 均完成离屏画面检查。
- 下一步进入最终需求逐项审计与全量回归，不以局部绿色测试代替成品验收。

## PH20 最终成品验收与生命周期收口（2026-07-11）

### 解决的问题

- 严格退出日志发现若在动画、刷怪或陷阱计时中切场景，`SceneTreeTimer` 与 `await tween.finished` 可能短暂持有协程资源。
- Boss 阶段 Director 在 Idle 也会自主处理，并可能在激活首帧同时触发多个定时技能。
- 相邻增援批次共享可变敌人池，快速触发时存在后批配置覆盖前批队列的风险。

### 收口

- 波次、增援和陷阱均改为场景节点自有 Timer；增援数据在入队时冻结。
- Boss Director 只在 Combat 运行，初始技能错峰并加入全局技能间隔；Dead/Idle 均停止处理。
- 白闪、飘字、Boss 胜利动画与演示转场改为 Tween 回调链，场景销毁不再悬挂协程。
- 建立 `VerificationClock` 与 `VerificationQuitter`，让专项测试延迟由节点拥有，并在异步入口完全退栈后退出。

### 最终证据

- Godot 全工程扫描干净。
- 24/24 验证场景严格无警告、无错误。
- 5/5 核心场景持续 600 帧无警告、无错误。
- 基地、靶场、战斗 HUD、怪物生态板与玩家六态板完成 1280×720 当前版本复核。

## PH17B 靶场实体陈列与数据驱动扩展（2026-07-16）

### 设计判断

训练靶场已有完整枪弹矩阵和隔离测试契约，但全枪械只显示为终端按钮，空间本身没有“武器实验室”的可感知选择过程。本轮把目录数据转成场景内实体陈列，同时保留终端作为精确组合工具。

### 交付

- 靶场扩建为 2600×1500 的独立实验室，西侧为全枪械陈列与弹药实验墙，东侧为四条实弹测试道。
- 新增数据驱动 `TrainingRackItem`：自动读取蓝图 ID、名称、Tier、标签与枪身轮廓，生成有唯一交互区的整齐四列货架。
- 靠近实体陈列位按 `E` 即可取用；枪身与弹药互相沿用当前选择，缺省时自动补齐另一半组合。
- 终端与实体货架共享唯一选择状态；`Tab` 可收起终端，方便观察远端靶标。
- 专项验证增加实体枪架/弹药墙数量、ID 唯一、位置不重叠和实体取用装配正确性检查。

### 验证

- 靶场专项验证通过：15 个实体陈列位、56 组枪弹组合、靶标行为、会话统计和局外隔离全部成立。
- 23/23 场景回归通过；主场景持续 300 帧无错误。
- 1280×720 实机帧检查确认全枪械与全弹药在出生视野中整齐可读。

## PH23–PH24｜2D 玩法等价迁移与真实视野/输入收口（2026-07-22）

### 核心修复

- 3D 房间恢复四档尺度、钥匙实体门、清房钥匙、命运开门、12+2 背包保险、地面掉落、完整枪械树、六基础怪/精英/Boss 与五类撤离契约。
- 修复枪身切换清空已装配模块、模块替换吞旧件、满包交换非原子、蓝图碎片误装备和满血误耗治疗剂。
- `PlayerVision3D` 改为独立玩法服务：96°/21m 前锥与 2.4m 近身区使用射线遮挡，同批采样生成地面可视区域；删除玩家视野灯依赖。
- 背包接入 I/Tab 真实输入并铺满视口；统一地下城模态输入锁。地下城、基地、靶场复用可见暂停层，Esc 先关模态再暂停。
- 射弹/战斗特效对象池、房间流送和灯光预算保留；程序合成音效增加缓存，避免高频射击反复分配 WAV。

### 验收

- 3D 专项覆盖等价核心、完整四主题流程、真实视野/输入、枪械背包原子交换和性能预算。
- 2D 回归覆盖地图、命运门、房间灯光、怪物生态、Boss、撤离、背包 UI、玩家状态机、靶场和视野系统。
- 实际项目渲染确认房间灯光、真实视野轮廓、小地图、低血暗角与 HUD 信息层级可读。

## PH25｜3D 暗室照明、枪械组合与怪物生态收口（2026-07-22）

### 核心修复

- 视野地面层改为无硬描边的柔边射线场，遮挡距离向内立即收紧、向外平滑恢复；朝向本地旋转，稳定状态不重复重建网格，并使用深度豁免避免沉入地板。
- 玩家增加一盏只负责阴影与明暗的前向探照灯；单位显隐仍只读取 `PlayerVision3D`，真实光照与玩法视野彻底解耦。
- 所有随机房间改为默认暗室：只生成一盏正中央顶灯与一个墙面 `E` 键开关，移除室内路灯和假地面光池。
- 开局区域放置可见的不同枪身拾取；修正弹丸以本地 `-Z` 为前向、`+Z` 为尾迹，补齐蓄力、炮塔、追踪、回归、枪挂枪、弹携枪、连锁、复制、过热等 3D 武器树适配。
- 七类怪物使用与模型轮廓同源的圆柱碰撞体，并恢复追猎突进、远程三连、召唤治疗、盾牌朝向格挡、自爆碎片、伏击潜伏和 Boss 阶段行为；掉落继续读取原 `LootModule`。
- 资产台账升级为 v1.6，新增唯一公共房灯开关根；把玩法、状态机与性能专项列为资产验收门槛。

### 验收

- `verify_3d_vision_input_flow.tscn`、`verify_full_3d_game_flow.tscn`、`verify_3d_fate_weapon_flow.tscn`、`verify_3d_enemy_behavior_flow.tscn`、`verify_3d_performance_budget.tscn` 全部通过。
- 代表性性能：全探索约 2812 节点，持续运行约 6.90ms，稳定视野网格重绘 0 次；未出现脚本错误。
- 实际 GPU 暗室/开灯双预览确认中央真实照明、探照灯阴影、柔边视野与家具轮廓符合废土题材。

## PH26｜真实探照灯、实际装备点击与 3D 背包投影（2026-07-22）

### 核心修复

- 将玩家照明从视野节点中拆出为可独立调参的 `PlayerFlashlight3D`：一盏投影聚光灯照亮前方场景，一盏无阴影近身补光保证角色和手持枪可读；玩法视野继续完全独立。
- 提高暗室环境基线、曝光和中央顶灯能量/覆盖范围，大房间开灯后不再只亮中央小块区域。
- 修复背包格子点击信号的重复参数绑定；拾取枪械后可通过实际左键槽位完成换枪、保留模块、旧枪回包和3D枪模同步。
- 新增世界/背包共用的 `ItemModelFactory3D`，背包用固定 96×96、只刷新一次的3D视口投射枪械、弹药、治疗物、钥匙、信标等道具。
- 资产 Skill 增加真实双灯结构、世界模型投影复用、单次渲染和真实槽位点击验收规则；资产台账升级至 v1.7。

### 验收

- 真实灯光、背包装备、四主题完整流程、玩法等价、性能预算和旧背包 UI 回归全部通过；完整流程峰值 674 节点。
- 性能专项：初始生成约 458ms，新房构建峰值约 19ms，持续帧段约 6.91ms，稳定视野重绘 0 次。
- GPU 1280×720 暗室、开灯和3D背包投影三张预览完成视觉复核。

## PH27｜360°逻辑视野与定向表现解耦（2026-07-22）

### 设计与实现

- 顶视角玩法中的单位显隐改为角色周围 360°、最远 21m：敌人、掉落、钥匙、危险区、服务装置和撤离信标只会被墙、关闭门及实体家具遮挡，不再因为位于角色身后而消失。
- 保留原 96°/21m 柔边扇形、2.4m 近身圈、49+28 固定射线采样、墙体裁切、无硬描边和地板深度豁免；它们现在只表达主要目光、准星和探照灯方向。
- `PlayerVision3D` 快照分别报告逻辑角度与表现角度，灯光、曝光和材质亮度继续完全不参与目标显隐。
- `shellstorm-art-assets` Skill、资产制作规范和公共视野资产记录同步采用“360°逻辑显隐 + 96°定向表现”契约。

### 验收

- 视野专项新增真实地面掉落回归：身后无遮挡时显示，身后隔实体墙时隐藏；正面遮挡、强灯不能透墙、柔边扇形与真实双灯结构继续通过。

## PH28｜3D 战斗生命、AI 与反馈闭环（2026-07-22）

### 核心修复

- 修正 Enemy3D 视线射线把玩家碰撞体误判为墙的问题；无遮挡敌人现在可靠从 idle/patrol 进入 alert/chase，实体墙仍能阻挡视线并触发搜索。
- 怪物生命与伤害改为“3D 类型基线 × 旧 2D 楼层/主题/精英倍率”，不再用旧 10–40 HP 直接覆盖；开局手枪普通 26 伤害不能秒杀满血基础怪。
- 玩家六态白名单与死亡终态保持不变；moving 新增位移驱动的独立组件循环，恢复 2D 版身体弹性、头部滞后和围巾动态，手与武器挂点继续固定同相。
- 玩家真实灯光升级为三层隔离灯组：环境主聚光、环境近身溢光和角色正面柔光；头顶溢光不再打到角色，只有环境主聚光投影。
- 房间中央顶灯能量提高约 22%；普通掉落和钥匙增加 0.32 秒上弹旋转回收动画。
- 钥匙改为在玩家附近的安全边界内生成；已清房返回时会补发遗漏奖励，同房只发一把，避免大房间看不到钥匙造成软锁。

### 验收

- 新增 `verify_3d_combat_progression_flow.tscn`，覆盖中央灯、钥匙近距生成/查重/补发和两类拾取动画。
- `verify_3d_enemy_behavior_flow.tscn`、`verify_base_world_flow.tscn`、`verify_3d_vision_input_flow.tscn` 已覆盖 AI/HP、六态移动动画与三层真实灯光。

## PH29｜3D 换弹覆盖状态与头顶进度反馈（2026-07-22）

### 核心实现

- 保留玩家六个互斥顶层状态，将 `reloading` 接入为可与 moving/hurt 并存的覆盖状态；状态快照直接投影武器真实装填生命周期。
- `WeaponModel3D` 新增开始、连续进度、正常完成/取消信号；换枪、补弹、清空和死亡会同步清理旧装填，避免延迟补满或 UI 残留。
- 唯一握持手与 `weapon_socket` 使用相同局部下压、操作、回位变换，枪模增加轻微局部俯仰/侧滚；全部 56 组枪弹组合复用同一动作。
- 在胶囊角色根增加 `ReloadProgress3D`：相机 billboard、固定世界尺寸、左端锚定、无光照双 QuadMesh，直接显示真实装填进度且不随准星根旋转。
- 资产 Skill、组件/状态契约和台账同步更新，不新增 AssetID。

### 验收

- `verify_3d_reload_state_flow.tscn` 通过：六态未扩张、moving 并存、换弹不能射击、手/枪挂点不分离、0→1 进度、头顶条、完成/补弹/换枪三条退出路径均正常。
- 基地、命运换弹、实际背包装备、3D玩法等价、四主题完整流程、靶场与性能预算全部回归通过；完整流程峰值 689 节点，性能持续段约 6.91ms。
- Apple M1 OpenGL 1280×720 实机画面确认头顶条在暗室与探照灯环境下可读，约半程填充与真实计时一致。

## PH30｜3D 角色与遭遇预览场（2026-07-23）

### 核心实现

- 新增独立 `Player3DStateGallery.tscn`：按钮直接触发 `Player3D` 的 idle/moving/dashing/hurt/locked/dead 六态、真实 `WeaponModel3D` 换弹覆盖、低血/无敌/沉默叠加层，以及手枪/霰弹/步枪切换。
- 预览场复用正式 `Enemy3D` 根，可选六类怪物、Boss、AI 开关、九态片段、受击与 Boss 血量阶段；不是展示用替身。
- 将已有 2D `ThemedNPC` 的配置和交互契约迁移为 `ThemedNPC3D`，支持三种 NPC 配置切换、范围提示、`E` 与按钮对话。
- `shellstorm-art-assets` Skill 新增交互式 3D 角色验收规则：必须复用真实状态机和注册根资产，并把 Gallery 作为验收场而非新的美术 AssetID。

### 验收

- `verify_player3d_state_gallery_flow.tscn` 通过：六态数量、真实换弹覆盖、Enemy3D/Boss、NPC 配置/对话、死亡终态与重置均成立。
- Apple M1 OpenGL 1280×720 预览确认按钮面板、换弹进度、胶囊阿凡达、怪物与 NPC 同屏可读。
- 资产台账更新 `CHR-PLY-CAPSULE01-3D` 和“原型角色”页；未创建重复的角色或敌人资产根。

## PH31｜3D 射击、蓄力与击飞动作覆盖层（2026-07-23）

### 核心实现

- 玩家继续保持六个互斥顶层状态；新增 `firing / charging / knockback` 作为动作覆盖快照，而非把射击或击飞堆成新的互斥状态。
- 射击只订阅 `WeaponModel3D.shot_fired`，蓄力直接读取 `charge_active / charge_ratio`，释放蓄力仍走正式武器发射逻辑。
- 受击记录命中方向、强度与时长，`hurt` 状态消费递减击退冲量；阿凡达以基础位移、后仰、压缩和弹性回弹表达反馈。
- 唯一手部和 `weapon_socket` 对换弹、射击、蓄力、击飞叠加相同的位置/旋转，保持握持相对向量固定；换枪同步清掉旧枪冷却、后坐与蓄力。
- 3D 角色与遭遇预览场新增“射击 / 开始蓄力 / 释放蓄力 / 受击击飞”控制，全部驱动真实 Player3D/WeaponModel3D。

### 验收

- `godot --headless --path . --scene res://verify_player3d_state_gallery_flow.tscn` 通过：六态未膨胀、真实射击/蓄力/释放、击飞受创、模块阿凡达和手/枪挂点关系均成立。
- Apple M1 OpenGL 1280×720 `verify_player3d_state_gallery_visual.tscn` 通过：蓄力覆盖层、胶囊角色、怪物与 NPC 同屏可读。
- 资产台账与 `shellstorm-art-assets` Skill 同步写入动作覆盖层来源和验收规则；未增加重复资产根。

## PH32｜3D 角色 DIY 组件、握持修正与动态增强（2026-07-23）

### 核心实现

- `Player3D` 新增纯表现的 `set_avatar_customization()` 接口，支持 body/head/hand/hat/glasses 五个模块；六态、碰撞、枪械树和灯光不读取该配置。
- 3D 胶囊角色新增三组身体/头/手变体，以及软帽、安全帽、密封兜帽、单目镜、双目护镜、宽面护目镜等独立程序组件。
- 唯一手和枪械挂点整体内收并更靠近握把；全部动作层共享位移/旋转，移动、射击与击飞时相对向量不变。
- 动态表现增加躯干反摆、头壳回摆、围巾滞后和射击抓握压缩；帽子/眼镜直接继承头部动态。
- 预览场新增 DIY 面板、五个下拉装配器、帽子/眼镜快捷切换与恢复默认按钮。

### 验收

- `verify_player3d_diy_flow.tscn` 通过：五槽真实换装、可见头部配件、内收握持点、移动/射击中手与挂点不分离及默认恢复均成立。
- 既有状态机、换弹覆盖与 OpenGL 预览回归通过；角色在暗场中可读，DIY 组件没有新增每帧网格分配。

## PH33｜方形猫角色原型与四模块节点（2026-07-25）

### 核心实现

- 复用 `CHR-PLY-CAPSULE01-3D` 玩家根，将 3D 表现改为概念图导向的方形橙猫；头、身、唯一手、脚是四个主模块。
- 方头、方身、方手、方脚和矩形豆豆眼使用 Godot 原生 `BoxMesh`；双耳使用三角锥体；身体只保留一个小型圆短尾，不制作长尾或嘴巴。
- 新增 `Head/Eyes/EyeL|EyeR`、`Head/Ears/EarL|EarR`、`Body/TailStub`、`Feet/FootL|FootR` 节点契约；旧 `Scarf` 仅隐藏保留以兼容已有场景。
- DIY 增加 `feet` 槽和猫橙默认组合；moving 增加双脚交替抬起与短尾轻摆，单手和枪械挂点继续全动作同相。

### 验收

- `verify_player3d_diy_flow.tscn` 覆盖四主模块、双眼、双耳、双脚、圆短尾、脚步交替、DIY 恢复和手/枪挂点关系。
- `verify_player3d_state_gallery_flow.tscn` 与 OpenGL 预览通过；六态、射击、蓄力、击飞、换弹、怪物/Boss/NPC 控件保持有效。

## PH34｜向下爬楼主循环与五层楼原型（2026-07-29）

### 设计与实现

- 正式入口从连续基地—野外改为 `TowerDescent3D`：玩家出生在 50×50m 楼顶，经 30×30m 设施层连续下行五个 30×30m 战斗层，第五层整合 Boss 与最终撤离。
- 塔楼继承 `Dungeon3D`，继续复用钥匙门、门后命运卡、怪物、掉落、背包、武器树、Boss 和撤离管线；旧 `BaseWorld3D` 与四主题关卡保留为兼容回归。
- 七层采用同一 XZ 建筑投影与 6m 真实层高；六个楼梯间使用带实体墙的折返斜坡、台阶表现和连续高度校正。首个入口固定在楼顶左侧，后续下行侧由种子确定且避开本层上行门。
- `DungeonRoom3D` 增加楼顶护栏、30m 固定楼板、设施层右侧通道与战斗层隔间；设施沿墙布置，中部保留通路，安全层默认开灯、禁用射击且不刷怪。
- 流送拆分为渲染/模拟与承重：上层渲染隐藏、AI/灯光冻结，远层墙门交互碰撞关闭，但所有已构建 `FloorBody` 支撑碰撞永久保留，防止遗留对象因卸载地板坠落。
- 小地图增加垂直堆叠投影；角色六态移动增加贴地速度，塔楼在斜坡范围按 XZ 进度连续映射真实 Y，修复下坡悬空。
- 资产层继续复用 `ENV-DUNGEON-RUNTIME-3D` 与公共门/设施/家具/灯/敌人根；资产台账只更新复用与验收说明，没有新建重复 AssetID。

### 验收

- `verify_tower_descent_flow.tscn` 通过：7 层、6 楼梯、5 战斗层、7 设施、命运门、设施模态、4m米制门到门楼梯、坐标、上层隐藏、楼板承重、Boss/撤离与 1819 节点预算成立。
- 旧基地兼容、3D 等价核心、四主题完整流程、360° 物理遮挡、背包装备、玩家六态和性能专项全部回归通过。
- 独占性能：初始生成 117.4ms、单房构建峰值 6.7ms、全探索 2950 节点（旧最重主题）、持续段 6.90ms；塔楼全构建 1819 节点。
- Apple M1 OpenGL 1280×720 楼顶、设施层、折返楼梯间与第一战斗层实机截图通过。

### 截图反馈修正

- 增加 `TowerGeometry3D` 米制单一来源：门、楼道和楼梯统一 4m 净宽，门外平台 3m，第一跑 10m，两跑中心距 5.2m，楼层高度 6m。
- 折返梯由十段绕行重做为七节点标准 U 形模块，门框回到真实门平面，楼道、平台与两跑只在端点连接。
- 修复角色无法出门：旧楼道盒体中心误放在路径 Y，入口顶面比屋顶高 0.12m；现将路径 Y 定义为可行走顶面，楼道/坡道盒体沿自身表面法线下移半厚度。
- 修复楼道转入坡道被挡：0.9m矮护栏仅保留中段，两端各留2.4m转向净口；跨门后立即剖切屋顶/立面渲染，碰撞和承重不删除。
- 屋顶补充向下 6m 的连续建筑外立面、结构带与门洞；真实 OpenGL 屋顶边缘和低跑楼梯画面复核通过。
- 专项不再瞬移验收：测试角色必须从屋顶门内真实经过门、3m平台、第一跑、转向平台、第二跑、下层平台并进入设施层；当前塔楼完全构建为1819节点。

## 2026-07-29 PH35 塔楼 Blender 模块化重制评审稿

- 按用户新规格冻结下一版塔楼：层高由6m提高50%至9m；“面积增加5倍”按面积严格计算，楼顶调整为111.803×111.803m（12,500㎡），设施/战斗层调整为67.082×67.082m（4,500㎡）。
- 新楼梯净宽统一6m，并拆成三个独立Blender组件：6×6m上下共用门口走道、15×6m/下降4.5m固定斜坡、6×14m折角走道；双跑总成为27×14×9m。
- 新增 `ENV-TOWER-DESCENT-KIT-3D` Blender评审母版，包含楼顶、基地层、战斗层、三个标准楼梯组件和完整双跑总成，共9个规范集合、122个网格。
- Blender QA确认面积、层高、坡度、组件尺寸、接口空物体和已应用缩放全部正确；生成楼层总览、组件总览和楼梯剖面三张评审图。
- 代码复核定位光标问题：`Player3D` 仍把鼠标射线固定投影到世界 `Y=0`，所以玩家下楼后光标留在楼顶；上层闪现来自楼梯剖切依赖旧 `_current_room_id` 与房间触发切换之间的过渡帧。后续导入时将承重碰撞、瞄准查询、渲染可见性拆分，并在跨门平面时先锁定隐藏来源层。
- 当前仅保存 `.blend` 源文件并通过 `.gdignore` 隔离；未导出GLB、未修改Godot塔楼运行时，等待用户确认比例后再接入。

## 2026-07-29 PH35 Blender 三层堆叠 v002

- 复核用户截图发现当前 Blender 仍是旧 `v001` 横向组件评审板：Outliner 为 `00_SPEC_GUIDES / 01_MODULE_*`，并非三层堆叠场景；同名旧窗口后续保存会覆盖后台生成文件，造成评审截图与磁盘内容不一致。
- 保留旧 `env_tower_descent_kit_top3d_v001.blend` 作为历史评审板，新增独立 `env_tower_descent_kit_top3d_v002.blend`，彻底隔离版本冲突。
- `v002` 只保留楼板、外墙、室内隔墙、门和两段实际楼梯，共7个规范集合、215个网格、15扇门；不再放置独立组件展示区。
- 楼顶/基地/怪物层按 `Z=0/-9/-18m` 真实堆叠；西侧楼梯连接楼顶→基地，东侧楼梯连接基地→怪物层；两段楼梯上下门中心严格同轴。
- Blender QA通过：面积、层高、楼顶开口、6m净宽、七节点双跑路径、门位、应用缩放和集合命名成立。仍未导出GLB、未接入Godot，等待用户评审。

## 2026-07-30 PH35 用户楼梯细化与五倍边长地图 v004

- 将用户当前 Blender 内存现场先完整保存为 `env_tower_descent_kit_top3d_v003.blend`，后续全部调整从该快照派生，未重跑旧生成脚本覆盖手工楼梯。
- 识别两套楼梯：右侧为普通楼层复用的通用版；左侧为通用版旋转 180° 并连接楼顶的特殊版。特殊版保留用户手工设置的三面墙高度差和收口墙。
- 消除特殊版复制产生的统一漂移 `(-0.8029,-0.5218,+0.5984)m`，把上下平台顶面对齐 `Z=0/-9/-18m`；为两侧上层门口补齐短连接板，未改变用户梯跑、折角、踏步和加宽下层平台。
- Outliner 新增父集合 `02_STAIRWELLS`，下分 `02A_STAIR_SPECIAL_ROOFTOP_TO_FACILITY` 与 `02B_STAIR_GENERIC_ROTATABLE`；清理 `.001/.002/.003` 后缀，并将完整总成挂到各自 ROOT。通用版以后只需移动 ROOT 或绕 Z 轴按 90° 旋转。
- 最新“长宽扩大 5 倍”按边长执行：三层总占地统一为 `335.410×335.410m`，现有 67.082m 基地/战斗核心和 111.803m 楼顶建筑面不缩放；用四块外围空楼板补齐每层，核心仍位于地图中央。
- 基地和战斗核心增加低围栏，并在东西两套楼梯体积范围保留大开口；楼梯继续挂在核心边缘，不移动到扩大后地图外缘。
- Blender QA 通过：259 个对象、241 个网格、12 块外围补板、11 段核心围栏、两套楼梯集合、ROOT 父子关系、应用缩放、连接板接缝、特殊墙高差和 335.410m 总占地均成立。
- 本轮仍只交付 Blender 源资产与评审图，未导出 GLB、未修改 Godot 运行时，等待用户在 `v004` 中继续调整细节。

## 2026-07-30 PH35 整层外壳五倍边长修正 v005

- 用户复核指出 `v004` 只把楼板补到 `335.410×335.410m`，楼顶女儿墙仍停在 `±55.902m`、基地/战斗满高外墙仍停在 `±33.541m`，视觉上仍是“小核心放在大地板上”。
- 先更新 PH35 规范，把“长宽扩大 5 倍”冻结为整座楼层的楼板与建筑外壳共同扩大；中央功能核心、室内隔墙、门、设施和两套手调楼梯不缩放。
- 从 `v004` 派生 `env_tower_descent_kit_top3d_v005.blend`：移除旧边界女儿墙和核心满高外墙，新增 4 段楼顶女儿墙、4 段基地外墙、4 段战斗层外墙；外墙外表面统一对齐 `X/Y=±167.705m`，与三层楼板形成同一 `335.410×335.410m` 建筑轮廓。
- 基地与战斗层的 67.082m 中央核心继续使用 11 段低围栏和楼梯门区表达边界；两套楼梯 ROOT、平台、特殊墙高、门、设施、内部隔墙及 12 块外围楼板均未位移或缩放。
- Outliner 在三层集合下新增 `01A_EXTERIOR_ROOFTOP_FULL_FOOTPRINT`、`03A_EXTERIOR_FACILITY_FULL_FOOTPRINT`、`05A_EXTERIOR_COMBAT_FULL_FOOTPRINT`，便于单独显示、调整和后续按层导出。
- Blender QA 通过：255 个对象、237 个网格、12 段总边界外壳、12 块外围楼板、11 段核心围栏、两套楼梯集合；应用缩放、9m 层高、ROOT 父子关系和 `±167.705m` 外墙边界均成立。当前 Blender 窗口已加载并保存 `v005`。
- 资产台账沿用 `ENV-TOWER-DESCENT-KIT-3D`，升级到 `v005 / v1.18`；本轮仍未导出 GLB、未修改 Godot 运行时，等待用户确认 Blender 比例与布局。

## 2026-07-30 PH35 五米模块化楼层与墙体 v006

- 先把用户当前带未保存标记的 `v005` Blender 内存状态另存为 `env_tower_descent_kit_top3d_v005_user_snapshot.blend`，再加载 `v006`，避免模块化改造覆盖用户手调楼梯。
- 全层尺寸对齐 5m 网格：建筑占地由 `335.410×335.410m` 规整为 `335×335m`（每边 67 格），中央基地/战斗核心由 `67.082m` 规整为 `65×65m`（每边 13 格）；楼梯根节点只向核心内侧校正约 `1.041m`，梯跑、折角、特殊墙高和开口朝向不变。
- 新建四个稳定子资产：`ENV-TOWER-FLOOR-TILE-5M`、`ENV-TOWER-WALL-SOLID-5M`、`ENV-TOWER-WALL-PARAPET-5M`、`ENV-TOWER-WALL-DOOR-5M`。门模块占一个 5m 墙位，保留 4m 净开口和独立 `DoorLeaf_OPEN`，便于 Godot 单独动画、碰撞和肉鸽触发。
- 楼顶、基地、战斗层的地砖与整层外边界改为共享母网格的未应用 5m Array 组装，表达 13,452 块地砖和 804 段外墙/女儿墙，但 Blender 场景只保留 432 个对象、114 个网格，避免逐块对象造成编辑与导出性能恶化。
- 基地核心使用 52 个可独立编辑的 5m 满高墙/门模块完整围合，不再使用低围栏；基地内部设施分区使用 21 个模块、5 个门。战斗层核心同样使用 52 个围合模块，内部隔间使用 32 个模块、6 个门，后续可直接按模块拆分为关卡拼装件。
- Outliner 新增 `10_MODULE_LIBRARY_5M`，下分 `10A_MOD_FLOOR_TILE_5M_U01`、`10B_MOD_WALL_SOLID_5M_U01`、`10C_MOD_WALL_PARAPET_5M_U01`、`10D_MOD_WALL_DOOR_5M_U01`；各层使用 `*_TILE_GRID_5M`、`*_OUTER_WALL_GRID_5M`、`*_CORE_WALL_GRID_5M`、`*_INTERIOR_WALL_GRID_5M` 规范集合归类。
- Blender QA 通过：`GRID_UNIT=5m`、`MAP_SIZE=335m`、`CORE_SIZE=65m`、18 个未应用 Array、158 个可编辑墙/门模块实例、16 个门模块、两套楼梯集合；旧大楼板、旧整面大墙和基地低围栏已清理。
- 当前仍只交付 Blender 源文件、构建/验证/渲染脚本与评审图；未导出 GLB、未修改 Godot 运行时，待用户确认模块比例与布局后再进入引擎拼装。

## 2026-07-30 PH36 99—95层塔楼 Godot 首批关卡

### 设计与资产接入

- 新增 `docs/PH36_塔楼五米模块Godot关卡Demo.md`，冻结335×335m整层、65×65m核心、5m模块网格、9m层高以及楼顶/99/98—95层标高。
- 从Blender `v006` 导出四个独立GLB：5m地砖、9m满高实墙、1.5m女儿墙和4m净宽带门墙；Godot导入与重复资产守卫通过。
- `TowerFloorStage3D` 使用共享导入Mesh与MultiMesh生成每层67×67网格和268段边界；楼梯开口格剔除，承重碰撞与渲染显隐完全分离。
- 资产台账升级到v1.20：母版与四个子资产由Blender评审/程序占位升级为 `godot_runtime / 原型已接入`，登记GLB路径、SHA-256和运行时职责。

### 玩法与关卡

- 正式入口生成楼顶、99层基地及98、97、96、95四个探索层，共26个逻辑房间；每个探索层含入口、中央、北、南、出口与固定电梯房。
- 北/南房型与电梯所接支路按塔楼种子变化；入口/出口保持交替180°楼梯接口，房型可变但电梯、门宽和清房节奏不变。
- 七个基地设施保留且基地安全层不刷怪、不掉清房钥匙；探索房继续复用怪物波次、搜索、掉落、清房钥匙、门后命运三选一与95层Boss/撤离。
- 每层电梯需亲自操作终端点亮；99层与已点亮楼层可往返，未探索深层不生成可选按钮，禁止越层开图。
- 新增塔楼HUD，常驻显示楼层、当前目标、电梯点亮楼层和真实HP进度。

### 楼梯、镜头和流送修复

- 楼梯统一为6m净宽、6m门厅、15m双跑、8m中心距、9m层差；折角平台改为11节点矩形走道，下层贴墙走道外让1m，护栏端部净空4m。
- 楼梯视觉面使用统一路径连续吸附高度，避免CharacterBody在坡道/平台盒体接缝被0.1m侧壁卡住；楼层承重、三面楼梯墙与护栏继续保留真实碰撞。
- 塔楼镜头抬高为更适合9m满高墙的俯视角；当前房间和楼梯墙按摄像机—角色真实射线智能剖切，不再固定隐藏世界+Z墙。
- 上层地板在下降22%后隐藏、下层落脚面从12%显示，修复进入下一层前被上层地板闪遮；鼠标瞄准平面改为角色当前物理Y。
- 楼顶使用日照、关闭玩家探照灯并显示99层外立面与城市下景；室内恢复探照灯、密闭曝光和种子化窗光。
- 运行时以当前层为中心最多加载5个物理层；远层房间、AI、灯、门提示和交互停用，承重楼板永久保留。

### 验收

- `verify_tower_descent_flow.tscn` 通过：真实完整下楼与完整回爬、五段楼梯、命运门、电梯禁越层、98层刷怪、95层Boss/撤离、楼层/鼠标坐标、视野遮挡、上层地板剖切、最多5层流送及3390节点预算成立。
- `verify_tower_descent_visual.tscn` 通过并保存楼顶城市、99层基地、98层战斗房与楼梯间四张Apple M1 OpenGL截图。
- `verify_3d_vision_input_flow.tscn`、`verify_3d_fate_weapon_flow.tscn`、`verify_3d_performance_budget.tscn`、`verify_runtime_safety_flow.tscn` 回归通过；性能回归记录生成260.0ms、最重房构建16.3ms、全探索2950节点、最大活动房间2、持续段6.89ms。

## 2026-07-30 PH37 Blender楼梯直入与种子化战斗层

### 楼梯与门禁修复

- 从用户手调 `env_tower_descent_kit_top3d_v006.blend` 的
  `Stair_Special_Rooftop_ROOT` 和 `Stair_Generic_Rotatable_ROOT` 直接导出两份GLB；
  Godot不再使用程序踏步冒充楼梯视觉。
- 以Blender实测中心线重做11节点通行路径和约15×27m楼板洞口；特殊楼顶梯只用于
  楼顶→99层，通用梯按接口方向旋转复用。真实下行与回爬均不再卡门厅、折角或接缝。
- 门策略拆分为清房、钥匙和命运三项：楼顶→99与99→98免费无命运，
  98入口门免费并触发一次命运，其余房门保持清房—钥匙—命运循环。

### 关卡与流送

- 98—95层从固定十字改为十字、北回形、南回形、折线四类种子模板；
  每层六房、15m房间、20m中心网格和5m正交走廊，入口/出口固定相对两侧。
- 同种子复现模板、房型与父子拓扑，换种子至少改变一层；小地图仍只揭示到访房间
  和已开启边，固定电梯房必须亲自点亮才可从99层选择。
- 楼梯过渡只显示相邻两层，房间/楼梯墙按摄像机实际射线剖切；上层楼板提前隐藏、
  下层落脚面提前接入，所有楼板承重常驻，运行楼层窗口最多5层。

### 验收

- Blender v006：`QA_PASS`，432对象、114 Mesh、335m整层、65m核心、2套楼梯集合。
- 塔楼专项通过：3252节点；真实双向楼梯、三类门策略、四层随机模板、电梯、
  98层怪物、95层Boss、瞄准平面、遮挡和≤5层流送全部成立。
- 3D性能专项：生成110.9ms、最重房构建8.5ms、持续帧段6.91ms、稳定视野重绘0次。
- 3D等价、四主题完整流程、视野、背包/枪械、换弹、命运、Boss、灯光与运行安全
  全部回归通过；Apple M1 OpenGL四张塔楼实机画面重新采样通过。
- 美术资产台账升级到v1.21，新增通用/楼顶特殊两个楼梯子AssetID并登记GLB校验值。

## 2026-07-30 PH38 八米层内镜头与纯物理遮挡

- 按新运行逻辑把塔楼摄像机从相对角色 `(0,22,10)`、约65.6°斜视改为
  `(0,8,0)`、90°垂直俯视。摄像机位于9米层高内部，水平偏移为0，
  不会在15米房间中越过满高墙。
- 删除摄像机射线驱动的房间墙/楼梯墙剖切、楼梯表面剖切和按下降12%/22%切换
  上下层楼板的逻辑；楼板、墙和楼梯改由实际空间关系与洞口自然遮挡。
- 流送窗口内楼板与外墙完整显示，已流送房间保持可见；仅窗口外楼层整体关闭，
  上下合计仍不超过5层，承重碰撞规则不变。
- 塔楼专项通过：3252节点；8米镜头、上下楼、鼠标当前层瞄准、三类门策略、
  电梯与95层Boss均未回退。OpenGL楼顶、99层、98层和楼梯四张PH38截图通过，
  没有上层楼板或墙覆盖镜头。
- 性能专项记录生成133.0ms、最重房构建7.8ms、持续帧段6.89ms、
  稳定视野重绘0次；360°逻辑视野与运行安全回归通过。
- 本轮明确不修改角色模型、1.8米碰撞体或v004动画缩放，等待下一步单独调整角色尺寸。

## 2026-07-30 PH39 一米角色视觉与碰撞统一

- 新增 `docs/PH39_一米角色视觉与碰撞统一.md`，先冻结完整视觉含耳朵1米、
  碰撞体1米和 `0.404040404` 统一线性缩放系数。
- 本轮沿用 `CHR-PLY-CAPSULE01-3D-BUNNY01` 及其组件 AssetID，以 v005 版本
  递增方式同步修改 Blender母版、分体GLB、Godot装配、挂点和动画米制位移；
  v004 保留作回退。
- v005 Blender实测包围盒 `0.689058×0.430000×1.000000m`、最低点0；
  七个分体GLB重新导出，Godot十个可见网格装配后最低Y=0、最高Y=1。
- `Player3D` 不可见碰撞体改为 `0.477778×1.000000×0.400000m`、中心Y=0.5；
  持枪模型、WeaponSocket、手脚、耳根、可穿戴、VFX、换弹条和全部米制动画
  平移同步缩放，角度、时长和玩法数值保持不变。
- 角色包围、武器姿势/碰撞、待机、六态、DIY、状态画廊、换弹、塔楼、
  四主题完整流程、3D等价、运行安全和性能回归通过；最终串行性能记录初始生成
  163.0ms、最重房构建7.4ms、持续帧段6.92ms、全探索2950节点。
- Apple M1 OpenGL已保存一米角色持枪对比及塔楼四张PH39实机图；资产台账升级
  到v1.22，现有稳定AssetID不变，登记v005路径、尺寸、关节和SHA-256。

## 2026-07-30 PH40 八米室内斜俯视摄像机

- 新增 `docs/PH40_八米室内斜俯视摄像机.md`，先冻结镜头Y=8米、水平后移
  5.5米、视线落点前推0.75米、基准FOV 55°和纯物理遮挡契约。
- 实装空旷位置约50.4°斜俯视；靠墙时二分求出墙前安全位置并保留0.35米余量，
  收镜即时、离墙后按7米/秒平滑恢复。镜头Y不降低，墙、楼板和楼梯继续完整渲染。
- 塔楼流程专项通过：人工满高墙能触发收镜且高度仍为8米；五段楼梯、当前层瞄准、
  命运门、电梯、怪物/Boss、最多5层流送和3252节点预算均未回退。
- Apple M1 OpenGL已保存楼顶、99层、98层和楼梯四张PH40实机图；楼梯踏步、
  竖直墙面和楼顶边缘恢复透视纵深。360°遮挡、输入与运行安全回归通过。
- 性能记录生成162.6ms、最重房间7.5ms、全探索2950节点、持续帧段6.90ms、
  稳定视野重绘0次。

## 2026-07-30 PH41 1.5米角色正式资产定版

- 用户确认1.5米预览比例后，从一米v005母版等比烘焙生成Blender v006；
  v005及其GLB保持不变作回退。
- v006重新导出七个pivot-local分体GLB和完整运行时GLB；Godot切换到v006，
  `Avatar3D`父节点缩放恢复为1，所有米制动画与附件改用`1.5 / 2.475`换算。
- 虚拟碰撞盒同步调整为`0.716667×1.5×0.6m`、中心Y=0.75m；武器、挂点、
  穿戴、换弹条和VFX保持与确认预览一致。
- Godot运行时网格包围盒实测Y=0—1.5m；虚拟碰撞、单/双手持枪均通过。
  五段9米楼梯完整下行与回爬通过，放大碰撞体没有卡住门厅、折角或平台。
- Apple M1 OpenGL已保存楼顶、99层、98层、楼梯间及单/双手持枪预览；
  最终性能为塔楼生成124.0ms、最重房间6.5ms、全探索2950节点、
  持续帧段6.90ms。
- 美术资产台账更新至v1.23，已登记v006母版、七个分体GLB、完整运行时GLB、
  Godot装配场景与最终SHA-256；v005一米版本继续保留作回退。

## 2026-07-30 PH42 统一太阳与物理遮光室内照明

- 塔楼自然光改为全楼层固定单太阳：能量0.72、暖色与阴影状态不随楼层变化；
  环境底光固定为0.07、雾密度固定为0.0012，室内明暗改由楼板、墙、门和
  楼梯间实体遮挡决定。
- 删除程序生成的自发光 `TowerWindow_*` 面板和 `TowerWindowNaturalLight`
  聚光灯；现阶段外墙全部按实墙处理，真实窗洞留待后续五米墙体模块。
- 房间顶灯、墙壁开关与基地常亮逻辑保留；角色探照灯安装高度按1.5米角色从
  2.25米修正为1.15米，近身溢光与角色补光坐标同步降低。
- 新增塔楼光照契约验收：楼顶与98层的太阳、环境光、雾参数完全一致，运行时
  假窗节点为0；视野仍只依赖距离和物理遮挡，不读取任何灯光状态。
- 塔楼、视野输入、房间灯、运行安全和性能回归通过；完整塔楼3242节点，
  全探索2950节点，持续帧段6.88ms。已保存楼顶、基地、98层关灯/开灯及楼梯间
  五张PH42 Compatibility实机预览。

## 2026-07-30 PH43 楼梯承重、下落落地与楼顶可读性

- 新增 `docs/PH43_楼梯承重与角色下落落地闭环.md`，冻结真实重力、连续楼梯
  承重、斜坡静止、八态状态机、掉落物承重探测和楼顶光照验收契约。
- `Player3D` 碰撞由方盒升级为高1.5米、半径0.34米胶囊；移除地面状态固定
  `Y=-3.2` 和楼梯路径每帧写Y，统一使用重力、`move_and_slide()`、短距地面
  吸附、0.10秒离地容错与最近安全地面恢复。
- 新增 `falling`、`landing` 顶层状态；下落期间有限空中控制，落地按冲击速度
  产生0.12—0.30秒停顿。bunny01 v006复用现有关节实现收腿、耳后扬、压缩和
  回弹，未新增角色模型或贴图。
- 两套9米楼梯直接为Blender v006中的六块 `*Walkable` 楼板生成同形碰撞，
  不再使用程序路径承重面；围护墙同样使用导入网格碰撞，表现流送隐藏时Walkable
  承重仍常驻。四向楼板洞口收紧到约15×30米，掉落物与钥匙生成前向下寻找承重面。
- 摄像机固定为角色相对坐标`(0,8,5.5)`、FOV 55°和既定斜俯视目标，每个物理帧
  覆盖外部位移与旋转；删除碰撞驱动的收镜、侧移和水平绕墙。楼顶天空反弹光保留，
  进入特殊楼梯围护体立即按室内开启探照灯。
- 塔楼真实下行至95层并完整回爬、垂直物理、坡面无输入静止、八态、1.5米碰撞、
  固定镜头、换弹、枪械姿势、完整战局、视野、命运武器和运行安全全部通过；
  完整塔楼3343节点；性能专项为生成270.7ms、最重房间10.8ms、全探索2950节点、
  持续段6.90ms。
  Apple M1 Compatibility已保存楼顶楼梯、连续坡段、下落与落地四张实机图。
- 美术资产台账升级到v1.25；角色、脚部、塔楼、地砖与两套楼梯沿用稳定AssetID，
  记录PH43运行时职责和八态动画映射。

## 2026-07-30 PH44 250米整层与4×4战斗层

- 新增 `docs/PH44_二百五十米塔楼与四乘四战斗层重构.md`，先冻结50×50块
  5米地砖、250×250米整层、65×65米基地核心、45×45米基础战斗房和
  90×90米Boss净战斗区。
- 普通战斗层改用4×4槽位的外围十二房环，中央2×2槽位留给双楼梯和10米主走廊；
  Boss层改为九个普通房间加一个占2×2槽位的90米Boss区。
- 楼顶大面积黑块按近镜头实体墙遮挡处理：固定镜头不移动、不旋转，只剖切真正
  位于镜头—角色之间的5米墙模块或楼梯围护渲染面，物理和逻辑遮挡保持启用。
- 实墙仍按5米模块拼缝显示，但同方向墙段改为MultiMesh批渲染、碰撞按门洞两侧
  合并；完整探索塔楼2783节点，扩大整层与房间后仍低于3500节点预算。
- 楼顶天空反弹光从2.6降至1.25；普通楼顶继续由统一太阳照明，门口近墙依靠
  局部渲染剖切解决，不再用全局提亮掩盖实体遮挡。
- 塔楼、完整3D战局、视野、命运武器、运行安全、垂直物理和性能专项全部通过；
  性能记录生成286.8ms、最重房间53.1ms、探索节点2950、持续段6.90ms。
- 美术资产台账升级到v1.26；地砖、三类墙体与两套楼梯全部沿用稳定AssetID，
  不新增GLB或贴图。

## 2026-07-30 PH45 手动探照灯与遮挡角色轮廓

- 新增 `docs/PH45_手动探照灯与遮挡角色轮廓.md`，先冻结F键手动灯光、禁止楼层
  自动切灯、固定镜头不改墙体渲染和遮挡时仅显示玩家轮廓的交互契约。
- 删除PH44整段墙体局部隐藏方向；固定镜头、建筑实体、阴影、门、楼梯碰撞和
  360°逻辑视野保持不变。
- `toggle_flashlight` 已绑定F键，探照灯默认关闭；往返楼顶、基地、战斗房和
  楼梯间均不会覆盖玩家开关状态，HUD同步加入快捷键说明。
- 删除墙体隐藏、同方向墙段扩展与恢复显示代码；改为四条镜头—角色身体射线，
  至少两条受阻时给角色和武器叠加无深度青色轮廓。墙体视觉与碰撞始终保留，
  遮挡解除后恢复角色原材质。
- 塔楼、视野输入、完整3D战局、垂直物理、运行安全和性能专项全部通过；
  塔楼2783节点，性能记录生成368.3ms、最重房间61.6ms、探索节点2950、
  持续段6.90ms。

## 2026-07-30 PH46 下方墙体镜头抬升

- 新增 `docs/PH46_下方墙体镜头抬升.md`，先冻结只检测画面下方墙体、禁止
  左右绕墙与Yaw旋转、上限不穿9米天花板和遮挡轮廓继续兜底的规则。
- 画面下方按角色到固定镜头的局部+Z通道判定；家具、怪物、掉落、楼板和左右墙
  均不得触发镜头运动。
- 已实现房间墙、走廊墙、楼梯围护、外边界和房门识别；距离驱动0—0.78米抬升，
  进入/离开采用不同指数平滑速率，最大镜头高度8.78米。
- 用户实测指出基地与楼梯房仍被黑墙遮挡；真实位置复现确认两处墙均已检测成功，
  距离分别1.05米与2米，镜头也已升至8.78米，根因是0.78米抬升不足以越过9米墙。
- 镜头运动修订为抬升收拢弧线：保持X和Yaw固定，升高同时沿固定Z轴收至
  0.15米后移；离墙后平滑恢复8米高度与5.5米后移。
- 实机截图继续发现楼梯门洞黑面，进一步定位为已开启门板上移到
  `Y=4.95—9.35m`，并与门楣、顶部框件共同包住8.78米镜头。只对相机实际进入
  或穿过直接视线的这3个五米门洞小网格做平滑材质淡出；墙门节点、碰撞和其他
  房间墙全部保留。
- 真实基地南墙验收为墙距1.05米、后移0.153米、无遮挡；真实楼顶特殊楼梯验收
  为墙距2米、后移0.150米、3个近景网格淡出、无遮挡。
- 用户复核发现8.78米抬升仍会进入9米标高处的上层楼板厚度与近裁剪危险区；
  最大额外抬升下调为0.30米，最高镜头8.30米，黑墙继续由固定Z收拢和门洞局部
  裁切解决，不再依赖接近天花板的高度。
- 塔楼、视野输入、完整3D战局、垂直物理、运行安全和性能回归通过；塔楼2783
  节点，性能记录生成111.9ms、最重房间7.6ms、探索节点2950、持续段6.89ms。

## 2026-07-30 PH47 门洞固定镜头与战斗层入口对齐

- 用户明确要求只取消门洞处的动态镜头，普通墙边规则继续保留；新增
  `docs/PH47_门洞固定镜头与战斗层入口对齐.md`，冻结6.5米门洞旁路和固定镜头
  过门契约。
- 复现99F→98F空楼板：98F楼梯下接口为`(35,-18,2.5)`，旧入口西门为
  `(55,-18,-27.5)`，平面错位36.06米；楼梯结束于核心边缘，战斗入口却在外围。
- 战斗层入口改为15×15米安全大厅，门与楼梯接口零误差；大厅后用一格5米直走廊
  连接45×45米战斗枢纽，命运卡和第一批怪物仍由枢纽入口门与枢纽房触发。
- 真实通用楼梯行走复现发现15米大厅北墙在`Y≈-10.1m`横切东侧梯跑；入口/出口
  大厅将按楼梯展开方向省略一面重复墙，由楼梯间自身三面围护继续封闭空间。
- 移除重复大厅墙后，角色已抵达98层下门厅，但GLB `EnclosureWall` 自动生成的
  三角网格碰撞仍在`X≈36.16m`封住出口；导入墙改为纯视觉，外围碰撞统一交给
  运行时三面围护，六块`Walkable`同形承重不变。
- 继续行走验证定位门口最终阻挡体为`SCAVENGE`大厅随机生成的`RoomFurniture3D`；
  楼梯大厅改用独立`STAIR_LOBBY`房型，禁止家具、搜索物、敌人与陷阱占用门厅。
- 楼梯大厅作为安全过渡区进入时常亮顶灯并禁用战斗输入；进入45米战斗枢纽后
  恢复战斗与刷怪，让98层关卡开始状态清晰可见。
- 门洞只作为镜头规则旁路，不删除门也不取消普通墙响应；真实基地南墙仍在
  `1.05m`距离触发抬升收拢，真实楼顶门洞则稳定回到约`(0,8,5.487)`且不淡出
  门板或门楣。
- 99F→98F实体行走通过全部11个楼梯路径点，进入15米门厅后自动亮灯并保持安全区；
  打开下一扇命运门后进入45米枢纽、恢复战斗并正常刷怪。
- 塔楼主流程、真实镜头、视觉、视野输入、完整3D战局、垂直物理、运行安全与性能
  专项全部通过；塔楼2351节点，性能记录生成351.2ms、最重房间8.5ms、
  探索节点2950、持续段6.95ms。

## 2026-07-30 PH48 双端楼梯门与行动路线关卡触发

- 新增 `docs/PH48_双端楼梯门与行动路线关卡触发.md`；按玩家真实下楼路线冻结
  “开上端门—通过楼梯—开下端门—激活安全厅—开命运门—进入战斗”的节奏。
- 定位楼梯墙不抬镜头的直接原因：`StairwellWall_*Body`未进入相机墙分类；
  同时PH47的6.5米圆形门旁路覆盖了楼梯平台。
- 门旁路改为只对已开启门生效的窄矩形门槛：横向2.15米、门平面前后0.90米；
  门旁墙、楼梯平台和梯跑重新参与普通墙镜头判定。
- 垂直边改为上下端门独立状态；打开上层门不再同步打开下层门，抵达下层后必须
  按E打开入口门，开门瞬间进入安全大厅并触发下一层关卡HUD和流送。
- 楼梯三面9米围护墙纳入实体穿透测试；导入视觉墙仍不重复生成三角网格碰撞。
- 对照Blender资产确认还存在第四块 `EnclosureWall_Inner` 视觉墙；只为这块带
  门厅缺口的内墙恢复同形碰撞，其他三块继续由规则Box承担，避免重新封死出口。
- 98层安全厅加入无碰撞发光路线带与双门槛线，在固定镜头下明确显示从楼梯入口
  到下一扇命运门的行动方向。
- 真实E键路线验证通过：未开98层下端门时当前房间保持99层，开门后才切换
  `floor_01_entry`并点亮安全厅；随后命运门、45米战斗枢纽和刷怪正常。
- 塔楼、真实楼梯镜头、OpenGL视觉、视野输入、完整3D战局、垂直物理、性能和
  运行安全全部通过；塔楼2374节点，性能记录生成496.0ms、最重房间42.1ms、
  探索节点2950、持续段6.90ms。
